# Reverse-Propagation Log

Pending-rebuild ledger for shared-module / JSON bug fixes that must propagate to
providers on their next rebuild. This is the machine-readable companion to the
narrative `REBUILD_TRACKER.md` (which explains the "why"); this file is the
source-of-truth list of *who is pending which fix*.

**How it works** (pull-based, one-at-a-time — no mass patching):
- A fix ships in a shared module / build script. It self-applies to each provider on
  that provider's next rebuild (the build script also deletes its `PENDING_UPDATES.txt`).
- `tools/flag_pending_fix.ps1` writes a `[FLAG:<id>]` stub into each still-pending
  provider's `docs/tracking/PENDING_UPDATES.txt` and appends a row here.
- `enforce.ps1` PHASE 1 FAILs any provider whose `PENDING_UPDATES.txt` has a non-`#`
  line — so a flagged provider cannot be tested until it is rebuilt.
- `tools/audit_reverse_propagation.ps1` reads this ledger + every `PENDING_UPDATES.txt`
  and reports pending/propagated status. Composed into `doctor.ps1`.
- **Hand-curate the Status column** as providers rebuild (mark applied vX.Y). A fix with
  zero remaining pending providers is fully propagated (leave the row for history).

| Fix ID | Description | Origin | Affected | Status |
|---|---|---|---|---|
| RND-62365 | VehicleMakeName QRDM result-mapping code source VEHICLE/VehicleType -> attributeType=VEHICLE_MAKE/codeTypeSource=NCIC (shared module _build_rms_bundle.ps1) | shared module; applied NJ v4.7, FL v6.8, HI v4.6, CA v2.10 | TX_TLETS, NY_NYSPIN_EJUSTICE, AZ_AZDPS | NY_NYSPIN_EJUSTICE cleared (v4.0, confirmed VEHICLE_MAKE/NCIC in rebuilt JSON, 2026-07-02); TX_TLETS cleared (v4.0, 2026-07-09, calls the shared Build-RmsBundle module -- confirmed VEHICLE_MAKE/NCIC present, verified again on the v4.6 rebuild 2026-07-21); pending: AZ_AZDPS |
| PARSECOMMSYS-ARGS | ParseCommsysName empty-args `_R $args`-drop bug (DL#-instead-of-name in CAD) | shared module 1951db4; applied NJ v4.0, CA v2.12 | TX_TLETS, NY_NYSPIN_EJUSTICE, AZ_AZDPS | NY_NYSPIN_EJUSTICE cleared (v4.0, confirmed non-empty ParseCommsysNameRuleHandler arguments in rebuilt JSON, 2026-07-02); TX_TLETS cleared (v4.0, 2026-07-09, calls the shared Build-RmsBundle module -- confirmed non-empty ParseCommsysNameRuleHandler arguments present, verified again on the v4.6 rebuild 2026-07-21); pending: AZ_AZDPS |
| EMIT-GUARDRAIL-SIM | emit_test_plan guardrail expectedKeyRef now simulated (was a structural heuristic that mislabelled in-state/OOS split routing). Regenerate TEST_PLAN + re-run/re-validate guardrail routing. Test-harness fix, NO config change -- clear this flag after the guardrail re-validation passes. | HI_HCJDC_OFML | CA_CLETS, NJ_NJCJIS | FULLY PROPAGATED 2026-07-02: NJ cleared (simulated expectedKeyRefs match live v4.8 guardrail logs); CA_CLETS cleared (live full re-run 93/93 PASS proved IA.QV/ID.L1/IR.QVC.O/IR.QVC.N/NLTS.KQ.O/IA.QB.H guardrail routings the old heuristic had mislabelled as OOS NLTS.*) |
| FORM-REACHABLE-COMBOS | Remove routing-affecting form defaults so every devdoc Basic combination is reachable FROM THE FORM. Prefills that satisfy a required field make a sibling combo win first-match forever. Per audit_query_trace: TX_TLETS/TX_TLETS_CCH = FRT=E + PlateYear=2026 (4 combos, incl. both OutofState paths - also restore RQ+QV, deleted v4.13 as false dead-combos); CA_CLETS = purposeCode=C (12 combos); HI_HCJDC_OFML = vehicleTypeCode=1 (2 combos). Also stop defaulting State where it is any[]-only (TX/AZ/NJ). Gated by enforce PHASE 2n. | TX_TLETS | CA_CLETS, HI_HCJDC_OFML, TX_TLETS_CCH | pending: CA_CLETS, HI_HCJDC_OFML, TX_TLETS_CCH (flagged 2026-07-30) |
| METADATA-DEMOTION-ADJUDICATE | audit_metadata CHECK 4e (new 2026-07-30) reports metadata-required fields built as any[]. Each must be promoted to set[] OR recorded in <PROVIDER>_ACCEPTED_DIVERGENCES.txt with a reason. Counts: AZ_AZDPS 9 (dexStateUserId handler-populated x4 + BirthDate/SexCode on DQSS/KQ -- AZ already carries 2 accepted NOTEs of the same shape), CA_eSUN 2, CA_SAN_LUIS_OBISPO 2, CA_VENTURA_COUNTY 2, OR_LEDS 1. Blocks enforce PHASE 2b until adjudicated. | audit_metadata | AZ_AZDPS, CA_eSUN, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY, OR_LEDS | pending: AZ_AZDPS, CA_eSUN, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY, OR_LEDS (flagged 2026-07-30) |
| NY-METADATA-PARSER-UNKNOWN | audit_query_trace reports 3 NY combos (DALL x2, RVEH) with an EMPTY set[]. Unresolved whether NY metadata genuinely omits Requirements for those combos or the parser misses NY XML shape. NY audit verdict is NOT trustworthy until this is settled -- resolve as step 1 of NY work, before acting on any NY finding. | audit_query_trace | NY_NYSPIN_EJUSTICE | pending: NY_NYSPIN_EJUSTICE (flagged 2026-07-30) |
| UNDER-REQUIRED-CHOICE-BRANCH | Metadata Choice branch requires a field we built as optional any[], so an incomplete wire request can be sent and passes gate 6d. CA_CLETS IG.QGH: metadata needs purposeCode+Name+(Age OR BirthDate); built any[BirthDate,age] means NEITHER is required and log CA_CLETS_v2.22_IG.QGH.txt shipped with neither. NY RVEHOUT is the same class (PlateTypeCode+PlateYear mandatory in alt2, built optional). Gate: tools\audit_requirement_fidelity.ps1. Fix = split the Choice into one combo per branch, never prefill a newly-mandatory field (BUILD_RULES 24). | _metadata_parse.ps1 Get-MetaAltSets unwrap fix | CA_CLETS | pending: CA_CLETS (flagged 2026-07-30) |
| GUN-NAME-CHOICE-IN-SET | GUN-BY-NAME <Choice>-IN-<Set> DEFECT (family-wide, verified per provider against ITS OWN metadata). A gun-owner name search built with BOTH qualifiers in any[] satisfies NO metadata variant: the CA-family metadata puts Choice[Age|BirthDate] INSIDE <Set> on the QGH/IG.QGH/QGH-equivalent Name combination, which makes exactly ONE of them MANDATORY (the only variant that makes it optional additionally requires SexCode, which none of these carry). Result: a Name-only gun query is accepted by the form and SENT, and it is a request the provider metadata calls invalid. Gate 6d catches it; 6c and 2i do NOT, because content and attribution cannot see a MISSING REQUIREMENT -- CA_CLETS shipped a committed PASS log doing exactly this. FIX = split into one combination per branch, each carrying its own discriminator in set[] (set[] has no OR), ordered by the devdoc listing order since the branches are disjoint peers specificity cannot separate. Precedent: CA_CLETS v2.23 (IG.QGH -> IG.QGH.A age / IG.QGH.B BirthDate, wire-proven: .A carried Age=35 no DOB, .B carried DOB=19900115 no Age) and CA_CONTRA_COSTA v2.2. SCOPE CONFIRMED 2026-07-31: exactly the 4 providers that BUILD a gun-by-name combo. CA_CLETS_OCATS and CA_SAN_LUIS_OBISPO build only the serial-number query (QGB) so they are NOT affected. CA_eSUN QGH is the identical defect (set[purposeCode,GunNameLast,GunNameFirst] any[GunBirthDate,GunAge]). CA_VENTURA_COUNTY IG.QGH is WORSE -- set[purposeCode,NameLast,NameFirst] any[] carries NO discriminator at all, so it can neither satisfy the mandatory-Choice variant nor even transmit Age/BirthDate as optionals. Read QIDM_REFERENCE.txt SECTION 1b before touching either, and CHECK THE PROVIDER DEVDOC: brackets mark optionals, so an unbracketed "Name, Age" means Age is MANDATORY. | CA_CLETS v2.23 | CA_eSUN, CA_VENTURA_COUNTY | pending: CA_eSUN, CA_VENTURA_COUNTY (flagged 2026-07-31) |
| validate-imgind-20b-l30 | validate.ps1 + audit_cross_provider.ps1: LIMITATION #30 now requires State in a set[]; ImageIndicator check moved to BUILD_RULES 20b (prefill REQUIRED for serialization -- the defect is a combo gated ImageIndicator NOT_EXISTS). render_officer_guide + suggest_field_labels: -Encoding utf8NoBOM is PS7-only, a hard parameter-binding failure under the 5.1 engine enforce/pipeline use. Rebuild to re-record scores; LA_LEMS also gains a real 20b WARN. | AZ_AZDPS | LA_LEMS | pending: LA_LEMS (flagged 2026-08-04) |
| testplan-platformfed-presence | emit_test_plan.ps1: platform-fed hidden fields (dexStateUserId) and form-shipped initialValues now count as ALREADY-PRESENT when simulating which combo fires. Previously badge-gated combos were dropped as unrunnable and the plan predicted the looser sibling the tenant would not fire. Re-emitting this provider's plan changes its test set, so it is deferred to the provider's OWN next rebuild + re-sweep. | AZ_AZDPS | NJ_NJCJIS | pending: NJ_NJCJIS (flagged 2026-08-05) |
| guardrail-formdefaults-namespace | emit_test_plan.ps1 guardrail path passed COMBO defaults (attribute-keyed: BadgeNumber, State) where Get-FiringKeyRef matches FORM fieldIds (dexStateUserId, RegistrationState) -- a silent no-op union, so guardrail expectedKeyRef could name the wrong combo. Fixed to pass the form defaults like the three structural call sites. This provider's plan gains 3 guardrail test(s); adopting changes its test set, so it is deferred to its OWN next rebuild + re-sweep. | AZ_AZDPS | NY_NYSPIN_EJUSTICE | pending: NY_NYSPIN_EJUSTICE (flagged 2026-08-05) |
| wiring-closure-class-J-routing-only | audit_wiring_closure gained class J ROUTING-ONLY FIELD (2026-08-07): an EXISTS condition gating a combination on a field that is in NEITHER that combination set[] NOR its any[] -- the officer value decides which query fires and is then dropped from the request. TN_TIES VehicleRegistrationQuery/RQ05 (OOS dealer plate, Nlets) is the ONE portfolio hit of 97 EXISTS conditions examined: it is gated RegistrationState EXISTS but carries State in neither set[] nor any[], so an out-of-state dealer-plate query transmits no destination state. Its RQ06 (handicap) and RQ07 (temp) siblings DO carry State, so this reads as an omission rather than a design decision. FIX at TN_TIES next rebuild: add RegistrationState to the RQ05 any[] (metadata permits it on the Nlets variant -- confirm against the RQ Requirements block before building). Not fixed now: one provider at a time, and TN_TIES is NEVER-TESTED with a separate parked prose divergence. | tools/audit_wiring_closure.ps1 class J | TN_TIES | pending: TN_TIES (flagged 2026-08-07) |
| sqvr-totals-denominator | audit_sqvr_integrity CHECK 2 broadened (2026-08-10): it previously matched 3 exact phrasings and so compared NOTHING on 17 of 20 providers while printing PASS. FL_FCIC now FAILs: its SQVR asserts "Total combos: 31 CommSys" against 30 in the emitted v7.18 JSON (audit_test_coverage). ADJUDICATE at FL next rebuild -- decide whether 30 is correct (v7.15-v7.17 removed a dead control, so the count plausibly dropped and the SQVR was not updated) or whether the SQVR counts something the tool does not, then correct the losing side. Doc-only: no wire impact, no version bump. | tools/audit_sqvr_integrity.ps1 | FL_FCIC | pending: FL_FCIC (flagged 2026-08-10) |
| ncic-image-default-y-everywhere | NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. | FL_FCIC v7.21 | AZ_AZDPS, CA_CLETS, CA_CLETS_OCATS, CA_CONTRA_COSTA, CA_eSUN, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY, HI_HCJDC_OFML, IL_LEADS_OFML, LA_LEMS, MD_METERS, NJ_NJCJIS, NM_NMLETS_OFML, NY_NYSPIN_EJUSTICE, OH_LEADS, OR_LEDS, TN_TIES, TX_TLETS, TX_TLETS_CCH | pending: AZ_AZDPS, CA_CLETS, CA_CLETS_OCATS, CA_CONTRA_COSTA, CA_eSUN, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY, HI_HCJDC_OFML, IL_LEADS_OFML, LA_LEMS, MD_METERS, NJ_NJCJIS, NM_NMLETS_OFML, NY_NYSPIN_EJUSTICE, OH_LEADS, OR_LEDS, TN_TIES, TX_TLETS, TX_TLETS_CCH (flagged 2026-08-12) |
| zz-append-probe | newline-safety probe | PROBE | CA_CLETS_OCATS | pending: CA_CLETS_OCATS (flagged 2026-08-13) |
| [FLAG:ncic-image-default-y-everywhere] | **RETIRED on 9 provider(s), 2026-08-14, Rob-approved -- and it corrects the row above, which was flagged to `all` before anyone COUNTED.** "Everywhere" is 14 providers, not 20. **6 of 20 build NO ImageIndicator control at all** (CA_CLETS, CA_CLETS_OCATS, CA_CONTRA_COSTA, CA_eSUN, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY -- verified absent from every QUERYINPUTFORM *and* every QIDM), so the rule is NOT-APPLICABLE; adding controls would OVER-PERMIT, the same adjudication that scoped HI to Vehicle-only and excluded IL's Article. **3 more are ALREADY CONFORMANT** with one Person control at 'Y' (NM_NMLETS_OFML, OR_LEDS, TN_TIES) -- there is no second entity to set. **AZ_AZDPS is RULED OUT AND IT WAS MEASURED:** ImageIndicator is in 2 set[]s there (DQPN/DQP) while Requestor and dexStateUserId are hidden AND prefilled, so v3.10's actual injection of 'Y' collapsed DQPN to [NameLast,NameFirst] (= DQN) and DQP to [OperatorLicenseNumber] (= DQ) and killed BOTH plain searches (BUILD_RULES 24). Rob's rule carries its own caveat -- "if it does not effect routing" -- and on AZ it does, so the blank is load-bearing: it keeps the photo path OPT-IN. Zero JSON change, zero version bump, zero test package archived on all 9. **STILL OWED, 7:** LA_LEMS needs a RULING (1 set[] + 2 conditions, already 'Y' on its only control); MD_METERS (2), NJ_NJCJIS (4), NY_NYSPIN_EJUSTICE (3), OH_LEADS (2), TX_TLETS (3), TX_TLETS_CCH (3) carry real 'N' values and take it at their OWN rebuild -- a WIRE change (the combo defaults[] twin is required, CAD ignores form initialValue), so each is a bump plus a full re-sweep. | (retirement) | AZ_AZDPS, CA_CLETS_OCATS, CA_CONTRA_COSTA, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY, CA_eSUN, NM_NMLETS_OFML, OR_LEDS, TN_TIES | RETIRED 2026-08-14 -- not a rebuild; recorded as closed |
| [FLAG:ncic-image-default-y-everywhere] | RETIRED on 1 provider(s) -- ALREADY CONFORMANT -- no flip needed, and this supersedes the flag text calling LA a ruling case. MEASURED from the emitted JSON: LA has exactly ONE ImageIndicator control (Person) and it already carries initialValue=Y, so Y-on-every-entity is satisfied -- there is no other entity carrying the field, and adding one would OVER-PERMIT (same adjudication as HI v4.17 Vehicle-only / IL v2.4 excluding Article). The flag text said LA had 1 set[] PLUS 2 conditions; measured, ImageIndicator is in DP set[] with ZERO conditions on it, so that half was imprecise. The set[] membership is a reason for CARE (BUILD_RULES 24 -- a prefill on a set[] field can collapse a combo onto a plainer sibling) not evidence that work is owed: audit_prefill_shadow and audit_combo_reachability are both clean on LA as built. SEPARATE AND STILL PARKED: LA carries a real BUILD_RULES 20b WARN (validator 63P/0F/1W) which is Rob-parked and NOT to be silenced -- it is unrelated to this flag and is untouched. Rob approved 2026-08-14. Zero JSON change, zero version bump. | (retirement) | LA_LEMS | RETIRED 2026-08-14 -- not a rebuild; recorded as closed |
| [FLAG:ncic-image-default-y-everywhere] | RETIRED on 1 provider(s) -- TAKEN at v4.16, 2026-08-14 -- the fix has LANDED, this is not a not-applicable retirement. ImageIndicator now defaults to Y in BOTH the form initialValue and every carrying combo defaults[] on Vehicle/Firearm/Article/Boat (4 authored form sites x3 layout variants + 6 combos); Person was already Y. Verified from the emitted v4.16 JSON with a different pattern than the edit used: 15 of 15 form controls and 8 of 8 combo defaults read Y, zero sites left on N. Measured safe first: ImageIndicator in 0 set[]s and 0 conditions (BUILD_RULES 24 and 20b both clear) and NJ devdoc has no must-be-filled-if-Image conditional. Cleared BY HAND because the pipeline completed and left the flag live -- the same auto-clear failure seen on HI v4.17 and IL v2.4. Full reasoning in the v4.16 BUILD_NOTES entry. OWED: 40-log re-sweep. | (retirement) | NJ_NJCJIS | RETIRED 2026-08-14 -- not a rebuild; recorded as closed |
| [FLAG:ncic-image-default-y-everywhere] | RETIRED on 1 provider(s) -- TAKEN at v4.20, 2026-08-14 -- the fix has LANDED, not a not-applicable retirement. ImageIndicator now defaults to Y in BOTH the form initialValue and every carrying combo defaults[] on Firearm/Article/Boat (3 authored form sites x3 variants + 10 combos); Person was already Y. Verified from the emitted v4.20 JSON: zero sites left on N. TX_TLETS T6 GATE CLEARED BY LOCATING THE OWNING HEADING, not by a bare grep: TX devdoc lines 129-130 DO carry must-be-filled-if-ImageIndicator=Y against EmailAddress and ReasonCode, but the nearest query heading above is DriverHistoryQuery (line 62), so it is scoped to DH -- Person, already Y, both fields auto-populated by hidden gate-feeders -- and this change touched only Firearm/Article/Boat. ImageIndicator in 0 set[]s and 0 conditions. Lockstep TX_TLETS_CCH v1.16 same day, BASE-SYNC bumped. Cleared BY HAND -- the pipeline left it live. Full reasoning in the v4.20 BUILD_NOTES entry. OWED: 92-log re-sweep. | (retirement) | TX_TLETS | RETIRED 2026-08-14 -- not a rebuild; recorded as closed |
| nameparts-untested-unfrozen | Test-scaffolding change, NOT a JSON change: _combo_value_resolver now supplies nameMiddle=A / nameSuffix=JR, and emit_test_plan no longer suppresses the trust warning for those four fieldIds. Regenerate this provider PLAN at its next rebuild so middle/suffix are finally exercised. WHY IT MATTERS: metadata declares Name as type=Name with FOUR components (First/Last/Middle/Suffix), so middle and suffix are metadata-defined CAPABILITY, and no test on any provider had ever filled them -- all 538 logs read DOE, JOHN. Not regenerated here on purpose (rule 8c): this provider is tenant-verified and a bigger plan would drop it to PARTIAL. AZ_AZDPS is running the discriminating test first because it has NO Foundation and NO LIVE tenant. AWAIT THAT RESULT BEFORE ACTING: DOE, JOHN A JR means the handler emits all four and HI_HCJDC_OFML in particular is under-wired (its name parts are in ZERO combination any[], so they can never reach the composite); DOE, JOHN means the handler drops components 3-4 and this becomes a platform LIMITATION to raise, not a wiring job. | AZ_AZDPS | HI_HCJDC_OFML | pending: HI_HCJDC_OFML (flagged 2026-08-17) |
| [FLAG:nameparts-untested-unfrozen] | RETIRED on 1 provider(s) -- RESOLVED at v4.19/v4.20. AZ_AZDPS answered the discriminating question on 10 captured wires -- FormatStringRuleHandler DOES emit all four components (DOE, JOHN A JR), so HI was genuinely under-wired as the flag predicted. v4.19 added nameMiddleDH/nameSuffixDH to the DH name any[] and nameMiddle/nameSuffix to DQ, and the v4.19 sweep captured DOE, JOHN A JR on both DQ and KQ -- shapes no HI log had ever carried. v4.20 then removed them from KQN, the OLN combo, where they were an OVER-PERMIT that put a surname-less <Name>A JR</Name> on the wire. Plan regenerated; middle/suffix now exercised. C3 itself is still unsettled and HI can no longer settle it. | (retirement) | HI_HCJDC_OFML | RETIRED 2026-08-17 -- not a rebuild; recorded as closed |
| [FLAG:nameparts-untested-unfrozen] | RETIRED on 1 provider(s) -- TAKEN at v4.24, WIRE-PROVEN not merely re-planned. The v4.24 plan fills nameMiddle in 2 tests (n22/n24 DLICN), nameSuffix in 2 (n23/n24), nameMiddleDH in 4 (n30/n33 DALHOUT, n36/n39 DALH), nameSuffixDH in 4 (n31/n33, n37/n39) -- counted from the emitted plan. The full 75-test sweep then RAN: 75/75 PASS, four log gates 75/75 each, ALL-PASS 5/5, enforce exit 0. NINE captured wires show the composite correct across ALL THREE name-bearing combos: "DOE, JOHN" / "DOE, JOHN A" / "DOE, JOHN JR" / "DOE, JOHN A JR" on DLICN, DALH and DALHOUT, degrading with no double space. THE COMMA-SPACE IS CORRECT HERE and deliberately differs from TX_TLETS, which emits "DOE,JOHN" with no space per the TCIC/TLETS manual Part 1 p125 Texas Driver License Search Criteria -- the two providers disagreeing is right, and had they matched one would have been wrong. AZ precondition was already satisfied (AZ v3.11 proved the handler emits all four components). Jira comment 800806. | (retirement) | NY_NYSPIN_EJUSTICE | RETIRED 2026-08-18 -- not a rebuild; recorded as closed |
| [FLAG:nameparts-untested-unfrozen] | RETIRED on 1 provider(s) -- TAKEN at v4.24, WIRE-PROVEN. The v4.24 plan fills nameMiddle in 2 tests (n22/n24 DLICN), nameSuffix in 2 (n23/n24), nameMiddleDH in 4 (n30/n33 DALHOUT, n36/n39 DALH), nameSuffixDH in 4 (n31/n33, n37/n39). The full 75-test sweep RAN: 75/75 PASS, four log gates 75/75 each, ALL-PASS 5/5, enforce exit 0, Jira comment 800806. NINE captured wires show the composite correct across ALL THREE name-bearing combos on DLICN, DALH and DALHOUT: DOE, JOHN then DOE, JOHN A then DOE, JOHN JR then DOE, JOHN A JR, degrading with no double space. The comma-space is CORRECT here (NCIC form) and deliberately differs from TX_TLETS, which omits it per the TCIC/TLETS manual Part 1 p125. THIS RETIREMENT WAS APPLIED TWICE: the first attempt was hand-edited and then silently destroyed, because enforce PHASE 2f runs audit_reproducible which EXECUTES the build script, whose obsolete Remove-Item deleted the entire file. That build-script block is now removed, so this retirement survives. | (retirement) | NY_NYSPIN_EJUSTICE | RETIRED 2026-08-18 -- not a rebuild; recorded as closed |
| [FLAG:ncic-image-default-y-everywhere] | RETIRED on 1 provider(s) -- RESOLVED at OH_LEADS v2.5 -- NCIC Image now defaults to Y on ALL THREE entities that carry the control (Person already Y; Article and Boat flipped), in BOTH places the rule requires: the form initialValue (ROW_ART_3, ROW_BOA_2) and the combo defaults[] CAD twin on all four carrying combos (Article QA.S/QA.N, Boat QB.H/QB.R), because CAD ignores form initialValue. ALL THREE OF THE FLAGS OWN PRECONDITIONS WERE MEASURED FIRST, not assumed: (1) ImageIndicator is in NO combinations set[] and in NO condition on this provider -- it rides in any[] on exactly 5 combos (DQ.O, QA.S, QA.N, QB.H, QB.R) -- so BUILD_RULES 24 does not bite here and the flip cannot shadow a combo, unlike AZ_AZDPS (2 set[]s) and LA_LEMS (1 set[] + 2 conditions) which the flag itself carves out for Robs ruling; (2) the TX_TLETS T6 gate is CLEAR -- OH_LEADS devdoc contains no must-be-filled-if / required-if conditional anywhere, and ImageIndicator appears only as a BRACKETED OPTIONAL in every Article combination (#1, #2) and every Boat combination (#1, #2, #3), so nothing becomes mandatory when it is Y; (3) OH_LEADS gates no combo on ImageIndicator NOT_EXISTS, so BUILD_RULES 20b does not apply. COST OF DOING IT HERE IS ZERO, which is why it was done now rather than deferred: OH_LEADS has never been USx-tenant-tested, holds no current-version logs, and IMPORT_LEDGER records v2.4 as never installed on any tenant -- so this wire change archives no test package and invalidates no import. That is the opposite of the six tenant-verified carriers, where the same flip costs a full re-sweep. | (retirement) | OH_LEADS | RETIRED 2026-08-18 -- not a rebuild; recorded as closed |
| plan-fillability-unfireable-tests | test_phase2.ps1 step [2] gained a real FILLABILITY check (2026-08-18) and it found unfireable tests in YOUR plan. Run: tools\test_phase2.ps1 -Provider <THIS PROVIDER>. Findings at flag time -- TN_TIES 4 BLANK + 4 UNSATISFIED, CA_eSUN 26 UNSATISFIED, MD_METERS 3 UNSATISFIED. WHAT IT MEANS: a BLANK fill is a plan test whose fieldId AND value are both empty, so the browser driver literally fills undefined; an UNSATISFIED is a test that omits a field the combo lists as mandatory, so the tenant correctly leaves Send DISABLED and NOTHING IS SENT. Either way the test cannot produce evidence. ROOT CAUSE IS ALMOST CERTAINLY MISSING TEST VALUES: emit_test_plan.ps1 already prints, by name, lines like "Vehicle RN set[]: OwnerLastName has no test value (unmapped in Get-TestValue)" -- that WARN existed all along and nothing consumed it, which is why this reached a live sweep. FIX: add the named fields to docs/reference/TEST_VALUE_OVERRIDES.txt (entity-scoped form <Entity>.<fieldId>=value is preferred so a value cannot leak onto another entity control), then re-emit the plan and re-run test_phase2 until it reports PRE-FLIGHT CLEAR. On OH_LEADS this took the plan from 50 tests with 2 BLANK / 7 UNSATISFIED to 56 tests / 151 fills all clean, and recovered a combo (ATDP) that had NO test at all. NOT A BUILD DEFECT and NOT a JSON change -- do not bump the version for it; it is test-package data. Verified NOT to false-fire: all 8 tenant-verified providers (AZ, CA_CLETS, FL, HI, IL, NJ, NY, TX -- 592 tests / 1948 fills) are clean, which is expected since a package that actually drove and passed cannot contain an unfireable test. | tools/test_phase2.ps1 fillability check | TN_TIES | pending: TN_TIES (flagged 2026-08-18) |
| [FLAG:validate-imgind-20b-l30] | RETIRED on 1 provider(s) -- RESOLVED at MD_METERS v2.1 by the v2.1 rebuild, which is exactly what this flag asked for ("Rebuild to re-record scores"). Validator scores re-recorded in STATUS.txt at v2.1. Confirmed NOT applicable in substance on this provider: LIMITATION #30 is satisfied (State rides in set[] on the OOS combos) and BUILD_RULES 20b does not bite because MD_METERS gates NO combination on ImageIndicator NOT_EXISTS -- that dead-branch prefill is the actual defect 20b names. Same disposition OH_LEADS reached for the identical flag. | (retirement) | MD_METERS | RETIRED 2026-08-19 -- not a rebuild; recorded as closed |
| [FLAG:ncic-image-default-y-everywhere] | RETIRED on 1 provider(s) -- RESOLVED at MD_METERS v2.1 -- and MD_METERS WAS THE LAST CARRIER, so this portfolio flag is now fully closed (16 providers -> 0). NCIC Image now defaults Y on every entity that defines the control: Vehicle (ZLRG.P, ZLRG.V, ZVEH.P, ZVEH.V) and Boat (ZBOA.H, ZBOA.R) flipped from N; Person (ZWAR.O/N, ZLDR.O/N, ZDRV.O/N) was already Y. Done in BOTH required places -- emitted JSON now reads 9 of 9 form initialValues at Y and 12 of 12 combo defaults[] at Y, because CAD ignores the form initialValue and a form-only flip would leave every CAD-originated Vehicle/Boat query still sending N. ALL PRECONDITIONS MEASURED, not assumed, exactly as the flag demands: (1) ImageIndicator is in NO combinations set[] and in NO condition on this provider -- it rides in any[] on all 12 combos -- so BUILD_RULES 24 cannot bite and the flip can shadow nothing, unlike AZ_AZDPS (2 set[]s) and LA_LEMS (1 set[] + 2 conditions) which the flag itself carves out; (2) the TX_TLETS T6 gate is CLEAR -- MD_METERS devdoc contains no must-be-filled-if / required-if conditional anywhere, so nothing becomes mandatory at Y; (3) no combination is gated ImageIndicator NOT_EXISTS. COST WAS ZERO: never USx-tenant-tested, 0 current-version logs, never imported -- so this wire change archived no test package, which is why it was done here rather than deferred again. Also applied the canonical NCIC Image label to all three controls (was "Image (optional)"). | (retirement) | MD_METERS | RETIRED 2026-08-19 -- not a rebuild; recorded as closed |
| [FLAG:plan-fillability-unfireable-tests] | RETIRED on 1 provider(s) -- RESOLVED at MD_METERS v2.1. test_phase2 step [2] had reported THREE unfireable plan tests -- T12/T13/T14 Person/ZWAR.O "mandatory OperatorLicenseExpirationYear is neither filled nor prefilled -- Send will stay DISABLED". Root cause was exactly what the flag predicted: emit_test_plan had no test value for the field, so it emitted tests that could not submit. NOT a build defect -- ZWAR.O genuinely mandates the licence expiry year (set = NameLast, NameFirst, OperatorLicenseExpirationYear, OperatorLicenseNumber, raceCode, SexCode) and the control already exists ("License Expiration Year (warrant search)"). FIXED AS TEST DATA, no JSON change for it: created docs/reference/TEST_VALUE_OVERRIDES.txt with Person.OperatorLicenseExpirationYear=2028, entity-scoped so it cannot leak onto another entity control. The value is NOT authority-derived (metadata types the field Alphanumeric with no size; the devdoc lists no values) and the file says so -- if METERS rejects it live, change the value not the build. Verified: fillability 39 tests / 129 fills, PASS, PHASE 2 PRE-FLIGHT CLEAR. Had these been driven, the tenant would have kept Send DISABLED and nothing would have been sent -- the failure mode that cost the OH_LEADS Vehicle sweep on 2026-08-18. | (retirement) | MD_METERS | RETIRED 2026-08-19 -- not a rebuild; recorded as closed |
| [FLAG:plan-fillability-unfireable-tests] | RETIRED on 1 provider(s) -- RESOLVED at TN_TIES v2.2. test_phase2 step [2] had reported EIGHT findings that were really THREE missing test values -- each unvalued mandatory field produced one BLANK FILL (fieldId and value both empty, so the driver fills undefined) AND one UNSATISFIED (the field never filled, so the tenant keeps Send DISABLED and NOTHING is sent): T13 Vehicle/RQ05 + T14 Vehicle/QV.D DealerLicensePlateNumber, T15 Vehicle/RQ06 HandicapPlacardNumber, T16 Vehicle/RQ07 TemporaryLicensePlateNumber. These are TN THREE SPECIALTY-PLATE PATHS (dealer, handicap placard, temporary tag), which is most of what distinguishes this provider 8 Vehicle combos from the portfolio usual 3-4 -- so four of eight Vehicle paths could never have been driven. NOT A BUILD DEFECT: every combo is metadata-faithful (RQ05 Set[DealerLicensePlateNumber], RQ06 Set[HandicapPlacardNumber], RQ07 Set[TemporaryLicensePlateNumber]) and every control exists; emit_test_plan simply had no value for the fields. FIXED AS TEST DATA with no JSON change for it: new docs/reference/TEST_VALUE_OVERRIDES.txt, entity-scoped so values cannot leak onto another entity control, and the file records that the values are NOT authority-derived (metadata types them Alphanumeric, the devdoc gives no formats) so a live rejection means changing the VALUE not the build. Verified: fillability 48 tests / 129 fills, PASS, PHASE 2 PRE-FLIGHT CLEAR. | (retirement) | TN_TIES | RETIRED 2026-08-19 -- not a rebuild; recorded as closed |
| [FLAG:wiring-closure-class-J-routing-only] | RETIRED on 1 provider(s) -- ADJUDICATED at TN_TIES v2.2 -- REGISTERED AS CORRECT-AS-BUILT, and the flag PRESCRIBED FIX WOULD HAVE BEEN A NEW DEFECT. The flag said: "FIX at TN_TIES next rebuild: add RegistrationState to the RQ05 any[] (metadata permits it on the Nlets variant -- confirm against the RQ Requirements block before building)". That confirm instruction is what saved it, because BOTH of the flag premises are FALSE. (1) "metadata permits it on the Nlets variant": there is EXACTLY ONE RQ05 in the entire metadata XML -- under VehicleRegistrationQuery, Set[DealerLicensePlateNumber] Any[InquiryTypeIndicator]. No State in Set, none in Any, and NO separate Nlets transaction defines RQ05 at all; every transaction in the file was searched for the keyRef, not just VehicleRegistrationQuery. So adding State to RQ05 any[] would transmit a field that transaction does not define -- an OVER-PERMIT, which audit_requirement_fidelity would then report as a fresh finding. (2) "Its RQ06 (handicap) and RQ07 (temp) siblings DO carry State, so this reads as an omission rather than a design decision": they do NOT. Metadata RQ06 is Set[HandicapPlacardNumber] Any[] and RQ07 is Set[TemporaryLicensePlateNumber] Any[]; our built RQ06/RQ07 likewise carry no State in set[] or any[]. There is no asymmetry, so there is no omission to infer. WHAT IS ACTUALLY HAPPENING, and it is the portfolio standard pattern: RQ05 and QV.D have IDENTICAL set[] (DealerLicensePlateNumber) and are separated ONLY by the State condition -- RQ05 gates RegistrationState EXISTS (out-of-state) and QV.D gates NOT_EXISTS (in-state). The officer State value SHOULD decide which fires and SHOULD NOT ride along, because the RQ05 transaction cannot carry it. Same shape already registered on OH_LEADS BoatQuery BQ.H, where the Nlets BQ transaction does not define the image flags so a hull+State+Image fill correctly does not carry them. Recorded in NM/TN registry as VehicleRegistrationQuery | RQ05 | RegistrationState | routing-only-not-transmitted. audit_wiring_closure class J will KEEP PRINTING this -- it is a wiring OBSERVATION, its own output says only classes A and C are officer-facing, and it does not read the registry. That is expected, not a regression. DO NOT widen RQ05 any[]. | (retirement) | TN_TIES | RETIRED 2026-08-19 -- not a rebuild; recorded as closed |
| plan-dedupe-vacuous-tests | emit_test_plan.ps1 gained two plan-only filters 2026-08-21: (1) DUPLICATE -- drop a test whose (entity, fill-set) is byte-identical to an earlier one, which makes the generator agree with audit_log_inflation attack A by construction; (2) NO-CONTEST GUARDRAIL -- drop a guardrail whose fill adds no competing identifier over the winner's own combo test, because the pair differs only by a presence condition and the loser can never match. YOUR PLAN WILL SHRINK when you regenerate; measured non-destructively on 2026-08-21: AZ -5, CA_CLETS -11, CA_SLO -1, CA_VENTURA -2, FL -14, HI -1, IL -1, LA -1, MD -3, NJ -2, TN -1, TX_TLETS_CCH -4. NO COVERAGE IS LOST -- identical fill means identical wire, and NY held 16/16 combos at 100% across the change. NOT DONE FOR YOU ON PURPOSE: if you are tenant-verified, regenerating now makes your existing logs ORPHAN (more logs than plan tests) and drops you out of ALL-PASS, which is why this is a flag and not a sweep. Do it at your OWN next rebuild, and archive the superseded duplicate logs in the same pass (NY put them in logs/<Entity>/_archive_dedupe_v<ver>/). Reference: NY_NYSPIN_EJUSTICE v4.26, 73 -> 65 tests, inflation A 8 -> 0, four log gates 65/65. | NY_NYSPIN_EJUSTICE | AZ_AZDPS | pending: AZ_AZDPS (flagged 2026-08-21) |
