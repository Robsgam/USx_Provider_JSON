NEW PROVIDER PROCEDURE -- SUPERSEDED (2026-07-29)
================================================================================

This file is no longer maintained. It was last updated 2026-05-21 and had drifted:
it omitted steps the current process requires (the devdoc pdftotext extraction and
extract_queries.ps1), while duplicating everything else.

THE MAINTAINED SOURCE IS THE SKILL:
    .claude/skills/usx-new-provider/SKILL.md   (updated 2026-07-24)

Use that. It is what an agent loads automatically when you say "onboard <STATE>" or hand
over a new provider XML, so it cannot silently diverge from what actually gets executed.

Supporting references:
    knowledge-base/BUILD_RULES.txt        Section 0 = naming rule, read BEFORE creating a folder
    knowledge-base/TESTING_REQUIREMENTS.txt  Section 16 = bulk onboarding workflow
    README.md                             project overview and the three commands

The previous 291-line content is preserved in git history (last full version at commit
2d9abfaf^ and earlier). Recovered with:
    git log --follow -- NEW_PROVIDER_PROCEDURE.txt
    git show <commit>:NEW_PROVIDER_PROCEDURE.txt

WHY THIS WAS COLLAPSED RATHER THAN UPDATED: two documents describing one process will
always drift, and the drift is invisible until someone follows the stale one. One source.
