# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Temp
*.tmp
*.bak
*~

# Automation capture archives (intermediate test artifacts)
automation/captures/

# On-demand version extracts (tools/get_provider_version.ps1). NOT committed:
# git already stores every version losslessly -- 671 retrievable artifacts, ~240MB
# if duplicated as files, plus a new exclusion needed in every active-JSON resolver.
_versions/
