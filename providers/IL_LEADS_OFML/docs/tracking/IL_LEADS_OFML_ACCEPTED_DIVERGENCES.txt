# SUPPRESSION-SCOPE: direction-aware
#   Set 2026-08-02. This registry was CREATED that day by accept_divergence, which defaulted new
#   files to legacy (broad) scoping -- 4 rows immediately produced 12 over-broad suppressions.
#   The tool now stamps this marker at creation; this file is the retrofit of the one it missed.
#   No legacy rows existed here to protect, so there was nothing narrowing could un-silence.
# IL_LEADS_OFML ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1 (CHECK 4 / 4d). Each non-comment line records a
# set/any divergence that is intentional and correct, so the gate treats it as [NOTE] not [FAIL].
# Format (pipe-delimited): query | keyRef | field | rule | reason | source | date
#

BoatQuery | BQ | State2 | devdoc-optional-unreachable | OUT OF SCOPE by portfolio decision (Rob 2026-08-02): multi-destination Nlets routing is not built on any provider. This is a REAL unwired optional, not a tool artefact -- verified against THIS provider's own authorities: its metadata places the field in <Set><Any> on this combination (optional, buildable) and its devdoc lists it in the bracketed optional set, so both authorities agree it could be carried. Recorded rather than built because supporting additional destination states is a product capability question, not a wiring omission, and it would add up to four more state dropdowns per affected card. Same call already applied to FL_FCIC. Rule class is 'other', so this row silences no check -- it exists so the finding is not re-derived as a defect. Surfaced by the spec-derived test plan (UNREACHABLE = a devdoc field with no form control), the only gate that looks in this direction. | metadata-vs-devdoc | 2026-08-02
BoatQuery | BQ | State3 | devdoc-optional-unreachable | OUT OF SCOPE by portfolio decision (Rob 2026-08-02): multi-destination Nlets routing is not built on any provider. This is a REAL unwired optional, not a tool artefact -- verified against THIS provider's own authorities: its metadata places the field in <Set><Any> on this combination (optional, buildable) and its devdoc lists it in the bracketed optional set, so both authorities agree it could be carried. Recorded rather than built because supporting additional destination states is a product capability question, not a wiring omission, and it would add up to four more state dropdowns per affected card. Same call already applied to FL_FCIC. Rule class is 'other', so this row silences no check -- it exists so the finding is not re-derived as a defect. Surfaced by the spec-derived test plan (UNREACHABLE = a devdoc field with no form control), the only gate that looks in this direction. | metadata-vs-devdoc | 2026-08-02
BoatQuery | BQ | State4 | devdoc-optional-unreachable | OUT OF SCOPE by portfolio decision (Rob 2026-08-02): multi-destination Nlets routing is not built on any provider. This is a REAL unwired optional, not a tool artefact -- verified against THIS provider's own authorities: its metadata places the field in <Set><Any> on this combination (optional, buildable) and its devdoc lists it in the bracketed optional set, so both authorities agree it could be carried. Recorded rather than built because supporting additional destination states is a product capability question, not a wiring omission, and it would add up to four more state dropdowns per affected card. Same call already applied to FL_FCIC. Rule class is 'other', so this row silences no check -- it exists so the finding is not re-derived as a defect. Surfaced by the spec-derived test plan (UNREACHABLE = a devdoc field with no form control), the only gate that looks in this direction. | metadata-vs-devdoc | 2026-08-02
BoatQuery | BQ | State5 | devdoc-optional-unreachable | OUT OF SCOPE by portfolio decision (Rob 2026-08-02): multi-destination Nlets routing is not built on any provider. This is a REAL unwired optional, not a tool artefact -- verified against THIS provider's own authorities: its metadata places the field in <Set><Any> on this combination (optional, buildable) and its devdoc lists it in the bracketed optional set, so both authorities agree it could be carried. Recorded rather than built because supporting additional destination states is a product capability question, not a wiring omission, and it would add up to four more state dropdowns per affected card. Same call already applied to FL_FCIC. Rule class is 'other', so this row silences no check -- it exists so the finding is not re-derived as a defect. Surfaced by the spec-derived test plan (UNREACHABLE = a devdoc field with no form control), the only gate that looks in this direction. | metadata-vs-devdoc | 2026-08-02
ArticleSingleQuery | (devdoc #2) | OwnerAppliedNumber | devdoc-combo-unbuilt | NOT BUILDABLE -- metadata has no variant that accepts it. Devdoc ArticleSingleQuery lists TWO combinations: '1. (In/Out) ArticleSerialNumber, ArticleTypeCode' and '2. (In/Out) OwnerAppliedNumber, ArticleTypeCode'. This provider's metadata defines exactly ONE ArticleSingleQuery combination -- QA{ArticleSerialNumber}, MAND=[ArticleSerialNumber, ArticleTypeCode] OPT=[OwnerAppliedNumber] -- so OwnerAppliedNumber is an OPTIONAL on the serial-number path, not a search key of its own. The built QA matches that metadata exactly. Building devdoc #2 would mean dropping ArticleSerialNumber from set[], emitting a request NO metadata variant accepts. Metadata is FIELD authority (KB rule B4). Officer impact: no article search by owner-applied number alone. Found independently by audit_devdoc_optionals AND the spec-derived plan's NO-FIRE. KEYREF IS '(devdoc #2)' DELIBERATELY, not the built 'QA': an existence-class rule naming a BUILT keyRef suppresses that combination's entire comparison in audit_requirement_fidelity -- measured, it dropped branches 9 -> 8 while silencing nothing. Same convention TX_TLETS uses for its unbuilt BoatQuery #2. | metadata-vs-devdoc | 2026-08-02
