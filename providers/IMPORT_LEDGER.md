# USx JSON Import Ledger

**Single source of truth for which JSON version is installed in which tenant.**
**⚠ PRODUCTION EXISTS — TWO LIVE TENANTS, TWO PROVIDERS: CA_CLETS v2.26 at MARIPOSA and
HI_HCJDC_OFML v4.15 at HDLE.** This line used to read *"No production environments exist yet —
everything below is import/test or customer staging."* That is no longer true, and it is the single
most consequential fact on this page: a defect in a live provider reaches officers, not testers.
Any change to a provider with a LIVE row below is a coordinated re-import, not a repo action.

**THE TWO LIVE ROWS ARE IN OPPOSITE STATES AND BOTH ARE DELIBERATE — do not "fix" either:**
- **Mariposa / CA_CLETS v2.26** — production is on the CURRENT version again, aligned with repo, catalog and release line. Frozen because it is live. (Two coordinated re-imports on 2026-08-17: v2.25 for the DeviceId production fix, then v2.26 for the purpose-code split.) Superseded note: It was re-imported to v2.25 that day (Foundation AND LIVE) and the repo then moved to **v2.26** (CA Purpose Code label + the DH purpose-code split). A SECOND production re-import in one day is Rob's call, not a repo action. Was aligned with repo, catalog
  and release line. Frozen because it is live.
- **HDLE / HI_HCJDC_OFML v4.15** — production is deliberately BEHIND the current v4.20, held until
  the NCIC hit-block mapping added at v4.16/v4.18 is verified against a real hit. The published
  artifact is intentionally ahead of the tenant. **Consequence while it is held: HDLE production
  discards NCIC hit content** (banner, caution/medical, extradition, warrant detail). That is the
  pre-existing portfolio-wide behaviour, not a new defect — but it is the reason the one-hit
  verification query is the highest-value outstanding test in the repo.

**HOW BOTH LIVE ROWS CAME TO BE RECORDED — the mechanism is the point, not the anecdote.** Neither
was discoverable from the repo. The capture tool cannot reach these tenants, so nothing here is
self-verifying: both exist only because Rob reported them (2026-08-13 Mariposa, 2026-08-14 HDLE),
and the HDLE one was initially missed because "foundation and live" was read as one tenant. **If a
LIVE tenant is not written here it is invisible to every gate, every status table and every
session.** Ask explicitly at each import; never infer.

There are THREE tenant classes (a live/production class was added 2026-08-13; the two-class model
below predates it and did not contemplate production), tracked three different ways:

| Class | What it is | How its version is tracked |
|---|---|---|
| **USx Provider Tenant** | One per provider. The import/live-test tenant. The **driver capture tool is locked to these tenants only.** | **Self-verifying from logs** — the newest version with non-archived logs in `providers/<P>/logs/<Entity>/` IS the version installed on that provider's USx test tenant (you can only capture logs against the tenant the tool is pointed at). No manual entry needed; compute it. |
| **Foundation Tenant** | Customer staging (e.g. Newark, Balcones Heights). The capture tool **cannot** reach these. | **Manual** — recorded here only from an actual import report (never from an "update X" instruction). This section is the durable home that memory used to hold. |
| **LIVE / Production Tenant** | A customer site in real service. Added 2026-08-13 with Mariposa (CA_CLETS); **HDLE (HI_HCJDC_OFML) added 2026-08-14 — there are TWO.** Recorded in section B, tagged **LIVE**. | **Manual**, same rule as Foundation — an actual import report only. **The difference is not how it is tracked but what it costs to be wrong:** a Foundation tenant is staging, a LIVE tenant is officers. Treat a LIVE version as frozen; do not bump a provider that has a LIVE row without saying so explicitly and getting the re-import coordinated. **A LIVE tenant can also be deliberately held BEHIND the current version** (HDLE is) — so a LIVE row being out of date is not automatically drift; read its note before acting. |

> **Derivation rule (USx Provider Tenants):** `logs at version X = proof X is installed on that provider's USx test tenant.` Recompute the column below anytime with `tools/portfolio_status.ps1` (its log-derived USx-tenant-test column) or the one-liner at the bottom of this file. Do NOT hand-maintain it — logs are the truth.

---

## A. USx Provider Tenants (one per provider)

Version column = newest-logged version (= proven install). "repo" = current versioned root JSON.
When repo > tenant, the newer build has been built but not yet re-imported+captured on the tenant.

| Provider | URL | DEX | Installed (logged) | Repo | Note |
|---|---|---|---|---|---|
| NJ_NJCJIS | usx-nj-njcjis.mark43.com | DEX-988 | **v4.17** | **v4.17** | **JSON attached to DEX-988 and the provider catalog updated 2026-08-20 (Rob) -- ticket AND catalog both on v4.17.** **v4.17 IMPORTED and TENANT-VERIFIED ALL-PASS 5/5 (41 logs, 41 PASS / 0 FAIL) on 2026-08-20.** Release line **comment 802981** (NEW; 795856 retained as the v4.16 history). **THE FIRST TIME IN THIS PROVIDER'S HISTORY THAT A MIDDLE NAME OR SUFFIX REACHES THE STATE** -- `NameMiddle`/`NameSuffix` controls added, composed into the `Name` attribute, and carried in `FULL`'s `any[]` (NOT `set[]`: the metadata mandates the COMPOSITE `Name`, so a driver with no middle name must stay searchable -- fidelity 10 branches / 0 UNDER / 0 OVER). Wire-proven: `FULL_af_NameMiddle` -> `DOE, JOHN A`, `FULL_af_NameSuffix` -> `DOE, JOHN JR`, against a `FULL` control of `DOE, JOHN`. Log-derived and self-verifying: 41 non-archived logs at v4.17, four log gates 41/41 each (6c content, 6d metadata, 2i attribution -- the named combo is what fired -- plan completeness ALL-PASS 5/5). 8 CommSys combos, all reachable. Validator 61P/0F/0W, enforce 47 PASS / 0 FAIL / 0 WARN. ⚠️ **TWO PROCESS FINDINGS WORTH KEEPING:** (1) the PLAN HAD TO BE REGENERATED before driving -- `reset_test_package` ran BEFORE the `any[]` entries were added, so the plan carried no name-component tests and a sweep would have passed 40/40 while proving nothing about the fix that caused the bump; the two proof tests are AUTO-GENERATED from `any[]` membership, not hand-written. (2) inflation 2/0/0/0, structural not a capture error -- `QA_af_ImageIndicator == QA_any` and `QB_af_ImageIndicator == QB_any` are byte-identical because those queries have exactly ONE `any[]` field, so 39 of 41 logs are distinct tests. Prior: v4.16 IMPORTED + TENANT-VERIFIED 2026-08-14, 40 logs, NCIC Image default `Y` on Vehicle/Firearm/Article/Boat |
| FL_FCIC | usx.fl-fcic.mark43.com (`usx.` dot form) | DEX-971 | **v7.24 (118 logs)** | **v7.24** | **v7.24 IMPORTED + TENANT-VERIFIED 2026-08-17 -- ALL-PASS 118/118** (Veh 20 / Per 27 / Gun 15 / Art 16 / Boat 40), four log gates 118/118 each, inflation 0/0/0/0, 30 combos all reachable. **Middle name + suffix now reach the wire on DL, DH and Boat** -- all four `<Name>` shapes emitted and correctly formed: `DOE, JOHN A JR` x4 (all components), `DOE, JOHN A` x4 (middle only), `DOE, JOHN JR` x4 (suffix without middle, degrading with no double space and no stray comma -- the AP #15 separator proof, `@(', ')` -> `@(', ',' ',' ')`), `DOE, JOHN` x9 (neither), with **0 `<Name>` on any OLN or non-name combination**. **THIS RESTORES CONTROLS THIS REPO DELETED at v7.17** (commit 56b8b7ca, 2026-08-02): they were genuinely unwired and `audit_wiring_closure` was right that their values went nowhere, but FL metadata declares request `Name` with FOUR components on DriverLicenseQuery, DriverHistoryQuery and BoatQuery alike -- that gate answers "is it wired?", only the metadata answers "should it exist?". Rob reversed the removal 2026-08-17. `nameSuffixDH` is NEW, not restored -- FL had no DH suffix control even before v7.17. No `set[]` touched and every addition is `any[]`-only, so **no routing changed**. Found by `audit_name_components.ps1` (new 2026-08-17); no prior gate could see it, because the test plan is generated FROM the JSON, so a missing control produces no test and can never fail. Release line posted as NEW comment 800053; 790815 retained as v7.23 history. Prior: **v7.23 IMPORTED + TENANT-VERIFIED 2026-08-12 -- ALL-PASS 110/110** (Veh 20 / Per 21 / Gun 15 / Art 16 / Boat 38), four log gates 110/110 each, submitted-vs-captured reconciled per entity. **Three contested decisions WIRE-PROVEN:** Boat Stolen Check 'Y' on 24 QB wires (18 Y / 6 N toggle / 0 absent); the BQ-ahead-of-QB reorder RECOVERED `BQBoatHullIdNumber` + `BQRegistrationNumber`, both fired where the prefill had shadowed them dead; `<State>NJ</State>` on 7/7 DH wires with 0 carrying FL, upholding FCIC's destination rule. **The sweep also produced LIMITATION #40 (LIVE-PROVEN): the wire is a UNION across every MATCHING combination, not the firing combo's field list** -- 38/38 Boat logs predicted, 0 mispredicted. Consequence: v7.23 removed `ImageIndicator` from all 4 FBQ combos (correct per metadata FBQ `<Any>` and devdoc items 1-4) yet 5 of 8 FBQ wires still carry it, because those fills also match a QB combination. Prior: **v7.23 BUILT 2026-08-12 -- RE-IMPORT + full 116-test re-sweep OWED.** Firearm row re-order (Gun Make to the first cell of row 2, beneath the Serial Number it qualifies; NCIC Image takes its row-1 slot) **and NCIC Image defaults to `Y` on all five entities** -- 5 form controls + all 25 combo `defaults[]` (CAD ignores form `initialValue`, so a form-only flip leaves CAD queries on `N`). Measured safe first: `ImageIndicator` is in 0 `set[]` / 0 conditions of 36 combinations, and reachability 30/30 + prefill-shadow 92 pairs 0 FAIL are unchanged; no devdoc "must be filled if Image = Y" conditional. **⚠️ ROB'S RULING OWED, gate NOT silenced:** `audit_cross_provider` hard-codes `Vehicle ImageIndicator='N'`, so FL now carries a permanent `[WARN] (expected 'N')`; a 20-provider sweep found every provider on Person=`Y` / others=`N`, so FL is the sole exception. The other 19 carry `[FLAG:ncic-image-default-y-everywhere]` and take it at their OWN rebuild -- which BLOCKS their enforce PHASE 1 meanwhile (verified on NJ), so 6 tenant-verified providers are un-done until then. **Also OPEN, not fixed:** metadata's FBQ `<Any>` omits `ImageIndicator` yet all 4 built FBQ combos carry it in `any[]`+`defaults[]` -- a real OVER-PERMIT invisible to `audit_requirement_fidelity` (`$formOnly` whitelist). Prior: **v7.20** -- cosmetic/layout pass on Rob's rendered-form review, **zero wire change**: four rows retired (Vehicle VIN-Seq joins the Decal row; Firearm two visible rows; Article Serial+Type+OAN on top with the rest combined below; Boat State/Stolen/NCIC Image lifted from the bottom to directly under the identifier row). DH State relabelled `Destination State (required, not FL)` -- both facts are needed, since "State (required)" on a Florida form invites the ONE destination FCIC says KQ cannot take. **Two requested items were REFUSED BY THE SOURCES and measured, not argued:** (a) an FL default on DH State -- FCIC 2026-06-12, "the destination to be something other than FL"; (b) a Boat Stolen Check default -- injected into a replica (all 3 layout variants, asserted in the reparsed JSON) and the owning gates returned `[FAIL] 4 dead combination(s) of 30 checked` at the SAME denominator as the clean baseline: FBQ Hull/Reg self-unsatisfiable and QB shadowing BQ Hull/Reg. I had predicted 2, so this was worth measuring. `audit_layout_flow` 4 findings -> 1, and that one was verified PRE-EXISTING by re-running the gate against the v7.19 artifact from git. **NOTHING ABOUT v7.20 IS WIRE-VERIFIED -- Rob's own sweep settles it** ("once we test live(not with you) we can finailize any assumptions"). Prior: **v7.19 BUILT 2026-08-12 and superseded the same day before any sweep;** Stolen Check defaults to 'Y' on Firearm + Article (form initialValue AND the combo defaults[] CAD twin -- CAD ignores form initialValue, so a form-only default would give CAD-originated queries no stolen check at all). **BOAT deliberately excluded:** there the same field IS the routing discriminator, gated EXISTS on QB and NOT_EXISTS on FBQ, so a default would kill the ordinary Boat registration search (BUILD_RULES 24). Prior: current (tenant-complete 2026-08-06, ALL-PASS 116/116, four log gates green). DEX-1283 fix: removed initialValue='X' from Attention (DH) + Requestor (VehReg/DH/Gun/Article/Boat, 6 hidden feeders) -- both confirmed resolving the real officer name on the wire with nothing in the source field. Full re-sweep: Vehicle 20 / Person 21 / Firearm 15 / Article 16 / Boat 44, matching the pre-rebuild v7.17 count exactly. Note: this tenant sends ORI/Mnemonic=CA1234567 regardless of provider |
| HI_HCJDC_OFML | usx-hi-hcjdc-ofml.mark43.com | DEX-1257 | **v4.20 (50 logs)** | **v4.20** | **v4.20 IMPORTED + TENANT-VERIFIED 2026-08-17 -- ALL-PASS 50/50** (Veh 16 / Per 18 / Gun 6 / Art 3 / Boat 7), four log gates 50/50, inflation 0/0/0/0, 12 combos all reachable. Middle name + suffix now reach the wire on the DL and DH NAME searches (`DQ`/`KQ` emit `DOE, JOHN A JR`) and deliberately NOT on the OLN lookups (`DQN`/`KQN`), whose metadata variants define no Name field -- 0 malformed names and 0 `<Name>` on OLN combos across all 50 logs. Release line posted as NEW comment 799997; 795241 retained as v4.18 history. Prior: **v4.18 IMPORTED + TENANT-VERIFIED 2026-08-14 -- ALL-PASS 46/46** (Veh 16 / Per 14 / Gun 6 / Boat 7 / Art 3), four log gates 46/46 each, inflation 0/0/0/0. Covers three bumps in one sweep: **v4.16** mapped the NCIC hit block (+24 response attributes, zero request change -- HI had been discarding wanted/missing-person hit content), **v4.17** took `[FLAG:ncic-image-default-y-everywhere]`, **v4.18** added `ScarMarkTattooCode`, the second spelling v4.16 missed. **The v4.17 default is WIRE-PROVEN, not assumed:** Vehicle wires carry `ImageIndicator=Y` x8 and `=N` x8 (the deliberate toggle tests) -- a stale pre-v4.17 build would have shown 16 `N`, so this doubles as the discriminator proving the right build was installed. ⚠️ **The hit block itself is NOT yet rendering-verified** -- no test query returned a real NCIC hit, so the 25 new QRDM attributes are proven present in the config and unexercised on a live response. Prior: current (tenant-complete 2026-08-10, ALL-PASS 46/46, four log gates green, inflation 0/0/0/0). DEX-1283 fix: removed the Attention `'X'` — form `initialValue` **and** both KQ/KQN combo `defaults[]`; HI was the last provider carrying it. **The re-sweep's discriminating observation came back POSITIVE: `<Attention>SGAMBELLONE R</Attention>` on 9/9 DH wires with no prefill and no default**, so the `any[]` membership alone feeds the handler and the v2.9 gate-feeder theory is refuted on this provider's own wires. Caveat kept deliberately: the CAD `defaults[]` half of the fix is verified by inspection only — no form-driven log exercises the CAD path |
| NY_NYSPIN_EJUSTICE | usx-ny-nyspin-ejustice.mark43.com | DEX-969 | **v4.24 (75 logs)** | **v4.24** | **v4.24 COVERAGE COMPLETED 2026-08-18 -- ALL-PASS 75/75** (Veh 19 / Per 33 / Gun 5 / Art 4 / Boat 14), four log gates 75/75 each, enforce 43 PASS / 0 provider-scoped FAIL-or-WARN. **NO JSON CHANGE and NOTHING TO IMPORT** -- same v4.24 build; only the test package grew, by the 6 Person tests that exercise the Name composite MIDDLE and SUFFIX components for the first time on this provider. WIRE-PROVEN across all three name-bearing combos (DLICN / DALH / DALHOUT): "DOE, JOHN" / "DOE, JOHN A" / "DOE, JOHN JR" / "DOE, JOHN A JR", degrading with no double space. The comma-space IS correct here (NCIC form) and deliberately differs from TX_TLETS, whose Texas DL search requires LASTNAME,FIRSTNAME with no space. Retires [FLAG:nameparts-untested-unfrozen]. **No Foundation or LIVE tenant carries NY**, so the provider tenant is the whole footprint. Prior: **v4.24 IMPORTED + TENANT-VERIFIED 2026-08-14 -- ALL-PASS 69/69** (Veh 19 / Per 27 / Gun 5 / Art 4 / Boat 14), four log gates 69/69 each, inflation 0/0/0/0, parity at rest (69 files / 69 stamps / 69 PASS). **NCIC Image now defaults to 'Y' on Vehicle, Article and Boat** -- form `initialValue` + the combo `defaults[]` CAD twin on all 9 carrying combos. WIRE-PROVEN BY RATIO INVERSION: Vehicle Y11/N8, Article Y2/N2, Boat Y6/N8 (inverted from v4.23), Person Y15/N12 UNCHANGED (already 'Y' on both `ImageIndicator` and `ImageIndicatorDH`), Firearm 0/0 because NY builds no Firearm image control -- that row confirms no field was added that the metadata does not define. **Three NY-specific behaviours re-verified in the same sweep:** `DALHOUT`+`DALLOUT` submit and resolve with `RegistrationStateDH=NJ` (the paths v4.20 left unsubmittable and v4.21 fixed); FIVE DL/DH isolation guardrails each resolving to a distinct pairing; both Vehicle home-state STRIP tests pass. Prior: current (tenant-complete 2026-08-07, ALL-PASS 69/69 at v4.23). DEX-1283 fix landed at v4.21 as a `requestorDH` set[]->any[] demotion, NOT the plain 'X' removal that worked on TX/FL/CA_CLETS -- on NY the field is metadata-mandatory in set[], so set[] membership makes the BROWSER gate Send and v4.20 left DALHOUT permanently unsubmittable. DEX-1284: Purpose Code dropdown reverted (LIMITATION #39, renders empty on this tenant); Vehicle home-state strip kept and live-proven |
| TX_TLETS | usx-tx-tlets.mark43.com | DEX-967 | **v4.21 (96 logs)** | **v4.21** | **v4.21 IMPORTED + TENANT-VERIFIED 2026-08-18 -- ALL-PASS 96/96** (Veh 19 / Per 38 / Gun 10 / Art 8 / Boat 21), four log gates 96/96 each, inflation 0/0/0/0, enforce 41 PASS / 0 provider-scoped FAIL-or-WARN. Release line **comment 800608** (NEW comment; 790861 retained as the v4.20 history). COSMETIC/layout only, NO wire change -- combination requirements byte-identical to v4.20, fidelity 19 branches 0/0, devdoc 20 compared / 0 unbuilt. L7 `MI` -> `Middle Name` on nameMiddle + nameMiddleDH; hidden Attention row moved to the bottom of the DH card; L2 messageKey-above-NameLast recorded as a DEX-1283 override rather than "fixed". **NAME COMPONENTS WIRE-PROVEN FOR THE FIRST TIME ON THIS PROVIDER:** `DOE,JOHN` / `DOE,JOHN A` / `DOE,JOHN JR` / `DOE,JOHN A JR`, degrading with no double space -- and the missing space after the comma is CORRECT per the TCIC/TLETS manual Part 1 p125 (`LASTNAME,FIRSTNAME`), NOT the `', '` that 18 of 20 providers use. Retires `[FLAG:nameparts-untested-unfrozen]`. Count moved 92 -> 96: Person +6 name-part tests, Vehicle -1 and Boat -1 vacuous guardrails dropped at the plan generator (a byte-identical duplicate pair, and a guardrail whose fills equalled its base combo test) -- all 19 combos still covered. **TX IS ALIGNED ON EVERY AXIS AT v4.21** -- repo v4.21 · provider tenant v4.21 (96 logs) · Balcones Heights TX Foundation v4.21 · DEX-967 attachment + catalog v4.21 · release line comment 800608. Foundation and catalog confirmed by Rob 2026-08-18; this row briefly recorded them as OPEN (correctly -- they are MANUAL records that only Rob can confirm, and asserting an install nobody reported is the worse error). Lockstep: TX_TLETS_CCH v1.17, BASE-SYNC v4.21, built + gated, still never tenant-tested. Prior: **v4.20 IMPORTED + TENANT-VERIFIED 2026-08-14 -- ALL-PASS 92/92** (Veh 20 / Per 32 / Gun 10 / Art 8 / Boat 22), four log gates 92/92 each, inflation 0/0/0/0, parity checked at rest (92 files / 92 stamps / 92 PASS / 0 dup filenames). **NCIC Image now defaults to 'Y' on Firearm, Article and Boat** -- form `initialValue` + the combo `defaults[]` CAD twin on all 10 carrying combos. WIRE-PROVEN BY RATIO INVERSION per entity: Firearm Y6/N4, Article Y4/N4, Boat Y12/N10 (all inverted from v4.19, where the default was 'N' and the toggle filled 'Y'), with Person Y22/N10 UNCHANGED as the control and Vehicle 0/0 because TX builds no Vehicle image control -- that last row confirms the change did not add a field the metadata does not define. TX's devdoc DOES carry a "Must be filled if ImageIndicator = Y" conditional (EmailAddress + ReasonCode) but it belongs to DriverHistoryQuery, i.e. Person, already 'Y' and auto-populated -- cleared by SCOPE, by locating the owning heading, not by assumption. Lockstep: TX_TLETS_CCH v1.16, BASE-SYNC bumped. Prior: current (tenant-complete 2026-08-06, ALL-PASS 92/92 at v4.19). DEX-1283 fix: removed initialValue='X' from Attention (DH) + EmailAddress (DL+DH) hidden feeders + matching combo defaults[] -- both confirmed resolving the real officer name/email on the wire with nothing in the source field. Lockstep: TX_TLETS_CCH v1.15 same fix, same day (built + gated, not yet tenant-tested) |
| CA_CLETS | usx-ca-clets.mark43.com | DEX-976 | **v2.26 (111 logs)** | **v2.26** | **v2.26 TENANT-VERIFIED 2026-08-17 -- ALL-PASS 111/111** (Veh 25 / Per 51 / Gun 13 / Art 10 / Boat 12), four log gates 111/111, inflation 0/0/0/0, 27 combos all reachable. Purpose-code SPLIT wire-proven: `<CaRequestPurposeCode>C` + `<PurposeCode>I` in ONE request, impossible before this version. CA Purpose Code label on all 6 cards + the DH double-mapped purpose code SPLIT (`purposeCodeNletsDH` now feeds `<PurposeCode>`; they shared one control and could never differ). Prior: **v2.25 TENANT-VERIFIED 2026-08-17 -- ALL-PASS 109/109** (Veh 25 / Per 49 / Gun 13 / Art 10 / Boat 12). Two fixes, both wire-proven: `<Authentication>/<DeviceId>` populated on 109/109 (the CA devdoc header field, required under mnemonic pooling -- this was a PRODUCTION FAILURE at Mariposa), and middle name + suffix now reach `<Name>` on all five name paths, each emitting all four shapes with DH isolation held. Prior: v2.24 (tenant-complete 2026-08-06, ALL-PASS 90/90). DEX-1283 fix: removed initialValue='X' from the Attention (DH) hidden feeder + matching combo defaults[] -- confirmed resolving the real officer name on the wire with nothing in the source field. Full re-sweep matches the pre-rebuild v2.23 count exactly |
| AZ_AZDPS | usx-az-azdps.mark43.com | DEX-974 | **v3.11 (58 logs)** | **v3.11** | **v3.11 IMPORTED + TENANT-VERIFIED -- ALL-PASS 58/58** (Veh 9 / **Per 29** / Gun 6 / Art 3 / Boat 11), four log gates 58/58, inflation 0/0/0/0, 15 combos. **Person grew 21 -> 29 on 2026-08-17: AZ RAN THE PORTFOLIO'S DISCRIMINATING NAME-PARTS TEST** and it came back POSITIVE -- `FormatStringRuleHandler` emits all four components (`DOE, JOHN A JR`) and degrades cleanly to `DOE, JOHN JR` with no double space when the middle name is absent. AZ was chosen for it precisely because it has NO Foundation and NO LIVE tenant, so a bump costs no coordinated re-import. That single result is what unblocked the middle/suffix work on FL v7.24, CA_CLETS v2.25, HI v4.20 and IL v2.8 -- the alternative reading (`DOE, JOHN`, handler drops components 3-4) would have made it a platform LIMITATION to raise instead of a wiring job. **This row read `50 logs / Per 21` until 2026-08-17** -- written before that re-sweep and never updated; corrected from `report_test_status` + a disk count, not from memory. Prior: **v3.11 IMPORTED + TENANT-VERIFIED 2026-08-12 -- ALL-PASS 50/50** (Veh 9 / Per 21 / Gun 6 / Art 3 / Boat 11), four log gates 50/50, inflation 0/0/0/0. **OUT-OF-STATE DRIVER HISTORY NOW WORKS AND IS WIRE-PROVEN** -- `<State>NJ</State>` on 7 of 7 DH logs, where at v3.9 the hidden AZ-pinned control made it unreachable. `<Attention>SGAMBELLONE R</Attention>` x7 still resolves; Stolen Check on the wire as both Y x10 and N x10; `<UserName>MK43RS</UserName>` 50/50. v3.11 restored the DH State default that v3.10 wrongly dropped (a set[] field with no value gates the browser Send button -- the NY v4.20 mechanism). Prior: v3.10 (cosmetic/layout pass, never swept -- superseded same day). The bump archived the v3.9 package (55 logs -> `logs/<Entity>/_archive_pre_v3.10/`), which is the accepted cost. **The WIRE IS PROVABLY UNCHANGED** -- all three bundles' QIDMs are byte-identical to v3.9, so this is layout and labels only. Prior: **v3.9 IMPORTED 2026-08-11 and SWEPT — ALL-PASS 55/55, first full sweep this provider has ever had.** The install is now **log-proven**, not reported: 55 non-archived v3.9 logs across all 5 entities, attack-B fingerprint 55/55 matching. Prior to today the tenant sat on v3.6. ⚠️ **CORRECTED 2026-08-11: this row previously read "nothing has ever been installed on this tenant", which was FALSE** — three independent artifacts refute it: `docs/reference/TENANT_PICKLISTS.json` is stamped `version=3.6` (a picklist scope capture requires a rendered form in the tenant), `logs/Person/_archive_pre_v3.6/` holds real v3.6 captures (`AZ_AZDPS_v3.6_ACWL.txt`, three DQ guardrail logs, …), and AZ's own BUILD_NOTES says at v3.7 *"Requires a tenant RE-IMPORT: v3.6 is installed and its Person logs are archived as evidence of a mispredicting plan."* The later BUILD_NOTES entries then cite THIS row's wrong claim back as authority ("NOT imported (IMPORT_LEDGER section A: installed = none)") — a records loop, and exactly why a tracking file is a claim and the artifact is the evidence. **v3.6 → v3.9 is a WIRE change** (v3.8 removed the Attention `'X'` and four Vehicle over-permits), so capturing against the installed v3.6 would file v3.9-named logs for a different payload — `audit_log_inflation` attack B by construction. 55 tests owed; first FULL sweep (Person was partially swept at v3.6). **Picklists are v3.6-era and `entities=Vehicle` ONLY** — the other four entities' dropdowns have never been scoped. v3.9 (2026-08-10) = DEX-1284 `NCIC Image` label; v3.8 = DEX-1283 Attention `'X'` removal + four Vehicle over-permits removed. DEX-974 had no JSON attached as of 2026-07-22 and carries no changelog comment to date. Confirm `dexStateUserId` populates on the FIRST query or all 5 badge combos silently fall back |
| OH_LEADS | usx-oh-leads.mark43.com | DEX-990 | none | **v2.10** | **JSON attached to DEX-990 and the provider catalog updated 2026-08-20 (Rob) -- ticket AND catalog both on v2.10.** **v2.10 IMPORTED and TENANT-VERIFIED ALL-PASS 5/5 (56 logs, 56 PASS / 0 FAIL) on 2026-08-20.** COSMETIC ONLY -- the parenthetical helper clause stripped from all 5 card titles that carried one (ARTICLE never had one). **NO wire change, and it is WIRE-PROVEN rather than asserted:** all 56 captured wires are byte-identical to their v2.9 archived counterparts with transaction ids normalised (56 identical / 0 different / 0 unpaired), the PROVIDER bundle hashes identically (sha 829d0311, 28647 chars both sides), and ARTICLE -- the single card not edited -- is the unchanged-fingerprint control. Log-derived and therefore self-verifying: 56 non-archived logs at v2.10, four log gates green (6c 56/56, 6d 56/56, 2i 56/56 attribution, plan completeness ALL-PASS 5/5), 21 CommSys combos all covered and all reachable, validator 78P/0F/0W, enforce 45 PASS / 0 provider-scoped FAIL-or-WARN. Release line **comment 802507** (NEW comment; 801112 retained as the v2.9 history, 801113 is the history anchor). **Three caveats carried over from v2.9 UNCHANGED and re-disclosed rather than dropped** -- a cosmetic release being the easiest place for a stale caveat to vanish: inflation 2/0/0/0 (RN_af_AddressCounty == RN_any, ATDP == ATDP_guardrail_vs_RQ.P, so 54 of 56 logs are distinct tests); the ATDP dealer-plate guardrail reproduces its base test wire so it confirms routing but adds no independent evidence; Middle/Suffix still unexercised (composed into Name but in no set[]/any[], wire reads `DOE, JOHN`) -- making that provable is a wire change. **One usability consequence recorded in BUILD_NOTES, not argued away:** OH sets no RegistrationState default (separate in/out-of-state keyRefs), so the removed title text had been the officer's only cue that blank State means Ohio; routing unaffected, cue now absent. Prior: **v2.9 IMPORTED and TENANT-VERIFIED 2026-08-18 -- this provider's FIRST EVER tenant install and first ever sweep**, 56/56, route v2.5 NCIC Image `Y` -> v2.6 ImageQuery REMOVED by directive (6 of 7 devdoc-Basic built BY DECISION) -> v2.7 middle/suffix controls + Stolen Check `Y` -> v2.8 never shipped -> v2.9 operator field-order pass + Purpose Code `C`. That sweep's FIRST attempt produced NO evidence and was discarded (plan carried blank fieldId/value, driver filled `undefined`, 3 RN tests never submitted); fixed as TEST DATA not a build change, and `test_phase2` step [2] gained a real fillability gate that now blocks the class |
| IL_LEADS_OFML | usx-il-leads-ofml.mark43.com | DEX-984 | **v2.8 (44 logs)** | **v2.8** | **v2.8 IMPORTED + TENANT-VERIFIED 2026-08-17 -- ALL-PASS 44/44** (Veh 14 / Per 12 / Gun 6 / Art 3 / Boat 9), four log gates 44/44 each, inflation 0/0/0/0, 9 combos all reachable. **Middle name + suffix now reach the wire on the DL name search (`Z2.N`)** -- all four `<Name>` shapes emitted and correctly formed: `DOE, JOHN A JR` (all components), `DOE, JOHN A` (middle only), `DOE, JOHN JR` (suffix without middle, degrading with no double space and no stray comma -- the AP #15 separator proof, `@(', ')` -> `@(', ',' ',' ')`), `DOE, JOHN`. **`<Name>` appears on `Z2.N` ONLY** -- 7 logs, zero on `Z2.O` (OLN) or any other entity, so identifier priority held. These controls are **NEW on IL, not a restoration** (unlike FL/OR, IL never carried them); single name path, so one row, one composite, one combination `any[]`. No `set[]` touched, every addition `any[]`-only, so **no routing changed**. Found by `audit_name_components.ps1` (new 2026-08-17). The sweep also absorbed a plan growth from 41 -> 44: Person +2 (the middle/suffix toggles) and Firearm +1 (valuing `firearmMake` in `_combo_value_resolver` on 2026-08-17, which had left IL reading PARTIAL). Release line posted as NEW comment 800073; 795242 retained as v2.7 history. **This row was BADLY STALE until 2026-08-17** -- it read installed `v2.2 (41 logs, ARCHIVED)` / repo `v2.3` with a re-import owed, through five version bumps; the 44 non-archived v2.8 logs are the install proof, since this tenant is log-derived. Prior: **v2.3 BUILT 2026-08-12 -- RE-IMPORT + full 41-test re-sweep OWED.** Stolen Check now defaults to 'Y' on ALL FOUR entities that carry it (Vehicle/Person/Firearm/Boat), form initialValue AND the combo defaults[] CAD twin on all 8 carrying combos. Safe: any[]-only everywhere, so no routing moved -- IL's Vehicle discriminator is RegistrationState (Z2.P EXISTS vs Z5 NOT_EXISTS) and is untouched. IL was the widest gap of the two non-conformant providers. Prior: **was current — tenant-complete 2026-08-07, ALL-PASS 41/41, four log gates green.** v2.2 imported by Rob and swept the same day: Vehicle 14 / Person 10 / Boat 9 / Firearm 5 / Article 3, 0 FAIL / 0 PENDING. Gates: 6c 41/41 content, 6d 41/41 metadata, 2i 41/41 attribution, plan completeness 5/5 entities. **All three identifier-priority guardrails are now LIVE-PROVEN on the wire**, which matters because v2.2 removed their on-screen hints: plate+VIN → `<LicensePlateNumber>` rides and `<VehicleIdentificationNumber>` is absent; OLN+Name → `<OperatorLicenseNumber>` rides and the literal `<Name` appears **0** times; hull+reg → `<BoatHullIdNumber>` rides and `<RegistrationNumber>` **0**. The cosmetic pass was label-only in fact, not just in intent. **The composite Name still serialises LAST-first** — `<Name>DOE, JOHN</Name>` on all five `Z2.N` logs despite the form now showing First before Last (form order and QIDM `sourceField` order are independent). Tenant picklists captured with v2.2 loaded: 18/18 dropdowns, 0 capture errors; the three 300-row truncation WARNs (`VehicleMakeCode`, `articleTypeCode`, `firearmMake`) were all resolved by live fill during the sweep — `CNST_FORD - FORD`, `BBICYCL - Bicycle`, `11 - 11 mm Mauser` all selected cleanly. Earlier history: v2.1 was installed first (evidenced by the picklist capture returning v2.1-only labels), then v2.2 over it; an early revision of this row wrongly said "no USx tenant provisioned", inferred from this ledger's own silence rather than verified. Capture extension covers this host as of manifest 0.4.0 |

All other providers (CA_CLETS_OCATS, CA_CONTRA_COSTA, CA_SAN_LUIS_OBISPO, CA_VENTURA_COUNTY,
CA_eSUN, LA_LEMS, MD_METERS, NM_NMLETS_OFML, OR_LEDS, TN_TIES,
TX_TLETS_CCH): **0 logs at any version → never installed on a USx test tenant.** Galvanized/built
in repo, awaiting first tenant import. (OH_LEADS was in this collapsed list until 2026-08-10; it now
has its own row above because its tenant host and DEX ticket were recovered. The others still have no
recorded tenant URL — that is a gap in this ledger, not evidence a tenant does not exist.)

---

## B. Foundation Tenants (customer staging — MANUAL, from import reports)

| Tenant | Provider | Version | Imported | Notes |
|---|---|---|---|---|
| Newark Foundation | NJ_NJCJIS | **v4.17** | **2026-08-20** | current -- **v4.16 -> v4.17, imported by Rob and reported same day**, in the same pass as the DEX-988 release line (comment 802981) and the catalog update. Picks up the name-component change: **a Newark officer can now enter a middle name and a suffix on a Driver License search, and both reach the state** -- before v4.17 the controls did not exist and the wire could only ever carry `DOE, JOHN`. Backed by the v4.17 sweep: ALL-PASS 41/41, four log gates 41/41, wire-proven `DOE, JOHN A` / `DOE, JOHN JR`. **NJ IS NOW ALIGNED ON EVERY AXIS at v4.17** -- repo v4.17 - provider tenant v4.17 (41 logs) - this Foundation tenant v4.17 - DEX-988 attachment + catalog v4.17 - release line comment 802981. Prior: v4.15→v4.16, imported by Rob and reported same day, in the same pass as the DEX-988 release-line edit and the catalog update. Picks up the NCIC-image change: **Vehicle, Firearm, Article and Boat now request an NCIC image by default** (form `initialValue` + the combo `defaults[]` CAD twin on all 6 carrying combos), which this provider had never done on those four entities. Backed by the v4.16 sweep, 40/40 ALL-PASS, with the change wire-proven by RATIO INVERSION on every affected entity (Vehicle Y9/N4, Firearm Y4/N2, Article Y1/N2, Boat Y4/N4 — at v4.15 those were reversed). Prior: v4.15 (2026-08-03), v4.14→v4.15 after the 36/36 re-sweep, picking up the Boat QBN `RegistrationNumber` fix — a hull search transmits the reg number instead of silently dropping it (devdoc BoatQuery #1), wire-proven in that sweep |
| Bert Anzini USx test tenant | NJ_NJCJIS | v4.9 | 2026-07-20 | **frozen on purpose** — CAD-config test only, any valid JSON suffices; do not flag drift |
| Miami Springs Foundation | FL_FCIC | **v7.24** | **2026-08-17** | current — v7.23→v7.24, imported by Rob after the v7.24 re-sweep (118/118 ALL-PASS). Picks up the middle-name + suffix restoration on DL/DH/Boat (`<Name>` now carries all four components; `nameSuffixDH` is new). No routing change — every addition is `any[]`-only. Prior: v7.18→v7.23 on 2026-08-13 |
| North Miami Foundation | FL_FCIC | **v7.24** | **2026-08-17** | current — v7.23→v7.24, same import pass as Miami Springs. Prior: v7.18→v7.23 on 2026-08-13 |
| Balcones Heights TX Foundation | TX_TLETS | **v4.21** | **2026-08-18** | current — **v4.20→v4.21, reported by Rob 2026-08-18** in the same pass as the DEX-967 attachment + catalog update. Picks up the v4.21 LAYOUT convergence: `MI`→`Middle Name` on both middle-name controls (the label said middle INITIAL on a 30-char field) and the hidden Attention feeder row moved to the bottom of the Driver History card. **NO wire change** — combination requirements are byte-identical to v4.20, so nothing this tenant asks of TLETS has changed. Backed by the v4.21 sweep: ALL-PASS 96/96, four log gates 96/96, inflation 0/0/0/0, release line comment 800608. Name components are wire-proven here for the first time (`DOE,JOHN A JR`), and the comma takes NO trailing space per TCIC/TLETS Part 1 p125. Prior: **v4.19→v4.20, the SECOND import to this tenant today**, reported by Rob in the same pass as the DEX-967 release-line edit and the catalog update. Picks up the NCIC-image change: **Firearm, Article and Boat now request an NCIC image by default** (form `initialValue` + the combo `defaults[]` CAD twin on all 10 carrying combos), which this provider had never done on those three entities. Person was already `Y`; Vehicle carries no image control here. Backed by the v4.20 sweep, 92/92 ALL-PASS, with the change wire-proven by RATIO INVERSION per entity (Firearm Y6/N4, Article Y4/N4, Boat Y12/N10 — inverted from v4.19) and Person Y22/N10 unchanged as the control. Prior, EARLIER THE SAME DAY: v4.19, which picked up the DEX-1283 fix: `initialValue='X'` removed from the Attention (DH) and EmailAddress (DL+DH) hidden gate-feeders plus the matching combo `defaults[]`. **Wire-neutral by design and that is the point** — the handlers resolve the real officer name/email with nothing in the source field, so the "X" this removes was only ever the raw pre-handler value visible in dex-log's Query String column. Backed by the v4.19 sweep, 92/92 ALL-PASS. **TX IS NOW ALIGNED ON EVERY AXIS** — repo v4.19 · provider tenant v4.19 · this Foundation tenant v4.19 · DEX-967 release line v4.19 (790861) · ticket attachment + catalog v4.19. (An earlier revision of this row warned that the catalog was stuck at v4.18; that came from section C being stale, not from a real gap, and Rob corrected it 2026-08-14. Removed rather than left as a false open item.) Prior: v4.18 (2026-08-03), v4.12→v4.18 after the 89/89 re-sweep, picking up v4.13 dead-RQ removal, v4.14 all-7-Vehicle-combos-reachable (prefills out), v4.15/v4.16 UI, v4.17 QV shadow removal, v4.18 17 dropped optionals |
| HDLE Foundation | HI_HCJDC_OFML | v4.15 | 2026-08-13 | ⚠️ **HELD AT v4.15 BY DECISION, 2026-08-14 -- not drift.** Rob: HDLE Foundation and live "will not be [updated] until we test it again." Repo, provider tenant, DEX-1257 and the catalog are all on **v4.18**; this tenant and live stay on v4.15 until the deferred half of that release is verified. **What is deferred and why it matters here:** v4.16/v4.18 map the NCIC hit block, which HI had been discarding entirely -- but that mapping is config-present and has NEVER been exercised against a live response, because no test query returned a real hit. Until it is, a wanted/missing-person hit returned to HDLE still loses its content on v4.15 AND would be unproven on v4.18, so holding costs nothing and swapping proves nothing. CAD functionality is being tested against this tenant, which is the second reason to coordinate rather than swap underneath them. **Re-import when the hit block is verified in the RMS UI.** HI_HCJDC_OFML's first Foundation tenant. **The only row in this table VERIFIED FROM THE TENANT ITSELF rather than from a verbal report** — Rob exported the installed config (`HDLE USx 8.13.26.json`) and it was compared against the repo build with `_json_canonical`: all 5 entity forms identical, all 6 QIDMs identical (44 combination/attribute signature lines, same keyRefs, same ORDER, same set[]/any[]/defaults[]/conditions[]), entire RMS bundle identical, and after normalising explicit nulls EVERY configuration in EVERY bundle hashes identically. Version read from the bundle descriptions: ENTITIES and provider bundle both say `v4.15`. See the verification note below |
| HDLE **LIVE** | HI_HCJDC_OFML | v4.15 | 2026-08-13 | **SECOND PRODUCTION INSTALL IN THE PORTFOLIO — recorded 2026-08-14 on Rob's report ("hdle has 4.15 live as well").** This row did not exist until now: when he said "hawaii foundation and live" earlier the same day I could not verify a live HI tenant, declined to invent the row, and flagged it — he then confirmed it. **HELD AT v4.15 BY DECISION**, same as HDLE Foundation: repo, provider tenant, DEX-1257 and the catalog are on v4.18 and this stays on v4.15 until the deferred hit-block mapping is verified. ⚠️ **THE OPERATIONAL CONSEQUENCE, STATED PLAINLY BECAUSE IT IS PRODUCTION:** v4.15 is a build that DISCARDS the NCIC hit block. Real officers querying HDLE today get a wanted/missing-person hit's *existence* but not its content — no banner, no caution/medical flag, no extradition limitation, no warrant detail. That is not a regression introduced by anything here; it is how every provider in the portfolio has always behaved, and HI v4.16 is the first fix anywhere. But on a LIVE tenant it is officers, not testers. **This is the strongest argument for prioritising the one-hit verification query** — until it passes, the fix cannot responsibly be pushed here, and until it is pushed, production keeps dropping the content. **Exact tenant name/URL NOT reported** — "HDLE LIVE" is what was said, not necessarily the tenant's name. Fill it in when known |
| Mariposa Foundation | CA_CLETS | **v2.26** | 2026-08-17 | **v2.26 IMPORTED 2026-08-17** (second import that day -- v2.25 for the DeviceId production fix, then v2.26 for the "CA Purpose Code" label + the DH purpose-code split). Aligned: repo v2.26 - this tenant v2.26 - LIVE v2.26 - DEX-976 release line v2.26 (comment 799898, posted NEW; 791400 retained as v2.25 history) - catalog v2.26. Prior: **v2.25 IMPORTED 2026-08-17**, reported by Rob same day ("maoriposa live and mariposa foundation have been updated"). Carries the CA devdoc header fix — `<Authentication>/<DeviceId>`, the agency-assigned CLETS Terminal Identifier, required wherever mnemonic pooling is used — plus middle name + suffix on all five name paths. Fully aligned: repo v2.25 · this tenant v2.25 · LIVE v2.25 · DEX-976 release line v2.25 (791400, edited in place) · catalog v2.25. **ONE THING TO READ HERE, and it is a tenant question not a JSON one:** in the USx provider tenant `<DeviceId>` resolves to the placeholder `MK43RS` (same as `UserName` and `api:Source`); at Mariposa `api:Source` carries `LAPTOP47EQ3RSJ`, so confirm `<DeviceId>` is the CLETS Terminal Identifier and not merely the workstation name. If it is the workstation name that is a device-registration config follow-up — NOT grounds to revert. Prior: v2.24 (2026-08-13, CA_CLETS's first Foundation tenant; that version was originally INFERRED from "the ca clets json" and confirmed explicitly 2026-08-14) |
| Mariposa **LIVE** | CA_CLETS | **v2.26** | 2026-08-17 | **v2.26 IMPORTED 2026-08-17 -- TWO coordinated production re-imports in one day**, both deliberate: v2.25 fixed a PRODUCTION FAILURE (missing `<Authentication>/<DeviceId>` under CLETS mnemonic pooling), then v2.26 landed the purpose-code work. v2.26 is wire-verified in the provider tenant at 111/111. **Exact tenant name/URL STILL NOT REPORTED** -- this row says "Mariposa LIVE" because that is what was said, not because it is the tenant's name. Prior: **v2.25 IMPORTED 2026-08-17** — production re-imported in the same pass as Foundation, so LIVE, Foundation, repo, release line and catalog are all on v2.25. **This bump was NOT routine: it fixed a PRODUCTION FAILURE.** Queries were failing at Mariposa because `<Authentication>/<DeviceId>` was absent — Mariposa uses CLETS mnemonic pooling, so ConnectCIC was silently falling back to the IP address of the ConnectCIC server on the state network and the state saw the wrong terminal. v2.25 is wire-verified in the provider tenant (109/109 logs carry a populated `<DeviceId>`). **⚠ FIRST PRODUCTION INSTALL IN THE PORTFOLIO**, and the reason a LIVE row is treated as frozen: real officers run real CJIS queries here, so every CA_CLETS bump is a coordinated live re-import, not a repo action. **Exact tenant name/URL still NOT reported** — this row says "Mariposa LIVE" because that is what was said, not because it is the tenant's name; fill it in when known. Rob, 2026-08-13, correcting his own earlier note: *"ca clets was importsed to mariposa foundation and live tenants  2 diffrent tenants"* — Foundation and LIVE are two separate tenants, which is the THIRD tenant class the original two-class taxonomy did not contemplate. Prior: v2.24 (2026-08-13). See the WARNING under section B |
| Lafayette Parish | LA_LEMS | **NOT OURS — hand-built by engineering** | in service as of 2026-08-13 | **The only tenant in this ledger running a JSON this repo did not build.** Copy held at `providers/LA_LEMS/source/Lafayette Parish LA_LEMS 8.13.2026.json` (Rob, 2026-08-13) as the reference to diff against if Lafayette reports a problem. Our `LA_LEMS_v3.0` is NOT installed anywhere. See the comparison in §B.1 below before answering any Lafayette question |

### B.1 Lafayette Parish LA_LEMS — the hand-built engineering JSON, and how it differs from ours

Recorded 2026-08-13. Rob supplied the deployed file so we can answer Lafayette issue reports against
what is **actually running there**, not against `LA_LEMS_v3.0`, which is installed on no tenant.
Reference copy: `providers/LA_LEMS/source/Lafayette Parish LA_LEMS 8.13.2026.json` (150 KB).

**Do not "fix" our build to match it, and do not treat it as authority.** It is a peer artifact
built by another team from the same devdoc + metadata. Where the two disagree, the devdoc and the
metadata XML decide — not this file. Its value is diagnostic: if Lafayette reports a symptom, the
difference list below is the candidate set.

| | Hand-built (deployed) | Ours (`LA_LEMS_v3.0`) |
|---|---|---|
| Top level | `{bundles[3], behaviors{}}` | `{bundles[3]}` — no `behaviors` |
| Bundle order | **`LA_LEMS` → `ENTITIES` → `RMS`** | `ENTITIES` → `LA_LEMS` → `RMS` |
| `queryLabel` | `LA_LEMS`, `LA_LEMS DL`, `LA_LEMS Driver History` | `Vehicle Registration`, `Driver License`, `Driver History`, … |
| `keyReference` style | concatenated field names (`OperatorLicenseNumberRegistrationStateAttention`) | metadata transaction keyRefs (`DP`, `DQ`, `QWA`, `QWDN`, `KQName`, `QG`, `QA`, `QB`, `BQ`) |
| DL combos | 2 | **4** (QWDN / QWA / DP / DQ) |
| Veh / DH / Boat / Gun / Article combos | 2 / 2 / 2 / 1 / 1 | 2 / 2 / 2 / 1 / 1 |
| DH `Attention` | **wired** — attribute `Attention` ← `Attention`, rule `CommsysGetLastNameFirstNameInitialRuleHandler`, in both combos' `any[]` | **absent from the DH QIDM entirely** |
| DH field isolation | shared pool (`NameLast`, `BirthDate`, …) | DH-suffixed (`NameLastDH`, `BirthDateDH`, …) |
| Boat QIF name | `ENTTIY_Boat` (transposed typo) | `ENTITY_Boat` |

**Four things worth knowing before you use this file:**

1. **It answers our 2 open LA_LEMS enforce FAILs.** `audit_devdoc_combinations` reports
   `DriverHistoryQuery #1` and `#2` as devdoc-listed but UNBUILT because mandatory `Attention` is
   wired nowhere in that query. The deployed JSON wires it exactly the way our own Automated
   Attention standard does (AP #27 feeder pattern, same handler we already run on our LA DL). That
   makes it a worked precedent for the fix — **evidence the shape is accepted in production, not
   authority that it is correct.** Still adjudicate against LA's metadata per `usx-adjudicate`.
2. **`behaviors` is real and in production.** `{"ParallelQuery": {function: parallelQueryHandler,
   isEnabled: false}}` — the top-level block documented in
   `knowledge-base/UNIVERSAL_SEARCH_HANDLERS.txt` that **no repo build emits**. Disabled here, so
   this is not evidence it does anything; it is evidence the platform accepts the key.
3. **ENTITIES is NOT first, and it is deployed. HYPOTHESIS, do not act on it.** CLAUDE.md states
   ENTITIES must be first or forms do not render (confirmed AZ v2.0). Either that rule is narrower
   than written, or Lafayette has a rendering fault nobody has reported. **Do not reorder any
   provider's bundles on the strength of this file.** Discriminating evidence would be a Lafayette
   user confirming the forms render.
4. **Its DL coverage is thinner than ours** (2 combos vs 4). If Lafayette reports "a driver search
   I expect does not run", that gap is the first place to look — not a defect in our build.

Regenerate any of the above with a JSON diff; nothing here is hand-maintained state.

### B.2 VERIFYING A FOUNDATION / LIVE INSTALL FROM A TENANT EXPORT (method, proven 2026-08-13)

The class table says the capture tool **cannot** reach Foundation or LIVE tenants, so their versions
are "manual, from an import report" — i.e. somebody's word. That is not the only option, and the
HDLE row above is the proof. **A tenant config EXPORT can be compared to the repo build directly**,
which turns a reported version into a verified one.

The method, in the order that matters:

1. **Read the version from the bundle `description`.** That is where the build stamps it
   (`Provider configuration for <PROVIDER> v<X.Y>`), and it is what enforce CHECK 3i reads. There is
   no top-level `version` field by design — the platform rejects a dotted string as `java.lang.Integer`.
2. **Do NOT compare bytes or file size.** The HDLE export is 232 KB against the repo's 936 KB and is
   the SAME CONFIG — our builds are written with very wide indentation. Size proves nothing.
3. **Compare canonically** with `tools/_json_canonical.ps1` (`ConvertTo-Canonical` + `Get-Sha256Hex`),
   per configuration, so a difference is located rather than merely detected. Reuse it; do not
   hand-roll a comparison (LAW 4).
4. **Normalise two known round-trip artifacts before concluding anything differs:**
   - **Explicit nulls.** The platform materialises omitted keys as `null` on export — HDLE carried 53
     `: null` against our 35, every one of them a `"conditions": null` on a combination that simply
     has no conditions. Semantically identical.
   - **JSON key order.** HDLE's ENTITIES `order` object lists `CAD_DISPATCH, FIRST_RESPONDER, default`
     where we write `default, CAD_DISPATCH, FIRST_RESPONDER`. Same three arrays, same contents. Key
     order in a JSON object is not semantic.

**⚠ THE RMS BUNDLE LOSES ITS VERSION ON ROUND-TRIP.** Our RMS description reads
`Provider configuration for <PROVIDER> v<X.Y> -- RMS bundle`; the HDLE export reads plain
`Provider configuration for RMS`. The platform overwrites it. So **never version-check an export off
the RMS bundle** — read ENTITIES or the provider bundle. This is the one difference that could be
mistaken for a real config drift, and it is not one.

## C. Published JSON — Jira ticket + Confluence catalog (MANUAL, from Rob's confirmation)

The THIRD destination class, added 2026-08-03. A/B above cover tenants; nothing tracked where a JSON
was *published* for other teams to pull, so "which version of FL is in the catalog" was not stale —
it was **unanswerable**, which is worse. Rob attaches the JSON to the DEX ticket and posts it to the
[USx CommSys Data Providers Catalog](https://mark43.atlassian.net/wiki/spaces/SEARCH/pages/6956810378/USx+CommSys+Data+Providers+Catalog);
the MCP tooling cannot attach files, so this is manual in both directions — recorded here on his
confirmation, never inferred from a version bump.

| Provider | Version | Attached to ticket | Posted to catalog | Notes |
|---|---|---|---|---|
| AZ_AZDPS | **v3.11** | **DEX-974, 2026-08-17** | **2026-08-17** | Rob's confirmation 2026-08-17: v3.11 JSON attached to DEX-974 and posted to the catalog. **FIRST publish AND first Jira comments this provider has EVER had** — DEX-974 carried no release statement for any version until today, while AZ sat tenant-verified. TWO comments, both new, no edits: **800100** the v3.11 release line (ALL-PASS 58/58) and **800101** the v1.0 → v3.11 version-history anchor. Posted release-first so the anchor could cite 800100's real id without an edit. Section 1 reads "initial post" rather than naming a superseded comment, because there genuinely was none. AZ has **no Foundation and no LIVE tenant**, so there is no section B or C row to match — which is exactly why AZ was chosen to run the portfolio's discriminating name-components test (a bump costs no coordinated re-import). ⚠️ DEX-974's other comment, **776896**, is Leo Hisoire's FB-6040 roadmap note — a third party's, never to be edited or counted as ours |
| FL_FCIC | **v7.24** | **DEX-971, 2026-08-17** | **2026-08-17** | after the v7.24 re-sweep (118/118 ALL-PASS); same pass as the Miami Springs + North Miami Foundation imports. Release line is DEX-971 comment **800053, posted as a NEW comment** — 790815 (v7.23) is left in place as history, per the rule Rob reversed on 2026-08-17 ("we can't keep erasing the previous posts everytime"). Supersedes the v7.23 publish below |
| FL_FCIC | v7.23 | DEX-971, 2026-08-13 | 2026-08-13 | after the v7.23 re-sweep (110/110 ALL-PASS); same pass as the Miami Springs + North Miami imports. Release line is DEX-971 comment 790815, EDITED IN PLACE from v7.18 → v7.23 (no sibling comment). Supersedes the v7.18 publish of 2026-08-06 |
| TX_TLETS | **v4.21** | DEX-967, **2026-08-18** | **2026-08-18** | Rob confirmed 2026-08-18: v4.21 JSON attached to DEX-967 and the catalog updated, same pass as the Balcones Heights v4.21 import. Release line is comment **800608, a NEW comment** (790861 retained as the v4.20 history, per the 2026-08-17 one-comment-per-release rule). Follows the 96/96 ALL-PASS v4.21 sweep. Supersedes the v4.20 publish below. PRIOR: Rob's confirmation 2026-08-14: v4.20 JSON attached to DEX-967 and the catalog updated, in the same pass as the v4.20 release line and the Balcones Heights import. Release line is comment **790861, EDITED IN PLACE** v4.19 → v4.20 (no sibling comment) — pre-edit body captured verbatim in `providers/TX_TLETS/docs/tracking/DEX_TICKET_ARCHIVE.md`. Follows the 92/92 ALL-PASS v4.20 sweep. Supersedes the v4.19 publish below |
| TX_TLETS | v4.19 | DEX-967, 2026-08-11 | 2026-08-11 | **CORRECTED 2026-08-14 on Rob's report ("tx catalog is 4.19 i think you missed that somewhere along the lines"). This row read v4.18 / 2026-08-03 and was STALE, not merely imprecise.** There was NO record of the v4.19 publish anywhere in the repo — not here, not in TX's DEX_TICKET.md, not in any commit — so it was never written down rather than written down and lost. Dated to 2026-08-11, the day comment 790861 was EDITED IN PLACE from v4.18 to v4.19 in the consolidation pass; the attachment and catalog moved with it and this section did not follow. **This is the third instance in one day of the repo's record of an EXTERNAL system being stale** (the other two: DEX_TICKET history lines saying 790861=v4.18 and 791400=v2.23, both already edited in place). The pattern is specific and worth naming: **section C and the DEX_TICKET pointer files are MANUAL, so they only ever record what somebody reported at the time — an in-place Jira edit updates the comment and nothing here.** When a release line is edited in place, update section C in the SAME pass. Prior: v4.18 (DEX-967, 2026-08-03) after the 89/89 ALL-PASS re-sweep, same pass as the then-current Balcones Heights import |
| NY_NYSPIN_EJUSTICE | **v4.24** | DEX-969, **2026-08-18** | **2026-08-18** | CONFIRMED CURRENT 2026-08-18: ticket attachment + catalog are at v4.24 and NOTHING WAS RE-UPLOADED, because the JSON never changed -- the 08-18 work was a completed test sweep (69 -> 75 logs) plus a records correction, not a build. Release line is now comment **800806**, a NEW comment naming 794205 as superseded-and-retained; 794205 had claimed TENANT-VERIFIED at 69/69 while six Person tests were still uncaptured. **No Foundation or LIVE tenant carries NY**, so the provider tenant plus this catalog entry is the entire footprint. PRIOR: v4.24 attached + catalog updated 2026-08-14 | Rob's confirmation 2026-08-14: v4.24 JSON attached to DEX-969 and the catalog updated, in the same pass as the v4.24 release line. Release line is comment **794205, EDITED IN PLACE** v4.23 → v4.24 (no sibling comment) — pre-edit body captured verbatim in `providers/NY_NYSPIN_EJUSTICE/docs/tracking/DEX_TICKET_ARCHIVE.md`, which matters more on this ticket than any other: NY carried 22 comments and EIGHT mutually inconsistent log counts before the 2026-08-11 consolidation, and the capture is what keeps superseded text recoverable now that the count lives in exactly one comment. Follows the 69/69 ALL-PASS v4.24 sweep. **No Foundation tenant carries NY, so section B stays empty by design — that is a recorded fact, not a missing row.** Supersedes the v4.23 publish below |
| NY_NYSPIN_EJUSTICE | v4.23 | DEX-969, 2026-08-07 | 2026-08-07 | after the DEX-1284 closure (69/69 ALL-PASS). Supersedes the v4.19 publish of 2026-08-03. No Foundation tenant carries NY, so there is no section B row to match |
| NJ_NJCJIS | **v4.17** | DEX-988, **2026-08-20** | **2026-08-20** | Rob's confirmation 2026-08-20: **v4.17 JSON attached to DEX-988 and the provider catalog updated**, in the same pass as the release line (comment 802981) and the Newark Foundation import. What other teams pulling this now get that v4.16 could not do: a Driver License search can carry a MIDDLE NAME and a SUFFIX, and both reach the state (`DOE, JOHN A` / `DOE, JOHN JR`, wire-proven in the v4.17 sweep). Prior: v4.16, attached + catalogued 2026-08-14 |
| CA_CLETS | **v2.26** | DEX-976, 2026-08-17 | 2026-08-17 | after the purpose-code release (111/111 ALL-PASS). Release line posted as a NEW comment 799898 -- the edit-in-place rule was REVERSED by Rob 2026-08-17, so 791400 stays as v2.25 history. Mariposa Foundation AND LIVE both re-imported to v2.26 the same day. Prior: v2.25, after the DeviceId + name-parts release (109/109 ALL-PASS). Release line 791400 EDITED IN PLACE to v2.25. Mariposa Foundation AND Mariposa LIVE both re-imported to v2.25 the same day |
| IL_LEADS_OFML | **v2.8** | **DEX-984, 2026-08-17** | **2026-08-17** | Rob's confirmation 2026-08-17: v2.8 JSON attached to DEX-984 and posted to the catalog, in the same pass as the v2.8 release line. Release line is comment **800073, posted as a NEW comment** -- 795242 (v2.7) is left in place and unedited, per the rule Rob reversed on 2026-08-17 ("we can't keep erasing the previous posts everytime"). Follows the 44/44 ALL-PASS sweep (middle name + suffix wired on `Z2.N`). **No section B row to match -- no Foundation tenant carries IL**, checked against section B rather than assumed, so the absence is a verified fact and not an omission. Supersedes the v2.7 publish below |
| IL_LEADS_OFML | v2.7 | DEX-984, 2026-08-13 | 2026-08-13 | Rob's confirmation 2026-08-13: v2.7 JSON attached to DEX-984 and posted to the catalog, in the same pass as the v2.7 release line (comment 795242, EDITED IN PLACE from v2.2 → v2.7). Follows the 41/41 ALL-PASS sweep that closed the portfolio's last owed test package. Supersedes the v2.2 publish of 2026-08-10 |
| HI_HCJDC_OFML | **v4.20** | DEX-1257, **2026-08-17** | **2026-08-17** | Rob's confirmation 2026-08-17: v4.20 JSON attached to DEX-1257 and the catalog updated. Release line = NEW comment 799997 (the edit-in-place rule was reversed by Rob 2026-08-17, so 795241 stays as v4.18 history). **NOTE: HDLE Foundation AND LIVE remain deliberately HELD at v4.15** pending the NCIC hit-block verification, so the published artifact is intentionally AHEAD of production -- that is a decision, not drift. Prior: v4.18 | DEX-1257, 2026-08-14 | 2026-08-14 | Rob's confirmation 2026-08-14: v4.18 JSON attached to DEX-1257 and the catalog updated, in the same pass as the v4.18 release line. Release line is comment **795241, EDITED IN PLACE** v4.15 → v4.18 (no sibling comment) — pre-edit body captured verbatim in `providers/HI_HCJDC_OFML/docs/tracking/DEX_TICKET_ARCHIVE.md`. Follows the 46/46 ALL-PASS v4.18 sweep. ⚠️ **THE PUBLISHED ARTIFACT IS AHEAD OF THE TENANTS ON PURPOSE.** Rob 2026-08-14: HDLE Foundation and live are **NOT** being updated to v4.18 until it is re-tested. So catalog/ticket = v4.18 while HDLE Foundation = v4.15 — a DELIBERATE, TRACKED gap, not drift. The reason is the deferred half of this release: v4.16/v4.18's NCIC hit-block mapping is config-present and has never been exercised against a live response, and those tenants are where CAD testing runs. **The Jira comment likewise omits the hit block** ("leave hit block out for now. We can update once we verify"). Supersedes the v4.15 publish below |
| HI_HCJDC_OFML | v4.15 | DEX-1257, 2026-08-10 | 2026-08-10 | Rob's confirmation 2026-08-10: v4.15 JSON attached to DEX-1257 and the catalog updated, in the same pass as the v4.15 changelog + release line (comment 795241). Follows the 46/46 ALL-PASS re-sweep that settled DEX-1283 on the wire (9/9 DH `<Attention>` resolved with no prefill, no default). Supersedes the v4.14 publish of 2026-08-04. No Foundation tenant carries HI |
| HI_HCJDC_OFML | v4.14 | DEX-1257, 2026-08-04 | 2026-08-04 | after the 46/46 ALL-PASS re-sweep. **No version bump** — v4.14 was already published; re-published as the re-verified artifact. Last of the original six |
| IL_LEADS_OFML | v2.2 | DEX-984, 2026-08-10 | 2026-08-10 | Rob's confirmation 2026-08-10. First publish for this provider — DEX-984 had zero comments and zero attachments before today. Follows the 41/41 ALL-PASS first-ever tenant sweep (v2.2) and the DEX-1284 convention pass + card collapse. No Foundation tenant carries IL, so there is no section B row to match |

> **TENANT INFO STAYS OFF THE TICKETS (Rob, 2026-08-03, restated).** Nothing in section B or C goes
> into a Jira comment — not the attachment, not the catalog post, not a Foundation import. **This
> overrides the ticket's own precedent:** DEX-967's v4.10 and v4.12 comments both end with an
> explicit `IMPORT: ... Balcones Heights` line, and v4.11 warned that Balcones had fallen behind, so
> the convention on that ticket was to publish it. It was raised and Rob confirmed: leave it off.
> A future session reading those older comments will think an IMPORT line is expected — it is not.

**Maintenance:** add/bump a row in A/B/C ONLY when an import or publish is actually reported (per
`project_deployment_tracking` memory: never record from an "update X to latest" instruction alone).
Deployment architecture (which provider goes to which foundation) → KB `TESTING_REQUIREMENTS.txt`
Section 17.

---

## Recompute Section A (logs → installed version)

```bash
cd providers
for p in */; do p="${p%/}"; [ -d "$p/logs" ] || continue
  ver=$(ls "$p"/logs/*/*.txt 2>/dev/null | grep -v _archive | grep -oE "_v[0-9]+\.[0-9]+_" | sort -uV | tail -1 | tr -d '_v')
  cnt=$(ls "$p"/logs/*/*.txt 2>/dev/null | grep -v _archive | grep -E "_v${ver}_" | wc -l)
  printf "%-22s installed=%-8s (%s logs)\n" "$p" "${ver:-none}" "$cnt"
done
```

_Last reconciled: 2026-08-07._

### TX_TLETS_CCH -- built, NOT YET IMPORTED (recorded 2026-07-30, TX_TLETS half corrected 2026-08-03)

- **TX_TLETS v4.18 -- SUPERSEDED, this row was WRONG.** It read "**not imported to any tenant** /
  tenant re-sweep owed from T1" as of 2026-07-30, and then the sweep happened: `usx-tx-tlets` now
  carries **89 v4.18 logs, ALL-PASS 89/89**, which the table above records. Logs ARE the install
  proof for a USx provider tenant, so v4.18 is installed and this section contradicted its own
  table for four days. Left in place rather than deleted because the contradiction is the lesson:
  a hand-written "not imported" note does not expire on its own, while the log-derived table does
  update — **when the two disagree, the table wins.**
- **TX_TLETS_CCH v1.14** -- built + gated, **never tenant-tested, not imported.** BASE-SYNC v4.18.
  Still accurate: 0 logs at any version.

Recorded because `audit_lifecycle.ps1` (enforce PHASE 2r) treats SILENCE as the defect: "built but
not imported" is a perfectly good state and must be WRITTEN, so that "where is v4.18 installed" is
never answered from memory.


### B.2 PRODUCTION TENANTS RUNNING NON-REPO, CLAUDE-BUILT JSONs — support to be assumed

Recorded 2026-08-20 on Rob's report: *"live production enviroment uses a non repo claude built json
at San Diego Sheriff's office. it is an eSun provider. We have the same situation with la lems at
LAfeyyete parish and New Oreleans PD. We will need to assume support for those providers. My plan is
to ingest what was built aand go from there in terms of fixing issues."*

**THIS CHANGES THE RISK ON TWO PROVIDERS THAT THE BUILD QUEUE CALLED "FREE".** CA_eSUN and LA_LEMS
have no logs in this repo, so a rebuild costs no re-sweep — but they are NOT greenfield. Real officers
are running DIFFERENT builds of both today, and we are taking over support. Our JSON is not the only
thing that has to be right; it has to be reconcilable with what is deployed.

| Tenant | Provider | Status | Reference copy held? |
|---|---|---|---|
| **San Diego Sheriff's Office** | CA_eSUN | **LIVE PRODUCTION**, non-repo build | **YES** — `providers/CA_eSUN/source/San Diego Sheriff CA_eSUN 8.17.2026.json` |
| **Lafayette Parish** | LA_LEMS | LIVE, hand-built by engineering | YES — `providers/LA_LEMS/source/Lafayette Parish LA_LEMS 8.13.2026.json`, analysed in §B.1 |
| **New Orleans PD** | LA_LEMS | **LIVE PRODUCTION**, non-repo build | **NO — NOT YET SUPPLIED.** Ask before answering any NOPD issue report |

**The San Diego file was nearly discarded.** It sat untracked as `department-export-71459778290 (1).json`
and I twice advised leaving it alone as scrap; it was even reported as the sole blocker keeping
`enforce`'s repo-wide git check at exit 1. It is in fact the production reference for a LIVE tenant —
the same class of artifact as Lafayette's, which has been tracked and analysed since 08-13. Renamed to
match that convention and committed.

**What it already tells us, measured 2026-08-20 (diagnostic only — NOT authority):** it carries
**2 bundles, no RMS bundle at all** (ours has 3), its description says `Provider configuration for
CA eSUN` with **no version string**, and its VehicleRegistration keyRefs are descriptive
(`RQLicensePlateTypeYearOut`) where ours are short (`RQ.P`). It spells the purpose field `PurposeCode`
where the CLETS family uses `CaRequestPurposeCode`, and omits `LicensePlateTypeCode` from that combo's
`set[]`. Its plate combo is structurally safe for the same reason ours is — after prefills the
variable set reduces to `[Plate, State]`.

**RULES FOR THESE FILES, same as §B.1:** the devdoc and metadata XML remain the authority for what we
BUILD; a deployed file is evidence of what a tenant is RUNNING, never a spec to copy. Its value is
diagnostic — when San Diego or Lafayette or NOPD reports a symptom, diff their build against ours and
the difference is the first place to look. **Do not "align" our JSON to a deployed file** on the
strength of the file alone.
