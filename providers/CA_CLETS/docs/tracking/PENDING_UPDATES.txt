# CA_CLETS PENDING UPDATES
#
# CLEARED 2026-07-31 at v2.23. Every item was resolved or registered with a cited authority.
# Kept as a record of what closed and why -- a bare empty file loses the reasoning.
#
# [FLAG:FORM-REACHABLE-COMBOS]  RESOLVED -- and the flag's DIAGNOSIS WAS WRONG for CA_CLETS.
#   It claimed "purposeCode=C (12 combos)" prefill-dead. But purposeCode is in EVERY CA combo's
#   set[], so prefilling it satisfies that field equally for all of them and cannot shadow one over
#   another. The 12 IV.4x/IL.A1 plate-type sub-variants are unreachable because IA.QV's set[] is a
#   strict SUBSET of theirs -- structural first-match shadowing, present with or without the
#   prefill. All 12 are registered; audit_query_trace reports 0 PREFILL-DEAD (recoverable).
#   Removing the prefill would have recovered NOTHING and broken CAD injection on every CA combo,
#   because CAD does not apply form initialValues (BUILD_RULES 12). A flag that names the wrong
#   cause is worse than no flag: it points the next session at a fix that makes things worse.
#
# [FLAG:UNDER-REQUIRED-CHOICE-BRANCH]  RESOLVED at v2.23 -- this one was REAL.
#   IG.QGH was set[purposeCode,Name] any[BirthDate,age], satisfying NO metadata variant: #4 requires
#   SexCode; #5/#6 put Choice[Age|BirthDate] INSIDE Set, which makes one of them MANDATORY. Split
#   into IG.QGH.A (Age) + IG.QGH.B (BirthDate), devdoc #2-before-#3. This is what made gate 6d FAIL
#   on 1 of 90 v2.22 logs while 6c and 2i both passed it -- content and attribution cannot see a
#   missing REQUIREMENT.
#
# [OPEN 2026-07-31] 2 UNDER / 7 OVER  RESOLVED -- four real fixes, five registrations:
#   REAL  IR.QVC.O no longer requires criminalIdNumber (metadata variant #3 makes CII/SSN OPTIONAL);
#         new IR.QVC.OS carries the SSN branch; ID.L1 gained socialSecurityNumber NOT_EXISTS. That
#         trio closed devdoc DriverLicenseQuery #6 "+[SocialSecurityNumber]" SILENTLY DROPPING the
#         officer's SSN -- the form accepted it and the wire never carried it.
#   REAL  NLTS.DQ.N gained SexCode in any[] (metadata variant 2 permits it; it was carried nowhere).
#   REAL  IR.QVC.C dropped 'age' -- neither metadata variant nor devdoc #5 defines it.
#   REG   IR.QVC.N / NLTS.DQ.N "under-required" discriminators: a LOOSER metadata variant makes them
#         legitimately optional. Promoting either into set[] would make the common Name+Sex and
#         Name+State searches unreachable.
#   REG   BoatQuery #4 and DriverHistoryQuery #1 NO-COMBO-FIRES: devdoc-vs-metadata conflicts where
#         metadata (field authority) requires more than the devdoc's optional brackets imply.
#         Unfixable without emitting requests the metadata calls invalid.
#   REG   VehicleRegistrationQuery #1 (FileCode/InfoCode): buildable, deferred on a product call --
#         needs two code values an officer would not have and no CAD field supplies.
#
# [FLAG:ncic-image-default-y-everywhere]  WITHDRAWN 2026-08-13 -- NOT APPLICABLE to CA_CLETS.
#   Rob's rule (2026-08-12) is that NCIC Image defaults to 'Y' on every entity. CA_CLETS has no
#   such field to default, so there is nothing to apply and nothing owed:
#     - 0 ImageIndicator form controls and 0 QIDM references in CA_CLETS_v2.24.json. Its single
#       occurrence is a QUERYRESULTDATAMAPPING targetField (sourceField 'Image') -- a RESPONSE
#       mapping every provider inherits from the shared Build-CommsysQrdm, not an input.
#     - CA_CLETS.xml defines ImageIndicator in exactly 4 places, ALL inside
#       CCHCriminalHistoryFQQuery and CCHCriminalHistoryIQQuery -- CCH transactions this provider
#       does not build. None of its 6 devdoc-Basic queries define the field, so adding a control
#       would OVER-PERMIT against field authority. Verified in the raw <Requirements>, the
#       sanctioned raw-XML exception for this question.
#   So this is NOT a deferral and NOT a skip -- there is no defect and no future work. Do not
#   re-flag CA_CLETS for this rule, and do not "fix" it by adding an ImageIndicator control.
#
#   HOW IT GOT HERE, because the mechanism is the real defect: flag_pending_fix.ps1 appended
#   without a leading newline onto a file whose last line was a '#' comment with no trailing
#   newline, so the flag became part of that comment. enforce.ps1 L207 skips lines starting with
#   '#', which means CA_CLETS reported NO unresolved items and PASSED PHASE 1 while every other
#   flagged provider was blocked. The outcome was accidentally correct here; the mechanism is not.
#   Found by the resume-sweep link/flag integrity check, not by any gate.
#   The flag text's claim "Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y'
#   and Vehicle/Firearm/Article/Boat='N'" is FALSE for the six CA providers, which carry no such
#   control at all -- treat that sentence as unverified wherever else it appears.
[FLAG:plan-dedupe-vacuous-tests] emit_test_plan.ps1 gained two plan-only filters 2026-08-21: (1) DUPLICATE -- drop a test whose (entity, fill-set) is byte-identical to an earlier one, which makes the generator agree with audit_log_inflation attack A by construction; (2) NO-CONTEST GUARDRAIL -- drop a guardrail whose fill adds no competing identifier over the winner's own combo test, because the pair differs only by a presence condition and the loser can never match. YOUR PLAN WILL SHRINK when you regenerate; measured non-destructively on 2026-08-21: AZ -5, CA_CLETS -11, CA_SLO -1, CA_VENTURA -2, FL -14, HI -1, IL -1, LA -1, MD -3, NJ -2, TN -1, TX_TLETS_CCH -4. NO COVERAGE IS LOST -- identical fill means identical wire, and NY held 16/16 combos at 100% across the change. NOT DONE FOR YOU ON PURPOSE: if you are tenant-verified, regenerating now makes your existing logs ORPHAN (more logs than plan tests) and drops you out of ALL-PASS, which is why this is a flag and not a sweep. Do it at your OWN next rebuild, and archive the superseded duplicate logs in the same pass (NY put them in logs/<Entity>/_archive_dedupe_v<ver>/). Reference: NY_NYSPIN_EJUSTICE v4.26, 73 -> 65 tests, inflation A 8 -> 0, four log gates 65/65. (flagged 2026-08-21)
