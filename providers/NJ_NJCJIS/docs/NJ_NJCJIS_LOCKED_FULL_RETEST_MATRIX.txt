NJ_NJCJIS -- FULL RETEST MATRIX (COMPLETE BASELINE)
====================================================
Generated: 2026-05-18
JSON: NJ_NJCJIS_BASE.json (deployed v3.0, locked v3.1)
Purpose: Complete retest of ALL combos with XML evidence -- fresh baseline

QIDMs: 6 | Combos: 13 | Visible fields: 28 | Hidden fields: 0
Entities: Vehicle (9 fields), Person (7), Firearm (5), Article (3), Boat (3)

Every test requires server log XML pasted into the log file.
post_test.ps1 -XmlRequest is MANDATORY -- blocks PASS without XML.
Use -Negative flag ONLY for tests where no request is sent.

================================================================================
VEHICLE -- 9 fields, 2 QIDMs, 7 combos
================================================================================
  Fields on form:
    licensePlateNumber        Text     maxLen=10
    registrationState         Select   STATE/NCIC             default=NJ
    randomRequest             Select   YES_NO_UNKNOWN/NCIC    default=N
    licensePlateTypeCode      Select   NCIC_LICENSE_PLATE_TYPE default=PC
    licensePlateYear          Text     maxLen=4               default=2026
    vehicleIdentificationNumber Text   maxLen=20
    ncicNumber                Text     maxLen=10
    vehicleMakeCode           Select   VEHICLE_MAKE/NCIC
    imageIndicator            Select   YES_NO_UNKNOWN/NCIC    default=N

  VehicleRegistrationQuery -- 4 combos (RQ, RQ_RAND, RQN, RQN_RAND)
  --------------------------------------------------------------------------

  T1   RENDER
       Action: Open Vehicle form. Screenshot or note all fields, types, defaults.
       Verify: 9 fields visible. State=NJ, RandomRequest=N, PlateType=PC,
               PlateYear=2026, ImageIndicator=N. VehicleMakeCode dropdown loads.
               VehReg checkbox auto-selected. VehStolen checkbox unchecked.

  T2   RQ BASIC -- plate only + defaults
       Fill: licensePlateNumber=<any plate>
       Leave: all defaults (State=NJ, Random=N, PlateType=PC, Year=2026, Image=N)
       Expect: VehicleRegistrationQuery fires, keyRef=RQ
       XML verify: LicensePlateNumber, State=NJ, RandomRequest=N,
                   LicensePlateTypeCode=PC, LicensePlateYear=2026, ImageIndicator=N
       RMS verify: vehicle.tag + vehicle.registrationStateAttrIds

  T3   RQ ALL ANY[] -- plate + all fields populated
       Fill: licensePlateNumber + vehicleMakeCode=FORD (or any make)
       Leave: all defaults
       Expect: RQ fires with all any[] fields in XML
       XML verify: all T2 fields + VehicleMakeCode absent (Make not on RQ combo)
       NOTE: VehicleMakeCode is NOT in VehReg any[]. It should NOT appear.

  T4   RQ_RAND -- plate + RandomRequest=Y (conditions routing)
       Fill: licensePlateNumber=<any plate>
       Change: RandomRequest from N to Y
       Leave: other defaults
       Expect: VehicleRegistrationQuery fires, keyRef=RQ_RAND
       XML verify: RandomRequest=Y, conditions block routes stolen-only
       RMS verify: vehicle.tag + vehicle.registrationStateAttrIds

  T5   RQ OOS -- plate + State=MA (out of state)
       Fill: licensePlateNumber=<any plate>
       Change: registrationState from NJ to MA
       Leave: other defaults
       Expect: RQ fires with State=MA
       XML verify: State=MA (not NJ)
       RMS verify: vehicle.registrationStateAttrIds = MA attribute ID (not NJ)

  T6   RQ ImageIndicator change -- plate + Image=Y
       Fill: licensePlateNumber=<any plate>
       Change: imageIndicator from N to Y
       Leave: other defaults
       Expect: RQ fires
       XML verify: ImageIndicator=Y (not default N)

  T7   RQN BASIC -- VIN only + defaults
       Fill: vehicleIdentificationNumber=<any VIN>
       Leave: all defaults, licensePlateNumber EMPTY
       Expect: VehicleRegistrationQuery fires, keyRef=RQN
       XML verify: VehicleIdentificationNumber, State=NJ, RandomRequest=N, ImageIndicator=N
                   LicensePlateNumber ABSENT (not in RQN set/any)
       RMS verify: vehicle.vinNumber + vehicle.registrationStateAttrIds

  T8   RQN_RAND -- VIN + RandomRequest=Y
       Fill: vehicleIdentificationNumber=<any VIN>
       Change: RandomRequest from N to Y
       Leave: licensePlateNumber EMPTY
       Expect: VehicleRegistrationQuery fires, keyRef=RQN_RAND
       XML verify: VehicleIdentificationNumber, RandomRequest=Y

  T9   RQN OOS -- VIN + State=NY
       Fill: vehicleIdentificationNumber=<any VIN>
       Change: registrationState from NJ to NY
       Leave: licensePlateNumber EMPTY
       Expect: RQN fires with State=NY
       XML verify: State=NY
       RMS verify: vehicle.registrationStateAttrIds = NY attribute ID

  T10  PRIORITY -- plate + VIN both filled
       Fill: licensePlateNumber + vehicleIdentificationNumber
       Leave: all defaults
       Expect: RQ fires first (plate combo higher priority than VIN combo)
       XML verify: check which keyRef appears. Both RQ and RQN may fire.
       NOTE: Document whether 1 or 2 transactions sent.

  VehicleStolenQuery -- 3 combos (QVP, QVV, QVN)
  --------------------------------------------------------------------------

  T11  QVP BASIC -- plate + Stolen checked
       Action: Check VehStolen checkbox, enter licensePlateNumber
       Fill: licensePlateNumber=<any plate>
       Expect: VehicleStolenQuery fires, keyRef=QVP
       XML verify: MessageType=VehicleStolenQuery, LicensePlateNumber,
                   State=NJ, ImageIndicator=N
       RMS verify: vehicle.tag + vehicle.registrationStateAttrIds

  T12  QVP + Make any[] -- plate + VehicleMakeCode + Stolen
       Fill: licensePlateNumber + vehicleMakeCode=FORD, Stolen checked
       Expect: QVP fires, Make appears in XML
       XML verify: VehicleMakeCode=FORD (any[] field on QVP)

  T13  QVP + VIN any[] -- plate + VIN + Stolen
       Fill: licensePlateNumber + vehicleIdentificationNumber, Stolen checked
       Expect: QVP fires, VIN appears as any[] field
       XML verify: VehicleIdentificationNumber present alongside LicensePlateNumber

  T14  QVV BASIC -- VIN + Stolen checked
       Fill: vehicleIdentificationNumber, Stolen checked, plate EMPTY
       Expect: VehicleStolenQuery fires, keyRef=QVV
       XML verify: VehicleIdentificationNumber, ImageIndicator=N
       RMS verify: vehicle.vinNumber

  T15  QVN BASIC -- NCIC number + Stolen checked
       Fill: ncicNumber, Stolen checked, plate EMPTY, VIN EMPTY
       Expect: VehicleStolenQuery fires, keyRef=QVN
       XML verify: NCICNumber, ImageIndicator=N
       RMS verify: NONE (NCIC# has no RMS mapping)

  Cross-QIDM and Deselect behavior
  --------------------------------------------------------------------------

  T16  v3.1 DESELECT -- VehReg stays, Stolen deselects VehReg
       Action: Enter plate with defaults. VehReg auto-selected. Then check Stolen.
       Expect: Stolen checkbox deselects VehReg (one-directional).
               Only VehicleStolenQuery fires. No error popup.
       XML verify: Only VehicleStolenQuery transaction in XML. No VehicleRegistrationQuery.

  T17  CROSS-QIDM -- Can VehReg + VehStolen co-fire?
       Action: Enter plate. VehReg selected. Check Stolen.
       Observe: Does checking Stolen deselect VehReg? If yes, they can't co-fire.
       Document: exact behavior of checkbox interaction.
       NOTE: v3.1 one-directional deselect means Stolen DESELECTS VehReg.
             To co-fire, user must re-check VehReg after checking Stolen.
             Test: check Stolen, then re-check VehReg. Submit.
       XML verify: Both transactions if possible, or document that platform prevents it.

================================================================================
PERSON -- 7 fields, 1 QIDM, 2 combos
================================================================================
  Fields on form:
    operatorLicenseNumber     Text     maxLen=20
    registrationState         Select   STATE/NCIC             default=NJ
    imageIndicator            Select   YES_NO_UNKNOWN/NCIC    default=Y
    nameFirst                 Text     maxLen=30
    nameLast                  Text     maxLen=30
    birthDate                 FormDate
    sexCode                   Select   SEX/NIBRS

  DriverLicenseQuery -- 2 combos (DQ, DQN)
  --------------------------------------------------------------------------

  T18  RENDER
       Action: Open Person form. Note all fields, types, defaults.
       Verify: 7 fields visible. State=NJ, ImageIndicator=Y.
               SexCode dropdown has M/F/U. BirthDate is date picker.

  T19  DQN BASIC -- OLN + State (set[] = operatorLicenseNumber, registrationState)
       Fill: operatorLicenseNumber=<any OLN>, registrationState=NJ (default)
       Leave: all other fields empty
       Expect: DriverLicenseQuery fires, keyRef=DQN
       XML verify: OperatorLicenseNumber, State=NJ, ImageIndicator=Y
       RMS verify: dlNumber + registrationStateAttrId

  T20  DQN OOS -- OLN + State=NY
       Fill: operatorLicenseNumber=<any OLN>
       Change: registrationState from NJ to NY
       Expect: DQN fires with State=NY
       XML verify: State=NY (not NJ)
       RMS verify: registrationStateAttrId = NY attribute ID

  T21  DQ BASIC -- Name + DOB (set[] = birthDate, nameLast, nameFirst)
       Fill: nameLast=<name>, nameFirst=<name>, birthDate=<date>
       Leave: OLN empty, State=NJ, Image=Y
       Expect: DriverLicenseQuery fires, keyRef=DQ
       XML verify: Name (formatted Last,First), BirthDate (MMddyyyy), State=NJ, ImageIndicator=Y
       RMS verify: firstName + lastName + dateOfBirth + registrationStateAttrId

  T22  DQ + SexCode any[] -- Name + DOB + Sex
       Fill: nameLast, nameFirst, birthDate, sexCode=F (or M)
       Expect: DQ fires, SexCode appears in XML
       XML verify: SexCode=F present in request
       RMS verify: sexAttrId populated

  T23  DQ + ImageIndicator change -- Name + DOB + Image=N
       Fill: nameLast, nameFirst, birthDate
       Change: imageIndicator from Y to N
       Expect: DQ fires
       XML verify: ImageIndicator=N (not default Y)

  T24  DQ OOS -- Name + DOB + State=NY
       Fill: nameLast, nameFirst, birthDate
       Change: registrationState from NJ to NY
       Expect: DQ fires with State=NY
       XML verify: State=NY
       RMS verify: registrationStateAttrId = NY attribute ID

  T25  DQ ALL ANY[] -- Name + DOB + Sex + State + Image (all populated)
       Fill: nameLast, nameFirst, birthDate, sexCode=M, registrationState=NJ, imageIndicator=Y
       Expect: DQ fires with all fields
       XML verify: all 5 attributes present

  T26  PRIORITY -- OLN + Name + DOB all filled
       Fill: operatorLicenseNumber + nameLast + nameFirst + birthDate + State=NJ
       Expect: DQN fires first (OLN combo, higher priority)
       XML verify: Check transaction count. DQN should fire. Does DQ also fire?
       NOTE: Document whether both combos fire or only DQN.

================================================================================
FIREARM -- 5 fields, 1 QIDM, 1 combo
================================================================================
  Fields on form:
    serialNumber              Text     maxLen=11
    firearmMake               Select   NCIC_FIREARM_MAKE/NJ_NIBRS
    gunCaliber                Select   NCIC_FIREARM_CALIBER/NJ_NIBRS
    gunModel                  Text     maxLen=20
    imageIndicator            Select   YES_NO_UNKNOWN/NCIC    default=N

  GunQuery -- 1 combo (QG), set[]=[serialNumber], any[]=[gunCaliber,firearmMake,gunModel,imageIndicator]
  --------------------------------------------------------------------------

  T27  RENDER
       Action: Open Firearm form. Note all fields.
       Verify: 5 fields visible. firearmMake dropdown loads. gunCaliber dropdown loads.
               ImageIndicator=N default.

  T28  QG BASIC -- serial only
       Fill: serialNumber=<any serial>
       Leave: all others empty/default
       Expect: GunQuery fires, keyRef=QG
       XML verify: GunSerialNumber, ImageIndicator=N

  T29  QG + Make any[] -- serial + firearmMake
       Fill: serialNumber + firearmMake=<select any>
       Leave: caliber empty, model empty
       Expect: QG fires, GunMake in XML
       XML verify: GunMake present, GunCaliber absent, GunModel absent

  T30  QG + Model any[] -- serial + gunModel
       Fill: serialNumber + gunModel=<any text>
       Leave: make empty, caliber empty
       Expect: QG fires, GunModel in XML
       XML verify: GunModel present, GunMake absent

  T31  QG ALL ANY[] -- serial + make + caliber + model + image
       Fill: all 5 fields
       Expect: QG fires with all attributes
       XML verify: GunSerialNumber, GunMake, GunCaliber, GunModel, ImageIndicator

================================================================================
ARTICLE -- 3 fields, 1 QIDM, 1 combo
================================================================================
  Fields on form:
    serialNumber              Text     maxLen=20
    articleTypeCode            Select   NCIC_ARTICLE_TYPE/CA_CLETS
    imageIndicator            Select   YES_NO_UNKNOWN/NCIC    default=N

  ArticleSingleQuery -- 1 combo (QA), set[]=[serialNumber,articleTypeCode], any[]=[imageIndicator]
  --------------------------------------------------------------------------

  T32  RENDER
       Action: Open Article form. Note all fields.
       Verify: 3 fields visible. articleTypeCode dropdown loads (CA_CLETS source).
               ImageIndicator=N default.

  T33  QA BASIC -- serial + type
       Fill: serialNumber=<any>, articleTypeCode=<select any>
       Leave: imageIndicator=N default
       Expect: ArticleSingleQuery fires, keyRef=QA
       XML verify: ArticleSerialNumber, ArticleTypeCode, ImageIndicator=N

  T34  QA + ImageIndicator change -- serial + type + Image=Y
       Fill: serialNumber, articleTypeCode
       Change: imageIndicator to Y
       Expect: QA fires
       XML verify: ImageIndicator=Y (not default N)

================================================================================
BOAT -- 3 fields, 1 QIDM, 2 combos
================================================================================
  Fields on form:
    registrationNumber        Text     maxLen=20
    imageIndicator            Select   YES_NO_UNKNOWN/NCIC    default=N
    boatHullIdNumber          Text     maxLen=20

  BoatQuery -- 2 combos (QB, QBN), QB set[]=[registrationNumber], QBN set[]=[boatHullIdNumber]
  --------------------------------------------------------------------------

  T35  RENDER
       Action: Open Boat form. Note all fields.
       Verify: 3 fields visible. ImageIndicator=N default.

  T36  QB BASIC -- registration number
       Fill: registrationNumber=<any>
       Leave: imageIndicator=N, boatHullIdNumber EMPTY
       Expect: BoatQuery fires, keyRef=QB
       XML verify: RegistrationNumber, ImageIndicator=N

  T37  QBN BASIC -- hull ID
       Fill: boatHullIdNumber=<any>
       Leave: imageIndicator=N, registrationNumber EMPTY
       Expect: BoatQuery fires, keyRef=QBN
       XML verify: BoatHullIdNumber, ImageIndicator=N

  T38  QB + ImageIndicator change -- reg + Image=Y
       Fill: registrationNumber
       Change: imageIndicator to Y
       Expect: QB fires
       XML verify: ImageIndicator=Y

  T39  PRIORITY -- reg + hull both filled
       Fill: registrationNumber + boatHullIdNumber
       Expect: QB fires first (reg combo higher priority)
       XML verify: which keyRef fires. Document if both fire.

================================================================================
NEGATIVE TESTS
================================================================================

  T40  VEHICLE NEGATIVE -- empty form
       Action: Open Vehicle form, do not fill anything. Attempt submit.
       Expect: Send button disabled or no transaction sent.
       Log with: -Negative flag

  T41  PERSON NEGATIVE -- Name only (no DOB)
       Fill: nameLast + nameFirst only. No DOB, no OLN.
       Expect: DQ does NOT fire (missing set[] field birthDate). No transaction.
       Log with: -Negative flag

  T42  VEHICLE NEGATIVE -- VIN only, Stolen unchecked
       Fill: vehicleIdentificationNumber only. VehReg selected (default), Stolen unchecked.
       Expect: RQN fires (VIN is set[] for RQN). VehStolen does NOT fire.
       XML verify: Only VehicleRegistrationQuery, keyRef=RQN. No VehicleStolenQuery.

================================================================================
SUMMARY
================================================================================

  Total tests: 42
  Combo coverage: 13/13 (100%)
  any[] field coverage: every any[] field tested in isolation + together
  OOS coverage: Vehicle RQ + RQN, Person DQN + DQ (4 OOS tests)
  Priority routing: Vehicle plate+VIN, Person OLN+Name+DOB, Boat reg+hull
  Cross-QIDM: VehReg + VehStolen deselect + co-fire attempt
  Negatives: empty form, missing set[] field, cross-QIDM isolation
  RMS co-fire: Vehicle (tag, vinNumber, registrationStateAttrIds),
               Person (dlNumber, firstName, lastName, dateOfBirth, sexAttrId, registrationStateAttrId)

  EXECUTION ORDER:
    Vehicle T1-T17  (render, all VehReg combos, all VehStolen combos, deselect, co-fire)
    Person  T18-T26 (render, DQN combos, DQ combos, OOS, priority)
    Firearm T27-T31 (render, basic, any[] isolation, all any[])
    Article T32-T34 (render, basic, ImageIndicator change)
    Boat    T35-T39 (render, basic QB+QBN, ImageIndicator, priority)
    Negatives T40-T42

  RULES:
    - Full page reload between every test (clear form bug)
    - XML from server logs into post_test.ps1 -XmlRequest
    - No PASS without XML (post_test.ps1 enforces this)
    - Use -Negative for T40, T41 only
    - Save and commit after EVERY test, not in batches
