NJ_NJCJIS -- SUPPORTED QUERY VALIDATION REPORT (SQVR)
======================================================
Last updated: 2026-05-04
JSON version: v2.0 BASE / v2.0-mc MC
Validator: 64 PASS / 0 FAIL / 3 WARN / 2 LIMITATION (2026-05-04, monorepo re-validation)
Live test: BASE 17/17 PASS, MC 57/57 PASS (2026-04-29)
Simulation: BASE 11/11 FIRE, MC 11/11 FIRE (2026-04-30)

NOTE: Live tests (2026-04-29) were performed without per-test XML log captures.
[CONFIRMED] markers are based on test summary results.
CommSys simulation logs with XML captures added 2026-04-30 (tests/ directory).

KNOWN LIMITATIONS (validator [LIMITATION] items, no action needed):
  1. DL combo order: DQN(2 set) before DQ(3 set) -- correct by operational priority (OLN before Name)
  2. Vehicle dual QIDMs: no queriesToDeselect -- by design, RandomRequest gates co-fire (LIMITATION #24)

FIXES APPLIED 2026-04-30:
  - AUTH combo: added keyReference "AUTH"
  - RMS Person/Vehicle QIDMs: added autoSelect=true
  - Validator: tuned 3 false positives (PlateType alt fieldId, VehicleYear result-only, ParallelQuery optional)

================================================================================
1. VehicleRegistrationQuery -- Vehicle Entity
================================================================================

  QIDM: VehicleRegistrationQueryQIDM
  QIF:  Vehicle
  Combos: RQ (plate+state), RQN (VIN)
  Optional fields: PlateYear, VehicleMakeCode, VehicleModelCode, ImageIndicator

  RQ -- Plate + State + PlateType + RandomRequest [CONFIRMED]
    set: licensePlateNumber, RegistrationState, LicensePlateType, RandomRequest
    any: PlateYear, VehicleMakeCode, VehicleModelCode, ImageIndicator
    keyReference: RQ
    Defaults: State=NJ, PlateType=PC, PlateYear=2026, RandomRequest=N, Image=N
    NCIC state reverse-lookup: NJ=69509884952, PA=69509887513

  RQN -- VIN + RandomRequest [CONFIRMED]
    set: VehicleIdentificationNumber, RandomRequest
    any: PlateYear, VehicleMakeCode, VehicleModelCode, ImageIndicator
    keyReference: RQN

  OOS -- Plate with non-NJ state [CONFIRMED]
    State=PA tested, correct keyRef selected

  Priority -- Plate+VIN both filled [CONFIRMED]
    RQ fires (plate wins), RQN does not override

  RMS co-fire [CONFIRMED]
    RMS Vehicle combo fires alongside CommSys on plate and VIN paths

================================================================================
2. VehicleStolenQuery -- Vehicle Entity
================================================================================

  QIDM: VehicleStolenQueryQIDM
  QIF:  Vehicle (shared with VehicleReg)
  Combos: QVN (NCIC#), QVP (plate), QVV (VIN)

  QVN -- NCIC Number [CONFIRMED]
    set: NCICNumber
    any: ImageIndicator
    keyReference: QVN

  QVP -- Plate [CONFIRMED via co-fire]
    set: licensePlateNumber, RegistrationState, LicensePlateType
    any: ImageIndicator
    keyReference: QVP
    Co-fires with VehicleReg RQ on shared fields (LIMITATION #1)

  QVV -- VIN [CONFIRMED via co-fire]
    set: VehicleIdentificationNumber
    any: ImageIndicator
    keyReference: QVV
    Co-fires with VehicleReg RQN on shared fields

================================================================================
3. DriverLicenseQuery -- Person Entity
================================================================================

  QIDM: DriverLicenseQueryQIDM
  QIF:  Person
  Combos: DQN (OLN), DQ (Name+DOB)
  Optional fields: SexCode, ImageIndicator

  DQN -- OLN + State [CONFIRMED]
    set: OperatorLicenseNumber, RegistrationState
    any: SexCode, ImageIndicator
    keyReference: DQN
    Defaults: State=NJ, ImageIndicator=Y

  DQ -- Name + DOB + State [CONFIRMED]
    set: Name, BirthDate, RegistrationState
    any: SexCode, ImageIndicator
    keyReference: DQ
    Name uses FormatStringRuleHandler: "LAST, FIRST"
    BirthDate uses CommsysParseDateRuleHandler: yyyy-MM-dd -> MMddyyyy

  OOS -- OLN with non-NJ state [CONFIRMED]
    State=NY tested

  Priority -- OLN+Name+DOB all filled [CONFIRMED]
    DQN fires (OLN wins over Name)

  Sex optional [CONFIRMED]
    SexCode=M -> NIBRS reverse-lookup -> 69509891711 (RMS attrId)
    CommSys XML: <SexCode>M</SexCode>

  RMS co-fire [CONFIRMED]
    RMS Person combo fires alongside CommSys on OLN and Name paths

================================================================================
4. GunQuery -- Firearm Entity
================================================================================

  QIDM: GunQueryQIDM
  QIF:  Firearm
  Combos: QG
  Optional fields: GunMake, GunCaliber, GunModel, ImageIndicator

  QG -- Serial Number [CONFIRMED]
    set: SerialNumber
    any: GunMake, GunCaliber, GunModel, ImageIndicator
    keyReference: QG

  QG + all optionals [CONFIRMED]
    Serial + Make + Caliber + Model + Image all present in XML

================================================================================
5. ArticleSingleQuery -- Article Entity
================================================================================

  QIDM: ArticleSingleQueryQIDM
  QIF:  Article
  Combos: QA

  QA -- Serial + ArticleType [CONFIRMED]
    set: SerialNumber, ArticleTypeCode
    any: ImageIndicator
    keyReference: QA

================================================================================
6. BoatQuery -- Boat Entity
================================================================================

  QIDM: BoatQueryQIDM
  QIF:  Boat
  Combos: QB (Registration), QBN (Hull ID)
  No State field on Boat (removed in v2 metadata)

  QB -- Registration Number [CONFIRMED]
    set: RegistrationNumber
    any: ImageIndicator
    keyReference: QB

  QBN -- Hull ID [CONFIRMED]
    set: HullIdentificationNumber
    any: ImageIndicator
    keyReference: QBN

================================================================================
RMS BUNDLE
================================================================================

  RMS co-fire tested on Vehicle (plate, VIN) and Person (OLN, Name) [CONFIRMED]
  Patches applied: 1 (Vehicle state), 3 (Person state), 6 (cleanup)

================================================================================
SUMMARY
================================================================================

  Total combos: 10 CommSys + 6 RMS
  [CONFIRMED]: ALL (17 BASE + 57 MC live tests)
  [PENDING]: NONE
  Simulation logs: 10 files (5 BASE + 5 MC) in tests/ with XML captures (2026-04-30)
