NJ_NJCJIS BASE v3.0 -- Test Log
============================================================
Date       : 2026-05-08
JSON       : NJ_NJCJIS_BASE.json
Entity     : Vehicle
Combo      : UI_CHECK (autoSelect: Stolen checkbox unchecked on form load, Registration checked)
Test type  : T1

================================================================================
FORM STATE (what was on the form when submitted)
================================================================================

  No submission -- UI observation only.

================================================================================
REQUEST SUMMARY
================================================================================

  N/A -- UI behavior test, no query submitted.

================================================================================
FIELD ANALYSIS
================================================================================

  autoSelect behavior observed:
    VehicleRegistrationQuery (autoSelect=true):  checkbox lights up when plate entered
    VehicleStolenQuery (autoSelect=false):       checkbox does NOT light up, no error

  Interpretation: autoSelect=true activates the query checkbox when set[] fields
  are populated. autoSelect=false prevents auto-activation -- officer must manually
  check. This is field-entry-triggered, not pre-checked on form load.

  Engineering doc correction: "automatically selected when the user opens the
  search form" is misleading. Actual behavior: auto-selected when set[] fields
  are populated by the user.

================================================================================
RESULT
================================================================================

  PASS

  Reason: VehicleRegistrationQuery auto-selects on plate entry. VehicleStolenQuery
  stays unchecked and shows no error. autoSelect=false confirmed working.

================================================================================
RAW XML REQUEST (from server logs)
================================================================================

N/A -- no query submitted.
