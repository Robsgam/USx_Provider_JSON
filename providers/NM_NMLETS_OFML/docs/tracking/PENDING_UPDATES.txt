# PENDING_UPDATES.txt -- NM_NMLETS_OFML
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Created 2026-08-01. Everything below is COMMENTED, so it does NOT block enforce -- these are open
# decisions and a standing finding, not owed rebuild work.
#
# â•â•â• OPEN DECISION FOR ROB -- DriverLicenseQuery: metadata and devdoc DIRECTLY CONFLICT â•â•â•
#   METADATA (keyRef QUERY, DriverLicenseQuery):
#       Set[ Choice[ OperatorLicenseNumber / Set[SexCode, BirthDate, Name] ],
#            PurposeCode,          <- MANDATORY (outer Set, OUTSIDE the Choice)
#            Attention,            <- MANDATORY (outer Set, OUTSIDE the Choice)
#            Any[State, ImageIndicator] ]
#   DEVDOC: does not list PurposeCode or Attention in this query AT ALL. Its field table is
#       BirthDate / Name / OperatorLicenseNumber / SexCode / State / State2-5, and its two
#       combinations are "1. (In/Out) BirthDate, Name, SexCode, [State..State5]" and
#       "2. (In/Out) OperatorLicenseNumber, [State..State5]" -- which built DL.NAME and DL.OLN
#       implement EXACTLY.
#   CONTRAST THE DH QUERY, which is why this was missed: metadata KQ puts Attention and PurposeCode
#       in <Any> there, so they ARE optional on DriverHistory, and the build correctly carries
#       purposeCodeDH as an optional. The build script header generalised that DH observation
#       ("No Attention handler in DH metadata") and never re-checked DL, where the shape differs.
#
#   THE TWO OPTIONS, both of which change officer-facing behaviour:
#     (a) Honour metadata -- add PurposeCode + Attention to both DL combos' set[]. An officer must
#         then supply two fields the documentation never mentions on EVERY licence search.
#     (b) Add them hidden and auto-populated (the AP #27 feeder pattern used elsewhere). This is
#         exactly what the never-automate-a-field-without-approval rule forbids doing unilaterally.
#   NOT DECIDED HERE. There is no wire evidence either way -- NM has never been USx-tenant-tested.
#   Same class as the AZ_AZDPS DriverLicenseQuery scope inversion.
#
# â•â•â• WHY THIS IS NOT IN THE ACCEPTED-DIVERGENCE REGISTRY â•â•â•
#   I tried to register it (rule demoted-to-any) under both the built keyRefs and the metadata keyRef.
#   NEITHER SUPPRESSED IT, AND THE TOOL IS RIGHT TO REFUSE. audit_requirement_fidelity line ~479:
#       if ($demoted.ContainsKey(...) -and (Test-Has $bAnyC $w)) { ...skip... }
#       "the registered decision is 'it RIDES IN any[]' -- if the field is absent from any[] TOO it
#        is not the situation that was granted, and must still report. Without this guard the entry
#        also excused a strictly WORSE state (field transmittable nowhere), which is what let
#        nj-drop-metadata-mandatory survive."
#   'demoted-to-any' grants "we carry it in any[] on purpose". NM carries these fields NOWHERE, which
#   is the strictly worse state, and a surviving mutation once taught the tool to refuse exactly that.
#   So the 4 UNDER-REQUIRED findings SHOULD keep reporting until the decision above is made.
#   The 6 ineffective rows I added were REMOVED rather than left: a registry row that looks like a
#   registration but suppresses nothing is clutter that reads like coverage.
#
# â•â•â• STILL OPEN, not blocking â•â•â•
#   Never USx-tenant-tested. v2.1 owes a full 5-entity sweep from T1.
#   audit_requirement_fidelity: 14 branches / 4 UNDER-REQUIRED / 0 OVER-PERMITTED -- all four are the
#     PurposeCode/Attention conflict above, i.e. ONE decision, not four defects.
#   audit_query_trace: 1 MISSING metadata combination to adjudicate against the devdoc Basic list.
#   build_phase1 random fuzz: 2 unaimed survivors to triage (drop-conditions @
#     VehicleRegistrationQuery[3]; select-to-input @ ENTITY_Person RaceCodeDH_Input).
#   ENFORCED 36 PASS / 0 FAIL / 0 WARN as of 2026-08-01.
# RETIRED 2026-08-14 -- ALREADY CONFORMANT -- no change needed. This provider has exactly ONE ImageIndicator control (Person) and it already carries initialValue=Y, so Y-on-every-entity is satisfied: there is no other entity carrying the field. Adding controls elsewhere would OVER-PERMIT (same adjudication as HI v4.17 Vehicle-only / IL v2.4 excluding Article). Verified from the emitted JSON. Zero JSON change, zero version bump.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
