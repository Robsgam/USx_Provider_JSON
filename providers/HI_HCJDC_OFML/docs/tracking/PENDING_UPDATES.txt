# PENDING_UPDATES -- empty. Resolved 2026-07-31 with NO version bump (46 logs preserved):
#  * M55S/vehicleYear OVER-PERMIT -- registered promoted-to-any, justified by devdoc L161 #4
#    '(Out) VehicleIdentificationNumber, [ImageIndicator, VehicleMakeCode, VehicleYear]'.
#  * 2 QV PREFILL-DEAD -- registered metadata-shadow-autofired. HI builds NO QV combo (RQ/RQV/
#    M55L/M55S only) and QV is the platform-auto-fired class ruled unbuilt on TX/FL/NJ.
#    Do NOT remove the vehicleTypeCode prefill to chase them.
# [FLAG:ncic-image-default-y-everywhere]  TAKEN at v4.17, 2026-08-13.
#   Cleared BY HAND -- line 3 of the template claims the build script removes this file on a
#   successful build and it DOES NOT. The v4.17 pipeline completed and left the flag live, exactly
#   as it did for IL_LEADS_OFML v2.4 the same day. flag_pending_fix.ps1 WRITES flags and nothing
#   RETIRES them. That is the second defect in the reverse-propagation mechanism today; the first
#   was a missing-newline append that glued CA_CLETS's copy of this same flag onto a '#' comment
#   and made it INERT.
#   WHAT WAS DONE: ImageIndicator now defaults to 'Y' in BOTH the form initialValue and every
#   carrying combo's defaults[] -- Vehicle form + RQ, RQV, M55L, M55S. Five sites, verified from
#   the emitted v4.17 JSON with a different pattern than the edit used (zero sites left on 'N').
#   MEASURED FIRST: ImageIndicator is any[]-ONLY on all four combos -- 0 set[], 0 conditions -- so
#   no prefill can hide a combination (BUILD_RULES 24). TX_TLETS T6 gate clear: HI's devdoc has no
#   conditional-requirement wording at all.
#   "EVERYWHERE" MEANS VEHICLE ON THIS PROVIDER. HI has exactly ONE ImageIndicator control and no
#   Person/Firearm/Article/Boat combo carries the field. Adding controls there would OVER-PERMIT --
#   same adjudication that made this flag a no-op on CA_CLETS and excluded Article on IL.
#   Full reasoning in the v4.17 BUILD_NOTES entry.
# RETIRED 2026-08-17 -- RESOLVED at v4.19/v4.20. AZ_AZDPS answered the discriminating question on 10 captured wires -- FormatStringRuleHandler DOES emit all four components (DOE, JOHN A JR), so HI was genuinely under-wired as the flag predicted. v4.19 added nameMiddleDH/nameSuffixDH to the DH name any[] and nameMiddle/nameSuffix to DQ, and the v4.19 sweep captured DOE, JOHN A JR on both DQ and KQ -- shapes no HI log had ever carried. v4.20 then removed them from KQN, the OLN combo, where they were an OVER-PERMIT that put a surname-less <Name>A JR</Name> on the wire. Plan regenerated; middle/suffix now exercised. C3 itself is still unsettled and HI can no longer settle it.
# [FLAG:nameparts-untested-unfrozen] Test-scaffolding change, NOT a JSON change: _combo_value_resolver now supplies nameMiddle=A / nameSuffix=JR, and emit_test_plan no longer suppresses the trust warning for those four fieldIds. Regenerate this provider PLAN at its next rebuild so middle/suffix are finally exercised. WHY IT MATTERS: metadata declares Name as type=Name with FOUR components (First/Last/Middle/Suffix), so middle and suffix are metadata-defined CAPABILITY, and no test on any provider had ever filled them -- all 538 logs read DOE, JOHN. Not regenerated here on purpose (rule 8c): this provider is tenant-verified and a bigger plan would drop it to PARTIAL. AZ_AZDPS is running the discriminating test first because it has NO Foundation and NO LIVE tenant. AWAIT THAT RESULT BEFORE ACTING: DOE, JOHN A JR means the handler emits all four and HI_HCJDC_OFML in particular is under-wired (its name parts are in ZERO combination any[], so they can never reach the composite); DOE, JOHN means the handler drops components 3-4 and this becomes a platform LIMITATION to raise, not a wiring job. (flagged 2026-08-17)
# [FLAG:plan-dedupe-vacuous-tests]  TAKEN AND DONE 2026-08-31 -- not deferred, not waived.
#   Plan regenerated at the CURRENT version (50 -> 49 tests), 1 now-unmatched log archived to
#   logs/Article/_archive_dedupe_v4.20/. Result 49 plan / 49 logs, four log gates 49/49 each
#   (6c, 6d, 2i, plan completeness ALL-PASS 5/5). ALL-PASS PRESERVED. NO version bump, NO re-sweep:
#   emit_test_plan only rewrites the plan file and reset_test_package fires on a VERSION change,
#   which this was not. The JSON was not touched.
#   PROVEN SAFE BEFORE ACTING (plan written to scratch first; the script ABORTS on any failure):
#     combos with >=1 test 12 before / 12 after -- zero lose coverage;
#     the archived log `QA_any` has an IDENTICAL-WIRE retained twin, `QA_af_relatedSearchHitIndicator`
#     (transaction id normalised out) -- so nothing unique left the package;
#     every new-plan label already had a log, so nothing needed re-driving.
#   ⚠️ RESIDUAL, NOT CAUSED BY THIS AND NOT FIXED BY IT -- audit_log_inflation attack A went
#   2 clone group(s) -> 1, and the survivor is a DIFFERENT class:
#       M55L_guardrail_vs_M55S  ==  M55L_guardrail_vs_RQV
#   Two guardrails with DIFFERENT fill-sets that emit IDENTICAL wire. The dedupe filter compares a
#   guardrail against the BASE test, so it cannot see two guardrails that are redundant against
#   EACH OTHER. Identical wire from different fills means the field distinguishing them does not
#   reach the wire on M55L -- which is either a legitimately un-defined optional on that variant or
#   a dropped value, and deciding which is a separate investigation (audit_optional_scope), not a
#   plan-dedupe item. Left OPEN deliberately rather than folded into this clear.
#   (original flag text retained below for the record)
# [FLAG:plan-dedupe-vacuous-tests] emit_test_plan.ps1 gained two plan-only filters 2026-08-21: (1) DUPLICATE -- drop a test whose (entity, fill-set) is byte-identical to an earlier one, which makes the generator agree with audit_log_inflation attack A by construction; (2) NO-CONTEST GUARDRAIL -- drop a guardrail whose fill adds no competing identifier over the winner's own combo test, because the pair differs only by a presence condition and the loser can never match. YOUR PLAN WILL SHRINK when you regenerate; measured non-destructively on 2026-08-21: AZ -5, CA_CLETS -11, CA_SLO -1, CA_VENTURA -2, FL -14, HI -1, IL -1, LA -1, MD -3, NJ -2, TN -1, TX_TLETS_CCH -4. NO COVERAGE IS LOST -- identical fill means identical wire, and NY held 16/16 combos at 100% across the change. NOT DONE FOR YOU ON PURPOSE: if you are tenant-verified, regenerating now makes your existing logs ORPHAN (more logs than plan tests) and drops you out of ALL-PASS, which is why this is a flag and not a sweep. Do it at your OWN next rebuild, and archive the superseded duplicate logs in the same pass (NY put them in logs/<Entity>/_archive_dedupe_v<ver>/). Reference: NY_NYSPIN_EJUSTICE v4.26, 73 -> 65 tests, inflation A 8 -> 0, four log gates 65/65. (flagged 2026-08-21)
