FL_FCIC_BASE.json -- Build Notes
=================================
Started  : 2026-04-07
Engineer : Gordon Hallof / Claude (claude-sonnet-4-6)
Directory: C:\Users\Gordon Hallof\FL_FCIC\

v3.8 (2026-05-12) -- RegistrationStateDH: isolate DH State from DL routing
  CHANGED: Added RegistrationStateDH field (FormSelect, STATE, initialValue=FL) on DH card.
           DH QIDM State attribute sourceField changed from RegistrationState to RegistrationStateDH.
           KQ combo set[] changed from RegistrationState to RegistrationStateDH.
           DL State (RegistrationState on Search Options card) remains blank — no cross-contamination.
  REASON:  Shared State field confused officers: blank=correct for DL (FDQ), but DH requires State.
           Filling State for DH caused DL to route OOS (DQ) instead of in-state (FDQ).
           DH-suffix pattern extended to State — consistent with all other DH fields.
  VALIDATOR: BASE 102P/0F/0W/0LIM | MC 102P/0F/0W/0LIM

v3.7 (2026-05-12) -- Person State label fix for DH reachability
  CHANGED: Person State label from "State (leave blank for FL)" to "State" (BASE + MC).
  REASON:  DH KQ combos require State in set[] — label told officers to leave it blank,
           making DH unreachable. Neutral label allows DL (blank=FDQ) and DH (filled=KQ).
  VALIDATOR: BASE 102P/0F/0W/0LIM | MC 102P/0F/0W/0LIM

v3.6 (2026-05-12) -- Metadata audit: PurposeCode any[], FRQ/QV field alignment, no PurposeCode default
  CHANGED: DH combos: PurposeCode moved from set[] to any[] (metadata says optional).
           FRQ Plate/VIN: added VehicleMakeCode, VehicleYear to any[].
           QV VIN: added RegistrationState to any[].
           Removed PurposeCode initialValue='C' from Person form (both BASE and MC).
  REASON:  Full metadata audit against FL_FCIC_METADATA_REFERENCE.txt. Combos were
           stricter than metadata requires. PurposeCode default removed per user request.
  VALIDATOR: BASE 102P/0F/0W/0LIM | MC 102P/0F/0W/0LIM

v3.5 (2026-05-12) -- One-directional queriesToDeselect fix + DH autoSelect
  CHANGED: Removed queriesToDeselect from DL QIDM (was bidirectional with DH).
           Changed DH QIDM autoSelect from false to true (BASE only — MC already had true).
  REASON:  Bidirectional queriesToDeselect causes platform deadlock/error popup (confirmed NJ v2.3/v2.8,
           CA_CLETS v1.7). One-directional pattern: DL=default (autoSelect=true, no deselect),
           DH=opt-in (autoSelect=true, deselects DL). Confirmed working on CA_CLETS v1.8 (18/18 PASS).
  VALIDATOR: BASE 102P/0F/0W/0LIM | MC 102P/0F/0W/0LIM

These notes capture every decision, problem, and fix made during the build of
FL_FCIC_BASE.json. For version history only, see VERSIONS.txt. For platform
limitations, see FL_FCIC_LIMITATIONS.txt.

================================================================================
PHASE 1 -- SOURCE ANALYSIS
================================================================================

Three source files were used:

  FL_FCIC.xml   -- FL FCIC transaction spec (fields, sizes, combinations).
                   This is the AUTHORITY for everything in the FL_FCIC bundle.
                   ~17,000+ lines. All field maxLengths and combination
                   requirements come from this file.

  HIDLE.json    -- HI HCJDC provider JSON. Used as a structural template for:
                     - QUERYRESULTDATAMAPPING (cloned, renamed to FL_FCIC)
                     - RMS bundle (copied entirely unchanged)
                     - Layout node structure reference

  PROMPTS_v1.0.txt -- Documents the NJ_NJCJIS build process. Used as the
                      methodology guide (build script pattern, helper functions,
                      bundle structure, ordering conventions).

The NJ_NJCJIS directory was also referenced for build script patterns:
  - PowerShell helper functions: N(), Inp(), Sel(), Dt(), BuildLayout(),
    AddCadNodes(), MakeLayouts()
  - ConvertTo-Json single-element array workaround (List<string>)
  - Bundle assembly pattern

================================================================================
PHASE 2 -- SUPPORTED QUERIES (scoped by user)
================================================================================

The following 6 queries were explicitly requested (out of all queries in XML):

  ArticleSingleQuery    (QA)              -- Article entity
  BoatQuery             (FBQ + QB)        -- Boat entity
  DriverHistoryQuery    (KQ)              -- Person entity (FL-specific)
  DriverLicenseQuery    (FDQ + QW + DQ)  -- Person entity
  GunQuery              (QG)              -- Firearm entity
  VehicleRegistrationQuery (FRQ + QV + RQ) -- Vehicle entity

Note: The full set of keys per query was not known until after the first import
(see Phase 5 below). Initial scoping only captured FL-specific keys.

================================================================================
PHASE 3 -- INITIAL BUILD (v1.0)
================================================================================

BUILD SCRIPT STRUCTURE
  Modeled directly on build_nj_njcjis.ps1. Key adaptations for FL:
  - FL uses FRQ (not RQ/RQN) for vehicle registration
  - FL uses FDQ (not DQ) for DHSMV driver license
  - FL uses FBQ (not BQ/BQN) for boat
  - FL uses KQ for driver history (no NJ/HI equivalent)
  - FL GunQuery has no GunCaliber field -- row omitted from Firearm form
  - ArticleTypeCode uses codeTypeSource='CA_CLETS' (NCIC_ARTICLE_TYPE lives
    under CA_CLETS in the platform, not under NCIC)
  - BoatHullIdNumber maxLength=62 per FL XML (vs 20 in NJ)

BUNDLE STRUCTURE DECISIONS
  - 3 bundles: FL_FCIC (provider), ENTITIES (forms, MARK43), RMS (unchanged)
  - QUERYINPUTFORM belongs ONLY in ENTITIES bundle -- adding to RMS causes
    each entity form to render twice in the UI
  - QUERYRESULTDATAMAPPING cloned from HIDLE (HI_HCJDC_Results), name and
    provider updated to FL_FCIC. Field mappings are inherited from HI and
    will need validation against actual FL FCIC responses once live

FORM LAYOUT DECISIONS
  - Single card per entity (all search paths on one card; platform routes
    automatically based on which fields are populated)
  - Vehicle form: no RegistrationState -- initial assumption was FRQ is FL-only
    so state field not needed (this was partially wrong; see Phase 5)
  - Person form: RegistrationState with initialValue='FL' -- needed for KQ
    (DriverHistoryQuery requires State)
  - Boat form: RegistrationState with initialValue='FL' -- used by FBQ
  - Firearm: GunMake as free-text FormInput (no confirmed code list for FL)
  - Article: ArticleTypeCode as FormSelect with codeTypeSource='CA_CLETS'
  - FIRST_RESPONDER layout cloned from CAD_DISPATCH for all 5 entities
  - Entity order: default=[Person,Vehicle,Firearm,Article,Boat]
                  CAD_DISPATCH=[Vehicle,Person,Firearm,Article,Boat]
                  FIRST_RESPONDER=[Vehicle,Person,Firearm,Article,Boat]

AUTHENTICATION CONFIG
  - handlerFunction: CommsysOriAuthenticationHandler
  - providerType: Commsys
  - Attributes: ORI (12), Mnemonic (25), UserName (dexStateUserId rule handler)
  - Rule: CommsysGetDexStateUserIdRuleHandler (same pattern as HI/NJ)

QUERYMESSAGEFORMAT
  - handlerFunction: CommsysWsiOutgoingMessageHandler
  - authenticationParent/payloadParent: LawEnforcementTransaction

v1.0 INITIAL CONFIG COUNT: 9 configurations in FL_FCIC bundle
  (before duplicate key fix -- see Phase 4)

================================================================================
PHASE 4 -- FIRST IMPORT ERROR: DUPLICATE KEYREFERENCE
================================================================================

IMPORT ERROR RECEIVED
  {"error":"Internal Server Error","details":[{"name":"Internal Server Error",
  "value":"Failed to import bundle: FL_FCIC_VehicleRegistrationQuery with error:
  Invalid Bundle configurations: [Duplicate key references found in query input
  data mapping.]"}]}

ROOT CAUSE
  The platform requires that each keyReference value appears AT MOST ONCE within
  a single QUERYINPUTDATAMAPPING's combinations array.

  The initial build had multiple combinations sharing the same keyReference in
  one mapping:
    - VehicleRegistrationQuery: FRQ appeared 3 times (Plate, VIN, Decal)
    - DriverLicenseQuery: FDQ appeared twice (OLN, Name)
    - DriverHistoryQuery: KQ appeared twice (Name, OLN)
    - BoatQuery: FBQ appeared twice (RegNumber, HullId)

FIX APPLIED
  Split each mapping with duplicate keyReferences into separate mappings --
  one mapping per primary search path. Multiple DIFFERENT keyReferences in
  one mapping ARE allowed (e.g., FDQ+QW in the same mapping is fine).

  Result: FL_FCIC bundle grew from 9 to 14 configurations:
    FL_FCIC_VehicleRegistrationQuery  FRQ  (Plate)
    FL_FCIC_VehicleVINQuery           FRQ  (VIN)
    FL_FCIC_VehicleDecalQuery         FRQ  (Decal)
    FL_FCIC_DriverLicenseOLN          FDQ  (OLN)
    FL_FCIC_DriverLicenseQuery        FDQ + QW  (Name -- different keys, OK)
    FL_FCIC_DriverHistoryQuery        KQ   (Name)
    FL_FCIC_DriverHistoryOLN          KQ   (OLN)
    FL_FCIC_BoatQuery                 FBQ  (RegNumber)
    FL_FCIC_BoatHullQuery             FBQ  (HullId)

USER CONSTRAINT
  "Do not use any other json in an attempt to fix" -- fix was applied purely
  from the build script using FL_FCIC.xml and HIDLE.json only.

IMPORT RESULT: Success after split.

================================================================================
PHASE 5 -- SECOND REVIEW: MISSING NCIC/NLETS COMBINATIONS (v1.0 → v1.1)
================================================================================

USER FEEDBACK
  "The import worked but it seems that you did not review the FL_FCIC.xml
  well enough when you built the json"

ROOT CAUSE OF GAP
  The initial XML analysis focused on FL-specific message keys (FRQ, FDQ, FBQ,
  KQ) and missed that the SAME Transaction nodes also contain standard NCIC and
  Nlets keys (QV, RQ, DQ, QB, QW+OLN) in their Combinations lists.

  The FL_FCIC.xml MessageKey list (lines 54-229) defines ALL keys including:
    QV  = NCIC Stolen Vehicle Query
    RQ  = Vehicle Registration Query (Nlets out-of-state)
    DQ  = Driver License Query (Nlets out-of-state)
    QB  = NCIC Stolen Boat Query
    QW  = NCIC Wanted Person Query

  These are not FL-specific but are fully valid combinations within the
  FL FCIC transaction spec and must be included.

FULL XML REVIEW -- COMBINATIONS FOUND AND MISSED

  VehicleRegistrationQuery (line 16594):
    FRQ+Decal    -- INCLUDED in v1.0
    FRQ+Plate    -- INCLUDED in v1.0
    FRQ+TitleLien -- MISSED in v1.0 (new mapping added in v1.1)
    FRQ+VIN      -- INCLUDED in v1.0
    QV+Plate     -- MISSED in v1.0 (added to VehicleRegistrationQuery mapping)
    QV+VIN       -- MISSED in v1.0 (added to VehicleVINQuery mapping)
    RQ+Plate     -- MISSED in v1.0 (added; requires Plate+PlateType+PlateYear+State)
    RQ+VIN       -- MISSED in v1.0 (added; requires VIN+State)

  DriverLicenseQuery (line 11821):
    FDQ+Name     -- INCLUDED in v1.0
    FDQ+OLN      -- INCLUDED in v1.0
    QW+Name      -- INCLUDED in v1.0
    QW+OLN       -- MISSED in v1.0 (added to DriverLicenseOLN; requires Name+OLN)
    DQ+Name      -- MISSED in v1.0 (added to DriverLicenseQuery; requires State)
    DQ+OLN       -- MISSED in v1.0 (added to DriverLicenseOLN; requires State)

  DriverHistoryQuery: Only KQ key in XML -- all combinations were INCLUDED in v1.0

  BoatQuery (line 6584):
    FBQ+Hull      -- INCLUDED in v1.0
    FBQ+Decal     -- MISSED in v1.0 (new mapping added in v1.1)
    FBQ+RegNumber -- INCLUDED in v1.0
    FBQ+TitleLien -- MISSED in v1.0 (new mapping added in v1.1)
    QB+Hull       -- MISSED in v1.0 (added to BoatHullQuery mapping)
    QB+CoastGuard -- MISSED in v1.0 (new mapping added in v1.1)
    QB+NCICNumber -- MISSED in v1.0 (new mapping added in v1.1)
    QB+ProcessControl -- MISSED in v1.0 (new mapping added in v1.1)

  GunQuery: Only QG key -- INCLUDED in v1.0
  ArticleSingleQuery: Only QA key -- INCLUDED in v1.0

REQUIREMENT DETAILS (from XML)

  QV+Plate  : set=[LicensePlateNumber]; any=[State]
  QV+VIN    : set=[VehicleIdentificationNumber]; any=[VehicleMakeCode]
  RQ+Plate  : set=[LicensePlateNumber, LicensePlateTypeCode, LicensePlateYear, State]; any=[]
  RQ+VIN    : set=[VehicleIdentificationNumber, State]; any=[VehicleMakeCode, VehicleYear]
  FRQ+TitleLien: set=[TitleLienInformation]; any=[]

  QW+OLN    : set=[Name, OperatorLicenseNumber]; any=[]
                (both Name and OLN required -- different from QW+Name which needs DOB)
  DQ+OLN    : set=[OperatorLicenseNumber, State]; any=[]
  DQ+Name   : set=[BirthDate, Name, SexCode, State]; any=[]

  QB+Hull   : set=[BoatHullIdNumber]; any=[RegistrationNumber]
  QB+CoastGuard: set=[CoastGuardDocumentNumber]; any=[]
  QB+NCICNumber: set=[NCICNumber]; any=[]
  QB+ProcessControl: set=[ProcessControlNumber]; any=[]
  FBQ+Decal : set=[DecalNumber]; any=[BoatHullIdNumber, RegistrationNumber]
  FBQ+TitleLien: set=[TitleLienInformation]; any=[BoatHullIdNumber, RegistrationNumber]

SPLIT STRATEGY FOR NEW COMBINATIONS
  Where different keys could share a mapping (no duplicate keyReference):
    VehicleRegistrationQuery mapping: FRQ+Plate, QV+Plate, RQ+Plate (3 keys, all different)
    VehicleVINQuery mapping:          FRQ+VIN,   QV+VIN,   RQ+VIN   (3 keys, all different)
    DriverLicenseOLN mapping:         FDQ+OLN, DQ+OLN, QW+OLN       (3 keys, all different)
    DriverLicenseQuery mapping:       FDQ+Name, QW+Name, DQ+Name     (3 keys, all different)
    BoatHullQuery mapping:            FBQ+Hull, QB+Hull               (2 keys, both different)

  Where QB appears multiple times it cannot be in the same mapping:
    QB+CoastGuard -> separate mapping (FL_FCIC_BoatCoastGuardQuery)
    QB+NCICNumber -> separate mapping (FL_FCIC_BoatNCICQuery)
    QB+ProcessControl -> separate mapping (FL_FCIC_BoatProcessControlQuery)

  Where FBQ would duplicate in existing mappings:
    FBQ+Decal     -> separate mapping (FL_FCIC_BoatDecalQuery)
    FBQ+TitleLien -> separate mapping (FL_FCIC_BoatTitleLienQuery)
    FRQ+TitleLien -> separate mapping (FL_FCIC_VehicleTitleLienQuery)

ENTITY FORM UPDATES FOR v1.1
  Vehicle: Added ROW_5
    - RegistrationState (no initialValue -- blank = FL-only, filled = out-of-state RQ)
    - TitleLienInformation (maxLength=8, for FRQ+TitleLien)

  Boat: Added ROW_3, ROW_4, ROW_5
    - ROW_3: CoastGuardDocumentNumber (8), NCICNumber (10)
    - ROW_4: TitleLienInformation (10), DecalNumber (20)
    - ROW_5: ProcessControlNumber (10)

  Person: No changes -- RegistrationState already present (needed for KQ and now DQ)
  Firearm, Article: No changes

NOTE on RegistrationState in Vehicle form
  Vehicle RegistrationState has NO initialValue (unlike Person/Boat which default FL).
  Rationale: FRQ is always FL-only and doesn't require State. Having State blank
  means only FRQ+QV fire (FL + NCIC stolen check). Filling State additionally
  triggers RQ (Nlets out-of-state). Defaulting to FL could cause unintended RQ
  routing for routine FL plate lookups.

v1.1 FINAL CONFIG COUNT: 20 configurations in FL_FCIC bundle

================================================================================
PHASE 6 -- SUPPORT FILES CREATED
================================================================================

All files modeled on the NJ_NJCJIS directory structure:

  build_fl_fcic.ps1    -- Main build script. Run with -Version "X.X"
                          Default version = 1.1 (update for each new version)
  bump_version.ps1     -- Archives FL_FCIC_BASE.json to archive/ before rebuild
  inspect_layout.ps1   -- Debug tool: dumps Vehicle default layout nodes
  VERSIONS.txt         -- Version history (what changed, why, known issues)
  FL_FCIC_LIMITATIONS.txt -- Platform limitations + FL-specific behavioral notes
  STATUS.txt           -- Quick-reference: current configs, keys, form fields
  BUILD_NOTES.txt      -- This file: full process narrative

================================================================================
PHASE 7 -- THIRD REVIEW: MISSING FIELDS AND COMBINATIONS (v1.1 → v1.2)
================================================================================

USER FEEDBACK
  "The person entity card does not appear to support DriverHistoryQuery as
  there are no purpose and attention fields. Are you sure you caught everything?"

ROOT CAUSE OF GAP
  v1.1 reviewed the XML Combinations lists carefully but did not audit the
  Fields list for each Transaction against what was actually in the mappings
  and forms. The Fields list is the authority for what attributes can be sent.

FULL FIELD AUDIT FINDINGS

  DriverHistoryQuery (line 11638) -- FIELDS MISSED:
    Attention       (maxLength=30) "periods should not be used with rank/initials"
    PurposeCode     (maxLength=1)  "valid values: A, C, D, E, F, H, J, O, R"
    Requestor       (maxLength=30) "attention" (alias for Attention in backend)
    Both fields appear in the ANY block of BOTH KQ combinations.
    Neither was in the mappings or Person form.

  GunQuery (line 15305) -- COMBINATIONS MISSED:
    QG+NCICNumber         set=[NCICNumber]; any=[GunMake, Image, RelatedHit, Requestor]
    QG+ProcessControl     set=[ProcessControlNumber]; any=[GunMake, Image, RelatedHit, Requestor]
    Both missed entirely. All GunQuery combos use QG -- each needs own mapping.
    NCICNumber and ProcessControlNumber were not in Firearm form.

  ArticleSingleQuery (line 1947) -- COMBINATIONS MISSED:
    QA+OwnerAppliedNumber  set=[ArticleTypeCode, OwnerAppliedNumber]; any=[Image, RelatedHit, Requestor]
    QA+NCICNumber          set=[NCICNumber]; any=[Image, RelatedHit, Requestor]
    QA+ProcessControl      set=[ProcessControlNumber]; any=[Image, RelatedHit, Requestor]
    All three missed entirely. All ArticleSingleQuery combos use QA -- each needs own mapping.
    OwnerAppliedNumber, NCICNumber, ProcessControlNumber were not in Article form.

FIXES APPLIED

  New mappings added (FL_FCIC bundle grew from 20 to 25):
    FL_FCIC_GunNCICQuery             QG+NCICNumber
    FL_FCIC_GunProcessControlQuery   QG+ProcessControl
    FL_FCIC_ArticleOwnerQuery        QA+OwnerAppliedNumber (requires ArticleTypeCode too)
    FL_FCIC_ArticleNCICQuery         QA+NCICNumber
    FL_FCIC_ArticleProcessControlQuery QA+ProcessControl

  Mapping attribute updates:
    $dhQueryName: + Attention (30), PurposeCode (1), Requestor (mapped from Attention)
    $dhQueryOLN:  + Attention (30), PurposeCode (1), Requestor (mapped from Attention)
    KQ combination any arrays updated to include Attention and PurposeCode

  Entity form updates:
    Person  ROW_4: + PurposeCode (1) + Attention (30)
    Firearm ROW_2: + NCICNumber (10) + ProcessControlNumber (10)
    Article ROW_2: + OwnerAppliedNumber (20, full width)
    Article ROW_3: + NCICNumber (10) + ProcessControlNumber (10)

LESSON LEARNED
  For every Transaction, audit BOTH the Fields list AND the Combinations list:
    Fields list   = what attributes can be sent + their sizes
    Combinations  = what routing keys and required field sets exist
  Missing a field from the Fields list means it can't be sent even if the user
  would need to provide it. Missing a Combination means a valid query path won't
  route.

================================================================================
PHASE 8 -- COMBINATION REQUIREMENT TIGHTENING (v1.2 → v1.3)
================================================================================

USER FEEDBACK
  "The Driver History checkbox should only light when the user places data into
   both Purpose and Attention fields."

ROOT CAUSE
  In v1.2, PurposeCode and Attention were placed in requirements.any (optional
  fields) for both KQ combinations. This meant the Driver History combination
  indicator could light up without those fields being filled -- the combination
  would fire on just OLN+State or Name+DOB+Sex+State alone.

  The correct intent: PurposeCode and Attention are REQUIRED for DriverHistoryQuery
  (KQ) to be a valid submission. Moving them to requirements.set enforces this at
  the combination indicator level -- the checkbox does not light until ALL set
  fields are populated.

CHANGES MADE

  FL_FCIC_DriverHistoryQuery (KQ + Name search):
    Before: set=[BirthDate, NameLast, NameFirst, SexCode, RegistrationState]
            any=[PurposeCode, Attention]
    After:  set=[BirthDate, NameLast, NameFirst, SexCode, RegistrationState,
                 PurposeCode, Attention]
            any=[]

  FL_FCIC_DriverHistoryOLN (KQ + OLN search):
    Before: set=[OperatorLicenseNumber, RegistrationState]
            any=[Attention, PurposeCode]
    After:  set=[OperatorLicenseNumber, RegistrationState, PurposeCode, Attention]
            any=[]

  build_fl_fcic.ps1 version param default updated from "1.2" to "1.3".
  Comments updated to reflect all-required semantics.

NO OTHER CHANGES
  No form fields added. No new configurations. No attributes changed.
  Config count remains 25 (FL_FCIC) + 5 (ENTITIES) + 6 (RMS) = 36.

================================================================================
PHASE 9 -- DRIVER LICENSE / DRIVER HISTORY MUTUAL EXCLUSION (v1.3 → v1.4)
================================================================================

HOW THIS WAS DISCOVERED
  After v1.3 was built, a question was raised: when the Driver History checkbox
  lights, can it automatically deselect Driver License? The initial assessment
  was that no such mechanism existed in the QUERYINPUTDATAMAPPING schema.

  However, while investigating whether the DriverHistoryQuery handler function
  (CommsysTransactionRequestHandler) was correct, HIDLE.json was inspected for
  comparison. Hawaii has both a DriverLicenseQuery and a DriverHistoryQuery
  mapping. Both use two fields not present in any of our FL_FCIC driver mappings:

    autoSelect         -- controls whether a combination self-selects when lit
    queriesToDeselect  -- lists query types to force-deselect when this one selects

  HIDLE.json pattern (confirmed source of truth):
    HI_HCJDC_DriverLicenseQuery:  autoSelect=true,  queriesToDeselect=["DriverHistoryQuery"]
    HI_HCJDC_DriverHistoryQuery:  autoSelect=false, queriesToDeselect=["DriverLicenseQuery"]

  This pattern fully enforces mutual exclusion and directly addresses the issue
  raised in v1.3: Driver History deselects Driver License when selected, and
  vice versa.

WHY autoSelect DIFFERS BETWEEN THE TWO QUERY TYPES
  Driver License (autoSelect=true): A DL query is the default behavior when a
  user enters person search fields. It should fire without requiring extra
  operator action. Auto-selecting the combination when fields are populated
  reflects the expected default workflow.

  Driver History (autoSelect=false): KQ is an intentional, Florida-specific
  query requiring PurposeCode and Attention -- fields with legal/policy
  significance. The operator must consciously activate the KQ combination.
  Requiring a manual click reinforces that this is a deliberate choice.

STATE OF FL_FCIC MAPPINGS BEFORE v1.4
  FL_FCIC_DriverLicenseOLN:    autoSelect=true (present), no queriesToDeselect
  FL_FCIC_DriverLicenseQuery:  no autoSelect, no queriesToDeselect
  FL_FCIC_DriverHistoryQuery:  no autoSelect, no queriesToDeselect
  FL_FCIC_DriverHistoryOLN:    no autoSelect, no queriesToDeselect

CHANGES MADE

  FL_FCIC_DriverLicenseOLN  (FDQ/DQ/QW + OLN):
    autoSelect = true              (already present -- no change)
    queriesToDeselect = ["DriverHistoryQuery"]   (added)

  FL_FCIC_DriverLicenseQuery  (FDQ/QW/DQ + Name):
    autoSelect = true              (added)
    queriesToDeselect = ["DriverHistoryQuery"]   (added)

  FL_FCIC_DriverHistoryQuery  (KQ + Name):
    autoSelect = false             (added)
    queriesToDeselect = ["DriverLicenseQuery"]   (added)

  FL_FCIC_DriverHistoryOLN  (KQ + OLN):
    autoSelect = false             (added)
    queriesToDeselect = ["DriverLicenseQuery"]   (added)

POWERSHELL NOTE
  queriesToDeselect is a single-element array. To prevent PowerShell's
  ConvertTo-Json from collapsing it to a plain string, it is typed explicitly:
    queriesToDeselect = [string[]]@('DriverHistoryQuery')
  This ensures the JSON output is ["DriverHistoryQuery"], not "DriverHistoryQuery".

LESSON LEARNED
  When using HIDLE.json as a template, inspect ALL fields on each configuration
  type -- not just the ones already present in the configurations being built.
  autoSelect and queriesToDeselect were present in HIDLE.json's driver mappings
  from the start but were not carried forward because the initial build focused
  on attributes, combinations, and routing keys. A full field comparison between
  HIDLE and each FL_FCIC mapping type should be done before declaring a mapping
  complete.

NO OTHER CHANGES
  No form fields, attributes, combinations, or other mappings changed.
  Total configuration count remains 25 (FL_FCIC) + 5 (ENTITIES) + 6 (RMS) = 36.

================================================================================
PENDING VALIDATION (as of v1.4)
================================================================================

  1. QUERYRESULTDATAMAPPING -- Inherited from HI_HCJDC. Field mappings need
     validation against actual FL FCIC XML responses once live queries are run.

  2. DriverHistoryQuery (KQ) -- FL-specific key not present in NJ/HI. Need to
     confirm platform accepts KQ as a valid keyReference.

  3. PurposeCode for KQ -- Valid values per XML: A, C, D, E, F, H, J, O, R.
     Currently FormInput (free text, maxLength=1). If the platform has a
     DRIVER_HISTORY_PURPOSE or similar code list, upgrade to FormSelect.

  4. FDQ/QW combination routing -- Two systems (DHSMV and FCIC/Nlets) share
     the DriverLicenseQuery. Routing behavior should be tested with live queries
     to confirm both systems respond as expected.

  5. QB+ProcessControl, QG+ProcessControl, QA+ProcessControl -- PCN-based
     combinations are unusual. Confirm platform accepts these routing paths.

  6. FIRST_RESPONDER layout key -- Not confirmed as a recognized platform key.

  7. Firearm Make -- Currently free-text FormInput. If a valid codeTypeSource /
     codeTypeCategory is identified for FL GunMake, upgrade to FormSelect.

  8. Vehicle RegistrationState default -- Consider initialValue='FL' if
     out-of-state RQ queries are uncommon and FL is the expected default.

  9. autoSelect / queriesToDeselect on non-DL/DH mappings -- Only the four
     driver mappings were audited against HIDLE.json for these fields. Vehicle,
     Gun, Boat, and Article mappings in HIDLE.json should be checked to confirm
     whether autoSelect or queriesToDeselect are used on those mapping types as
     well. If present, they may need to be carried forward into FL_FCIC.

================================================================================
================================================================================
v2.6  2026-04-22  ATTENTION FIELD FIX -- match CA_eSUN / LA_LEMS pattern
================================================================================

ROOT CAUSE IDENTIFIED (2026-04-22):
  FL_FCIC had Attention as a visible FormInput with Attention in set[] on both
  DriverHistory QIDMs. Because set[] requires all fields to be populated before
  the combination fires, the platform auto-filled the Attention field with the
  officer's "LASTNAME F" (CommsysGetLastNameFirstNameInitialRuleHandler behavior)
  so the combination could fire. This produced correct output but through an
  implicit platform mechanism rather than explicit configuration.

  Confirmed by comparing CommSys XML logs:
    HIDLE/HI: Attention in any[] + user types value -> sends user value
    FL_FCIC:  Attention in set[] + no form entry   -> platform auto-fills "HALLOF G"
    LA_LEMS: Attention in any[] + handler on attr + no form field -> sends "LASTNAME F"

CORRECT PATTERN (CA_eSUN, LA_LEMS):
  - CommsysGetLastNameFirstNameInitialRuleHandler on the Attention QIDM attribute
  - No Attention FormInput on the form (handler reads from user session, not form)
  - Attention in any[] (optional -- fires automatically via handler)

CHANGES MADE (v2.6):
  1. FL_FCIC_DriverHistoryQuery -- Attention attribute:
       Added rule: { function: "CommsysGetLastNameFirstNameInitialRuleHandler" }

  2. FL_FCIC_DriverHistoryOLN -- Attention attribute:
       Added rule: { function: "CommsysGetLastNameFirstNameInitialRuleHandler" }

  3. FL_FCIC_DriverHistoryQuery -- combination KQ:
       Moved "Attention" from set[] -> any[]
       set now: [BirthDate, NameLast, NameFirst, SexCode, RegistrationState, PurposeCode]
       any now: [Attention]

  4. FL_FCIC_DriverHistoryOLN -- combination KQ:
       Moved "Attention" from set[] -> any[]
       set now: [OperatorLicenseNumber, RegistrationState, PurposeCode]
       any now: [Attention]

  5. Person form -- all three layout variants (default, CAD_DISPATCH, FIRST_RESPONDER):
       Removed Attention_Input node (FormInput fieldId=Attention)
       Removed Attention_Input from ROW_4.nodes
       Updated ROW_4 templateColumns: ["6","6"] -> ["12"] (PurposeCode now full-width)

ARCHIVE: archive/FL_FCIC_v2.6_2026-04-22.json

================================================================================
================================================================================
v2.6 REBUILD  2026-04-22  Rebased onto v2.5 (prior v2.6 was on wrong base)
================================================================================

ISSUE: Prior v2.6 was applied to FL_FCIC_BASE.json which was the pre-v2.5 file.
  It was missing 16 QIDMs that existed in v2.5 (VehicleVINQuery, DriverLicenseOLN,
  BoatHullQuery, GunNCICQuery, DriverHistoryOLN, and other NCIC/ProcessControl variants).

FIX: Rebuilt v2.6 from v2.5 as the correct base, re-applying only:
  1. queryLabel standardization (Article Query->Article, Boat Query->Boat, Firearm Query->Firearm)
  2. Attention field fix (Attention already had handler in v2.5; moved set[]->any[],
     removed Attention_Input from Person form all 3 layout variants)

NOTE: v2.5 already had CommsysGetLastNameFirstNameInitialRuleHandler on the
  Attention attribute in FL_FCIC_DriverHistoryQuery. The prior v2.6 base file
  did not, hence the handler appeared to be new. It was not -- it was in v2.5.

FINAL v2.6 STATE:
  Base    : FL_FCIC_v2.5_2026-04-21.json
  Changes : queryLabel fixes (3) + Attention set[]->any[] (2 combos) + Attention_Input removed (3 layouts)
  QIDMs   : 8 (6 CommSys + 2 RMS) -- matches v2.5
  Archive : archive/FL_FCIC_v2.6_2026-04-22.json (overwritten with correct version)

================================================================================
================================================================================
v2.6 ADDENDUM  2026-04-23  PurposeCode: FormInput -> FormSelect
================================================================================

CHANGE: PurposeCode field on Person form upgraded from FormInput to FormSelect.

BEFORE (v2.2-v2.5):
  "type": { "resolvedName": "FormInput" }
  "props": { "fieldId": "PurposeCode", "label": "Driver History Purpose", "maxLength": "1" }

AFTER (v2.6):
  "type": { "resolvedName": "FormSelect" }
  "props": { "fieldId": "PurposeCode", "attributeTypeId": "DEX_INQUIRY_PURPOSE_CODE",
             "label": "Driver History Purpose", "initialValue": "C" }

REASON: Confirmed pattern from CA_eSUN and LA_LEMS. PurposeCode has a platform-managed
  code list under DEX_INQUIRY_PURPOSE_CODE. Using FormSelect populates a dropdown of valid
  purpose codes with 'C' (Criminal) as the default. FormInput (free text, maxLength=1) was
  functional but bypassed the platform's managed list.

Applied to all 3 layout variants: default, CAD_DISPATCH, FIRST_RESPONDER.
QIDM attributes and combinations unchanged (PurposeCode fieldId stays the same).

================================================================================
================================================================================
2026-04-23  FIELD CONFIG REPORT generated and committed
================================================================================

Generated a full field-by-field configuration report for all 5 entity QIFs in v2.6.
Covers: TYPE, FIELD_ID, LABEL, HIDDEN, ATTRIBUTE_TYPE_ID, CODE_TYPE_CATEGORY,
        CODE_TYPE_SOURCE, INIT_VAL, MAX_LEN, codeTypeProvider.

Output: docs/base/FIELD_CONFIG_REPORT_FL_FCIC.txt
Committed: commit 3c40933

KEY OBSERVATIONS FROM REPORT:
  1. Boat RegistrationState -- FormSelect with initialValue='FL' but NO attributeTypeId=STATE.
     Unlike Vehicle and Person (which use the NCIC pattern: attributeTypeId=STATE + codeTypeProvider=NCIC),
     Boat RegistrationState has no attribute type. Boat state won't reverse-lookup.
     ACTION NEEDED: Verify against FL_FCIC XML spec; may need dual-field or NCIC pattern.

  2. Article ImageIndicator / RelatedHitSearchIndicator -- FormCheckbox (not FormSelect).
     Vehicle, Firearm, Person use FormSelect YES_NO_UNKNOWN/NIBRS with initialValue='Y'.
     Article and Boat use FormCheckbox. These won't send 'Y'/'N' the same way.
     ACTION NEEDED: Verify intended behavior against FL_FCIC XML spec.

================================================================================
================================================================================
v3.0  2026-05-01  COMPLETE REBUILD -- 8-QIDM merged architecture
================================================================================

MOTIVATION
  v2.x had 25 split QIDMs (one per combo path) to avoid duplicate keyReferences
  within a single QIDM. This worked for import but violated LIMITATION #2 (one
  QIDM per entity+query pair -- only the first is evaluated, rest silently
  ignored). The v3.0 rebuild merges all split QIDMs into 8 single QIDMs using
  invented keyReferences (per LIMITATION #21) and separate platform query types
  for co-fire paths.

BUILD APPROACH
  Complete from-scratch rebuild using scripts/build_fl_fcic.ps1.
  Modeled on NJ_NJCJIS v2.0 build script (proven 66 PASS / 0 FAIL).

  Key patterns adopted from NJ:
  - PSCustomObject + @() arrays (fixes PS 5.1 single-element serialization)
  - Deep-copy layouts via ConvertTo-Json | ConvertFrom-Json (fixes CAD/FR leak)
  - BuildMultiCardLayout with card definition hashes
  - AddCadNodes/AddFrNodes with proper FormSelect/FormCheckbox CAD fields
  - ConvertTo-Json -Depth 100 (not 30)

ARCHITECTURE -- 8 COMMSYS QIDMs

  1. VehicleRegistrationQuery (6 combos: RQ x2 + FRQ x4)
     Plate+PlateType+PlateYear+State, VIN+State, Plate, VIN, Decal+Year, TitleLien
     Invented keyRefs: RQLicensePlateNumber, RQVehicleIdentificationNumber,
       FRQLicensePlateNumber, FRQVehicleIdentificationNumber, FRQDecalNumber,
       FRQTitleLienInformation

  2. VehicleStolenQuery (2 combos: QV x2)
     Plate, VIN -- separate platform query type, co-fires with VehicleReg
     Invented keyRefs: QVLicensePlateNumber, QVVehicleIdentificationNumber

  3. DriverLicenseQuery (4 combos: DQ x2 + FDQ x2, autoSelect=true)
     OLN+State, OLN, Name+DOB+Sex+State, Name+DOB+Sex
     queriesToDeselect: [DriverHistoryQuery]
     Invented keyRefs: DQOperatorLicenseNumber, FDQOperatorLicenseNumber,
       DQName, FDQName

  4. WantedPersonQuery (2 combos: QW x2)
     OLN+Name, Name+DOB -- separate platform query type from DL
     Invented keyRefs: QWOperatorLicenseNumber, QWName

  5. DriverHistoryQuery (2 combos: KQ x2, autoSelect=false)
     OLN+State+Purpose, Name+DOB+Sex+State+Purpose -- DH-suffix fields
     queriesToDeselect: [DriverLicenseQuery]
     Attention: handler-only (NOT in combo requirements)
     Invented keyRefs: KQOperatorLicenseNumber, KQName

  6. GunQuery (3 combos: QG x3)
     Serial, NCIC#, PCN
     Invented keyRefs: QGGunSerialNumber, QGNCICNumber, QGProcessControlNumber

  7. ArticleSingleQuery (4 combos: QA x4)
     Serial+Type, OAN+Type, NCIC#, PCN
     Invented keyRefs: QAArticleSerialNumber, QAOwnerAppliedNumber,
       QANCICNumber, QAProcessControlNumber

  8. BoatQuery (12 combos: BQ x3 + FBQ x4 + QB x5)
     BQ: Name+DOB+State, Hull+State, Reg+State
     FBQ: Hull, Reg, Decal, TitleLien
     QB: CoastGuard, Hull, Reg, NCIC#, PCN
     Invented keyRefs: BQName, BQBoatHullIdNumber, BQRegistrationNumber,
       FBQBoatHullIdNumber, FBQRegistrationNumber, FBQDecalNumber,
       FBQTitleLienInformation, QBCoastGuardDocumentNumber, QBBoatHullIdNumber,
       QBRegistrationNumber, QBNCICNumber, QBProcessControlNumber

ENTITY FORM CHANGES (vs v2.x)
  Vehicle: Added ImageIndicator (FormSelect YES_NO_UNKNOWN, N)
           Added LicensePlateYear initialValue='2026'
           RegistrationState: no initialValue (LIMITATION #30)
  Person:  3-card layout: Search Options, Driver License, Driver History
           ImageIndicator on Search Options card (Y for Person)
           DH-suffix fields on separate card
           Attention removed from form (handler-only)
           PurposeCode as FormSelect DEX_INQUIRY_PURPOSE_CODE (C)
  Firearm: ImageIndicator (FormSelect YES_NO_UNKNOWN, N)
  Article: ImageIndicator (FormSelect YES_NO_UNKNOWN, N)
  Boat:    Added Name/First/DOB fields for BQ combos
           ImageIndicator (FormSelect YES_NO_UNKNOWN, N)
           RegistrationState: no initialValue (LIMITATION #30)

RMS PATCHES APPLIED
  Patch 1: RegistrationState in Vehicle licensePlateIn any[]
  Patch 3: registrationState attr on Person QIDM (useAttributeId=true)
  Patch 6: Removed dead HIDLE fields (OOS + Owner + SSN attrs/combos)
  Patch 7: autoSelect=true on both Vehicle and Person RMS QIDMs

FIXES FROM v2.x
  - BOM: UTF8Encoding($false) -- no BOM in output
  - Bundle order: ENTITIES first
  - QIDM merge: 25 split -> 8 merged (LIMITATION #2 resolved)
  - ImageIndicator: FormSelect YES_NO_UNKNOWN on all 5 entities
  - CAD/FR layout: proper deep copy, FormSelect CAD fields, FormCheckbox FR
  - Attention: removed from combo any[] (handler fills regardless)
  - RMS Vehicle autoSelect: added (Patch 7)
  - Requestor/RelatedHitSearchIndicator: removed from QIDM attrs (no form fields)

VALIDATOR RESULT: 100 PASS / 0 FAIL / 1 WARN / 4 LIMITATION

================================================================================
================================================================================
v3.1  2026-05-07  Monorepo rebuild + Patch 8 + VehicleMakeCode fix
================================================================================

CHANGES FROM v3.0:
  1. Patch 8: LicensePlateNumberIn -> licensePlateNumber (CAD auto-populate)
  2. VehicleMakeCode: FormInput -> FormSelect (NCIC_FIREARM_MAKE/NCIC cross-provider fix)
  3. Monorepo rebuild with latest tools (validate.ps1, build_report.ps1)
  4. Reports regenerated at docs/base/ and docs/mc/

REASON:
  - CAD auto-populate standardization (Patch 8, all providers)
  - Cross-provider field type consistency audit (VehicleMakeCode)
  - Monorepo migration standardization

VALIDATOR RESULT:
  BASE: 101 PASS / 0 FAIL / 1 WARN / 4 LIMITATION
  MC:   101 PASS / 0 FAIL / 1 WARN / 5 LIMITATION

================================================================================

================================================================================
v3.4 (2026-05-11) -- LIMITATION elimination pass
================================================================================
CHANGED: DH-suffix fieldIds, queriesToDeselect, combo ordering, camelCase conversion
RESULT: BASE 101P/0F/0W/0LIM | MC 102P/0F/0W/0LIM

