# SUPPRESSION-SCOPE: direction-aware
#   Opted in 2026-08-01. Without this marker a registry row suppresses EVERY check for its
#   (query, keyRef, field) triple regardless of what the recorded decision actually said -- so a row
#   granting "this field may ride in any[]" also silences the OPPOSITE defect (a field wrongly
#   promoted INTO set[]), and the gate goes mute somewhere it was never granted.
#   Narrowing is STRICTER and CAN turn a provider red by un-silencing a real finding. That is the
#   point: if it does, investigate the finding -- do NOT remove the marker.
#   Measured before/after opting in; result recorded in the commit.
# CA_eSUN ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1. Format: query | keyRef | field | rule | reason | source | date
# Matching is on the first three columns (query | keyRef | field), case-insensitive.
#
# CONTEXT (v2.0 galvanization, 2026-07-23): the OOS Vehicle combos promote RegistrationState to
# set[] (metadata has it in any[]). Intentional -- OOS routing requires State present, and the combo
# also carries a RegistrationState EXISTS condition so the OOS vs in-state pair (RQ.P/QV.P, RQ.V/QV.V)
# is mutually exclusive (same class as CA_CLETS / CA_CLETS_OCATS / CA_VENTURA_COUNTY). State remaining
# in any[] would let the OOS combo fire without State and shadow the in-state combo. The DL/DH/Boat
# OOS combos keep State in any[] (existence-only EXISTS-gated), so only the two Vehicle combos diverge.

VehicleRegistrationQuery | RQ.P | RegistrationState | promoted-to-set | OOS plate combo requires State; EXISTS-gated for in/out mutual exclusion | design-decision | 2026-07-23
VehicleRegistrationQuery | RQ.V | RegistrationState | promoted-to-set | OOS VIN combo requires State; EXISTS-gated | design-decision | 2026-07-23
ArticleSingleQuery | devdoc #1 | ArticleSerialNumber | devdoc-optional-unreachable | DEVDOC-vs-FORM-DESIGN divergence, not a routing defect. caRequestPurposeCode is metadata-MANDATORY on every CA_eSUN combination but appears in NO devdoc item (the devdoc treats it as a provider-wide precondition, not a per-combination field). CA_eSUN deliberately leaves it officer-selectable with NO initialValue -- CLAUDE.md records it as "CaRequestPurposeCode (visible Inp, officer-selectable)" -- so a simulated devdoc-mandatory-only fill lacks it and no combo can match. The built QA combo is metadata-EXACT: set[caRequestPurposeCode, serialNumber] any[articleBrand, articleCategory, articleTypeCode], matching the metadata QA{ArticleSerialNumber} alternative field for field. Contrast CA_CLETS/CA_CONTRA_COSTA, which default purposeCode to C and therefore show 0 FAIL here. Registered rather than "fixed": adding an initialValue would change officer-facing behaviour on a documented design decision, and Rob rules on that. Verified 2026-07-31. | metadata-vs-devdoc | 2026-07-31
VehicleRegistrationQuery | 4 | LicensePlateTypeCode | built-as | Metadata 4{LicensePlateNumber} SET=[CaRequestPurposeCode,LicensePlateNumber,LicensePlateTypeCode] (raw <Requirements>, EMPTY <Any>) differs from built QV.P SET=[CaRequestPurposeCode,LicensePlateNumber] ONLY by LicensePlateTypeCode. That control is form-PREFILLED 'PC' because the sole OUT-OF-STATE plate variant RQ{LicensePlateNumber} MANDATES it (SET=[PurposeCode,plate,PlateTypeCode,PlateYear]) and metadata defines NO looser OOS plate variant -- so the prefill is load-bearing: removing it would leave every out-of-state plate query with no combination able to fire. With the prefill in place 4's VARIABLE set[] is [LicensePlateNumber], IDENTICAL to QV.P's, an exact collision no ordering can separate (BUILD_RULES 24; AZ_AZDPS DQPN/DQP precedent). Routing IMPOSSIBILITY, not a preference. v2.4 instead removed LicensePlateTypeCode/LicensePlateYear from QV.P's any[] and its inert CAD defaults[], taking fidelity to 0 OVER-PERMITTED with branches held at 27. | metadata-vs-devdoc | 2026-08-20
DriverLicenseQuery | L1.N | BirthDate | demoted-to-any | THE GATE IS WRONG HERE AND THE RAW XML SETTLES IT. audit_requirement_fidelity reports 'built L1.N OVER-PERMITTED: BirthDate'. Read from the raw <Requirements> per <Combination> (the sanctioned raw-XML exception), DriverLicenseQuery L1{Name} is: Set[CaRequestPurposeCode, Name, Any[Choice[Age / BirthDate]]]. A <Choice> inside <Any> makes BOTH branches OPTIONAL (usx-build Step 1), so BirthDate IS defined on this branch and riding it in any[] is exactly correct. The gate enumerates <Any>'s direct <Field> children and does not descend into a nested <Choice>, so a Choice-inside-Any optional reads to it as undefined -- the same flattening problem the Source Authority table already documents for METADATA_REFERENCE.txt, one level up. I removed BirthDate at v2.4 on the strength of a BROKEN PROBE of my own (mine read <Any> as empty everywhere because in this schema <Any> is NESTED INSIDE <Set>, so .Requirements.Any returned nothing) and restored it at v2.5 once the OuterXml was dumped. Note the DriverHistoryQuery L1{Name} sibling genuinely has NO <Any> at all, which is why BirthDateDH stays OUT of L1.N.DH -- the two are not symmetric. TOOLING GAP RECORDED, not fixed here: descending into Choice-inside-Any is a shared-tool change needing the 6-provider regression fixture and a 20-provider sweep, and some of the portfolio's 34 OVER-PERMITTED may be this same false class. | metadata-vs-devdoc | 2026-08-20
