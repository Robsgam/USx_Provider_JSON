CA_eSUN -- Build Notes
======================
Provider: CA_eSUN
State: California
v2.1  2026-09-02  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild


v1.1  2026-09-02  RND-71815 -- PurposeCode RENDERED AS A RADIO GROUP. SAN DIEGO SHERIFF ONLY.
  *** THIS IS A DELIBERATE ONE-OFF, OUTSIDE THE NORMAL PROCESS. *** Rob 2026-09-02: "this will be
  considered a one off for San diego sheriif only as it stands" and "recall this is a one off so it
  can be outside the noraml process". It applies to CA_eSUN and to NO other provider; the other 19
  keep PurposeCode exactly as they have it. Do not propagate. Do not treat the gate state below as
  a standard to be met -- v1.0 is a faithful capture with pre-existing live defects, and v1.1
  changes ONE thing on top of it.
  CHANGED: PurposeCode type.resolvedName FormSelect -> FormRadioGroup on ALL FIVE entities
    (Vehicle, Person, Firearm, Article, Boat) across all three layout variants = 15 controls.
    displayName -> 'Radio Group'.
  LAYOUT CORRECTED ON ROB'S EYE, 2026-09-02: "so the example in the jira had the radio boxes across
    the top   this json lists then on the uper left".
    props.direction 'column' -> 'row'. I had taken 'column' from Peter's reference node shape in
    the ticket, but his SCREENSHOTS show the options laid out horizontally -- the snippet and the
    images disagree, and the images are what SDSO asked for.
    THE 'UPPER LEFT' WAS A SECOND, SEPARATE CAUSE and direction alone would not have fixed it.
    PurposeCode was ALREADY in row 1 of every card (i.e. already at the top), but its row was
    NARROW: templateColumns=[3] on Vehicle -- a quarter width -- and [6] on the other four. Two
    long labels ("C - Criminal Justice", "I - Immigration Enforcement") cannot lay out horizontally
    in a quarter-width column, so they stacked at the left. All 15 rows widened to ['12'].
    SAFE BECAUSE MEASURED: each of those rows contains PurposeCode and NOTHING else (verified per
    row before widening; the script SKIPS any row with more than one child), so widening displaces
    no sibling control. This is also the ticket's own last requirement -- "ideally should be able to
    control width of the radio choice label, and have text wrap if field is narrow".
  DEFAULT 'C' ADDED ON ROB'S INSTRUCTION ("and we need a default of c please"): props.initialValue
    = 'C' on all 15 controls -- "C - Criminal Justice", confirmed present in the tenant picklist.
    THIS DIVERGES FROM THE TICKET, DELIBERATELY AND ON THE OPERATOR'S CALL: RND-71815 requirement 2
    says "default state : none of the options is selected (same as dropdown behavior)".
    IT IS SAFE HERE, AND THAT IS A MEASURED CLAIM, NOT AN ASSUMPTION. A prefill on a set[] field
    normally hides every combination that needs the field ABSENT (BUILD_RULES 24, which killed 35
    combos across 6 providers). PurposeCode is in the set[] of ALL 25 combinations here -- so it
    cannot shadow one combination over another; it cancels out of every comparison. Exactly the
    CA_CLETS purposeCode='C' precedent that CLAUDE.md records as harmless for this same reason.
    Plan regenerated after the prefill was added: still 35 tests, unchanged shape.
    NOTHING ELSE TOUCHED -- verified by reading the emitted file back: 8 QIDMs / 25 combinations
    before AND after, 15 of 15 converted, 0 FormSelect PurposeCode remaining.
  VALIDATOR UNCHANGED: 59 PASS / 7 FAIL / 53 WARN / 5 LIMITATION, identical to v1.0. The validator
    does not whitelist resolvedName, so the new control type passes through it untouched. That is
    information, not reassurance -- it means the validator can tell us nothing about whether
    FormRadioGroup actually renders.
  THE DRIVER HAD TO BE FIXED FIRST, and this was measured, not guessed: PurposeCode sits in the
    set[] of ALL 25 combinations, and the driver had ZERO radio handling (grep 'radio' returned
    nothing in usx_lib.js or driver.js). Converting without driver support would have left Send
    disabled on all 35 plan tests -- a total test failure, not a partial one. usx_lib.js gained
    findRadioGroup + selectRadioGroup at BUILD 2026-09-02a (commit 5ac1a26c) before this bump.
  STILL UNVERIFIED, AND THE FIRST RETEST IS THE TEST OF IT: FormRadioGroup is not confirmed to
    render at all. RND-71815 is still 'To Do' and the node shape came from a Jira comment, not from
    a shipped example. If the control does not render, PurposeCode has no input and every query
    fails -- which is exactly why the driver's [USx-RAD] logging dumps the options it found.
    ALSO OPEN on that ticket: its requirement "must select a button for search to proceed" is
    harmless HERE (PurposeCode is mandatory in every eSUN combination anyway) but would be a WIRE
    change on any provider where the field rides in any[]. Another reason this stays a one-off.
  TENANT PICKLIST CONFIRMS THE FIT: PurposeCode returns exactly TWO options at San Diego --
    "C - Criminal Justice" and "I - Immigration Enforcement" -- so a radio pair is the right shape,
    and DEX_INQUIRY_PURPOSE_CODE demonstrably resolves on this tenant (contrast LIMITATION #39,
    where it returned ZERO options on NY and forced a revert to FormInput at v4.23).
  COST PAID: the v1.0 package -- 25 logs, all PASS -- is archived to logs/<Entity>/_archive_pre_v1.1/.
    Plan and picklist scope regenerated at v1.1. A full re-drive is owed.
  v1.0 REMAINS RECOVERABLE EXACTLY AS INGESTED: tag CA_eSUN-v1.0-baseline, carrying both the
    working JSON (SHA CA7F4493...) and the verbatim tenant export (SHA 06C85659...).
        git checkout CA_eSUN-v1.0-baseline -- providers/CA_eSUN
  IMPORT ARTIFACT: docs/deliverables/CA_eSUN_v1.1_RADIOBUTTON_FIRST_RESPONDER_ONLY.json -- byte-identical to the root
    JSON, named so the radio build is identifiable at a glance (Rob's request). The ROOT stays
    CA_eSUN_v1.1.json because Get-ProviderRootJson matches ^<PROVIDER>_v[\d.]+\.json$ exactly and
    any extra token makes every gate stop finding the provider.

*** THE VERSION LINE RESTARTS AT v1.0. THIS IS NOT A REGRESSION FROM v2.6. ***
Rob, 2026-09-02: "i want to start the built line at this json not our process ... we need to start
it over from zero", and "for now the json is the authority and we need to catalog it as version
1.0." The prior 2.x line (v2.0 - v2.6, devdoc+metadata-driven, generated by
scripts/build_ca_esun.ps1) is RETIRED and removed from the working record. It remains fully
recoverable from git history -- 76 commits touched this provider, the last being v2.6 at a9d7f821.

v1.0  2026-09-02  HAND BUILT BY ENGINEERING -- captured from the San Diego Sheriff live tenant
  ORIGIN: not built by this repo's process. This is the configuration engineering hand-built and
    that San Diego Sheriff actually runs, exported from the tenant on 2026-09-02 12:55.
  VERBATIM BASELINE ARCHIVE: source/CA_eSUN_v1.0_PRE_RADIOBUTTON_SDSO_BASELINE_2026-09-02.json
    SHA-verified byte-identical to the download. That file is the archive point and is never edited.
  THE WORKING v1.0 DIFFERS FROM THE ARCHIVE IN EXACTLY TWO WAYS, both mechanical, both required:
    1. UNWRAPPED. The download is a DEPARTMENT EXPORT: {departmentBundle:{bundles,behaviors},
       m43Forms}. Our validator rejects that outright -- "[FAIL] Missing top-level 'bundles' array"
       -- so it can be neither validated nor imported as-is. Taking departmentBundle as the root
       yields {bundles, behaviors}, which is EXACTLY the root shape of the previous SDSO export, so
       the unwrap is lossless. m43Forms was null; nothing was discarded. Fidelity re-verified after
       every write: 2 bundles / 16 configurations / 8 QIDMs / 25 combinations / behaviors present.
    2. VERSION STAMPED INTO THE BUNDLE DESCRIPTION. Was "Provider configuration for CA eSUN" --
       carrying no version at all. CLAUDE.md's Versioning Policy requires the version to live in
       the filename AND the bundle description (enforce CHECK 3i reads it), and without it
       reset_test_package failed outright with "[ERROR] Could not determine version for CA_eSUN".
       That tool derives the version from the build script or a versioned description, and a
       hand-built JSON has neither.

  WHAT IT CONTAINS (8 QIDMs / 25 combinations, no RMS bundle):
    Vehicle  VehicleRegistrationQuery  6   QVLicensePlateNumberIn, IdentificationNumberInOut,
                                           4LicensePlateTypeIn, RQLicensePlateTypeYearOut,
                                           VPNameBirthDateIn, NameAddressIn
    Vehicle  CAILojackQuery            1   LojackID
    Person   DriverLicenseQuery        6   Person AFS 2, DriverHistoryQuery 4
    Person   AFS                       2   PurposeCodeNameLastBirthDate, PurposeCodeNameLastAge
    Person   DriverHistoryQuery        4   L1Name, KQName, KQOperatorLicenseNumberIn/Out
    Firearm  GunQuery                  3   Boat BoatQuery 2, Article ArticleSingleQuery 1
  TWO QUERIES THIS REPO HAS NEVER BUILT: CAILojackQuery and AFS. They are in scope by Rob's ruling
    (the live JSON is the authority), NOT adjudicated against the devdoc Basic list.
  ALSO PRESENT AND NEW TO US: a top-level `behaviors` block (ParallelQuery, isEnabled=false),
    parallelQueryHandler, IgnoreUserValueRuleHandler x6, CommsysArticleAttributeRuleHandler x4,
    and DEX_INQUIRY_PURPOSE_CODE x15 -- SDSO is a tenant where that code table IS provisioned
    (cf. LIMITATION #39, where it resolved to ZERO options on NY).

  *** KNOWN DEFECTS CARRIED IN DELIBERATELY -- v1.0 IS A FAITHFUL CAPTURE, NOT A CLEAN BUILD ***
  Validator on v1.0: 59 PASS / 7 FAIL / 53 WARN / 5 LIMITATION.
  THE 7 FAILs ARE A LIVE REGRESSION INTRODUCED BETWEEN THE TWO EXPORTS, and this is measured, not
  inferred -- the SAME validator on the 2026-08-17 export reads 64 PASS / 1 FAIL:
      5x AP #1  'State'   attributeTypeId=STATE on the form, NO codeTypeProvider on the QIDM attr
                          -> sends the NUMERIC RMS attribute ID instead of the 2-letter code.
                          On VehicleRegistrationQuery, DriverLicenseQuery, DriverHistoryQuery,
                          AFS and BoatQuery.
      2x AP #2  'SexCode' attributeTypeId=SEX without codeTypeProvider -> same numeric-ID defect.
                          On DriverLicenseQuery (pre-existing, present on 8.17 too) and
                          DriverHistoryQuery (NEW).
  The Aug-17 export CARRIED "codeTypeProvider": "NCIC" on those attributes; the Sep-02 export has
  REMOVED them. Six new FAILs, one cause.
  THIS ALSO EXPLAINS THE STRAY LITERAL 59872938171. It appears in four Vehicle routing conditions
  as `RegistrationState IN ["CA","null","59872938171"]` -- an 11-digit NUMERIC ATTRIBUTE ID. The
  gate was widened to match the numeric value BECAUSE the field is sending the numeric value. The
  same defect surfacing twice: once on the wire, once as a workaround in the routing gate. The
  literal string "null" in that list is the same shape of workaround.
  FIX IS OWED, NOT APPLIED HERE. Rob is providing a tenant with the attribute populated for
  testing; the correction lands as the first change on the v1.x line, together with RND-71815.

  verify_build ON v1.0: 50 FAIL / 11 WARN. Grouped by CLASS rather than listed, because 50 lines
  is a catalogue and not a to-do list. Every one is carried in from the live config:
      x16  QIDM attr sourceField not present in the QIF fieldIds -- a DEAD REFERENCE the officer
           cannot fill (Person 11, Vehicle 2, Firearm 1, Boat 1, Article 1). 'ORI' is the bulk of
           it and is legitimate (AUTH envelope, see below); the rest need adjudication.
      x8   SHADOW -- a combo fires BEFORE another on the earlier one's own minimal set[] payload
           (DriverLicenseQuery 4, DriverHistoryQuery 2, VehicleRegistrationQuery 2). These are
           potentially UNREACHABLE live search paths and are the most serious class here.
      x7   combination with NO `state` property (required on CommSys QIDMs)
      x6   missing queryLabel  (matches the 6-of-8 empty labels noted above)
      x6   Name combo missing the OLN > Name guardrail (OLN + Name co-entry bleeds Name)
      x3   other identifier-priority guardrails missing (Hull > Reg, plate > VIN)
      x2   primaryFieldReference naming an attribute the QIDM does not declare
      x1   queryLabel outside the standard set
      x1   State field label missing its routing hint
      x1   banned pattern
  NONE OF THESE ARE BEING FIXED IN v1.0. The baseline's job is to record what San Diego runs.

  OTHER CARRIED-IN ITEMS (recorded, not fixed):
    23 VALUE-COMPARISON routing conditions and ZERO existence conditions
       (IN 11 / EXCLUSIVE 7 / NOT_EQUALS 4 / NOT_IN 1). This is the poisoned-array class the rest
       of the portfolio was converted away from -- a value-comparison operator poisons the WHOLE
       conditions array.
    3x LIMITATION #30  RegistrationState carries initialValue='CA' on Vehicle, Person AND Boat
       while separate In/Out combos exist -- a prefill on a routing field, entangled with the
       IN [CA,...] gates above.
    1x AP #14 / LIMITATION #25  DL + DH share the Person form with NO DH-suffix fieldIds.
    queryLabel is EMPTY on 6 of 8 QIDMs; there is no entity display order[]; there is no RMS
       bundle at all.

  NO BUILD SCRIPT EXISTS, AND THAT IS THE POINT. v1.0 is a captured artifact, so
    enforce PHASE 2f (reproducibility: committed JSON == a fresh build) CANNOT pass until a build
    script is written that reproduces it. That is inherent to starting the line at a hand-built
    JSON and is not a gate to be worked around.

  CATALOGUING PRODUCED IN THIS PASS (Rob: "put together a picklist and test plan for the json so we
  can catalog it properly before we make the changes"):
    logs/CA_eSUN_PICKLIST_SCOPE.json + .console.js -- 15 select fields across 5 entities:
        Article  PurposeCode, ArticleTypeCode
        Vehicle  LicensePlateTypeCode, PurposeCode, RegistrationState, VehicleMakeCode
        Person   PurposeCode, SexCode, RegistrationState
        Firearm  FirearmCaliber, FirearmMake, FirearmType, PurposeCode
        Boat     PurposeCode, RegistrationState
    logs/CA_eSUN_TEST_PLAN_v1.0.json -- 35 tests
        Vehicle 15 / Firearm 7 / Article 5 / Boat 4 / Person 4
    logs/CA_eSUN_TEST_PLAN_SPEC_v1.0.json -- the independent devdoc+metadata plan
    docs/reference/TEST_VALUE_OVERRIDES.txt REWRITTEN for the v1.0 field namespace. The previous
        contents targeted the retired 2.x names (VehNameLast/GunAge/...) which do not exist here;
        v1.0 uses the portfolio-standard NameLast/NameFirst/BirthDate/Age on every entity.
        Trust issues went 37 -> 20.
    THE 20 REMAINING WARNINGS ARE ALL 'ORI' AND ARE CORRECT. 12 combinations reference ORI in
        any[] and NO form control for it exists on ANY entity -- it rides in the AUTHENTICATION
        envelope, not typed by an officer. Inventing a test value would make the driver type into
        a control that does not exist.
    FirearmCaliber / FirearmType test values are PROVISIONAL and flagged in the overrides file:
        they are codeTypeCategory dropdowns not yet confirmed against San Diego's own code tables.
        Confirm via the picklist capture BEFORE driving -- a value outside the tenant list cannot
        be selected and the test cannot fire (the CA_CLETS_OCATS v2.10 empty-Article-dropdown
        defect, where every request-side gate was green and the control offered zero options).

  NEXT, IN ORDER, NONE OF IT DONE YET:
    1. Picklist capture on a San Diego tenant -> confirms PurposeCode/FirearmType/FirearmCaliber
       and settles whether DEX_INQUIRY_PURPOSE_CODE resolves there.
    2. RND-71815 -- render Search Purpose as radio buttons (FormRadioGroup). Rob is sourcing a
       tenant with the attribute populated. NOTE the open question already raised on that ticket:
       its requirement "must select a button for search to proceed" is a WIRE change if applied to
       a field that rides in any[], and our test driver has NO radio support at all (zero
       occurrences of "radio" in automation/extension) -- it drives react-select only.
    3. The AP #1 / AP #2 codeTypeProvider fix.
================================================================================
v2.0 -- 2026-09-02 -- ENGINEERED LINE OPENS (separate from the SDSO live line)
================================================================================
CHANGED:
  A1. OriginatingAgencyORI attribute removed from all 7 CommSys QIDMs.
  A2. bare 'ORI' removed from 20 combinations' any[] (the second half of the drop,
      missed on the first pass and caught by re-reading the fidelity finding).
  B.  Combinations ordered most-specific-first in 4 QIDMs, with an identifier-priority
      tiebreak (Plate > VIN > Hull > OLN > Reg > Serial > SSN) applied only between
      combos with no subset relationship. OLN now beats Name on DL and DH; Hull beats
      Reg on Boat.
  C.  RQLicensePlateTypeYearOut: LicensePlateTypeCode + LicensePlateYear any[] -> set[].
  D.  RegistrationState initialValue 'CA' cleared on 9 controls (3 entities x 3 layout
      variants) -- LIMITATION #30. Rob's call, asked before the sweep.
REASON:
  v1.0/v1.1 are a faithful capture of the hand-built San Diego Sheriff configuration and
  carry its defects unchanged. v2.0 is the first build of a SEPARATE engineered line and
  fixes what can be fixed without touching cards, controls, widths or labels.
  The ORI drop is justified by devdoc line 8: <OriginatingAgencyORI> is for on-behalf-of
  another agency or an entry/edit transaction (Entry|Modify|Clear|Locate|Cancel). We build
  neither. Proven inert first: across 25 committed v1.1 logs it carried the SAME value as
  <Authentication>/<ORI> ('MK1234567' x21), so removal changes no transmitted value.
MEASURED v1.1 -> v2.0:
  requirement fidelity   25 branches / 18 OVER / 1 UNDER  ->  27 branches / 0 / 0
  query trace            26 built / 1 PREFILL-DEAD        ->  27 built / 0 / 0 / 0
  combo reachability     3 registered dead                ->  23 checked, all reachable
  audit_prefill_shadow   3 FAIL                           ->  0 FAIL (41 pairs)
  wiring closure         38 breaks                        ->  11 breaks
  verify_build           50 FAIL / 11 WARN                ->  35 FAIL / 10 WARN
  validator              59P/7F/53W/5LIM                  ->  59P/7F/53W/2LIM
  test plan              35 tests                         ->  43 tests (Person 4 -> 12)
NOT CHANGED, DELIBERATELY:
  The 7 validator FAILs. Five are AP #1 on State and the LIVE WIRE REFUTES them --
  <State>CA</State> x5 and <State>GA</State> x6 across 25 logs, real codes. The other two
  are SexCode AP #2 and are UNTESTED, not passed: SexCode reached zero wires because both
  combos carrying it were dead. Now reachable, so v2.0's sweep can settle it.
  The 35 remaining verify failures and 11 wiring breaks need FORM controls
  (ArticleTypeCodeRemainder, Attention x2, 8 vehicle attrs on the Person QIDM) -- v3.

================================================================================
v2.2 -- 2026-09-02 -- IDENTIFIER-PRIORITY GUARDRAILS
================================================================================
CHANGED:
  F. 14 NOT_EXISTS guardrail conditions across 12 combinations, enforcing the
     portfolio identifier priority: Plate > VIN > Name (Vehicle), OLN > Name
     (DriverLicense + DriverHistory), Serial > Name (Firearm), Hull > Reg (Boat).
     AFS and ArticleSingleQuery excluded -- name-only and single-identifier
     respectively, so a guardrail would have nothing to defer to.
  Deliverable copies now emitted as CA_eSUN_v<X.Y>_RADIOBUTTON.json for every
  version of this line, byte-identical to the root and hash-checked.
REASON:
  v2.1 had ZERO NOT_EXISTS conditions, so it had no identifier-priority guardrails
  and emit_test_plan generated NO guardrail tests (it derives them FROM NOT_EXISTS).
  Over-filled forms were governed by COMBO ORDER alone -- correct as far as it goes,
  but ordering only decides between combos that both match and is invisible to every
  gate that reads conditions.
EFFECT (measured):
  plan 41 -> 53 tests; kind=guardrail 0 -> 12
  reachability 23 checked, all reachable   prefill-shadow 0 FAIL (41 pairs)
  fidelity 27 branches / 0 UNDER / 0 OVER  validator 59P/7F/53W/2LIM (unchanged)
  layout still identical to v1.1 apart from the 9 State defaults
OPEN RISK -- THE SWEEP IS THE TEST:
  Most guarded combos already carry IN / NOT_EQUALS / EXCLUSIVE conditions, so these
  are MIXED condition arrays. The documented poisoned-array model says a mixed array
  is disabled entirely, which would make the guardrails inert AND break the State
  gating with them. That model is already contradicted here (T9 submits with State
  blank, T10 does not with State=GA, five runs) but mixed arrays are UNPROVEN.
  Failure signature to watch: an in-state combo firing on an out-of-state fill.
