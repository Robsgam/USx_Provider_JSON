# PENDING_UPDATES.txt -- CA_eSUN
# Lines without a leading '#' block enforce.ps1 and prevent testing.
#
# CLEARED 2026-08-01 at v2.2. Every item resolved against CA_eSUN's OWN devdoc + metadata.
# Kept as a record of what closed and why -- a bare empty file loses the reasoning.
#
# [FLAG:GUN-NAME-CHOICE-IN-SET]  RESOLVED at v2.1 -- QGH split into QGH.A (GunAge) + QGH.B
#   (GunBirthDate), one combination per <Choice> branch, because set[] has no OR. Verified against
#   this provider's own metadata, which puts Choice[Age|BirthDate] INSIDE <Set> on the Name variant.
#
# [FLAG:METADATA-DEMOTION-ADJUDICATE]  RESOLVED at v2.2. audit_metadata is now 159 PASS / 0 FAIL /
#   1 WARN, and the single WARN is a gate artefact, not a defect (see below).
#
# 55 devdoc-optional NO-FIRE FAILs -> 0, and the cause was NOT a per-query build error.
#   CA_eSUN's OWN devdoc, "Transaction Requirements" (lines 19-31):
#     "Beginning July 21, 2021 CLETS requires a Purpose Code field to be supplied with every CLETS
#      transaction ... Any transaction which does not supply a valid Purpose Code value will be
#      REJECTED BY CLETS. ConnectCIC does not default the value of field; it must be provided by the
#      partner implementation as a user selectable field ... mandatory for all transactions."
#     Values: C = Criminal Justice, I = Immigration Enforcement, U = Title 8 s1325 USC.
#   caRequestPurposeCode is therefore a TRANSACTION-ENVELOPE requirement, not a per-query search
#   field -- which is exactly why the devdoc's per-query "Possible Combinations" lists never mention
#   it, why all 20 built combos carry it in set[], and why every devdoc-faithful fill omitted it and
#   matched nothing. One missing envelope field, 55 reported FAILs.
#   TWO FIXES, each independently justified rather than applied to satisfy a gate:
#     1. COMBO DEFAULT on all 20 combos. CAD does not apply form initialValues (BUILD_RULES 12), so
#        a CAD-dispatched query omitted the field entirely and CLETS would REJECT the transaction.
#        0 of 20 had a default. That was a live defect on the CAD path, gate or no gate.
#     2. FORM initialValue 'C'. Risk is asymmetric: blank = guaranteed CLETS rejection, whereas C
#        (Criminal Justice) can never silently become an immigration-enforcement purpose. The control
#        stays a VISIBLE, EDITABLE Inp on all five cards, so it remains "user selectable" exactly as
#        the devdoc requires -- pre-selected, not locked.
#   Prefilling it is LOAD-BEARING, not a BUILD_RULES 24 violation: it is mandatory in EVERY
#   combination, so satisfying it equally for all of them cannot shadow one path over another.
#   Applied as a build-time normalization loop (one rule, 20 sites) -- not a post-build JSON patch.
#
# [RESOLVED] verify_build CHECK 14 vs the devdoc order -- the decision point recorded here on
#   2026-07-31 is CLOSED, and it was closed the way it should have been: by fixing the SHARED TOOL,
#   not by hand-patching this provider. CHECK 14 had only tested combo A's primaryFieldReference and
#   never asked whether A's set[] was even satisfiable by the payload; it now requires that too. All
#   20 providers pass with zero regressions and CA_eSUN went 1 FAIL -> 0. Neither of the check's
#   original suggestions was taken: a GunAge EXISTS condition would have been a tautology (GunAge is
#   already in QGH.A's set[]), and reordering QGH.B first would have created a real devdoc-order
#   INVERSION -- trading a true finding for a false one.
#
# KNOWN GATE ARTEFACT, build is CORRECT (1 audit_metadata WARN, does not block enforce):
#   "serialNumber: QIF maxLength 20 > XML maxLength 14 (GunSerialNumber) -- server may reject"
#   The form is already right: the FIREARM card's serialNumber is maxLength 14 and the ARTICLE card's
#   is 20, each matching ITS OWN metadata field (GunSerialNumber 14, ArticleSerialNumber 20). The two
#   controls live on different entity QIFs and legitimately share the fieldId 'serialNumber'.
#   audit_metadata does not scope this check by QIF, so it compares the Article control against the
#   Gun field. Not hand-patched: narrowing the control to 14 would TRUNCATE legitimate 20-character
#   article serials to satisfy a check. Fixing the tool is a shared-tool change touching all 20
#   providers and is not being done mid-provider.
#
# STILL OPEN (not blocking; recorded so it is not mistaken for finished):
#   Never USx-tenant-tested. v2.2 owes a full 5-entity sweep from T1.
# RETIRED 2026-08-14 -- This provider builds NO ImageIndicator control at all -- verified from the emitted JSON (absent from every QUERYINPUTFORM and every QIDM). The flag is NOT-APPLICABLE, not deferred. This is the SAME ruling already applied to CA_CLETS on 2026-08-13; it simply was never propagated to its five siblings. Adding controls to satisfy the rule would OVER-PERMIT -- the identical adjudication that scoped HI_HCJDC_OFML v4.17 to Vehicle-only and excluded Article on IL_LEADS_OFML v2.4. Zero JSON change, zero version bump, zero test package archived.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
