# CA_eSUN — DEX ticket pointer

**DEX-1312** — [CA-eSUN] USx Provider Build (parent, Task, In Progress)
**DEX-1313** — USX Search Purpose as Radio buttons (subtask, "For SDSO only.")
https://mark43.atlassian.net/browse/DEX-1312 · https://mark43.atlassian.net/browse/DEX-1313

## Posted

POSTED: v2.2 comment 811409 2026-09-03

Build notes for v1.0 / v1.1 / v2.2 on **DEX-1313**, following Rob's own comment 811380 which
explains what each JSON is and ends "See build notes below:". Four-section format per
`knowledge-base/JIRA_COMMENT_TEMPLATE.txt`.

**Read the marker narrowly.** It records that a comment was posted for the v1.x/v2.x
RADIOBUTTON line. It says nothing about **v3.0**, the mainline built from scratch, which is a
separate line and has its own release line still owed.

## What was deliberately left off the comment

The two carried items — the 7 validator FAILs that the 53 captures refute, and the 6d
BirthDate over-permit — were drafted and Rob removed them. That is the template's rule (the
"known limits" and "documented skips" sections were deleted from the ticket format on
2026-08-13), and its condition is that the content must live in the repo first. It does:
`docs/tracking/CA_eSUN_BUILD_NOTES.txt`, block "v2.2 — KNOWN ITEMS CARRIED, NOT FIXED".
Do not re-add them to the ticket.

## Open on the ticket, and they are Rob's, not mine

- **Screenshots or a video** of the radio-button change — Gordon Hallof, comment 810901.
  Needs the FIRST_RESPONDER view.
- **Extend FR radio buttons to all CA JSONs** — Gordon's proposal, comment 810924. Five more
  providers; needs an explicit decision before anyone assumes it.
- **Leo's buy-off** — Gordon asked whether Peter had run it past him. Unanswered.

## Standing

Jira remains HELD. Approval is per provider and per post, and never carries — the approval for
811409 covers 811409 only.
