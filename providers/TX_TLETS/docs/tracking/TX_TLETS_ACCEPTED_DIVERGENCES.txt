# TX_TLETS ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1 (CHECK 4 / 4d). Each non-comment line records a
# set/any divergence that is intentional and correct, so the gate treats it as [NOTE] not [FAIL].
# Format (pipe-delimited): query | keyRef | field | rule | reason | source | date
#

BoatQuery | BQRegistrationNumber | registrationState | promoted-to-set | Intentional out-of-state boat routing: BQ combos are state='Out', so destination State is required (set[]) to force the OOS boat query; metadata lists State in any[]. Pre-existing since v3.x (Boat unchanged by v3.9 Attention conversion); surfaced by CHECK 4d on rebuild. | metadata-vs-devdoc | 2026-06-22
BoatQuery | BQBoatHullIdNumber | registrationState | promoted-to-set | Intentional out-of-state boat routing: BQ combos are state='Out', so destination State is required (set[]) to force the OOS boat query; metadata lists State in any[]. Pre-existing since v3.x (Boat unchanged by v3.9 Attention conversion); surfaced by CHECK 4d on rebuild. | metadata-vs-devdoc | 2026-06-22
DriverLicenseQuery | RSDWW | OperatorLicenseNumber | shadow-unbuilt-REVISIT | RSDWW ('Driver License Inquiry by OLN') is a duplicate-input twin of the built DQOLN combo -- both require exactly set[OperatorLicenseNumber], so only one is reachable under first-match routing; RSDWW left unbuilt (same class as QW/QV shadows + the unbuilt plain VehicleRegistrationQuery merged into VehicleInsuranceRegistrationQuery). Covered by DQOLN; devdoc gate (enforce 2e) + audit_metadata reachability both pass. REVISIT: confirm in a live test whether the platform auto-sends RSDWW server-side, or whether its by-OLN response scope differs from the generic DQ -- if it returns distinct data, promote it (needs a real discriminator, not just a second identical-set combo). | metadata-vs-devdoc | 2026-07-28
VehicleInsuranceRegistrationQuery | RQLicensePlateNumber | FinancialResponsibilityType | dead-combo-REVISIT | RQLicensePlateNumber is unreachable: REGLicensePlateNumber is ordered first and its only extra set field is FinancialResponsibilityType, which the form pre-fills E (v4.3, Rob call -- devdoc stated default), so REG always matches first. Same for RQVehicleIdentificationNumber behind VINVehicleIdentificationNumber. Both metadata-real keyRefs, so the plain-registration path is currently unbuildable. The v4.12 test logs named for these two actually exercised their shadowers -- the RQLicensePlateNumber log wire carries BOTH FinancialResponsibilityType (REG set) and LicensePlateTypeCode (RQ set), i.e. the LIMITATION #1 union pool. DECISION PENDING (Rob): (a) drop FRT initialValue so blank routes RQ / filled routes REG-VIN -- reverses the v4.3 default call but makes both paths real; (b) drop both RQ combos as subset-shadows (v4.9 QV precedent); (c) keep as documented dead. Found by audit_combo_reachability.ps1 2026-07-29. | metadata-vs-devdoc | 2026-07-29
VehicleInsuranceRegistrationQuery | RQVehicleIdentificationNumber | FinancialResponsibilityType | dead-combo-REVISIT | RQLicensePlateNumber is unreachable: REGLicensePlateNumber is ordered first and its only extra set field is FinancialResponsibilityType, which the form pre-fills E (v4.3, Rob call -- devdoc stated default), so REG always matches first. Same for RQVehicleIdentificationNumber behind VINVehicleIdentificationNumber. Both metadata-real keyRefs, so the plain-registration path is currently unbuildable. The v4.12 test logs named for these two actually exercised their shadowers -- the RQLicensePlateNumber log wire carries BOTH FinancialResponsibilityType (REG set) and LicensePlateTypeCode (RQ set), i.e. the LIMITATION #1 union pool. DECISION PENDING (Rob): (a) drop FRT initialValue so blank routes RQ / filled routes REG-VIN -- reverses the v4.3 default call but makes both paths real; (b) drop both RQ combos as subset-shadows (v4.9 QV precedent); (c) keep as documented dead. Found by audit_combo_reachability.ps1 2026-07-29. | metadata-vs-devdoc | 2026-07-29
