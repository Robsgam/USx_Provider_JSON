# PENDING_UPDATES.txt -- TX_TLETS
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
[FLAG:FORM-REACHABLE-COMBOS] Remove routing-affecting form defaults so every devdoc Basic combination is reachable FROM THE FORM (see the ledger row). TX_TLETS: remove FRT=E + PlateYear=2026 prefills, RESTORE the RQ+QV plate/VIN combos deleted at v4.13 as false dead-combos (they are the devdoc OutofState paths), order most-specific-first, split RQ(VIN)/QV(VIN) with a RegionId EXISTS condition, and stop defaulting State (any[]-only). Gated by enforce PHASE 2n. (flagged 2026-07-30)
