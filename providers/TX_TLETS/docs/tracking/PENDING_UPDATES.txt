# PENDING_UPDATES.txt -- TX_TLETS
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
# RETIRED 2026-08-14 -- TAKEN at v4.20, 2026-08-14 -- the fix has LANDED, not a not-applicable retirement. ImageIndicator now defaults to Y in BOTH the form initialValue and every carrying combo defaults[] on Firearm/Article/Boat (3 authored form sites x3 variants + 10 combos); Person was already Y. Verified from the emitted v4.20 JSON: zero sites left on N. TX_TLETS T6 GATE CLEARED BY LOCATING THE OWNING HEADING, not by a bare grep: TX devdoc lines 129-130 DO carry must-be-filled-if-ImageIndicator=Y against EmailAddress and ReasonCode, but the nearest query heading above is DriverHistoryQuery (line 62), so it is scoped to DH -- Person, already Y, both fields auto-populated by hidden gate-feeders -- and this change touched only Firearm/Article/Boat. ImageIndicator in 0 set[]s and 0 conditions. Lockstep TX_TLETS_CCH v1.16 same day, BASE-SYNC bumped. Cleared BY HAND -- the pipeline left it live. Full reasoning in the v4.20 BUILD_NOTES entry. OWED: 92-log re-sweep.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
# RETIRED 2026-08-18 -- TAKEN at v4.21. The flag's stated action was "Regenerate this provider PLAN at its next rebuild so middle/suffix are finally exercised", and that rebuild has now happened: logs/TX_TLETS_TEST_PLAN_v4.21.json holds 98 tests, of which nameMiddle is filled in 4 (n23/n27 DQName, n32/n36 CPLName), nameSuffix in 4 (n24/n27, n33/n36), nameMiddleDH in 2 (n44/n49 KQName) and nameSuffixDH in 2 (n45/n49). COUNTED FROM THE EMITTED PLAN, not assumed -- and my first probe read 0 because `fills` is an ARRAY of {fieldId,value} objects, not a property bag, so `.PSObject.Properties.Name` returned array members; that exact array-probe error is recorded in SESSION_STATE. The flag's own precondition ("AWAIT THAT RESULT") is also satisfied: AZ_AZDPS v3.11 proved DOE, JOHN A JR on 10 captures, so FormatStringRuleHandler emits all four components and this was a wiring job, not a platform LIMITATION. The v4.20 logs the flag was protecting are all archived (92 -> _archive_pre_v4.21/), so there is no stale-coverage risk left to guard. OWED: the 98-test sweep itself, which is stage 4, not this flag.
# [FLAG:nameparts-untested-unfrozen] Test-scaffolding change, NOT a JSON change: _combo_value_resolver now supplies nameMiddle=A / nameSuffix=JR, and emit_test_plan no longer suppresses the trust warning for those four fieldIds. Regenerate this provider PLAN at its next rebuild so middle/suffix are finally exercised. WHY IT MATTERS: metadata declares Name as type=Name with FOUR components (First/Last/Middle/Suffix), so middle and suffix are metadata-defined CAPABILITY, and no test on any provider had ever filled them -- all 538 logs read DOE, JOHN. Not regenerated here on purpose (rule 8c): this provider is tenant-verified and a bigger plan would drop it to PARTIAL. AZ_AZDPS is running the discriminating test first because it has NO Foundation and NO LIVE tenant. AWAIT THAT RESULT BEFORE ACTING: DOE, JOHN A JR means the handler emits all four and HI_HCJDC_OFML in particular is under-wired (its name parts are in ZERO combination any[], so they can never reach the composite); DOE, JOHN means the handler drops components 3-4 and this becomes a platform LIMITATION to raise, not a wiring job. (flagged 2026-08-17)
