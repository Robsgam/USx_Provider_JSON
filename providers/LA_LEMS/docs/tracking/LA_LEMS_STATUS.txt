LA_LEMS -- STATUS
=================
LABELS PROVISIONAL -- refine wording during manual form use; not a graded test case.
Current: v3.2 | 65P/0F/0W/0LIM | Updated 2026-08-20
  THE BUILD_RULES 20b WARN IS RESOLVED AT v3.1, NOT SILENCED -- 0W is now real.
  It had been parked because both obvious fixes looked worse than the warning. Reading the metadata
  dissolved that dilemma: the discriminator was never ImageIndicator. The MessageKey table names the
  two transactions outright -- DP = "Driver License Photo Inquiry", DQ = "Nlets Driver License Query"
  -- and their own <Requirements> agree (DP <Set> OLN+ImageIndicator, <Any> Attention, and NO State
  field at all; DQ <Set> OLN, <Any> State..State5 + ImageIndicator). So DP/DQ is an IN-STATE vs
  OUT-OF-STATE split and now routes on RegistrationState EXISTS/NOT_EXISTS, the same in/out pattern
  used portfolio-wide. RegistrationState carries no initialValue here (LIMITATION #30), so BOTH
  branches are live -- previously DQ was PERMANENTLY DEAD and the officer could never reach Nlets.
  ImageIndicator is now routing-INERT and keeps initialValue='Y' plus the combo defaults[] twin, so
  [FLAG:ncic-image-default-y-everywhere] applies to LA with NO carve-out -- LA is no longer a second
  AZ_AZDPS. DP also stopped carrying RegistrationState in any[] (its variant defines no State field,
  so that was an over-permit).
Provider: LA_LEMS
Methodology: Galvanized (single versioned JSON, native PascalCase USx CAD fields)
USx tenant test: NOT STARTED -- all query paths PENDING

ARCHITECTURE
============
  Bundles: 3 (ENTITIES first, LA_LEMS second, RMS third)
  Entities: Vehicle, Person, Firearm, Article, Boat
  Queries: 6 (VehicleRegistrationQuery, DriverLicenseQuery, DriverHistoryQuery,
           GunQuery, ArticleSingleQuery, BoatQuery)
  Combos: 12 total
  Provider-specific patterns:
    - DL+DH DH-suffix fieldIds + queriesToDeselect
    - Attention auto-handler (AP #27, hidden feeder pattern)
    - DP/DQ routing toggle
    - OOS RegistrationState EXISTS/NOT_EXISTS gates
    - Identifier-priority guardrails (Plate>VIN, OLN>Name, Hull>Reg)

TEST MATRIX
===========
  Not yet generated. The full USx-tenant-test matrix is produced by
  reset_test_package.ps1 / emit_test_plan.ps1 when testing begins.
  All query paths currently PENDING (see docs/tracking/LA_LEMS_SQVR.txt).

NOTES
=====
  Galvanized to single-JSON PascalCase (see docs/tracking/CHANGELOG_LA_LEMS.md for full history).
  NOT yet USx-tenant-tested at v3.0.
