# PENDING_UPDATES.txt -- LA_LEMS
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
[FLAG:validate-imgind-20b-l30] validate.ps1 + audit_cross_provider.ps1: LIMITATION #30 now requires State in a set[]; ImageIndicator check moved to BUILD_RULES 20b (prefill REQUIRED for serialization -- the defect is a combo gated ImageIndicator NOT_EXISTS). render_officer_guide + suggest_field_labels: -Encoding utf8NoBOM is PS7-only, a hard parameter-binding failure under the 5.1 engine enforce/pipeline use. Rebuild to re-record scores; LA_LEMS also gains a real 20b WARN. (flagged 2026-08-04)
[FLAG:testplan-platformfed-presence] emit_test_plan.ps1: platform-fed hidden fields (dexStateUserId) and form-shipped initialValues now count as ALREADY-PRESENT when simulating which combo fires. Previously badge-gated combos were dropped as unrunnable and the plan predicted the looser sibling the tenant would not fire. Re-emitting this provider's plan changes its test set, so it is deferred to the provider's OWN next rebuild + re-sweep. (flagged 2026-08-05)
# RETIRED 2026-08-14 -- ALREADY CONFORMANT -- no flip needed, and this supersedes the flag text calling LA a ruling case. MEASURED from the emitted JSON: LA has exactly ONE ImageIndicator control (Person) and it already carries initialValue=Y, so Y-on-every-entity is satisfied -- there is no other entity carrying the field, and adding one would OVER-PERMIT (same adjudication as HI v4.17 Vehicle-only / IL v2.4 excluding Article). The flag text said LA had 1 set[] PLUS 2 conditions; measured, ImageIndicator is in DP set[] with ZERO conditions on it, so that half was imprecise. The set[] membership is a reason for CARE (BUILD_RULES 24 -- a prefill on a set[] field can collapse a combo onto a plainer sibling) not evidence that work is owed: audit_prefill_shadow and audit_combo_reachability are both clean on LA as built. SEPARATE AND STILL PARKED: LA carries a real BUILD_RULES 20b WARN (validator 63P/0F/1W) which is Rob-parked and NOT to be silenced -- it is unrelated to this flag and is untouched. Rob approved 2026-08-14. Zero JSON change, zero version bump.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
