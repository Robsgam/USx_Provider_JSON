OH_LEADS -- Build Notes
=======================
Provider: OH_LEADS
State: Ohio
Network: LEADS (Law Enforcement Automated Data System)
v2.6  2026-08-18  ImageQuery REMOVED by directive -- APPROVED SKIP, before testing starts
  CHANGED: The ImageQuery QIDM (OH_LEADS_ImageQuery) is gone. Emitted JSON now holds 6 CommSys
    QIDMs / 21 combos, zero occurrences of "ImageQuery", zero of the "Driver Photo" queryLabel,
    and NO live keyReference "BMVIMS" (the only surviving BMVIMS string is one descriptive
    sentence in the DriverHistoryQuery description, which is history, not wiring).
  REASON: Rob 2026-08-18 -- "the image query needs to go away  be sure to remove it before we
    start". Removed BEFORE the first import/test sweep so no captured log is ever attributable to
    a transaction we do not ship.

  IT WAS BUILT AT v2.4 FOR THE OPPOSITE REASON, AND THAT IS NOT A CONTRADICTION -- IT IS SUPERSEDED.
  audit_supported_queries CHECK 0 had reported ImageQuery as devdoc-Basic yet neither built nor
  documented as a skip, and no-combo-left-behind requires one or the other. v2.4 chose "build it".
  The directive chooses "skip it", and the skip is now DOCUMENTED with named authority, which is
  exactly what CHECK 0 asks for. Both states satisfy the gate; only one satisfies the operator.
  Same disposition FL_FCIC has carried for ITS ImageQuery all along ("ImageQuery = user-approved
  scope" in its registry, "APPROVED SKIP" in its BUILD_NOTES). DO NOT "restore" it to green a gate.

  IN-BAND DRIVER PHOTOS ARE UNAFFECTED, and this is the distinction that matters operationally:
  ImageIndicator still rides in DriverLicenseQuery DQ.O any[] at 'Y', so a licence check still asks
  for the photo INSIDE the DL query. What was removed is the SEPARATE standalone photo transaction
  (one combination, keyReference BMVIMS, Set[OperatorLicenseNumber] only, autoSelect=$false so it
  rendered as a named opt-in "Driver Photo" checkbox), not the image capability.

  THE REMOVAL COULD NOT ORPHAN ANYTHING, and that is a property of how v2.4 built it rather than
  luck -- worth stating because it is why this is a 1-line bundle change and not a layout pass:
    - it carried NO form controls of its own (it fired off the Driver License card's existing OLN),
    - it had NO queriesToDeselect wiring in either direction,
    - it had NO card of its own.
  So nothing was left dangling. The OLN control stays -- DriverLicenseQuery DQ.O requires it, and
  it is still present in all 3 layout variants. Verified from the emitted JSON, not the intent.

  IT ALSO DISSOLVED A TOOLING CONFLICT THAT v2.5 HAD TO RECORD AS UNRESOLVED. v2.5 noted that the
  registry row "DriverHistoryQuery | BMVIMS | dropped-combo" tripped audit_requirement_fidelity's
  OVER-SUPPRESSION NOTE and could not be re-keyed, because audit_query_trace matches the registry
  on the BARE metadata keyRef while fidelity's prefix bridge over-suppresses on that same string --
  the two gates wanted opposite spellings. That NOTE fired precisely BECAUSE BMVIMS named a combo
  we built here. With ImageQuery gone there is no built BMVIMS, so the NOTE is gone and fidelity
  reports 22 branches / 0 UNDER / 0 OVER cleanly. The conflict was real; it is simply no longer
  reachable on THIS provider. It will return on any provider that builds a shared keyRef.

  THE ONE EXPECTED WARN, AND WHY IT IS NOT SILENCED: audit_supported_queries CHECK 2 reports
  "supported 'Driver Photo | OperatorLicenseNumber' has NO combo in JSON (intentional skip?
  document it)". That check has NO skip-marker mechanism -- any devdoc-supported query without a
  combo warns unconditionally. The row could be deleted from
  docs/reference/OH_LEADS_SUPPORTED_QUERIES.txt to make it disappear, and that was REJECTED: that
  file is the DEVDOC ground truth, ImageQuery genuinely IS devdoc-supported, and CHECK 0 compares
  the built queries against exactly that list -- so deleting the row would falsify the authority
  and weaken the gate that motivated all of this. The WARN is the skip being VISIBLE. enforce does
  not escalate it (PHASE 2e passes).
  Registered: ImageQuery | (devdoc #1) | OperatorLicenseNumber | devdoc-combo-unbuilt.
  Keyed on the DEVDOC ITEM, never a keyRef -- an unbuilt-class rule naming a keyRef suppresses any
  combo built under that name, and BMVIMS is a keyRef the metadata SHARES across three
  transactions. Nothing is built under it today; keying on the devdoc item keeps the row correct if
  something ever is. Measured: 22 branches before AND after registering, no OVER-SUPPRESSION NOTE.
  Also corrected the reference extract, which asserted "ALL SEVEN BASIC QUERIES ARE NOW BUILT".

  PHASE 1 at v2.6: [1] devdoc combinations all accounted for - [2] optionals all route and transmit
  - [3] 21 combos, most-specific-first respected - [4] 22 branches, 0 under / 0 over - [5] 24 built,
  0 prefill-dead, 0 shadow, 1 missing (= this approved skip) - [6] gate efficacy 8/8 killed.
  Fuzz: 1 survivor, "set-to-any @ DriverLicenseQuery[2] NameFirst" -- the documented composite-Name
  granularity limit (the mutation leaves NameLast in set[], which is what fidelity's surname guard
  checks), a CORRECT survivor and seed-dependent (it did not recur at seed 962590 on v2.5).
  enforce -Provider OH_LEADS exits 0. Validator recorded in STATUS.txt.
  STILL OWED: never USx-tenant-tested and never imported -- v2.6 owes a first import to
  usx-oh-leads.mark43.com and then a full 5-entity sweep from T1.


v2.5  2026-08-18  NCIC Image defaults to Y on ALL entities -- the Rob-held flag closed at zero cost
  CHANGED: ImageIndicator flipped 'N' -> 'Y' on Article and Boat, in BOTH places the rule requires --
    the form initialValue (ROW_ART_3, ROW_BOA_2) and the combo defaults[] CAD twin on all four
    carrying combos (Article QA.S/QA.N, Boat QB.H/QB.R). Person was already 'Y'. Emitted JSON now
    reads 9 of 9 form controls at 'Y' and 5 of 5 combo defaults at 'Y'.
  REASON: [FLAG:ncic-image-default-y-everywhere] -- Rob 2026-08-12, "ncic image should default to y
    everywhere". THE DEFAULTS[] TWIN IS NOT OPTIONAL: CAD ignores the form initialValue, so a
    form-only flip would leave every CAD-originated Article/Boat query still sending 'N'.

  ALL THREE OF THE FLAG'S OWN PRECONDITIONS WERE MEASURED, NOT ASSUMED. The flag says explicitly
  "BEFORE APPLYING, MEASURE THIS PROVIDER", because the same flip is WRONG on two providers:
    (1) ImageIndicator is in NO combination's set[] and in NO condition here -- it rides in any[] on
        exactly 5 combos (DQ.O, QA.S, QA.N, QB.H, QB.R). So BUILD_RULES 24 does not bite: a prefill
        can only shadow a combo when the prefilled field is a routing DISCRIMINATOR in a set[].
        Contrast AZ_AZDPS (2 set[]s -- v3.10 proved the flip collapses DQPN->DQN and DQP->DQ, killing
        both plain searches) and LA_LEMS (1 set[] + 2 conditions). Those two need Rob's ruling; OH
        needed only a measurement. VERIFIED AFTER THE REBUILD, not just predicted: audit_query_trace
        reports 25 built / 0 prefill-dead / 0 shadow, and combo reachability is clean.
    (2) The TX_TLETS T6 gate is CLEAR. OH_LEADS' devdoc contains no "must be filled if" / "required
        if" conditional anywhere, and ImageIndicator appears only as a BRACKETED OPTIONAL in every
        Article combination (#1 ArticleSerialNumber+ArticleTypeCode, #2 NCICNumber) and every Boat
        combination (#1 hull, #2 NCICNumber, #3 RegistrationNumber). Nothing becomes mandatory at 'Y'.
    (3) BUILD_RULES 20b does not apply -- OH gates no combo on ImageIndicator NOT_EXISTS.

  WHY NOW, AND WHY THIS IS THE CHEAPEST POINT IN THE PORTFOLIO TO DO IT: this is a WIRE change, which
  is exactly why the flag was held on the six remaining carriers -- on a tenant-verified provider it
  archives the whole test package and owes a full re-sweep of Rob's hands-on time. OH_LEADS costs
  NONE of that: never USx-tenant-tested, no current-version logs, and IMPORT_LEDGER records v2.4 as
  never installed on any tenant. Deferring it would have bought a second rebuild for zero benefit.
  CLAUDE.md's count of "OH_LEADS (2)" was RIGHT and worth stating, because the raw grep says 6: there
  are 9 ImageIndicator controls = 3 entities x 3 layout variants (default/CAD_DISPATCH/FIRST_RESPONDER),
  so 6 controls at 'N' was 2 ENTITIES at 'N'. Counting controls instead of entities would have made
  the doc look wrong when it was not.

  ONE ADVISORY WARN REMAINS, AND IT IS A GATE ARTEFACT -- VERIFIED, NOT ASSUMED, AND DELIBERATELY
  NOT SUPPRESSED:
    audit_requirement_fidelity reports "DriverLicenseQuery / BMVIMS -> built 'DQ.O' UNDER-REQUIRED:
    SocialSecurityNumber (ABSENT)", and audit_query_trace reports the same item as "[MISSING] BMVIMS
    set[SocialSecurityNumber]". They are ONE underlying thing, and the build is correct.
    READ FROM THE RAW XML <Requirements> PER <Combination> -- the one sanctioned raw-XML exception:
      DQ{OperatorLicenseNumber}    (line 5098) = Set[OperatorLicenseNumber], Any[ImageIndicator, State]
                                                 -- SSN APPEARS NOWHERE IN IT. This is what DQ.O builds.
      BMVIMS{SocialSecurityNumber} (line 5428) = Set[SocialSecurityNumber], Any[ReasonCode, Requestor,
                                                 UserName] -- a DIFFERENT transaction variant.
    BMVIMS is "Image Query by Operator License Number" and we build it under ImageQuery; OH's own
    SUPPORTED_QUERIES extract records that "keyRef BMVIMS is shared with DriverLicenseQuery". So this
    is the BUILD_RULES 13 keyRef-collision class (key by (query, keyRef), never a bare name) surfacing
    in a gate's branch MAPPING: the gate force-mapped an unbuilt variant onto the nearest OLN-primary
    built combo and then reported the mismatch as a defect in that combo.
    AND IT IS OUT OF DEVDOC-BASIC SCOPE ANYWAY. The devdoc's ImageQuery section lists exactly ONE
    combination -- "1. (In/Out) OperatorLicenseNumber, [ReasonCode, Requestor, UserName]" -- with
    OperatorLicenseNumber marked M and NO SocialSecurityNumber field anywhere in its table. A metadata
    combination with no devdoc counterpart is not a gap in either direction. OH builds no SSN control
    at all (Build-RmsBundle is called here without -KeepSsn).
    WHY NO REGISTRY ROW WAS WRITTEN, having tried one: any unbuilt-class rule naming keyRef 'BMVIMS'
    suppresses the comparison of the combo we DO build under that keyRef. Measured: registering it
    dropped branches-compared 24 -> 22 while the finding count went to 0, and the tool itself emitted
    "[NOTE] REGISTRY OVER-SUPPRESSION RISK: ... names a combo that IS BUILT (in ImageQuery)". The row
    was reverted and branches confirmed back at 24. A registration that costs coverage and buys
    nothing is worse than none -- so this stays VISIBLE as an advisory WARN with the evidence here.
    PRE-EXISTING PORTFOLIO RESIDUE, NOT INTRODUCED BY v2.5: the measured baseline is 421 branches /
    11 UNDER / 34 OVER across all 20 providers (unchanged, and matching CLAUDE.md), with findings on
    8 of them. The 6-provider regression fixture is intact at 0/0 (116 branches). Fixing the gate's
    mapping would move 8 providers' numbers at once, so it is a portfolio decision
    (ENGINEERING_STANDARD 4.5 -- SWEEP is Rob's direction only), NOT part of OH's rebuild.
    ALSO NOTED, PRE-EXISTING AND NOT FIXED HERE: the registry's existing row
    "DriverHistoryQuery | BMVIMS | OperatorLicenseNumber | dropped-combo" trips the same
    OVER-SUPPRESSION NOTE for the same reason. It cannot simply be re-keyed: audit_query_trace matches
    the registry on the BARE metadata keyRef string (that is why the QV/QV.P alias row exists in
    PENDING_UPDATES), while audit_requirement_fidelity's prefix bridge over-suppresses on that same
    string. The two gates want opposite spellings, so this is a genuine tooling conflict to be fixed
    in the gates, not a row-spelling problem to churn.

  FUZZ NOTE: the earlier "set-to-any @ DriverLicenseQuery[2] NameFirst" survivor did NOT recur at
  seed 962590 (8/8 caught). It is the documented composite-Name granularity limit -- the mutation
  leaves NameLast in set[], which is exactly what fidelity's surname guard checks -- so it is a
  correct survivor, and a survivor is a CANDIDATE, not a verdict.

  Validator: see STATUS.txt for the recorded score. enforce -Provider OH_LEADS exits 0.
  STILL OWED: never USx-tenant-tested -- v2.5 owes a full 5-entity sweep from T1, and a first import.


v2.4  2026-08-10  CARD COLLAPSE 14 -> 6 + ImageQuery BUILT (the two items v2.3 left open)
  Both were flagged at v2.3 as "Rob's call". His call was: do them. So they are done, in one version.

  (A) CARD COLLAPSE 14 -> 6, every title now carrying its query paths.
    Vehicle 6 -> 1  (was OPTIONS / PLATE / DEALER PLATE / VIN / NAME / SSN)
    Person  5 -> 2  (Driver License + Driver History)
    Boat    3 -> 1  (was OPTIONS / HULL / REGISTRATION)
    Firearm 1, Article 1 -- unchanged in count, titles rewritten to carry paths.
    OH was the ONLY provider of nine with 14 cards and ZERO path-carrying titles; every other one
    runs 5-6 cards with 5-6 such titles (NJ 3 of 5). Vehicle alone was six boxes for one entity.
    ZERO ROUTING CHANGE: not one set[], any[], condition, default, fieldId or QIDM moved. Only the
    card grouping, the row/column geometry (now a uniform 4/4/4 grid) and the titles.
    PERSON STAYS TWO CARDS, NOT ONE. The DH-suffix fieldIds (OperatorLicenseNumberDH, NameFirstDH,
    BirthDateDH, SexCodeDH, purposeCodeDH) are a SEPARATE FIELD POOL -- that is the isolation
    mechanism that keeps a DH fill from co-firing DriverLicenseQuery, not a layout accident. Merging
    them into one card would put two pools in one box and invite exactly the confusion the DH-suffix
    design exists to prevent. HI/NY/TX/AZ all keep Person at two cards for the same reason.
    THE HELPER LABELS CAME OUT AS A CONSEQUENCE, WHICH IS THE WHOLE POINT: 18 at v2.2 -> 8 at v2.3
    -> 2 now. The six that survived v2.3 -- 'Date of Birth (out-of-state)', 'Sex (out-of-state)',
    'Serial Number (with Article Type)', 'Dealer Plate Type (with Plate Number + Type)',
    'Vehicle Make (with State, optional)', 'Vehicle Year (with State, optional)' -- were all saying
    WHICH COMBO NEEDS THIS FIELD, which is precisely a card title's job. Once the titles said it, the
    labels could go bare without losing information. Stripping them at v2.3 (before the titles
    existed) would have made the form LESS usable, which is why they were deliberately left then.
    The only two helpers remaining are the standing exemptions: 'Attention (auto)' on a HIDDEN
    control, and 'State (leave blank for OH)' -- State keeps its routing hint by convention (IL
    carries the identical 'State (leave blank for IL)').

  (B) ImageQuery BUILT -- the last unbuilt devdoc-Basic query. See the block in the build script for
    the full reasoning; in short: audit_supported_queries CHECK 0 had been reporting it as
    "devdoc-Basic but NOT BUILT, each must be a documented skip" and it was documented NOWHERE.
    devdoc line 248 gives "(In/Out) OperatorLicenseNumber, [ReasonCode, Requestor, UserName]";
    metadata v9 gives ONE Combination BMVIMS{OperatorLicenseNumber} = Set[OperatorLicenseNumber]
    Any[ReasonCode, Requestor, UserName]. Built EXACTLY to the metadata -- set[OperatorLicenseNumber],
    any[] empty -- and it needed no new form control because the DL card's OLN already feeds it.
    autoSelect = $false ON PURPOSE. A standalone photo transaction must not co-fire on every OLN
    entry, or every routine licence check silently pulls a photo the officer never requested (and OH
    already has ImageIndicator on the DL card for the in-band image). $false makes it a named opt-in
    checkbox via queryLabel, the TX_TLETS_CCH pattern for officer-chosen transactions. The DL card
    title now says "tick DRIVER PHOTO for a photo-only request" so the control is discoverable.
    THE OPTIONAL TRIO IS NOT WIRED, AND THAT IS NOT A DROPPED OPTIONAL. UserName is the transaction
    ENVELOPE user already supplied by AUTH -- a form control would duplicate it (the class where 55
    "dropped optional" FAILs on FL traced to one envelope field). Requestor and ReasonCode have no
    control anywhere on OH, so there is no officer input to discard; the devdoc-optional rule is
    about not throwing away what was typed.
    WHY THE CLOSED LOOP HID IT FOR SO LONG: OH_LEADS_SUPPORTED_QUERIES.txt was PROVISIONAL and
    "DERIVED FROM JSON COMBOS" -- generated from the JSON, then compared to the JSON. ImageQuery was
    absent from the JSON, therefore absent from the list, therefore nothing reported it. Only CHECK 0,
    which reads the DEVDOC instead, could see it. The extract is now CONFIRMED (gating) and carries
    'Driver Photo | OperatorLicenseNumber'. Unlike AZ, no line had to be REMOVED here -- every entry
    was real; the defect was what the list did NOT contain.

  REGISTERED THE NEW QUERY KIND WITH EVERY CONSUMER, not just the provider. 'Driver Photo' was the
    first new queryLabel in the portfolio, so the first build FAILED verify_build CHECK ("queryLabel
    not in standard set") -- correctly. Added to tools/verify_build.ps1 $validLabels AND to the
    CLAUDE.md queryLabel table. Both additive, so neither can redden another provider.

  GATES: validator 80 PASS / 0 FAIL / 0 WARN · verify 17 PASS / 0 WARN / 0 FAIL · supported-queries
    23 PASS / 0 FAIL / 0 WARN with all SEVEN Basic transactions built and the extract GATING ·
    reachability 20 combinations all reachable · wiring closure clean on all ten classes · devdoc
    combinations 0 FAIL / 20 compared · Craft.js tree verified intact (0 null/scalar `nodes` lists,
    the failure mode a whole-tree recase once caused). Combos 21 -> 22.
  STILL ADVISORY, unchanged and NOT introduced here: fidelity reports 1 UNDER-REQUIRED
    (DQ.O missing SocialSecurityNumber) -- the shared-keyRef artifact. BMVIMS names both
    DriverLicenseQuery's OLN variant and its SSN variant, the tool bridges on the bare keyRef and
    compares against their union. DL-by-SSN is NOT in OH's Basic DL list (five combinations, OLN and
    Name only), so it is correctly unbuilt. Deliberately NOT registered: naming the BUILT combo DQ.O
    with an existence-class rule would suppress its entire comparison.
  COST: none. OH is NEVER-TESTED and NOT imported, so the bump archived 0 logs / 0 SQVR markers.

v2.3  2026-08-10  DEX-1283 Attention 'X' + DEX-1284 label conformance (OH was the portfolio outlier)
  CHANGED, all officer-facing or wire-value; ZERO QIDM / combo / routing / fieldId change:
    (1) DEX-1283: the hidden DH feeder loses initialValue='X' and BOTH KQ.N/KQ.O combo defaults[]
        lose Attention='X'. Control, 'attention' in any[], and the
        CommsysGetLastNameFirstNameInitialRuleHandler attribute are untouched -- only the literal.
    (2) 'License Number' -> 'OLN' on BOTH the DL and DH controls. OH was the ONLY provider of nine
        not using the canonical label (8 of 9 already did).
    (3) THREE different image labels -- 'Image (optional)', 'Image (in-state blank, optional)',
        'Image (out-of-state OLN, optional)' -> 'NCIC Image' on all three. Canonical on the six
        providers that carry the field; OH had three spellings of one field.
    (4) THREE stolen-check labels -- 'Y for NCIC stolen-gun/article/boat check (optional)' ->
        'Stolen Check'. Canonical on FL/HI/IL/NY/TX.
    (5) '(optional)' suffixes stripped from County Code, Purpose Code, Make, Caliber. OH went 18
        helper labels -> 8; the leanest finished providers (CA/IL/NY) sit at 0 '(optional)'.
    (6) FOUR '# LABEL-OVERRIDE:' tags added.
  WHY (6) IS PART OF (4)/(5) AND NOT AN AFTERTHOUGHT. Stripping the helpers produced FOUR FRESH
    verify_build CHECK 15 WARNs (AddressCounty, firearmMake, gunCaliber, relatedHitSearchIndicator
    -- "any[]-only label has no routing qualifier"). The label and its override tag are ONE change:
    IL_LEADS_OFML carries 16 such tags, which is exactly how it holds 0 '(optional)' at 0 WARN. I
    found this by measuring how IL avoids the WARN rather than by adding '(optional)' back, which
    would have undone the lean pass to satisfy a gate. All four are now [INFO] with a cited reason.
  THE STANDARD WAS MEASURED, NOT REMEMBERED. Across the seven tenant-complete providers:
    OLN exactly 'OLN' = 8/9 · 'NCIC Image' = 6/6 that have the field · 'Stolen Check' = 5/5 that
    have it. Those three are settled, so OH was brought to them. **'(optional)' is NOT settled** --
    CA/IL/NY are at 0 but NJ 18, HI 12, FL 3, TX 3, so that pass is still rolling out and OH moving
    to 0 puts it ahead of four finished providers rather than "into line" with all of them. Stating
    that instead of implying uniformity.
  FLAG CLEARED: [FLAG:validate-imgind-20b-l30] asked for exactly "rebuild to re-record scores" and
    the rebuild did it -- validator records 78 PASS / 0 FAIL / 0 WARN where STATUS.txt had been stuck
    at 77P since the 08-07 validate.ps1 change. Nothing in the build needed altering for it: State is
    already in set[] on the OOS combos (LIMITATION #30 satisfied) and OH gates NO combo on
    ImageIndicator NOT_EXISTS, which is the defect 20b actually names. **The flag did NOT self-clear
    on rebuild** -- CLAUDE.md claims the build script clears it; OH's has no such logic. Cleared by
    hand; do not rely on the auto-clear.
  STILL OWED AND NOT DONE HERE -- TWO ITEMS, both stated rather than buried:
    (a) CARD COLLAPSE. OH has 14 cards and ZERO titles carrying a query path. Every other provider
        has 5-6 cards with 5-6 path-carrying titles (NJ 3 of 5). Vehicle alone is SIX cards
        (OPTIONS/PLATE/DEALER PLATE/VIN/NAME/SSN) where IL/HI/FL collapsed theirs to one. This is
        the IL v2.1 pass and it is a genuine layout redesign, not a label edit -- which paths share
        a card and what each title says is a design decision that Rob's rendered-form review owns.
        The 6 remaining helper labels ('Date of Birth (out-of-state)', 'Sex (out-of-state)',
        'Serial Number (with Article Type)', 'Dealer Plate Type (with Plate Number + Type)',
        'Vehicle Make/Year (with State, optional)') are COUPLED to it: they say WHICH combo needs
        the field, which is precisely what a path-carrying card title absorbs. Strip them before the
        titles exist and the form gets less usable, not more.
    (b) ImageQuery IS DEVDOC-BASIC AND IS NOT BUILT. audit_supported_queries CHECK 0 reports it as
        "devdoc-Basic but NOT BUILT -- each must be a documented skip", and it is documented nowhere.
        It is BUILDABLE, not blocked: OH metadata defines ImageQuery (v9, 1 combination) and the
        devdoc gives "(In/Out) OperatorLicenseNumber, [ReasonCode, Requestor, UserName]" -- every one
        of those fields already exists on OH's DL card. So this is a real coverage gap under
        no-combo-left-behind, and the choice between BUILD IT and a user-approved skip is Rob's, not
        mine. Not silently skipped and not silently built.
  GATES: validator 78 PASS / 0 FAIL / 0 WARN, verify CLEAN (4 label overrides accepted as INFO).
  COST: none. OH is NEVER-TESTED and NOT imported, so the bump archived 0 logs / 0 SQVR markers.

v2.3-superseded  2026-08-10  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild

v2.2  2026-08-01  RQ.P State promoted to set[] (portfolio fidelity sweep, commit c3b76a94)
  CHANGED: RQ.P -- State PROMOTED into set[] to match metadata RQ{LicensePlateNumber}.
  REASON: Found by the portfolio fidelity sweep (413 branches / 29 UNDER / 49 OVER across 20
    providers, while the six tenant-verified providers were 0/0). An UNDER-REQUIRED set[] is the
    severe class: the query can fire WITHOUT the field and the request is invalid, and neither 6c nor
    2i can see it -- a missing requirement is invisible to content and attribution checks. Recovered
    2026-08-03 from commit c3b76a94; this entry read "Rebuilt via pipeline.ps1 / Scheduled rebuild".

v2.1  2026-08-01  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild

v2.0  2026-07-23  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild


--------------------------------------------------------------------------------
v1.1  2026-05-07  MC multi-card layout + PlateType/PlateYear defaults
--------------------------------------------------------------------------------
CHANGED : Phase 2 multi-card layout for MC variant (14 cards). PlateType=PC and
          PlateYear=2026 defaults applied to both BASE and MC. Rebuilt both
          variants from updated build scripts.
REASON  : Standard Phase 2 progression -- split entity forms into one card per
          search path. PlateType/PlateYear defaults per cross-provider audit.
SCRIPT  : scripts/build_oh_leads.ps1 + scripts/build_oh_leads_mc.ps1
OUTPUT  : OH_LEADS_BASE.json + OH_LEADS_MC.json (+ readable variants)
RESULTS : BASE: 76P/0F/4W/4LIM | MC: 76P/0F/3W/4LIM

--------------------------------------------------------------------------------
v1.0  2026-05-06  Initial build -- 6 basic queries, 23 combos
--------------------------------------------------------------------------------
CHANGED : Built from scratch using OH_LEADS.xml metadata + HIDLE.json.
          6 basic queries: VehicleRegistrationQuery (9 combos), DriverLicenseQuery (4),
          DriverHistoryQuery (3), GunQuery (1), ArticleSingleQuery (2), BoatQuery (4).
          23 total combos. DL+DH co-fire with DH-suffix fieldIds.
          VehReg includes owner search combos (RS=SSN, RN=Name) and DealerPlateType (ATDP).
          BMVIMS combo in DriverHistoryQuery. No State initialValue (LIMITATION #30).
          5 entities: Vehicle, Person, Firearm, Article, Boat.
REASON  : New provider onboarding -- Ohio LEADS via bulk build pipeline.
SCRIPT  : scripts/build_oh_leads.ps1 + scripts/build_oh_leads_mc.ps1
OUTPUT  : OH_LEADS_BASE.json + OH_LEADS_MC.json (+ readable variants)
RESULTS : BASE: 76P/0F/4W/4LIM | MC: 76P/0F/3W/4LIM

================================================================================
v1.3 (2026-05-11) -- LIMITATION elimination pass
================================================================================
CHANGED: Combo ordering
RESULT: BASE 77P/0F/0W/0LIM | MC 77P/0F/0W/0LIM

