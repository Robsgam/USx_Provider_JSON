OR_LEDS -- Build Notes
=======================

DOCUMENTED SKIP (recorded 2026-08-11) -- ImageQuery: UNBUILDABLE, not deferred.
  audit_supported_queries CHECK 0 reports "devdoc-Basic but NOT BUILT: ImageQuery -- each must be a
  documented skip", and it had never been written down. Now it is, with the reason:
  OR_LEDS metadata contains ZERO ImageQuery transactions (verified from the raw XML:
  //Transaction[@name='ImageQuery'] -> 0 nodes, 0 Combinations). Metadata is FIELD authority, so
  there is no transaction to build against and no request shape that any variant would accept.
  Same class as AZ_AZDPS's GunQuery #1/#2 (devdoc lists a path metadata does not define).
  This is CLOSED, not owed. Contrast LA_LEMS, where ImageQuery IS defined (1 combination,
  DP{OperatorLicenseNumber}) and therefore genuinely is owed at that provider's own turn.
v2.4  2026-08-19  VehicleMakeCode + VehicleYear removed -- the vehicle query cannot carry them; 0F/0W
  CHANGED:
    - VehicleMakeCode and vehicleYear removed from all three RQ combos' any[] (RQ.PO, RQ.V, RQ.P).
    - Their two QIDM attributes removed, and both form controls removed with ROW_VEH_VIN_2.
      The VIN card is now VIN only.
    - REGISTERED VehicleRegistrationQuery | RQ.V | VehicleMakeCode and | VehicleYear as
      devdoc-optional-unreachable.
  REASON: audit_requirement_fidelity reported 4 OVER-PERMITTED --
    "RQ -> built 'RQ.PO' OVER-PERMITTED: VehicleMakeCode; VehicleYear" and the same on RQ.V. We were
    transmitting two fields this transaction does not define.

  THE DEVDOC LISTS THEM AND THE METADATA DOES NOT -- and that is the whole finding, not a technicality.
  Devdoc VehicleRegistrationQuery combination #2 reads "(In) VehicleIdentificationNumber,
  [VehicleMakeCode, VehicleYear]", so both ARE devdoc optionals on the VIN search. But read from the
  raw XML <Requirements> per <Combination>, the sanctioned exception, this provider's
  VehicleRegistrationQuery has exactly TWO variants and neither defines either field:
      RQ{LicensePlateNumber}            Set[LicensePlateNumber]  Any[LicensePlateTypeCode, LicensePlateYear, State]
      RQ{VehicleIdentificationNumber}   Set[VehicleIdentificationNumber]  Any[State]
  EVERY transaction in the XML was searched, not just VehicleRegistrationQuery: VehicleMakeCode and
  VehicleYear appear ONLY on Wanted-Person transactions -- QLW, QW, and the PEWantedPerson
  Entry/Modify/Supplemental family (EW, ET, SW, RW, MW, MT, MWXW, XN, EN). That is a different product
  surface (person records), and none of it is the vehicle registration query. So the fields are not
  merely unmapped here; they are undefined for this query anywhere in the provider's own metadata.
  Metadata is FIELD authority (Rob 2026-08-18: "meta data wins over dev doc built to the spec"), so the
  devdoc bracket cannot license transmitting them. This is the structural mismatch a FLAT devdoc
  optional list cannot express, and it is the same rule class this provider ALREADY carries seven times
  (RelatedHitSearchIndicator on QA/BQ.H/BQ.R/QG, Name and BirthDate on DQ.N) -- so the disposition was
  set by this provider's own precedent, not invented.

  REMOVED IN FULL, NOT JUST FROM any[] -- that is the part worth remembering. Dropping the fields from
  any[] alone would have left two QIDM attributes sourcing nothing (wiring class B ORPHAN ATTRIBUTE)
  and two officer-facing controls whose input goes nowhere (class A DEAD CONTROL -- the officer types a
  make and year and the request silently discards them, which is WORSE than not offering the field at
  all, and it is the exact defect audit_wiring_closure was built for after CA_SAN_LUIS_OBISPO shipped a
  visible prefilled Purpose Code that was in no combination's any[]). So the attributes and the whole
  ROW_VEH_VIN_2 went too. Verified: fidelity 4 OVER -> 0 with branches HELD at 8, and
  audit_wiring_closure 0 breaks across all ten classes.
  Registration measured too: 8 branches before AND after the two rows, so they RECORD without buying
  silence.

  A HONEST SIDE EFFECT, recorded rather than left to be discovered: OR_LEDS now has NO VehicleMakeCode
  form field, so audit_gate_efficacy reports "vehiclemake-as-input [INVALID] N/A -- this provider has no
  VehicleMakeCode form field, so the VehicleMakeCode gate has nothing to check here". Gate efficacy
  reads KILLED 7 / 7, SURVIVED 0, INVALID 1 -- it went from 8 applicable mutations to 7. That means
  verify_build's VehicleMakeCode check is now VACUOUS on this provider, exactly as CLAUDE.md already
  records for NJ_NJCJIS and HI_HCJDC_OFML, which build no such field either. The harness says so out
  loud instead of quietly scoring 7/7 as if nothing changed, which is why this is acceptable: a
  mutation that cannot be applied is reported as INVALID, not as a pass.
  It also sidesteps LIMITATION #38 here: the VEHICLE_MAKE dropdown transmits the raw attribute code
  (CNST_FORD) where providers require a code-manual value. OR no longer sends VehicleMakeCode at all,
  so that parked portfolio-wide defect has one fewer carrier -- a consequence, not the motive.

  TWO EXPECTATIONS I ARRIVED WITH AND BOTH WERE WRONG -- measured, not assumed:
    1. SESSION_STATE said "NEXT: OR_LEDS v2.3 (last middle/suffix RESTORATION)". audit_name_components
       reports 0 blocking findings / 4 components examined / C1 0 / C2 0 / C3 0 -- OR's name components
       are already complete. Nothing was owed and nothing was invented. That note is STALE.
    2. I expected enforce to be blocking. It arrived at ENFORCED 40 PASS / 0 FAIL / 0 WARN, so this was
       a raise-the-ceiling pass on an already-clean provider, not a repair.

  THE FUZZ SURVIVOR from the first run ("drop-conditions @ VehicleRegistrationQuery[2]") did not recur
  at seed 857311 (8/8 caught). Triage if it returns: [2] is RQ.P set[LicensePlateNumber] gated
  RegistrationState NOT_EXISTS, and [0] is RQ.PO with the IDENTICAL set[] gated EXISTS -- the standard
  in/out fork. Dropping RQ.P's condition is close to a semantic no-op because first-match ordering
  already hands the State-present case to RQ.PO, and the two combos' field pools differ only by
  RegistrationState, so the union (LIMITATION #40) carries the same content either way.

  PHASE 1 at v2.4: [1] all devdoc combinations accounted for - [2] optionals all route and transmit -
  [3] 9 combos, most-specific-first respected - [4] 8 branches, 0 UNDER / 0 OVER - [5] 8 built,
  0 prefill-dead, 0 shadow, 0 MISSING - [6] gate efficacy 7/7 killed (1 INVALID by design) -
  [6b] fuzz 8/8 caught. enforce -Provider OR_LEDS: ENFORCED 40 PASS / 0 FAIL / 0 WARN, exit 0.
  Cost: zero test package (0 current-version logs, never imported).
  STILL OWED: never USx-tenant-tested, never imported.


v2.3  2026-08-17  RESTORE middle name + suffix on DriverLicenseQuery
  CHANGED: Wired the metadata-defined middle-name and suffix components of the composite `Name`
    field so they reach the wire: added the two form controls per name path, appended them to that
    query's composite Name sourceField, and added them to every name-search combination's any[].
    FormatStringRuleHandler separators went @(', ') -> @(', ',' ',' ') -- AP #15 requires fields-1
    separators, so four sourceFields need three. Name rows regrouped to carry all four parts, with
    mandatory qualifiers moved to their own row beneath (layout rules L8/L2). No set[] was touched
    and every addition is any[]-only, so NO ROUTING CHANGED.
  REASON: Found by tools\audit_name_components.ps1 (new 2026-08-17) -- the authority->built gate at
    COMPONENT granularity. This provider's own metadata declares request `Name` with FOUR components
    (First/Last/Middle/Suffix) on the queries built here, but middle and suffix had no form control,
    so the officer could not enter them at all. No existing gate could see it: every other gate
    enumerates the JSON and is therefore closed under what we built, and because the test plan is
    generated FROM the JSON, a missing control produces no test and so can never fail.
    CAPABILITY IS WIRE-PROVEN, not theoretical: AZ_AZDPS v3.11 and CA_CLETS v2.25 (109/109 logs)
    both emit <Name>DOE, JOHN A JR</Name> and degrade cleanly to "DOE, JOHN JR" when the middle name
    is absent -- no double space, no stray comma.  RESTORATION. v2.2 (commit 56b8b7ca, 2026-08-02) deleted ROW_PER_NAME_2, which held nameMiddle and
    nameSuffix, as dead controls -- the v2.2 note in this file states the reasoning verbatim: "the
    Name attribute sources only [NameLast, NameFirst], so an officer's middle name or suffix was
    silently discarded". That observation was correct and the remedy was backwards: OR metadata
    declares request Name with four components, so the sourceField should have been extended rather
    than the controls removed. Rob reversed the call on 2026-08-17.


Provider: OR_LEDS
State: Oregon
Network: LEDS (Law Enforcement Data System)
v2.2  2026-08-02  Remove 3 dead officer controls (Rob 2026-08-02, commit 56b8b7ca)
  CHANGED: Deleted the visible-but-unwired Person nameMiddle and nameSuffix, and Firearm
    gunTypeCode. No wire change -- none of the three was ever transmitted.
  REASON: audit_wiring_closure class A (dead control). Name attributes source only
    [NameLast, NameFirst], so a middle name or suffix was silently discarded. gunTypeCode was the
    worse case -- not merely unwired but UNWIREABLE: OR's XML defines GunTypeCode exactly once,
    under CPICBIGunQuery, a transaction this build does not carry. It comes back WITH its attribute
    and combination if CPICBIGunQuery ever enters scope. Rob's call was remove, not wire. Recovered
    2026-08-03 from commit 56b8b7ca; this entry read "Rebuilt via pipeline.ps1 / Scheduled rebuild".

v2.1  2026-08-02  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild

v2.0  2026-08-01  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild


--------------------------------------------------------------------------------
v1.1  2026-05-07  MC multi-card layout + PlateType/PlateYear defaults
--------------------------------------------------------------------------------
CHANGED : Phase 2 multi-card layout for MC variant (11 cards). PlateType=PC and
          PlateYear=2026 defaults applied to both BASE and MC. Rebuilt both
          variants from updated build scripts.
REASON  : Standard Phase 2 progression -- split entity forms into one card per
          search path. PlateType/PlateYear defaults per cross-provider audit.
SCRIPT  : scripts/build_or_leds.ps1 + scripts/build_or_leds_mc.ps1
OUTPUT  : OR_LEDS_BASE.json + OR_LEDS_MC.json (+ readable variants)
RESULTS : BASE: 58P/0F/2W/1LIM | MC: 58P/0F/1W/1LIM

--------------------------------------------------------------------------------
v1.0  2026-05-06  Initial build -- 5 basic queries, 8 combos
--------------------------------------------------------------------------------
CHANGED : Built from scratch using OR_LEDS.xml (metadata v5) + HIDLE.json.
          5 basic queries: VehicleRegistrationQuery (2 combos), DriverLicenseQuery (2),
          GunQuery (1), ArticleSingleQuery (1), BoatQuery (2).
          8 total combos. NO DriverHistoryQuery (not in metadata or devdoc).
          Invented keyRefs: DQ.N/DQ.O, RQ.P/RQ.V, BQ.R/BQ.H (duplicate keyRefs in metadata).
          No State initialValue (LIMITATION #30 -- In vs Out routing).
          Date format: yyyyMMdd. RelatedHitSearchIndicator Y-only flag.
          5 entities: Vehicle, Person, Firearm, Article, Boat.
REASON  : New provider onboarding -- Oregon LEDS via bulk build pipeline.
SCRIPT  : scripts/build_or_leds.ps1 + scripts/build_or_leds_mc.ps1
OUTPUT  : OR_LEDS_BASE.json + OR_LEDS_MC.json (+ readable variants)
RESULTS : BASE: 58P/0F/2W/1LIM | MC: 58P/0F/1W/1LIM

================================================================================
v1.3 (2026-05-11) -- LIMITATION elimination pass
================================================================================
CHANGED: Combo ordering, Patch 6 cleanup
RESULT: BASE 58P/0F/0W/0LIM | MC 58P/0F/0W/0LIM

