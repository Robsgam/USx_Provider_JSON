# SUPPRESSION-SCOPE: direction-aware
#   Opted in 2026-08-02 as part of the portfolio sweep Rob authorised after it was MEASURED rather
#   than assumed. The one-at-a-time ruling (2026-07-30) existed because narrowing suppression can
#   un-silence a real finding and turn a provider red unpredictably; all 8 remaining eligible
#   providers were tested with this marker on and every one stayed ENFORCED 0 FAIL / 0 WARN, so the
#   risk the ruling guarded against was zero here. Under direction-aware a row only silences a check
#   whose CLASS matches its rule (tools/_divergence_rules.ps1); 'other' rows silence nothing.
# AZ_AZDPS ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1 (CHECK 4). Each non-comment line records a set/any
# divergence that is intentional and correct, so the gate treats it as [NOTE] not [FAIL].
# Format (pipe-delimited): query | keyRef | field | rule | reason | source | date
# Matching is on the first three columns (query | keyRef | field), case-insensitive.
#
# ============================================================================================
# v3.5 (2026-08-04) -- SEVEN ROWS DELETED. This file shrank from 15 rows to 3, and not one of the
# twelve was suppressing something that still needed suppressing:
#
#   * 'AzAzdpsDriverLicenseQuery | ACWL | Name | devdoc-optional-unreachable' WAS NOT A DIVERGENCE,
#     IT WAS A BUG. It argued "metadata makes SexCode MANDATORY on every name variant ... there is NO
#     looser name variant". That is true of the AzAzdps-prefixed transaction and FALSE of the
#     devdoc-Basic one, whose DQ{Name} = Set[Name] Any[BirthDate, SexCode, State] is exactly the
#     looser variant it said did not exist. The row was scoped to the wrong transaction, so it
#     silenced a real defect -- an officer holding only a name could not run a DL query at all.
#     v3.5 switched to the Basic transaction and the finding it suppressed no longer arises.
#     THE LESSON: a registration inherits the scope of whatever the build happened to implement. When
#     the build is wrong about scope, the registry inherits the error and then defends it.
#
#   * The four 'DQ | SexCode/BirthDate' + 'DQSS | SexCode/BirthDate' rows and the four DH
#     'KQ | BirthDate/SexCode(+DH)' rows argued the UNDER-REQUIRED direction ("do not promote these
#     into set[], it would make an OLN-only search unreachable"). That argument was CORRECT and was
#     answering a question nobody had asked: the gate's finding was OVER-PERMITTED, i.e. the field is
#     transmitted on a variant that does not define it. The third option -- remove it from any[]
#     entirely -- appears nowhere in those reasons. One of them even stated the over-permit as a
#     benefit: "lets an OLN search that happens to carry them TRANSMIT them instead of dropping them."
#     Both authorities disagree: metadata DQ{OLN} = Set[OLN] Any[State] and KQ{OLN} = Set[State, OLN]
#     Any[PurposeCode, Attention], and devdoc DriverHistoryQuery #1 brackets ONLY [Attention,
#     PurposeCode]. v3.5 strips the fields; the rows are stale and gone. DQSS is deleted outright.
#
#   * The ACQA / ACQG rows are RIGHT ON SUBSTANCE and were only MIS-KEYED -- see below.
#
# STILL TRUE, AND STILL ENFORCED BY CONDITIONS RATHER THAN BY THIS FILE: identifier priority is
# enforced by existence-only EXISTS/NOT_EXISTS conditions on the CommSys combos, NOT by set->any
# demotion. The v3.0 doc falsely claimed demotion alone made the platform "serialize ONLY the intended
# identifier". It does not: the platform unions the set[]+any[] of EVERY combo whose set[] is satisfied
# (LIMITATION #1), so a lower-priority identifier left in a sibling's any[] still bleeds into the pool.
# Current gates (v3.5, SSN dropped from the DL cascade with DQSS):
#   Vehicle Plate>VIN : ACVRV -> LicensePlateNumber NOT_EXISTS
#   DL      OLN>Name  : DQPN / ACWL / DQN -> OperatorLicenseNumber NOT_EXISTS
#   DH      OLN>Name  : KQH -> OperatorLicenseNumberDH NOT_EXISTS
#   Boat    Hull>Reg  : ACQB / BQ -> BoatHullIdNumber NOT_EXISTS
# The higher-priority identifier is ALSO dropped from every sibling combo's any[] (gate-xor-companion,
# verify_build CHECK 14). The badge combos (ACWL/ACQB/ACQBH) additionally carry dexStateUserId EXISTS
# so they do not shadow the no-badge fallbacks (DQN/BQ/BQH), which is metadata-faithful: only
# ACWL/ACQB(H) require BadgeNumber, the DQ{Name}/BQ/BQH variants do not.
# ============================================================================================
#
# --- UNBUILT devdoc combinations. Both are genuinely unimplementable: AZ's metadata defines NO field
#     at all for the identifier the devdoc item requires, and each transaction has exactly ONE variant.
#     KEYREF RE-POINTED v3.5 (was 'ACQA' / 'ACQG', i.e. the BUILT combo). An existence-class rule
#     (not-built|unbuilt|shadow|dead-combo|dropped-combo|missing-primary-combo) makes
#     audit_requirement_fidelity skip that keyRef's ENTIRE comparison, so naming a live combo silently
#     dropped coverage while the finding count stayed 0 -- the tool warned about exactly this with
#     "[NOTE] REGISTRY OVER-SUPPRESSION RISK ... names a combo that IS BUILT". Pointing at the devdoc
#     item instead keeps the acceptance and restores the comparison: audit_devdoc_combinations matches
#     on (query, field) and never reads this column, so its [NOTE] is unaffected. ---
ArticleSingleQuery | (devdoc #2/#3) | OwnerAppliedNumber | devdoc-combo-unbuilt | Devdoc ArticleSingleQuery #2/#3 require OwnerAppliedNumber, but AZ metadata contains ZERO OwnerAppliedNumber field definitions and ACQA has exactly ONE variant: Set[BadgeNumber, ArticleTypeCode, ArticleSerialNumber] Any[RelatedHitSearchIndicator]. The built ACQA matches that variant exactly. There is nothing to build against. | metadata-vs-devdoc | 2026-08-01
GunQuery | (devdoc #1) | BirthDate | devdoc-combo-unbuilt | Devdoc GunQuery #1 is "(In/Out) BadgeNumber, BirthDate, Name" (gun by owner name), but AZ metadata ACQG has exactly ONE variant: Set[BadgeNumber, GunSerialNumber] Any[GunMake, GunModel, GunCaliber, RelatedHitSearchIndicator] -- there is NO name-based gun variant to implement. The built ACQG matches the only variant exactly. | metadata-vs-devdoc | 2026-08-01
GunQuery | (devdoc #2) | ConcealedWeaponPermitNumber | devdoc-combo-unbuilt | Devdoc GunQuery #2 is "(In/Out) BadgeNumber, ConcealedWeaponPermitNumber", but AZ metadata contains ZERO ConcealedWeaponPermitNumber field definitions and ACQG has only the GunSerialNumber variant. | metadata-vs-devdoc | 2026-08-01
