================================================================
  STATUS: SC_SLED
LABELS PROVISIONAL -- refine wording during manual form use; not a graded test case.
  Version: v1.5
  Updated: 2026-09-16
================================================================

CURRENT STATE
=============
  Phase: 1 COMPLETE (QIDM confirmation) -- layout built in the same pass
  JSON: SC_SLED_v1.5.json  --  79P/0F/2W/1LIM
  Score: 79P/0F/2W/1LIM  (BASE 79P/0F/2W/1LIM)  -- the 2 WARNs are Option A, deliberate
  Import: v1.0 IMPORTED to usx-sc-sled (73046844870) 2026-09-16 -- the FIRST automated import in the project, proven by the bundle table going 0 -> 3 (ENTITIES/SC_SLED/RMS). v1.1/v1.2/v1.3 were never imported. v1.4 WAS imported 2026-09-16 (counters moved ENTITIES 600->601, SC_SLED 3->4). v1.5 -- tab reorder -- NOT YET IMPORTED. NOTE: that tenant's Export JSON returns no config (CLICKED-BUT-NO-JSON), so no content hash or version string can be read from it -- the bundle NAMES are the evidence.
  USx tenant test: NOT STARTED (0 of 5 entities)

  9 QIDMs / 18 combinations / 7 QIFs = SEVEN TABS (v1.3). Wanted Person and Administrative Message each have their OWN TAB; Person is down to 2 cards / 10 fields. AdministrativeMessage has its OWN TAB, declaring targetEntity='Article' -- tabs are keyed by QUERYINPUTFORM, not by entity (CAPABILITY #47). Article, not Person, because LIMITATION #28 breaks codeTypeProvider reverse-lookup on a second QIF and all three such controls are on Person.
  First ground-up provider since the toolchain changed; first build authored with
  the Build-Qidm / Build-QidmAttribute / Build-QidmCombo helpers.

WHAT THE FIRST IMPORT MUST SETTLE (it is a real test, not a formality)
=====================================================================
  1. THE VEHICLE CO-FIRE. A plate entry fires QVRQ.P and QV.P; a VIN entry fires
     QVRQ.V and QV.VM. QVRQ is "SC Vehicle Stolen/Reg Inquiry", so the stolen check
     goes out twice. Rob 2026-09-14: "build both and we can sort it out." Neither
     masking mechanism is available yet -- queriesToDeselect is REFUTED for this
     shape (NY v2.8) and autoSelect=$false has ZERO tenant-proven carriers -- so
     this import is the discriminating test. Capture BOTH transactions per fill.
  2. THE PERSON CO-FIRE. Name+Sex+BirthDate fires QWDQ and QWA.N; wanted twice.
  3. ADMINISTRATIVEMESSAGE (STATUS: HYPOTHESIS). It is the 6th card and the only
     non-search transaction. Confirm the FIVE REAL ENTITIES STILL RENDER with it
     present. If they do not, remove it -- Rob: "we can alwasy remove it later."
  4. THE NCIC STATE PATTERN (ST-1), required on any new provider's first import.

TEST MATRIX
===========
  Entity              Query                    Combo       Result    Date
  ------------------  -----------------------  ----------  --------  ----------
  (no tests yet -- provider has never been imported)

  Planned coverage is the 18 combinations in SC_SLED_SQVR.txt, every marker
  [PENDING]. A marker moves only on a committed log carrying the CommSys request.

NOTES
=====
  Scope: the devdoc "Basic Query Transactions" list, 9 entries. The metadata XML
  defines 67 transactions; the other 58 are NCIC ENTRY/MODIFY/CANCEL/LOCATE, CCH,
  WMPI and Orion -- out of scope by the two-authority rule, not by omission.

  Registered divergences (5, all reasoned in SC_SLED_ACCEPTED_DIVERGENCES.txt):
    - DriverLicenseQuery QWDQ / ImageIndicator  -- devdoc-optional-unreachable
    - WantedPersonQuery QWA.P  / VIN + Make     -- demoted-to-any (nested-Set group)
    - WantedPersonQuery QWA.VM / Plate + State  -- demoted-to-any (nested-Set group)
