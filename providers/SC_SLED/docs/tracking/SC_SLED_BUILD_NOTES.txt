================================================================
  BUILD NOTES: SC_SLED
  Created: 2026-09-14
================================================================
v1.13  2026-09-18  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild

v1.12  2026-09-18  Pipeline rebuild
  CHANGED: Rebuilt via pipeline.ps1
  REASON: Scheduled rebuild

v1.11  2026-09-17  THE AM MESSAGE IS A MULTI-LINE BOX -- FormTextarea, and it is LIVE-PROVEN
  CHANGED: ADMINISTRATIVE MESSAGE -- FreeText control `Inp` -> `Txa`, i.e. resolvedName
              'FormInput' -> 'FormTextarea'. fieldId, label and maxLength='501' all UNCHANGED, so
              the wire contract and every QIDM reference are untouched. Emitted in all 3 layout
              variants (default / CAD_DISPATCH / FIRST_RESPONDER).
           NEW SHARED HELPER `Txa` in tools/_build_layout_helpers.ps1 (exports line updated).
           THREE GATES TAUGHT THE NEW TYPE: validate.ps1 (fieldId-presence check),
              audit_cross_provider.ps1 (field-type consistency) and audit_cad.ps1 (x2) all
              whitelisted control types BY NAME and would have SKIPPED a FormTextarea silently.
  REASON: Rob 2026-09-17: "so you now need to make message multiline". The AM carries a
           501-character free-text message to another agency in what was a ONE-LINE box.
  WHY THIS IS NOW BUILDABLE AT ALL -- PLATFORM_CONSTRAINTS CAPABILITY #48. That slot held a
           LIMITATION reading "THERE IS NO MULTI-LINE / TEXTAREA FORM CONTROL", backed by four
           sources. It was FALSE, and it was retired the same day by importing a throwaway probe
           (tools/build_multiline_test.ps1) to this provider's own tenant. Three of the four
           sources were USAGE CENSUSES -- 0 uses means UNTESTED, never IMPOSSIBLE.
  ⚠️ THE NAME IS LOWERCASE-'a' `FormTextarea`. `FormTextArea` renders NOTHING (exact-match
           resolver) -- which is why this went in as a HELPER rather than a hand-written node:
           the failure is one keystroke wide and completely silent.
  ⚠️ THIS BUYS WRAP-AND-SCROLL, NOT LINE BREAKS, and the label deliberately does not promise them.
           MEASURED on the round-2 wire test (docs/evidence/2026-09-17_MULTILINE_TEST_round2_*):
           the control TRANSMITS (3/3 captures), but neither Enter nor Shift+Enter inserts a
           newline -- BOTH SUBMIT THE FORM -- and a PASTED newline becomes ~124 spaces before it
           even reaches form state, so three five-letter words cost 262 of the 501 characters.
           The officer cannot make a break by typing, which is the only reason the budget is safe.
  ⚠️ OPEN, AND IT IS A POSSIBLE REGRESSION: maxLength enforcement is UNMEASURED on this control.
           It is honoured on FormInput and relied on portfolio-wide. The probe tenant was restored
           to v1.10 before this could be tested, so measure it on the v1.11 import -- paste MORE
           than 501 characters into the message box and see whether it truncates.
  ⚠️ NOT FIXED HERE AND BIGGER THAN THE BOX: pressing Enter SENDS THE AM. On a query that wastes a
           transaction; on an AM it TRANSMITS A HALF-WRITTEN MESSAGE to the ORI it is addressed to.
           Standard HTML form behaviour, so it applies to the single-line FormInput too and is NOT
           a regression introduced here -- no config prop to suppress it appears across 7,414
           control nodes, so it is a PRODUCT ask. This is the thing to raise about the AM.
  NOT TENANT-TESTED: SC_SLED has never been swept, so no test package was archived by this bump.

v1.10  2026-09-17  SEX AND RACE ARE DROPDOWNS AGAIN -- the v1.3 "capability given up" was HALF WRONG
  CHANGED: WANTED -- SexCode Inp -> Sel codeTypeCategory='NIBRS_SEX' + codeTypeSource='NIBRS',
              label 'Sex -- type M, F or U (optional)' -> 'Sex'.
              raceCode Inp -> Sel NIBRS_RACE + NIBRS, label -> 'Race'.
              LicensePlateStateCode Inp -> Sel NJ_NIBRS_STATE + NJ_NIBRS, label -> 'State'
              (**HYPOTHESIS** -- see below; the other two are not).
              Plate-row widths 3/3/3/3 -> 4/2/3/3 (State's label shrank, VIN displays narrower).
           VEHICLE -- row 2 widths 5/3/2/2 -> 3/3/2/4: VIN displays narrower and State gets the
              width, so its label stops being squished. VIN maxLength untouched at 20 (metadata size);
              only the COLUMN it displays in shrank.
           VALIDATOR 77P/0F/2W/2LIM -> 77P/0F/0W/2LIM. The two WARNs are GONE because the defect
              they described was FIXED, not because anything was silenced.
           REGISTRY -- the SCOPED raceCode and SexCode rows are DELETED; only RegistrationState
              remains. 8 rows -> 6.
  REASON:  Rob 2026-09-17: "on vin  shorten teh field length that displays  the helper for stae is
           getting squished  on wanted person plate state should be state and use the drop down  sex
           should just say sex and race should just say race".

           "SEX SHOULD JUST SAY SEX" IS A DROPDOWN REQUEST. Those labels only read cleanly if the
           CONTROL supplies the values -- 'Sex' over a free-text box tells the officer nothing about
           what to type. So this reopened the question v1.3 had closed.

           ** AND v1.3's REASONING WAS HALF WRONG. It made Sex, Race and Plate State type-ins
           because this card shares an entity with Firearm, LIMITATION #28 breaks codeTypeProvider
           reverse-lookup on a two-QIF entity, and AP #1 turns that into an internal numeric row id
           on the wire. Every clause is true -- and it covers ONLY ONE OF TWO MECHANISMS. #28 breaks
           the attributeTypeId + codeTypeProvider REVERSE-LOOKUP. A control carrying its OWN
           codeTypeCategory/codeTypeSource resolves its list LOCALLY and the SELECTED CODE reaches
           the wire; no reverse-lookup exists to break. THE PROOF WAS ALREADY IN THIS BUILD:
           GunMake, GunCaliber and ArticleTypeCode are dropdowns on two-QIF entities and have worked
           since v1.2. Three controls were given up without asking why the ones beside them were fine.

           BOTH PAIRINGS COME FROM THE AUTHORITY (FIELD_REFERENCE Section 3 table), not from taste:
             NIBRS_RACE | NIBRS -- "Sends W/B/I/A/U etc." Confirmed NY v1.12. BUILT AND
               TENANT-PROVEN by MD_METERS (ALL-PASS, 47 logs) and LA_LEMS, both with NO
               codeTypeProvider and NO attributeTypeId on the QIDM attribute. That exact shape was
               copied, and SC's RaceCode attribute already matched it -- so NO attribute changed.
               AP #3 forbids attributeTypeId=RACE anyway, making this the only correct race dropdown.
             NIBRS_SEX | NIBRS -- "Sends M/F/U to CommSys. Use ONLY if RMS sex filtering is not
               needed." THE PRECONDITION WAS CHECKED, NOT ASSUMED: Build-RmsBundle emits RMS QIDMs
               for Vehicle and Person ONLY, and this card is on targetEntity='Firearm', so nothing
               here feeds an RMS sex filter and the NJ v3.22/v3.29 RMS-400 failure mode
               (useAttributeId + a string -> array wrap) cannot arise. The PERSON tab is untouched
               and keeps the full attributeTypeId=SEX + codeTypeProvider=NIBRS pattern, which is
               correct there: Person is single-QIF AND does feed RMS.
             ** FIELD_REFERENCE's fallback prose also says to KEEP codeTypeProvider='NIBRS' beside
               the category. DELIBERATELY NOT DONE: zero providers build that combination, MD and LA
               both ship the category with a bare attribute, and a codeTypeProvider on a control that
               already yields a code string is the shape validate.ps1 watches for (AP #11). Built
               precedent beat the prose. If a wire capture ever shows a missing SexCode tag here,
               that is the first line to revisit.

  THE VALIDATOR HAD TO BE FIXED, AND IT IS THE SAME RULING AS ImageIndicator.
           With the control correct, validate.ps1 raised a THIRD WARN -- "SexCode field has
           codeTypeCategory='NIBRS_SEX' -- use attributeTypeId=SEX + codeTypeProvider=NIBRS
           instead". The check demanded the PREFERRED pattern unconditionally, so on a multi-QIF
           entity it was instructing a correct build to adopt a broken one. Rob's standing ruling on
           exactly this shape (2026-08-12, ImageIndicator): "this should not generate a warn when
           its the proper way to do this. whatever is triggering the warning needs to be fixed."
           The check is now #28-aware: it counts QIFs per targetEntity and accepts NIBRS_SEX + NIBRS
           as CORRECT when that count is > 1, reporting [INFO] with the entity and the count so the
           non-preferred choice stays VISIBLE rather than silently tolerated.
           NOTE the RACE check immediately above it had ALWAYS accepted the equivalent control-side
           NIBRS_RACE -- sex was simply inconsistent with race, which is why race raised nothing.
           LAW 2, PROVEN BOTH WAYS BY PLANTED MUTATION -- and each mutation ASSERTS IT CHANGED THE
           FILE before the verdict is believed, because my first attempt was mangled by shell quoting,
           no-oped, and returned a clean 0 WARN that would have "proven" the fix while testing nothing:
             * incomplete pairing (category kept, source broken), multi-QIF entity -> 3 WARNs
             * valid pairing but the entity retargeted to be SINGLE-QIF             -> 3 WARNs
             * the real v1.10 (valid pairing, multi-QIF)                            -> 0 WARNs
           REGRESSION SWEEP, all 21 providers re-validated after the change: every score identical to
           its CLAUDE.md baseline, 0 FAIL / 0 WARN everywhere. The change only ADDS an acceptance
           path, and that was measured rather than argued.

  ** PLATE STATE IS A HYPOTHESIS AND IS THE ONLY RISK IN THIS VERSION.
           NJ_NIBRS_STATE | NJ_NIBRS -- "NJ/NY state list. Sends 2-letter code. NCIC gives empty
           dropdown." A 2-letter code is exactly what SC wants and the mechanism is the same
           control-side one that makes Sex and Race safe. But:
             * ZERO CARRIERS. All 69 state dropdowns in the portfolio use attributeTypeId='STATE';
               not one uses a control-side state category. Measured, not assumed.
             * The source is NJ_NIBRS and whether it RESOLVES on a non-NJ tenant is UNVERIFIABLE
               from this repo -- CLAUDE.md records that same open question for VEHICLE_BODY_STYLE.
             * FAILURE MODE IS A REGRESSION, not a wrong value: an EMPTY dropdown, where today an
               officer can at least type 'SC'.
           It is LOUD, not silent. DISCRIMINATING TEST: open the Wanted Person tab after this import
           and look at the State list. If empty, revert that ONE control to Inp ... '2' with the
           2-letter hint in the label -- the v1.9 shape, which worked.
           BOAT'S State IS DELIBERATELY NOT CONVERTED IN THIS BUMP even though the same escape
           applies. One experiment per import: if two dropdowns changed at once and one came back
           empty, the import would teach nothing extra. Its registry row records that decision and
           says to convert it once the Wanted list is seen to populate.

  ** STILL NOT DELIVERED: the AM multi-row text box. Rob: "the admin message line still looks the
           same" -- correct, because NOTHING WAS CHANGED THERE and nothing can be. There is no
           multi-line control in the platform's vocabulary: probe_control_types.ps1 measured it
           across 21 provider JSONs AND 66 tenant configs including NOT-OUR-BUILD, and a FormInput
           has only ever carried fieldId/label/maxLength/initialValue. This is an ABSENCE, not a
           deferral. The only way forward is a throwaway probe import with a candidate control --
           the method that established CAPABILITY #47 and LIMITATION #46 on this provider.

v1.9  2026-09-17  LAYOUT PASS -- name controls go FIRST-LAST-MIDDLE-SUFFIX everywhere, rows resequenced
  CHANGED: VEHICLE -- now 2 rows. `RegistrationState` moves to the END of row 2 (cols 5/3/2/2) and
              ROW_VEH_3 is deleted.
           PERSON  -- row 2 carries all four name components in First, Last, Middle, Suffix order
              (was Last/First/Middle with Suffix orphaned onto the DOB row). Row 3 is now
              DOB / Sex / NCIC Image.
           WANTED  -- six rows resequenced to lead with, in order: first name, plate, OLN, NCIC,
              SSN, expanded name. Name row in First/Last/Middle/Suffix order; OLN promoted to the
              FIRST field of its row and that row is now 3rd; the plate row (formerly last) is 2nd.
           BOAT    -- `BoatHullIdNumber` label 'Hull ID (takes priority over registration)' ->
              'Hull ID'. `RegistrationState` label -> 'State (leave blank for SC)'.
           AM      -- `DestinationCode` label -> 'Destination ORI (required)'. ROW_AM_3 cols 4/4 ->
              6/6, which clears the ONLY `audit_layout_flow` advisory on this provider (L6
              ROW-NOT-12: two fields at 4 sum to 8 and left 4 columns of dead space on the right).
              Pre-existing since v1.2 and surfaced by enforce PHASE 2w during this pass; fixed here
              because a layout pass is exactly when it should be, not left for the next reader.
           Validator 77P/0F/2W/2LIM, UNCHANGED from v1.8.
  REASON:  Rob 2026-09-17, after confirming v1.8 rendered correctly: "on veh lets move state to the
           end of the second line  veh will have 2 lines  person  do first last middle suffic on
           second line  wanted person  do tope line in first last middel suffix format  we need to
           use that format everythwere  second line put oln as first fiedl then make the oln line
           3rd row  wanted rows should start with frist name  plate oln ncic ssn and expanded name
           move the last line with plate  and make that the second line  on boat remove the helper
           on hull id  i asked for tht previously  clean up state helper to just say leave blank for
           SC  on admi ... change helper on destiantion ori to just say required".

           ⚠️ NOT ONE WIRE VALUE MOVED, AND THIS IS THE POINT WORTH RECORDING. Every fieldId,
           maxLength, node id, QIDM attribute, combination and set[]/any[] is byte-identical to
           v1.8; only row membership, row sequence and `templateColumns` changed, plus three labels.
           In particular the composite Name attribute still declares
           sourceField @('NameLast','NameFirst','NameMiddle','NameSuffix') feeding
           FormatStringRuleHandler, so all three name-bearing queries still emit the authoritative
           ConnectCIC format `LAST, FIRST MIDDLE SUFFIX`. VERIFIED on the emitted JSON, not assumed.
           **DO NOT "make the QIDM consistent" with the new control order** -- reordering sourceField
           WOULD change the wire, and LAST-first is the format rule every provider is cross-checked
           against (FIELD_REFERENCE Section 7). "Use that format everywhere" is about what the
           OFFICER READS left to right; the handler reads the attribute, never the layout.

  BOAT STATE HELPER: the dropped '2-char code;' hint costs nothing -- the control's maxLength is 2,
           so the box physically cannot take a third character. The hint was restating what the
           control already enforces.

  AM 'required' ON DestinationCode IS A DEVDOC-FAITHFUL LABEL, NOT A CLAIM ABOUT THE METADATA.
           Metadata makes only FreeText mandatory; the devdoc marks DestinationCode mandatory, and
           the build follows metadata (so the field stays in any[], NOT set[]). The label now states
           the devdoc's expectation plainly, which is what an officer needs, and no combination
           changed -- an AM with no destination still matches `AM` set[FreeText] exactly as before.

  ⚠️ ONE ITEM NOT DELIVERED, and it is a CAPABILITY question rather than a preference:
           "on admi  can we make the text line multirow like a box for text". There is no
           multi-line control available to build. MEASURED, not assumed -- `probe_control_types.ps1`
           (new) censuses every control node in all 21 provider JSONs AND all 66 tenant configs
           pulled off the platform, including NOT-OUR-BUILD ones authored by other engineers:
               FormInput  FormSelect  FormDate  FormCheckbox  FormRadioGroup
           `FormRadioGroup` (5 nodes, the CA_eSUN experiment) is the ONLY type we do not build
           ourselves. There is NO textarea, no multi-line type, and the entire prop vocabulary ever
           set on a FormInput is fieldId / label / maxLength / initialValue -- no rows, no
           multiline, no height. Inventing a `resolvedName` the Craft.js renderer does not know
           would render NOTHING, silently: that is the retired whole-tree-recase failure verbatim,
           where collapsing `nodes` left only tab names showing.
           `m43Forms` was checked as a second source and yields no resolvedName at all in the one
           export that carries it -- not evidence either way, recorded so nobody re-checks it.
           DISCRIMINATING TEST if Rob wants it: a throwaway probe JSON with a candidate control
           imported to usx-sc-sled (which carries nothing precious), read the rendered card. That is
           exactly how CAPABILITY #47 and LIMITATION #46 were established on this provider.
           The message field is already full-width (cols '12') with maxLength 501; that is the most
           the proven vocabulary allows.

v1.8  2026-09-17  ADMINISTRATIVE MESSAGE IS LAST, as asked -- and the tab-order rule is now MEASURED
  CHANGED: 1. `$amForm` AND `$amQuery` move targetEntity 'Article' -> 'Boat'. Both, not one: see below.
           2. `$entityOrder` last element 'Article' -> 'Boat'.
           3. Boat's `RegistrationState` control: `Sel` (attributeTypeId='STATE') -> `Inp`, 2 chars,
              label 'State (2-char code; leave blank for SC)'.
           4. BoatQuery's `State` attribute: `-CodeTypeProvider 'NCIC'` REMOVED.
           No combination, keyRef, set[]/any[] or field id changed. Validator 78P -> 77P/0F/2W/2LIM
           (the lost PASS is the Boat State dropdown check; nothing regressed).
  REASON:  Rob 2026-09-17, on the rendered v1.7 tenant: "tab order is still wrong ... put admin at
           the end like i asked". He had asked for this order on 09-16 and v1.7 did not deliver it.

           WHAT HE READ, AND WHAT IT PROVED. v1.7 rendered:
               Vehicle | Person | Wanted Person | Firearm | Article | ADMIN MESSAGE | BOAT
           against the requested:
               Vehicle | Person | Wanted Person | Firearm | Article | BOAT | ADMIN MESSAGE
           Six of seven were right. That one swap settled a HYPOTHESIS this script had been carrying
           since v1.3, and it was HALF WRONG:
             1. The order array lists ENTITY names and DUPLICATES CARRY NO INFORMATION. v1.7 shipped
                ('Vehicle','Person','Firearm','Firearm','Article','Boat','Article') -- two `Firearm`
                and two `Article` entries, and nothing in that array can say which FORM takes which
                slot. It is AMBIGUOUS BY CONSTRUCTION: it orders the UNIQUE entities, nothing more.
             2. Within one entity, its forms render in CONFIGURATIONS[] ORDER. This half WAS right,
                and it is why Wanted Person correctly preceded Firearm.
             3. CONSEQUENCE, and this is the constraint that actually governs the layout:
                SAME-ENTITY TABS ARE ALWAYS ADJACENT, so a tab is LAST only if it sits on the LAST
                entity. On Article, Administrative Message was GLUED to Article at position 6 and
                pushed Boat to 7. NO REORDERING OF THE ORDER ARRAY COULD HAVE FIXED IT -- which is
                why v1.8 moves the HOST rather than the order.

           THE QIDM HAD TO MOVE WITH THE FORM, and getting this wrong would have failed silently.
           LIMITATION #26: the platform evaluates every QIDM of an entity against THAT ENTITY'S
           shared field pool. A QIF on Boat with its QIDM still on Article means FreeText lands in
           the BOAT pool while the QIDM reads the ARTICLE pool -- set[FreeText] is never satisfied,
           NOTHING fires, and no gate or error says so. Verified on the emitted JSON: AM QIDM
           targetEntity=Boat.

  THE PRICE, PAID IN $boatLayout AND RECORDED RATHER THAN HIDDEN:
           Boat is now a TWO-QIF entity, and LIMITATION #28 is explicit -- "when multiple QIFs target
           the same entity, codeTypeProvider reverse-lookup on QIDM attributes fails; the QIDM sends
           the raw attribute ID instead of the resolved code." Boat's State was the exact rescue
           pattern: `Sel` with `attributeTypeId='STATE'` (which alone sends the platform's internal
           numeric row id -- AP #1) paired with `-CodeTypeProvider 'NCIC'` on the attribute to resolve
           it back to a code. Keeping the dropdown would have put a numeric id on the wire where SC
           expects a 2-character state code. So the control is a type-in and the codeTypeProvider is
           gone with it -- there is no attribute id left to reverse-look-up.

           THE COST WAS MEASURED BEFORE CHOOSING, not rationalised after:
             - State is `any[]`-ONLY on BOTH Boat combos (QBBQ.H set[BoatHullIdNumber] any[State];
               QBBQ.R set[RegistrationNumber] any[State]). A pure optional, NOT a routing
               discriminator, so there is no BUILD_RULES 24 exposure.
             - The label already told the officer to leave it blank in state.
             - Precedent on this same provider: Wanted Person's LicensePlateStateCode became a
               type-in at v1.3 for this identical reason.

           WHY BOAT AND NOT ARTICLE BORE IT -- an asymmetry read from the emitted JSON, not assumed.
           Article hosts a second QIF for FREE: `ArticleTypeCode` resolves itself on the CONTROL
           (codeTypeCategory NCIC_ARTICLE_TYPE / codeTypeSource CA_CLETS) and a census of every
           attribute carrying codeTypeProvider or attributeTypeId shows NO Article attribute uses
           either -- so #28 has nothing to break there. Boat could not: its State depended on the
           reverse-lookup. Article is simply not the last entity, so it could not host the last tab.

  ⚠️ A FALSE ALARM I RAISED AND THEN REFUTED, kept here because I nearly acted on it: seeing
           GunMake/GunCaliber on ENTITY_Firearm (shared with Wanted Person since v1.3) I flagged the
           same #28 exposure. The census above says NO Firearm attribute carries codeTypeProvider
           either -- those dropdowns resolve on the control like Article's. There was nothing to
           break and there never had been. Reading the emitted JSON killed it in one command.

  ⚠️ THE DUPLICATE ORDER ENTRIES ARE KEPT ON PURPOSE. They are inert by rule 1 above, and trimming
           the array to the 5 unique entities in the SAME bump would be two experiments at once: if
           tabs then went missing, nothing would say whether the host move or the trimmed array
           caused it. One change per import.

v1.7  2026-09-16  OLN>NAME GUARDRAIL ON DriverRegistrationQuery -- the co-fire's missed cost
  CHANGED: `DQ.RN` gains `conditions: [{ field: [OperatorLicenseNumber], operator: NOT_EXISTS }]`.
           One combination, one condition. No field, label, layout, order or attribute change.
           Validator 78P/0F/2W/2LIM (unchanged). Registry: +1 row.
  REASON:  v1.6 SHIPPED COMMITTED WITH THREE BLOCKING FAILS AND I CALLED IT DONE OFF THE VALIDATOR.
           The validator read 78P/0F, the build printed "Validation passed", and the JSON was
           committed in d538fbac -- but `enforce` had not been read. It reported:
               [FAIL] verify_build -- DQ.RN missing OperatorLicenseNumber NOT_EXISTS
                      (OLN>Name guardrail; OLN+Name co-entry will bleed Name into OLN XML)
               [FAIL] DriverRegistrationQuery #1 +[ImageIndicator]        -> not transmitted
               [FAIL] DriverRegistrationQuery #1 +[ImageIndicator,State]  -> not transmitted
           This is the SECOND time on this provider that "the validator passed" was mistaken for
           "enforce passed" (v1.2 was the first). The validator does not run verify_build, the
           devdoc-optional gate, or the parity cross-check.

           THE DEFECT WAS REAL AND WAS CREATED BY v1.6's CARD MERGE. Through v1.5 this query read
           DR-SUFFIXED controls, so an OLN typed into the Driver License card could not reach it.
           v1.6 pointed it at the SHARED controls, and `DQ.RN` is ordered FIRST -- so an
           OLN + Name + DOB fill matched `DQ.RN`, sent a NAME search, and DISCARDED THE OLN.
           `DQ.RO` never ran, because only ONE combination fires per QIDM. The officer's strongest
           identifier was silently dropped. The co-fire itself is unaffected: DriverLicenseQuery
           still sends its own query off the same fill, and now DriverRegistration sends `DQ.RO`.

           THE OTHER FIX -- putting OperatorLicenseNumber in DQ.RN's any[] -- IS REFUSED, and the
           raw <Requirements> is why (the sanctioned raw-XML exception):
               DriverRegistrationQuery  DQ{Name}  Set[BirthDate, Name]
                                                  Any[State, State2..State5, SexCode]
           It does not define OperatorLicenseNumber AT ALL. Adding it would OVER-PERMIT a tag the
           transaction has no slot for. DriverLicenseQuery's `QWDQ` DOES carry OLN in its Any[],
           which is precisely why verify_build flagged one combo and not the other -- the two
           transactions are asymmetric in the metadata and the build now matches each one.

  THE TWO ImageIndicator FAILS ARE A REGISTRATION, NOT A FIX -- same adjudication as QWDQ.
           `DQ{Name}`'s Any[] has no ImageIndicator either; only `DQ{OperatorLicenseNumber}` does,
           and that is `DQ.RO`, where it is wired with the 'Y' default. The devdoc lists
           ImageIndicator FLAT across both variants (devdoc = one optional list per query; metadata
           scopes optionals PER VARIANT). An identical row for DriverLicenseQuery | QWDQ has been in
           SC_SLED_ACCEPTED_DIVERGENCES.txt since 2026-09-14 and reads [NOTE]; this one was owed the
           moment the shared NCIC Image control started reaching this query. Row added, verified to
           clear the FAIL rather than to look like it did.

  ALSO FIXED, SHARED TOOL: `audit_simulator_parity.ps1`'s behavioural cross-check asserted a model
           this provider refutes. It recomputed each plan test's keyRef with `Get-FiringKeyRef`,
           which walks EVERY QIDM on the entity and returns the first match ANYWHERE -- i.e. "one
           query fires per entity". So it reported 4 FAILs of the form "plan expects 'QV.P' but the
           canonical walk fires 'QVRQ.P'" when BOTH fire and the plan was right. Acting on that
           would have meant deleting the co-fire tests. It now asks the OWNING QIDM
           (`Get-FiringKeyRefForQidm`) when the plan's keyRef is traceable to one, falling back to
           the entity walk otherwise so no test leaves the denominator.
           MEASURED BOTH WAYS: 1416 tests compared before, 1416 after (0 indeterminate both), and a
           planted WITHIN-QIDM mismatch on SC_SLED test n=3 still FAILs -- so the narrowing did not
           blind it.


  ALSO FIXED, SHARED TOOL #2: `emit_test_plan.ps1` was EVICTING QUERYINPUTFORMS. Its lookup was
           `$qifByEntity[$targetEntity] = $qif` -- a plain hashtable -- so on an entity carrying more
           than one form the LAST one silently discarded every earlier one, and only its controls
           counted as fillable. THIS PROVIDER IS THE ONLY ONE IN THE PORTFOLIO IT COULD AFFECT and
           that is measured, not assumed: `tools\_probes\probe_multi_qif_entities.ps1` (new) reports
           **1 of 21** -- SC_SLED, Firearm = ENTITY_WantedPerson (21 controls) + ENTITY_Firearm (4),
           Article = ENTITY_Article (2) + ENTITY_AdministrativeMessage (6). Every other provider is
           one-QIF-per-entity, the standing shape LIMITATION #28 describes.
           WHAT IT COST HERE: `ENTITY_Firearm` sorts after `ENTITY_WantedPerson`, so all 21 Wanted
           Person controls vanished and the plan printed **24 lines of "any[X] -- no control on the
           Firearm form; treated as platform-DERIVED, not typed"** for fields that are visible
           type-in boxes on the Wanted tab -- every one of those optionals would have gone UNDRIVEN
           and therefore UNPROVEN. The union is the correct model, not a convenience: LIMITATION #26
           records that the FIELD POOL IS SHARED across all QIFs on one entity, which is the very
           fact that makes #28 break codeTypeProvider reverse-lookup here.
           MEASURED: plan 37 -> 61 tests with all 24 omissions gone, then 61 -> **65** once the
           recovered optionals got test values. Parity cross-check 1416 -> 1445 compared, 0 disagree.
           PS 5.1 TRAP HIT ON THE WAY: the first version stored a `List[object]` and `@($list)`
           THROWS "Argument types do not match" on 5.1, which is the engine enforce actually uses.
           Plain arrays now.

  TEST VALUES ADDED for the recovered Wanted Person optionals, sizes/values read from the devdoc
           WantedPersonQuery M/C/O row rather than guessed: `ExpandedNameSearchCode=Y` (O size 1,
           "Y, N, or blank"; the control ships with no initialValue so Y is a real toggle test),
           `FBINumber=123456AB1` (O size 9), `MiscellaneousNumber=MISC0000012345` (O size 15).

  ⚠️ ONE COVERAGE HOLE LEFT OPEN ON PURPOSE, AND IT NEEDS ROB: the AdministrativeMessage's five
           `DestinationCode` optionals have NO test value, and emit_test_plan WARNs 5x about it.
           That WARN is being LEFT IN rather than silenced. An AM is not a search -- it DELIVERS
           free text to the ORI(s) named in those fields, into a queue a human at that agency reads.
           Inventing a 9-character ORI-shaped value does not make a test value, it picks a
           RECIPIENT, and a wrong guess sends an unsolicited message to a real South Carolina
           agency under the tenant's ORI. The obviously-safe self-addressed option cannot be written
           into the build: `Build-Auth` takes the ORI from the TENANT at runtime. To close it, read
           usx-sc-sled's own ORI after the import and paste it in -- the commented line and the full
           reasoning are in docs/reference/TEST_VALUE_OVERRIDES.txt. Consequence stated plainly: the
           AM set[FreeText] path WILL be driven; its five optionals will not, so nothing proves
           those tags transmit. It is this provider's only such hole.

v1.6  2026-09-16  DRIVER LICENSE + DRIVER REGISTRATION CO-FIRE, plus a label/layout pass
  CHANGED: 1. CO-FIRE. `CARD_PER_DR` and every DR-suffixed control DELETED. DriverRegistration's
              attributes and combinations now point at the SHARED Driver License controls, and
              `-QueriesToDeselect @('DriverLicenseQuery')` is GONE -- that deselect was the one
              thing preventing the co-fire. Person: 2 cards/10 fields -> ONE card. DL card title
              now says both queries are sent.
           2. HELPER TEXT REMOVED, per request: Vehicle VIN, Wanted VIN, Wanted Plate Number,
              Wanted NCIC Number, Article Serial Number.
           3. WANTED ROW ORDER: Name first, DOB/Sex/Race/OLN second, NCIC + case number third.
              Sequence only -- same rows, same field ids, no wiring touched.
           Validator 79P/0F/2W/1LIM -> 78P/0F/2W/2LIM.
  REASON:  Rob 2026-09-16: "on person remove help on vin  lets co fire driver reg and driver license
           since they are idnetical combos  remove helper on atricle serial  on eanted lets move the
           name row to the top  dob line to 2nd  and top row down 2  remove helper on ncic and plat
           enumber".

           WHY THE CO-FIRE IS SOUND, not just permitted. Through v1.5 these two were ISOLATED on
           purpose: metadata gives BOTH transactions a keyRef of `DQ` with IDENTICAL mandatory sets,
           a collision ordering cannot separate. That reasoning was about telling them APART. Rob's
           call is that they should not be: same search, one fill, both queries. The mechanism was
           already PROVEN on this provider -- Vehicle carries reg+stolen on one card and
           test_commsys shows one plate fill firing QVRQ.P AND QV.P. Verified after the change: an
           OLN/name fill fires QWDQ (DriverLicenseQuery) AND DQ.RN (DriverRegistrationQuery).
           LIMITATION #2 is one QIDM per (targetEntity, QUERY) and these are DIFFERENT queries, so
           two on Person is as legal as two on Vehicle.
           Person keeps exactly ONE QIF, so its codeTypeProvider reverse-lookup stays intact (#28).

  THE NEW 2nd LIMITATION IS EXPECTED, NOT A REGRESSION:
      [LIMITATION] Person: 2 QIDMs have autoSelect=true on a SINGLE QIF without queriesToDeselect
                   -- co-fire lacks toggle control
  That is the gate describing exactly what was asked for. Vehicle already carried the identical
  line for reg/stolen. Two LIMITATIONs now, one per co-firing entity.

  ⚠️ VIN AMBIGUITY RESOLVED BY DOING BOTH, and saying so: the request said "on person remove help
  on vin" but PERSON HAS NO VIN CONTROL. The VINs carrying helper text were Vehicle's ("plate takes
  priority if both are entered") and Wanted's ("wanted vehicle -- needs make"), so both were
  stripped rather than guessing which was meant.
  Checked before trusting the Article edit: the pattern could also have matched FIREARM's serial,
  but that label was ALREADY bare, so no unintended scope. Verified against the pre-edit backup.

v1.5  2026-09-16  TAB ORDER set to the sequence Rob asked for
  CHANGED: Nothing but ordering. No field, combination, attribute or QIDM touched.
           `$entityOrder`     : Vehicle, Person, Firearm, Firearm, Article, Boat, Article
           `-Configurations`  : vehicleForm, personForm, wpForm, firearmForm, articleForm,
                                boatForm, amForm   (moved wpForm up from 6th to 3rd)
           79P/0F/2W/1LIM unchanged.
  REASON:  Rob 2026-09-16: "we need to reorder the tabs  veh per wanted person firearm articel boat
           admin mesage  in that order please".

  ⚠️ THE ORDER ARRAY LISTS ENTITY NAMES AND TWO PAIRS OF TABS SHARE AN ENTITY, so `Firearm` and
  `Article` each appear TWICE and the array ALONE cannot say which form takes which slot:
      1 Vehicle        -> vehicleForm
      2 Person         -> personForm
      3 Firearm  (1st) -> wpForm        <- Wanted Person
      4 Firearm  (2nd) -> firearmForm
      5 Article  (1st) -> articleForm
      6 Boat           -> boatForm
      7 Article  (2nd) -> amForm        <- Administrative Message
  [Likely] duplicates resolve by the CONFIGURATIONS order, so that array is now in the same
  sequence and the mapping above holds IF that assumption is right.

  STATUS: HYPOTHESIS -- and this is a NEW unknown created by CAPABILITY #47, not covered by it.
  #47 proved 8 forms produce 8 TABS; nobody recorded what ORDER they came out in, so the tie-break
  rule for a duplicated entity name is UNMEASURED. The probe's own order array listed `Person` four
  times and the observation was a COUNT ("8 tabs showing"), not a sequence.
  DISCRIMINATING TEST: import and read the tab strip. If Wanted Person and Firearm appear swapped
  -- or Administrative Message lands next to Article instead of last -- the tie-break is something
  other than configuration order, and the next thing to try is reordering ONLY the configurations
  array, since `$entityOrder` is already in the requested sequence.
  Do not write the tie-break rule into the KB until the rendered strip has been read.

v1.4  2026-09-16  The AM field suffix comes back OFF -- it was never needed and it broke a gate
  CHANGED: FreeTextAM / DestinationCode1-5AM renamed back to FreeText / DestinationCode1-5, in the
           form controls and the QIDM sourceFields alike. Nothing else. 79P/0F/2W/1LIM unchanged.
  REASON:  v1.2 suffixed these ids as belt-and-braces against LIMITATION #26's shared field pool.
           IT PROTECTED AGAINST NOTHING: Article's own controls are ArticleSerialNumber and
           ArticleTypeCode, so there was never a name in common with FreeText/DestinationCode -- the
           pool was already disjoint by construction.
           AND IT COST A BLOCKING GATE. `audit_devdoc_combinations` compares the devdoc's field
           names against the combination's SOURCEFIELDS, canonicalised by `Get-CanonicalToken`,
           which strips the established isolation suffixes `dh$` / `cch$` / `dr$` -- not `am$`. So
           `freetextam` stopped matching the devdoc's `FreeText` and enforce reported
           "AdministrativeMessage #1 is devdoc-listed but UNBUILT: mandatory field(s) FreeText,
           DestinationCode wired nowhere" on a provider that builds it.
           THE OTHER FIX WAS REJECTED ON PURPOSE: adding `am$` to that canonicaliser would widen a
           shared function feeding a BLOCKING gate across all 21 providers, and `am$` is a much
           riskier string to strip globally than `dh`/`dr`. Widening a portfolio-wide canonicaliser
           to accommodate one provider's unnecessary cosmetic choice is the wrong trade. Use a
           suffix here only if a REAL collision appears, and add `am$` in the same change.

  ⚠️ HOW IT GOT SHIPPED IN v1.2 AT ALL -- a process failure, not a build one. I committed v1.2
  calling it clean on the strength of the VALIDATOR (79P/0F/0W) and never completed an
  `enforce -Provider SC_SLED` run on it. The devdoc-combination gate lives in enforce PHASE 2p, not
  in the validator, so the break was invisible to the thing I checked. It surfaced only when v1.3's
  enforce finally ran. "The validator passed" is not "enforce passed", and I have the rule written
  down in two places.

  ALSO IN THIS VERSION, from the same enforce run -- the CROSS-PROVIDER consequence of v1.3's
  Option A, which I had not anticipated:
      [FAIL] raceCode: FormInput in SC_SLED but FormSelect in 9 providers
      [FAIL] SexCode:  FormInput in SC_SLED but FormSelect in 20 providers
  `audit_cross_provider` CHECK 5 enforces field-type consistency portfolio-wide, so making those
  two controls type-ins on one provider blocked the whole audit. Registered in
  tools\config\accepted_field_divergences.json -- but SCOPED, which needed a tool change:
  that registry keyed on fieldId ALONE, so an unscoped row would have told all 21 providers a
  FormInput SexCode is fine and silenced a genuine accident anywhere else. CHECK 5 now honours an
  optional `providers` array and FAILS any unlisted provider on the minority side. Proven both
  ways: scoped to SC_SLED it reports INFO and the audit PASSES; deliberately mis-scoped to TN_TIES
  it FAILS naming SC_SLED as not excused. Rows without `providers` keep their old meaning.

v1.3  2026-09-16  WANTED PERSON GETS ITS OWN TAB -- and three dropdowns became type-ins to pay for it
  CHANGED: 1. `CARD_PER_WANTED` REMOVED from the Person QIF. Person: 3 cards/20 fields -> 2/10.
           2. `ENTITY_WantedPerson` added as its own QUERYINPUTFORM, targetEntity='Firearm'.
              SEVEN TABS now. Ordered before Administrative Message.
           3. THE NEW TAB IS SELF-CONTAINED: it gained its own Name (Last/First/Middle/Suffix),
              BirthDate, SexCode, OperatorLicenseNumber and ImageIndicator controls -- 13 fields
              -> 18.
           4. THREE CONTROLS CONVERTED Sel -> Inp, and their `-CodeTypeProvider` removed from the
              `$wpAttrs` attributes ONLY: raceCode (NIBRS), SexCode (NIBRS), LicensePlateStateCode
              (NCIC). Labels now carry the valid values ("type M, F or U" / "type W, B, I, A or U"
              / "type the 2-letter code").
           Validator 79P/0F/0W/1LIM -> 79P/0F/2W/1LIM.
  REASON:  Rob: "now move wanted person to its own tab", then after being shown the cost: "start
           with option A  we can work backwards if we have to".
           Possible at all because of CAPABILITY #47 -- tabs are keyed by QUERYINPUTFORM, not by
           entity. It also delivers the ORIGINAL request behind all of this: Person is decrowded,
           halved from 20 fields to 10.

           WHY 'Firearm' -- MEASURED, with the denominator printed first because the first run of
           that probe extracted 0 fields and would have called every host "disjoint" vacuously.
           LIMITATION #28 breaks codeTypeProvider reverse-lookup on any entity with two QIFs and
           #26 names the cause as a SHARED FIELD POOL, so overlap against the 13 WP field ids was
           measured per candidate:
               Firearm  7 fields  overlap 0  DISJOINT  <- chosen; also the only single-combo host
               Boat     6 fields  overlap 0  DISJOINT
               Article  5 fields  overlap 0  DISJOINT  (already hosts Administrative Message)
               Vehicle 10 fields  overlap 3  Plate / VIN / MakeCode -- rejected

           WHY THREE CONTROLS AND NOT ONE -- I TOLD ROB IT WAS ONE AND THAT WAS WRONG. The
           WantedPersonQuery QIDM declares codeTypeProvider on RaceCode, SexCode AND
           LicensePlateStateCode. The validator had flagged only raceCode because SexCode's control
           was not yet on the form; adding it would have flagged too. On a two-QIF entity all three
           lose reverse-lookup and AP #1 says the platform then sends its INTERNAL NUMERIC ROW ID
           instead of the code -- silently wrong Race/Sex/State values to SLED, which is worse than
           a clumsier control. Rob was told the corrected cost before this shipped.
           Person's OWN SexCode/SexCodeDR dropdowns are UNTOUCHED: Person still has exactly one
           QIF, so its reverse-lookup still works. The trade is scoped to the new tab.

  THE 2 WARNs ARE DELIBERATE AND ARE NOT SILENCED:
      [WARN] ENTITY_WantedPerson SexCode field missing attributeTypeId='SEX'
      [WARN] ENTITY_WantedPerson SexCode field missing codeTypeProvider='NIBRS'
  That is precisely the trade above, stated by the gate. Registering them away would hide the one
  thing worth remembering if we work backwards. Same posture as OH_LEADS' deliberate ImageQuery
  WARN -- correct signal, not noise.

  ⚠️ STATUS: HYPOTHESIS on the disjoint-pool argument. #28's stated design constraint is absolute
  ("to use codeTypeProvider reverse-lookup, each entity must have exactly ONE QIF"); the
  zero-overlap reasoning above is an INFERENCE from its root cause. The remaining codeTypeProvider
  consumers on Firearm are now NONE, so nothing on that entity is exposed -- but if a future field
  adds one, it is at risk. DISCRIMINATING TEST on the next import: submit a Wanted Person query
  with Race and Sex filled and read the wire. Codes mean the inference holds; numeric ids mean #28
  applies regardless of overlap.

  TO WORK BACKWARDS, as Rob reserved the right to: restore the three controls to `Sel` with their
  codeTypeProvider and move CARD_PER_WANTED back into the Person QIF. That is the v1.2 shape.

  TWO MISTAKES CAUGHT BY GATES RATHER THAN BY ME, recorded so the pattern is visible:
    - a `perl` substitution matched in MORE THAN ONE QIDM and stripped codeTypeProvider from
      DriverLicenseQuery's SexCode as well. The validator caught it ("sends numeric ID (AP #2)")
      and it was restored by line number. Second time in one session that a global pattern hit an
      unintended second site -- the first retargeted ENTITY_Person to Article.
    - an earlier delete never ran because its shell command had a parse error, leaving the Wanted
      Person card DUPLICATED in both Person and the new form. Caught by counting occurrences
      before building, not by reading the diff.

v1.2  2026-09-16  ADMINISTRATIVE MESSAGE GETS ITS OWN TAB -- tabs are per-FORM, not per-entity
  CHANGED: 1. The AM card REMOVED from the Vehicle QIF. Vehicle: 2 cards/10 fields -> 1 card/4,
              i.e. back to what it was at v1.0.
           2. `ENTITY_AdministrativeMessage` RESTORED as its own QUERYINPUTFORM -- but declaring
              targetEntity='Article' (a RECOGNISED value) rather than 'AdministrativeMessage'.
              QIFs 5 -> 6, so SIX TABS. It is ordered LAST so the five familiar tabs keep their
              positions. `Article` appears TWICE in all three order arrays, on purpose.
           3. All six AM controls RENAMED with an AM suffix: FreeTextAM, DestinationCodeAM,
              DestinationCode2AM..5AM. The QIDM attributes keep their unsuffixed NAMES and point
              their sourceFields at the new ids; the combination's set[]/any[] carry the suffixed
              SOURCEFIELDS while primaryFieldReference keeps the attribute name 'FreeText'.
           4. QIDM TargetEntity 'Vehicle' -> 'Article'.
              Validator 78P/0F/0W/1LIM -> 79P/0F/0W/1LIM.
  REASON:  CAPABILITY #47, LIVE-PROVEN: TABS ARE KEYED BY QUERYINPUTFORM, NOT BY ENTITY.
           Evidence: docs\evidence\ENTITY_PROBE_RESULT_2026-09-16.txt. Rob refused the v1.1
           compromise -- "there has to be a way to have more than 5  this is just a card on the veh
           page.   i want it compeltely seperated" -- and then refused the reasoning behind it too:
           "are you sure you are testing this correctly  i feel like 5 is arbitraty". He was right
           both times. A probe declaring 8 forms (three of them sharing targetEntity='Person')
           rendered EIGHT TABS. So a transaction needs no entity of its own to get a tab.

           WHY 'Article' AND NOT 'Person' -- the validator decided this, not taste. The first v1.2
           attempt targeted Person and FAILED on LIMITATION #28: a SECOND QIF on an entity breaks
           codeTypeProvider reverse-lookup for that entity, and all three of SC_SLED's
           codeTypeProvider controls (SexCode, SexCodeDR, raceCode) are on Person. Article carries
           none and is the smallest pool in the build (1 card / 2 fields / 1 combo).

           WHY THE AM SUFFIX IS LOAD-BEARING: LIMITATION #26 -- the field pool IS SHARED across
           QIFs on one entity (confirmed, FL_FCIC, three tests with a single-QIF control) -- and
           LIMITATION #1 makes the wire a UNION across every matching combination. Unique field ids
           are what stop an Article fill from satisfying or over-sending into this form. Verified:
           an Article fill reports `[SKIP] AM -- missing set: FreeTextAM`.

           ⚠️ THIS WAS REDISCOVERED, NOT DISCOVERED, and the repo said so. LIMITATION #28 is a
           limitation ABOUT multi-QIF entities and has been in PLATFORM_CONSTRAINTS.txt since
           2026-04. I surveyed 21 builds and 66 deployed tenant configs hunting for evidence and
           never grepped the constraints file for the MECHANISM -- the exact routing CLAUDE.md's
           Source Authority table exists to force. The validator is what pointed me at it.

  STILL OPEN, unchanged by this version: enforce 2p/2q remain UNPROVEN for this transaction --
  emit_test_plan_spec's query->ENTITY map still assumes a `*Query` suffix, so the SPEC plan covers
  5 entities. The devdoc PARSER sees it (19 items).

v1.1  2026-09-16  AdministrativeMessage rehosted onto Vehicle -- the sixth entity does not render
  ⚠️ SUPERSEDED BY v1.2 AFTER ~40 MINUTES, AND TWO CLAIMS BELOW ARE REFUTED. Kept verbatim because
  it is the honest record of what was believed, but do not read it as current:
    - "A CARD LIVES INSIDE AN ENTITY, so the only way to make this transaction reachable is to host
      it on one of the five" -- FALSE. Tabs are keyed by QUERYINPUTFORM (CAPABILITY #47), so a
      transaction can have its own TAB while declaring a recognised entity value.
    - "ALSO REFUSED ... WantedPersonQuery can only be reorganised WITHIN Person's cards" -- FALSE
      for the same reason. It CAN be moved to a tab of its own.
  What survives from this entry: LIMITATION #46 itself (an UNRECOGNISED targetEntity value renders
  nothing) and the measurement that the v1.0 build was structurally correct.
  CHANGED: 1. `ENTITY_AdministrativeMessage` (targetEntity='AdministrativeMessage') DELETED.
              QIFs 6 -> 5; the ENTITIES order arrays are back to the five real entities.
           2. The ADMINISTRATIVE MESSAGE card (FreeText + DestinationCode1-5, 3 rows, 6 controls)
              MOVED into the Vehicle QIF as its second card. Vehicle: 1 card/4 fields -> 2/10.
           3. `SC_SLED_AdministrativeMessage` QIDM TargetEntity 'AdministrativeMessage' -> 'Vehicle'.
              Attributes, the single `AM` combination and all field wiring are UNCHANGED.
           4. Every `STATUS: HYPOTHESIS` marker for this transaction removed and replaced with the
              measured outcome. Validator 80P/0F/0W/1LIM -> 78P/0F/0W/1LIM (two PASS lines were the
              deleted QIF's).
  REASON:  LIMITATION #46, measured on the FIRST AUTOMATED IMPORT. Rob: "i did not see a admin card
           doublecheck your work". It was double-checked and THE v1.0 BUILD WAS CORRECT -- ENTITIES
           was bundle [0], the QIF existed, ALL THREE order arrays named AdministrativeMessage, and
           the tenant provably received that payload (read-back byte-exact, bundle table 0 -> 3).
           A correctly-formed sixth entity is SILENTLY DROPPED. Measured portfolio-wide the same
           day: all 21 providers use exactly Person/Vehicle/Firearm/Article/Boat, and
           AdministrativeMessage had exactly one carrier -- the one that vanished.

           The v1.0 hypothesis block asked two questions and got a split answer, which is why the
           caution it was built with was worth it:
             1. Do the five still render, in order?        YES -- blast radius ZERO.
             2. Does an AdministrativeMessage form appear?  NO.

           A CARD LIVES INSIDE AN ENTITY, so the only way to make this transaction reachable is to
           host it on one of the five. WHY VEHICLE is a weak tiebreak and is documented as such in
           the build script: Vehicle had the most room, Person is already 3 cards/20 fields, and
           Rob's own direction was to DECROWD Person. An admin message is NOT a vehicle search and
           the card title says so. Moving it elsewhere is a one-line change.

           NO ROUTING RISK, checked not assumed: the `AM` combination requires FreeText, which
           appears on no other card, so a plate/VIN fill reports `[SKIP] AM -- missing set:
           FreeText`; and the Vehicle combinations require plate or VIN, which appear on no AM row.
           Three QIDMs now share targetEntity=Vehicle, which LIMITATION #2 permits -- it is one
           QIDM per (targetEntity, QUERY), and AdministrativeMessage is a distinct query.

  ALSO REFUSED BY THE SAME MEASUREMENT, and recorded so it is not re-attempted: Rob asked to "try
  moving wanted person off as well". NOT AVAILABLE -- there is no sixth entity to move it to, so
  WantedPersonQuery can only be reorganised WITHIN Person's cards. Person stays at 3 cards.

  NOT A DEFECT, corrected here because I reported it as one: `audit_devdoc_combinations` notes that
  no combination's set[] covers [FreeText, DestinationCode]. That is the devdoc/metadata divergence
  this script already documents -- the devdoc marks DestinationCode mandatory, the metadata puts all
  five destination codes in <Any>, and METADATA IS FIELD AUTHORITY (Rob 2026-08-18). The build is
  right; the note is expected and should be registered rather than "fixed".

  STILL OPEN: enforce 2p/2q remain UNPROVEN for this transaction. `emit_test_plan_spec`'s
  query->ENTITY map is a fifth place assuming a `*Query` suffix, so AdministrativeMessage's devdoc
  fields land in "UNREACHABLE (devdoc field not on form)" and the SPEC plan still covers 5 entities.
  The devdoc PARSER itself now sees it (18 -> 19 items, measured; no other provider moved).


v1.0 (2026-09-14)
  CHANGED: Initial standup
  REASON:  New provider onboarding

--- 2026-09-16 -- ROB'S RULING ON THE VEHICLE CO-FIRE. NO BUILD CHANGE. ---
  Rob: "lets check the veh reg and veh stolen co fire  not sure what teh spec is but for now lets
  build it that way"

  CHECKED, and the answer is a MEASUREMENT off the emitted v1.0, not a restatement of the note
  that was already in the file. One plate fill (LicensePlateNumber=ABC1234, State=NJ) fires BOTH
  Vehicle QIDMs:

    SC_SLED_VehicleRegistrationQuery -> QVRQ.P
        set[LicensePlateNumber, LicensePlateTypeCode, LicensePlateYear]  any[RegistrationState]
        wire: LicensePlateNumber, LicensePlateTypeCode=PC, LicensePlateYear=2024, State
    SC_SLED_VehicleStolenQuery       -> QV.P
        set[LicensePlateNumber]  any[VehicleIdentificationNumber, RegistrationState, VehicleMakeCode]
        wire: LicensePlateNumber, VehicleIdentificationNumber, VehicleMakeCode, State

  CORRECTION TO THE EXISTING NOTE: the two are NOT the same request. CLAUDE.md and SESSION_STATE
  both say "QV.P set[Plate] equals QVRQ.P once PlateType/PlateYear are form-prefilled ... so the
  stolen check goes out twice". The CO-FIRE is real and confirmed; the claim of equality is not --
  QVRQ.P carries PlateType+PlateYear, QV.P carries VIN+MakeCode. What duplicates is the STOLEN
  CHECK specifically, because QVRQ is the COMPOUND key ("SC Vehicle Stolen/Reg"), so a stolen
  check rides inside QVRQ and again as the standalone QV.

  RULING: BUILD IT THAT WAY -- both queries stay, the co-fire stays, nothing is masked. That is
  what v1.0 already does, so there is NO JSON CHANGE, NO VERSION BUMP, and the test package is not
  archived. The action here is to record the decision so the 1 LIMITATION stops reading as an open
  blocker: it is now an ACCEPTED design pending SC's corrected docs, which Rob has requested.
  Masking remains unavailable anyway -- queriesToDeselect is refuted for this shape (NY v2.8) and
  autoSelect=$false has zero tenant-proven carriers.

  STILL OWED once the corrected SC docs land: re-adjudicate the reg/stolen split against the new
  authority. The co-fire is the SYMPTOM of the compound keys, not a separate defect.
