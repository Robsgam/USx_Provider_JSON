# PENDING_UPDATES.txt -- TN_TIES
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
# RETIRED 2026-08-19 -- ADJUDICATED at TN_TIES v2.2 -- REGISTERED AS CORRECT-AS-BUILT, and the flag PRESCRIBED FIX WOULD HAVE BEEN A NEW DEFECT. The flag said: "FIX at TN_TIES next rebuild: add RegistrationState to the RQ05 any[] (metadata permits it on the Nlets variant -- confirm against the RQ Requirements block before building)". That confirm instruction is what saved it, because BOTH of the flag premises are FALSE. (1) "metadata permits it on the Nlets variant": there is EXACTLY ONE RQ05 in the entire metadata XML -- under VehicleRegistrationQuery, Set[DealerLicensePlateNumber] Any[InquiryTypeIndicator]. No State in Set, none in Any, and NO separate Nlets transaction defines RQ05 at all; every transaction in the file was searched for the keyRef, not just VehicleRegistrationQuery. So adding State to RQ05 any[] would transmit a field that transaction does not define -- an OVER-PERMIT, which audit_requirement_fidelity would then report as a fresh finding. (2) "Its RQ06 (handicap) and RQ07 (temp) siblings DO carry State, so this reads as an omission rather than a design decision": they do NOT. Metadata RQ06 is Set[HandicapPlacardNumber] Any[] and RQ07 is Set[TemporaryLicensePlateNumber] Any[]; our built RQ06/RQ07 likewise carry no State in set[] or any[]. There is no asymmetry, so there is no omission to infer. WHAT IS ACTUALLY HAPPENING, and it is the portfolio standard pattern: RQ05 and QV.D have IDENTICAL set[] (DealerLicensePlateNumber) and are separated ONLY by the State condition -- RQ05 gates RegistrationState EXISTS (out-of-state) and QV.D gates NOT_EXISTS (in-state). The officer State value SHOULD decide which fires and SHOULD NOT ride along, because the RQ05 transaction cannot carry it. Same shape already registered on OH_LEADS BoatQuery BQ.H, where the Nlets BQ transaction does not define the image flags so a hull+State+Image fill correctly does not carry them. Recorded in NM/TN registry as VehicleRegistrationQuery | RQ05 | RegistrationState | routing-only-not-transmitted. audit_wiring_closure class J will KEEP PRINTING this -- it is a wiring OBSERVATION, its own output says only classes A and C are officer-facing, and it does not read the registry. That is expected, not a regression. DO NOT widen RQ05 any[].
# [FLAG:wiring-closure-class-J-routing-only] audit_wiring_closure gained class J ROUTING-ONLY FIELD (2026-08-07): an EXISTS condition gating a combination on a field that is in NEITHER that combination set[] NOR its any[] -- the officer value decides which query fires and is then dropped from the request. TN_TIES VehicleRegistrationQuery/RQ05 (OOS dealer plate, Nlets) is the ONE portfolio hit of 97 EXISTS conditions examined: it is gated RegistrationState EXISTS but carries State in neither set[] nor any[], so an out-of-state dealer-plate query transmits no destination state. Its RQ06 (handicap) and RQ07 (temp) siblings DO carry State, so this reads as an omission rather than a design decision. FIX at TN_TIES next rebuild: add RegistrationState to the RQ05 any[] (metadata permits it on the Nlets variant -- confirm against the RQ Requirements block before building). Not fixed now: one provider at a time, and TN_TIES is NEVER-TESTED with a separate parked prose divergence. (flagged 2026-08-07)
# RETIRED 2026-08-14 -- ALREADY CONFORMANT -- no change needed. This provider has exactly ONE ImageIndicator control (Person) and it already carries initialValue=Y, so Y-on-every-entity is satisfied: there is no other entity carrying the field. Adding controls elsewhere would OVER-PERMIT (same adjudication as HI v4.17 Vehicle-only / IL v2.4 excluding Article). Verified from the emitted JSON. Zero JSON change, zero version bump.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
# RETIRED 2026-08-19 -- RESOLVED at TN_TIES v2.2. test_phase2 step [2] had reported EIGHT findings that were really THREE missing test values -- each unvalued mandatory field produced one BLANK FILL (fieldId and value both empty, so the driver fills undefined) AND one UNSATISFIED (the field never filled, so the tenant keeps Send DISABLED and NOTHING is sent): T13 Vehicle/RQ05 + T14 Vehicle/QV.D DealerLicensePlateNumber, T15 Vehicle/RQ06 HandicapPlacardNumber, T16 Vehicle/RQ07 TemporaryLicensePlateNumber. These are TN THREE SPECIALTY-PLATE PATHS (dealer, handicap placard, temporary tag), which is most of what distinguishes this provider 8 Vehicle combos from the portfolio usual 3-4 -- so four of eight Vehicle paths could never have been driven. NOT A BUILD DEFECT: every combo is metadata-faithful (RQ05 Set[DealerLicensePlateNumber], RQ06 Set[HandicapPlacardNumber], RQ07 Set[TemporaryLicensePlateNumber]) and every control exists; emit_test_plan simply had no value for the fields. FIXED AS TEST DATA with no JSON change for it: new docs/reference/TEST_VALUE_OVERRIDES.txt, entity-scoped so values cannot leak onto another entity control, and the file records that the values are NOT authority-derived (metadata types them Alphanumeric, the devdoc gives no formats) so a live rejection means changing the VALUE not the build. Verified: fillability 48 tests / 129 fills, PASS, PHASE 2 PRE-FLIGHT CLEAR.
# [FLAG:plan-fillability-unfireable-tests] test_phase2.ps1 step [2] gained a real FILLABILITY check (2026-08-18) and it found unfireable tests in YOUR plan. Run: tools\test_phase2.ps1 -Provider <THIS PROVIDER>. Findings at flag time -- TN_TIES 4 BLANK + 4 UNSATISFIED, CA_eSUN 26 UNSATISFIED, MD_METERS 3 UNSATISFIED. WHAT IT MEANS: a BLANK fill is a plan test whose fieldId AND value are both empty, so the browser driver literally fills undefined; an UNSATISFIED is a test that omits a field the combo lists as mandatory, so the tenant correctly leaves Send DISABLED and NOTHING IS SENT. Either way the test cannot produce evidence. ROOT CAUSE IS ALMOST CERTAINLY MISSING TEST VALUES: emit_test_plan.ps1 already prints, by name, lines like "Vehicle RN set[]: OwnerLastName has no test value (unmapped in Get-TestValue)" -- that WARN existed all along and nothing consumed it, which is why this reached a live sweep. FIX: add the named fields to docs/reference/TEST_VALUE_OVERRIDES.txt (entity-scoped form <Entity>.<fieldId>=value is preferred so a value cannot leak onto another entity control), then re-emit the plan and re-run test_phase2 until it reports PRE-FLIGHT CLEAR. On OH_LEADS this took the plan from 50 tests with 2 BLANK / 7 UNSATISFIED to 56 tests / 151 fills all clean, and recovered a combo (ATDP) that had NO test at all. NOT A BUILD DEFECT and NOT a JSON change -- do not bump the version for it; it is test-package data. Verified NOT to false-fire: all 8 tenant-verified providers (AZ, CA_CLETS, FL, HI, IL, NJ, NY, TX -- 592 tests / 1948 fills) are clean, which is expected since a package that actually drove and passed cannot contain an unfireable test. (flagged 2026-08-18)
# CLEARED 2026-08-24 at v2.4 -- plan regenerated by the rebuild: 55 -> 54 tests. That delta is
# the dedupe PLUS the collapsed dealer pair (QV.D removed), so it is not directly comparable to
# the -1 measured on 08-21. No coverage lost: 19 combinations all reachable, zero QV.* plan tests
# remain, and the v2.3 logs did not exist (never tenant-tested), so nothing was archived.
# [FLAG:plan-dedupe-vacuous-tests] emit_test_plan.ps1 gained two plan-only filters 2026-08-21: (1) DUPLICATE -- drop a test whose (entity, fill-set) is byte-identical to an earlier one, which makes the generator agree with audit_log_inflation attack A by construction; (2) NO-CONTEST GUARDRAIL -- drop a guardrail whose fill adds no competing identifier over the winner's own combo test, because the pair differs only by a presence condition and the loser can never match. YOUR PLAN WILL SHRINK when you regenerate; measured non-destructively on 2026-08-21: AZ -5, CA_CLETS -11, CA_SLO -1, CA_VENTURA -2, FL -14, HI -1, IL -1, LA -1, MD -3, NJ -2, TN -1, TX_TLETS_CCH -4. NO COVERAGE IS LOST -- identical fill means identical wire, and NY held 16/16 combos at 100% across the change. NOT DONE FOR YOU ON PURPOSE: if you are tenant-verified, regenerating now makes your existing logs ORPHAN (more logs than plan tests) and drops you out of ALL-PASS, which is why this is a flag and not a sweep. Do it at your OWN next rebuild, and archive the superseded duplicate logs in the same pass (NY put them in logs/<Entity>/_archive_dedupe_v<ver>/). Reference: NY_NYSPIN_EJUSTICE v4.26, 73 -> 65 tests, inflation A 8 -> 0, four log gates 65/65. (flagged 2026-08-21)
