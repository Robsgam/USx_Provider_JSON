# SUPPRESSION-SCOPE: direction-aware
#   Opted in 2026-08-01. Without this marker a registry row suppresses EVERY check for its
#   (query, keyRef, field) triple regardless of what the recorded decision actually said -- so a row
#   granting "this field may ride in any[]" also silences the OPPOSITE defect (a field wrongly
#   promoted INTO set[]), and the gate goes mute somewhere it was never granted.
#   Narrowing is STRICTER and CAN turn a provider red by un-silencing a real finding. That is the
#   point: if it does, investigate the finding -- do NOT remove the marker.
#   Measured before/after opting in; result recorded in the commit.
# MD_METERS ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1 (CHECK 4 / 4d / 5). Each non-comment line records a
# divergence that is intentional/documented, so the gate treats it as [NOTE] not [FAIL].
# Format (pipe-delimited): query | keyRef | field | rule | reason | source | date
#

# RETIRED 2026-08-28 -- THE ROW'S PREMISE WAS FALSE, AND IT ASSERTED OWED WORK THAT DOES NOT EXIST.
#   It read: "metadata GunQuery has a GunMake-primary search path (gun-by-make) with no JSON combo ...
#   Build the GunMake-primary combo (e.g. ZGUN.M) at MD's next scheduled rebuild and remove this line."
#   Verified against the raw XML (the sanctioned per-Combination <Requirements> exception): MD's
#   metadata GunQuery holds EXACTLY ONE combination --
#       <Combination keyReference="ZGUN" primaryFieldReference="GunMake">
#         Set[GunCaliber, GunMake, GunSerialNumber]        (all three mandatory, no <Any>)
#   and the built ZGUN is set=[gunCaliber, firearmMake, serialNumber] -- an EXACT match. There is no
#   second gun-by-make path. The row mistook primaryFieldReference="GunMake" for "a search path keyed
#   on GunMake alone"; primaryFieldReference names which field the variant is INDEXED by, not a
#   looser requirement set. A KEYREF IS NOT A VARIANT and a primaryFieldReference is not a combination.
#   COST OF LEAVING IT: it instructed the next rebuild to build ZGUN.M -- a combination the metadata
#   does not define -- which would have OVER-PERMITTED and created a dead combo (identical set[] to
#   ZGUN, so no fill could separate them). It survived v2.1, v2.2 and v2.3, i.e. three rebuilds that
#   each read "build this at the next rebuild".
#   WHY NO GATE CAUGHT IT: audit_registry_currency only checks DIRECTION-class rows (to-any/to-set).
#   All 3 of MD's rows are existence-class, so it reported "[PASS] 0 stale of 0 checkable" and then
#   honestly FAILed itself -- "parsed rows but checked ZERO of them -- this run is not evidence."
#   Portfolio-wide that blind zone is 193 of 263 rows (73%); see FINDINGS_REGISTER section 5.
# GunQuery | ZGUN | GunMake | missing-primary-combo | (text above) | metadata-vs-json | 2026-06-23
#
# ...AND RETIRING IT UN-SILENCED A REAL DEFECT, WHICH IS THE ROW BELOW. Exactly what this file's
#   SUPPRESSION-SCOPE header predicts: "Narrowing is STRICTER and CAN turn a provider red by
#   un-silencing a real finding. That is the point: if it does, investigate the finding -- do NOT
#   remove the marker." Investigated; the finding is real and is NOT the one the old row described.

# ---------------------------------------------------------------------------------------------
# RETIRED 2026-08-31 at v2.4 -- SUPERSEDED BY A BUILD CHANGE, NOT BY A BETTER RULE NAME.
#   The row below sat here from 2026-08-28. Its diagnosis was right and its RULE NAME was inert:
#   audit_metadata CHECK 5 honours only an 'existence'-class rule, so
#   `primaryfieldreference-is-our-label-not-an-attestation` silenced nothing and the provider was
#   left carrying a live `[FAIL] GunMake: QIDM has attribute but NO combo uses it as
#   primaryFieldReference`. I wrote a registration and did not verify it suppressed anything --
#   the exact trap usx-tooling Step 6 names ("does it suppress anything AT ALL?").
#   FIXED THE RIGHT WAY: the row's own closing instruction was *"align the label at MD's next
#   rebuild if one happens for another reason; do NOT bump for it alone."* v2.4 was already
#   bumping for the DL State fix, so ZGUN's primaryFieldReference is now 'GunMake', matching the
#   single metadata combination it implements. Measured before AND after: audit_metadata
#   0 FAIL (114 PASS), audit_requirement_fidelity UNCHANGED at 15 branches / 0 UNDER / 0 OVER --
#   so the alignment bought a real clearance and cost no coverage. No suppression is needed now.
#   The reasoning is preserved verbatim below and in the build script above ZGUN, because the part
#   worth keeping is *why not to build a ZGUN.M* -- see the 2026-06-23 retraction further up.
#
# RETIRED 2026-08-31 (v2.4, label aligned): GunQuery | ZGUN | GunMake | primaryfieldreference-is-our-label-not-an-attestation | NOT A DEFECT. THE COMBINATION IS BUILT CORRECTLY; ONLY OUR INTERNAL LABEL DIFFERS. Metadata GunQuery holds ONE combination, indexed GunMake: Set[GunCaliber, GunMake, GunSerialNumber], all three mandatory, no <Any>. Built ZGUN is set=[gunCaliber, firearmMake, serialNumber] -- the SAME FIELD SET, which is the thing that reaches the wire. It declares primaryFieldReference "GunSerialNumber" where metadata says "GunMake". ROB'S RULING 2026-08-28: *"we are building the combos, not attesting to the keyref commsys will use."* primaryFieldReference is platform-internal, the same class as keyReference -- and keyReference is PROVEN to reach the wire ZERO times (a request carries <MessageType><QueryName> plus the FIELDS and nothing else, verified against a real capture). So a label difference cannot change what METERS receives or how it routes. NO WIRE CHANGE, NO VERSION BUMP, NO RE-SWEEP: the 46-log ALL-PASS package captured 2026-08-28 stands. WHERE primaryFieldReference DOES MATTER, and why this is still worth a row: it is how OUR OWN TOOLING picks which metadata alternative a built combo implements, when a keyRef carries several -- CA_CLETS IR.QVC has Name / OperatorLicenseNumber / CriminalIdNumber / SSN variants under one keyRef, and comparing a built CII combo against the {Name} variant produced 5 false findings. GunQuery has exactly ONE metadata combination, so there is no sibling to mis-select and the narrowing is unambiguous whatever the label says. Align the label at MD's next rebuild if one happens for another reason; do NOT bump for it alone. THIS ROW REPLACES a 2026-06-23 row that claimed metadata had a separate "gun-by-make search path with no JSON combo" and instructed the next rebuild to build ZGUN.M -- FALSE (there is no second combination; it mistook primaryFieldReference for a looser requirement set), and acting on it would have invented a combination the metadata does not define, with a set[] identical to ZGUN and therefore dead. | metadata-vs-json | 2026-08-28

DriverLicenseQuery | ZLDR.O | YearsPastViolationsWanted | metadata-optional-unreachable | Metadata DriverLicenseQuery holds FIVE combinations; we build FOUR. The unbuilt one is ZDRV{OperatorLicenseNumber} = Set[OperatorLicenseNumber] Any[YearsPastViolationsWanted, ImageIndicator], and YearsPastViolationsWanted is defined ONLY there -- it appears ZERO times in the emitted JSON, so no officer can supply it. NOT BUILDING IT IS CORRECT AND IS THE ONLY CORRECT CHOICE: its set[] is [OperatorLicenseNumber], BYTE-IDENTICAL to built ZLDR{OperatorLicenseNumber}, and the keyRef never reaches the wire (the request carries MessageType + fields only), so building both would emit identical bytes on the same fill -- a guaranteed dead combo that audit_combo_reachability would FAIL. ZLDR is the right survivor because it is the variant with a devdoc counterpart (DL #4 "(In/Out) OperatorLicenseNumber, [State]") and it carries State, which the devdoc documents; ZDRV{OLN} under DriverLicenseQuery has NO devdoc counterpart at all. The field is ALSO not recoverable by widening: adding it to ZLDR.O any[] would OVER-PERMIT, because ZLDR{OperatorLicenseNumber} Any = [State, ImageIndicator] and does not mention it -- the same shape as OH_LEADS DL/BMVIMS. Devdoc never mentions the field. ROW IS FIELD-SCOPED ON PURPOSE: an existence-class row naming ZDRV would also suppress DriverHistoryQuery/ZDRV which IS built, the OH_LEADS BMVIMS over-suppression (branches 24 -> 22). Branches-compared measured 15 before and after this row. The decision existed only as a build-script header comment ("YearsPastViolationsWanted dropped ... orphaned-field removal, per TX precedent") where no gate reads it; ENGINEERING_STANDARD 5 requires it recorded here. | metadata-vs-devdoc | 2026-08-28
DriverLicenseQuery | ZWAR.N | State | devdoc-optional-unreachable | Firing variant ZWAR{Name} = Set[BirthDate, Name, RaceCode, SexCode] Any[ImageIndicator] -- State is NOT defined on it. State appears only on the SIBLING variant ZWAR{OperatorLicenseNumber} = Set[Name, OperatorLicenseExpirationYear, OperatorLicenseNumber, RaceCode, SexCode] Any[State, ImageIndicator]. So a NAME-based licence search cannot carry a state and adding it to ZWAR.N any[] would OVER-PERMIT. | metadata-vs-devdoc | 2026-08-01
DriverHistoryQuery | ZDRV.N | Name | devdoc-optional-unreachable | Devdoc DriverHistoryQuery #1 is "BirthDate, Name" with no SexCode, but THIS providers metadata ZDRV{Name} = Set[BirthDate, Name, SexCode] Any[ImageIndicator] makes SexCode MANDATORY, and there is no looser name variant (the only sibling is ZDRV{OperatorLicenseNumber}). The built name combo matches ZDRV{Name} exactly. METADATA IS FIELD-AUTHORITY, devdoc is QUERY-authority (KB rule B4), so SexCode stays in set[]. Building a Name+DOB-without-Sex combination would emit a request NO metadata variant accepts -- it would MANUFACTURE a wire-invalid request. Verified against the raw XML Requirements 2026-08-01. | metadata-vs-devdoc | 2026-08-01
