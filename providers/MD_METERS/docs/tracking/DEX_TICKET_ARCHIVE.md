# MD_METERS — DEX-987 pre-edit comment archive

**Why this file exists.** `JIRA_COMMENT_TEMPLATE.txt`: *"there is no delete-comment tool ... any edit
is IRREVERSIBLE — capture the original body before overwriting it."* Under the old
one-comment-per-release rule the SAME comment id was rewritten each release, so without a capture the
previous release statement was gone for good. Pattern established on NJ_NJCJIS 2026-08-14.

Newest capture first. Each entry is the VERBATIM body as it stood immediately before an edit.

---

## NOTHING HAS EVER BEEN OVERWRITTEN ON DEX-987. There is nothing to archive, and that is a POSITIVE statement.

DEX-987 carries exactly **one** comment — `808820`, the v2.4 release line, posted **2026-08-31** — and it has
**never been edited**. So no prior body was destroyed and none is missing from this file.

**This file exists precisely to say that.** An ABSENT archive file is ambiguous: it reads identically
whether nothing was ever overwritten or whether something was overwritten and not captured. Those are
opposite facts, and `FL_FCIC`'s archive documents the second case (comment `790815` edited in place
from v7.18 to v7.23 before the archive practice existed, body unrecoverable). Leaving this file out
would have made MD_METERS indistinguishable from that. Created 2026-08-31 after
`audit_provider_uniformity` reported `DEX_TICKET_ARCHIVE.md present on 11/13, MISSING on: MD_METERS,
OR_LEDS` — the only unexplained divergence across 13 tenant-complete providers x 66 artifact tokens.

**Why it was not created earlier, and why it could be now.** It was deliberately deferred while Jira
was HELD: creating one alone could not clear that FAIL, because the other of the two was the missing
half. Both have now posted (OR_LEDS comment 807713 on 2026-08-28, MD_METERS comment 808820 on 2026-08-31), so the deferral reason expired.

**The rule going forward, from the 2026-08-17 reversal** (*"post as a new comment and leave the other
comments there"*): a new release gets a NEW comment naming `808820` as the one it supersedes and saying
`808820` is RETAINED AS HISTORY. **Do not edit `808820`.** If it ever is edited, capture its full body
here FIRST — the edit is irreversible and there is no delete-comment tool.
