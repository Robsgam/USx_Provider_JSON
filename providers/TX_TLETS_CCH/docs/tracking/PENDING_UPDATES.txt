# PENDING_UPDATES.txt -- TX_TLETS_CCH
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# A flag is NOT cleared by building. Retire it explicitly once the fix is in the JSON:
#   tools\flag_pending_fix.ps1 -Retire -FixId <id> -Providers <P> -Description '<what was done>'
#
# RETIRED 2026-08-21 -- UN-CLEARABLE BY CONSTRUCTION, so it was noise, not signal. This flag could
# only ever be cleared by a tenant sweep, and testing for this provider is now PARKED by operator
# directive (docs/tracking/TEST_PARKED.txt: proof of concept for the base<->variant parallel build,
# no tenant need yet). ENGINEERING_STANDARD LAW 2b: a FAIL that cannot be cleared by fixing the
# thing it points at is noise. Left as a comment, not deleted, because the wiring it describes IS
# still true and un-swept -- if the park is ever lifted, un-comment this line and it blocks again.
# [FLAG:nameparts-untested-unfrozen] Test-scaffolding change, NOT a JSON change: _combo_value_resolver now supplies nameMiddle=A / nameSuffix=JR, and emit_test_plan no longer suppresses the trust warning for those four fieldIds. Regenerate this provider PLAN at its next rebuild so middle/suffix are finally exercised. WHY IT MATTERS: metadata declares Name as type=Name with FOUR components (First/Last/Middle/Suffix), so middle and suffix are metadata-defined CAPABILITY, and no test on any provider had ever filled them -- all 538 logs read DOE, JOHN. Not regenerated here on purpose (rule 8c): this provider is tenant-verified and a bigger plan would drop it to PARTIAL. AZ_AZDPS is running the discriminating test first because it has NO Foundation and NO LIVE tenant. AWAIT THAT RESULT BEFORE ACTING: DOE, JOHN A JR means the handler emits all four and HI_HCJDC_OFML in particular is under-wired (its name parts are in ZERO combination any[], so they can never reach the composite); DOE, JOHN means the handler drops components 3-4 and this becomes a platform LIMITATION to raise, not a wiring job. (flagged 2026-08-17)
# RETIRED 2026-08-27 -- retired at the TX_TLETS_CCH v1.18 rebuild -- the plan was regenerated under the dedupe filters in that build
# [FLAG:plan-dedupe-vacuous-tests] emit_test_plan.ps1 gained two plan-only filters 2026-08-21: (1) DUPLICATE -- drop a test whose (entity, fill-set) is byte-identical to an earlier one, which makes the generator agree with audit_log_inflation attack A by construction; (2) NO-CONTEST GUARDRAIL -- drop a guardrail whose fill adds no competing identifier over the winner's own combo test, because the pair differs only by a presence condition and the loser can never match. YOUR PLAN WILL SHRINK when you regenerate; measured non-destructively on 2026-08-21: AZ -5, CA_CLETS -11, CA_SLO -1, CA_VENTURA -2, FL -14, HI -1, IL -1, LA -1, MD -3, NJ -2, TN -1, TX_TLETS_CCH -4. NO COVERAGE IS LOST -- identical fill means identical wire, and NY held 16/16 combos at 100% across the change. NOT DONE FOR YOU ON PURPOSE: if you are tenant-verified, regenerating now makes your existing logs ORPHAN (more logs than plan tests) and drops you out of ALL-PASS, which is why this is a flag and not a sweep. Do it at your OWN next rebuild, and archive the superseded duplicate logs in the same pass (NY put them in logs/<Entity>/_archive_dedupe_v<ver>/). Reference: NY_NYSPIN_EJUSTICE v4.26, 73 -> 65 tests, inflation A 8 -> 0, four log gates 65/65. (flagged 2026-08-21)
# [FLAG:plan-dedupe-prefill-normalise]  CLEARED 2026-09-01 AS NOT-APPLICABLE -- NOT "done", and the
#   distinction matters. Rob: "clear but leave it as no current plan to test  it was a build proof of
#   concept."
#   THE PLAN WAS NOT REGENERATED, DELIBERATELY. Regenerating would take it 181 -> 178 and leave this
#   provider looking like it holds a current, drive-ready test plan. It does not: TESTING IS PARKED
#   (docs/tracking/TEST_PARKED.txt, Rob 2026-08-21) because TX_TLETS_CCH exists as a BUILD PROOF OF
#   CONCEPT for parallel base+variant construction -- there is no tenant need and no sweep planned.
#   Measured state at the moment of clearing: 0 current-version logs, 181 plan tests on disk,
#   TEST_PARKED marker present.
#   ⚠️ IF THE PARK IS EVER LIFTED, REGENERATE THE PLAN BEFORE THE FIRST SWEEP. The plan on disk is
#   STALE by 3 tests against the current generator (measured in isolation across all 20 on
#   2026-08-31: 181 -> 178; the only other movers were HI 49->48, CA_SAN_LUIS_OBISPO 59->58,
#   CA_VENTURA_COUNTY 95->94, all three since picked up). Sweeping on the stale plan would drive 3
#   byte-identical duplicate requests and produce clone-group logs.
#   THAT RISK IS NOW GATED RATHER THAN LEFT TO MEMORY, which is why clearing this is safe: a sweep
#   here would land the provider in ALL-PASS, and audit_test_coverage -Gate would then evaluate it --
#   with audit_log_inflation attack A reporting the clone groups. So the failure mode announces
#   itself instead of depending on someone reading this comment.
#   WHY NOT-APPLICABLE RATHER THAN DONE: the flag's substance is "regenerate your plan before you
#   sweep". This provider is not going to sweep. Recording it as DONE would assert work that was not
#   performed; leaving it BLOCKING would hold enforce hostage to a task nobody intends to do, which
#   LAW 2b calls noise. Base<->variant LOCKSTEP IS NOT PARKED and is unaffected (audit_variant_sync).
#   (original flag text retained below for the record)
# [FLAG:plan-dedupe-prefill-normalise] emit_test_plan now normalises out a fill that merely RESTATES the form default before the byte-identical-fill dedupe: the form ships that value whether or not the driver types it, so two tests differing ONLY by such a fill submit identical state and emit identical wire. Found on HI_HCJDC_OFML v4.20, where M55L_guardrail_vs_M55S (2 fills) and M55L_guardrail_vs_RQV (same 2 plus vehicleTypeCode=1, which IS HI's Vehicle form default) survived as separate tests and left audit_log_inflation attack A at 1 clone group after the plan-dedupe pickup cleared the other. A fill carrying a DIFFERENT value than the default is a real distinction and is KEPT (a toggle test must differ from its default). YOUR PLAN WILL SHRINK when you regenerate; measured in isolation across all 20 on 2026-08-31 (before-tool vs after-tool, both to scratch): only 4 move -- HI 49->48, CA_SAN_LUIS_OBISPO 59->58, CA_VENTURA_COUNTY 95->94, TX_TLETS_CCH 181->178; the other 16 are byte-identical. NO COVERAGE IS LOST: identical submitted state means identical wire. NOT DONE FOR YOU because it is out of the in-scope provider (rule 8c) -- but note you are NEVER-TESTED at your current version, so you have NO logs to orphan and the pickup is free: just regenerate the plan before your first sweep. If you sweep on the stale plan instead, you will produce 1-3 byte-identical log pairs and audit_log_inflation attack A will report them. Reference: HI_HCJDC_OFML 49->48, clone groups 1 -> 0, four log gates 48/48. (flagged 2026-08-31)
