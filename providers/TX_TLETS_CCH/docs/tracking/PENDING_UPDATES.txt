# PENDING_UPDATES.txt -- TX_TLETS_CCH
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
[FLAG:testplan-platformfed-presence] emit_test_plan.ps1: platform-fed hidden fields (dexStateUserId) and form-shipped initialValues now count as ALREADY-PRESENT when simulating which combo fires. Previously badge-gated combos were dropped as unrunnable and the plan predicted the looser sibling the tenant would not fire. Re-emitting this provider's plan changes its test set, so it is deferred to the provider's OWN next rebuild + re-sweep. (flagged 2026-08-05)
[FLAG:guardrail-formdefaults-namespace] emit_test_plan.ps1 guardrail path passed COMBO defaults (attribute-keyed: BadgeNumber, State) where Get-FiringKeyRef matches FORM fieldIds (dexStateUserId, RegistrationState) -- a silent no-op union, so guardrail expectedKeyRef could name the wrong combo. Fixed to pass the form defaults like the three structural call sites. This provider's plan gains 3 guardrail test(s); adopting changes its test set, so it is deferred to its OWN next rebuild + re-sweep. (flagged 2026-08-05)
