# SUPPRESSION-SCOPE: direction-aware
#   Opted in 2026-08-02 as part of the portfolio sweep Rob authorised after it was MEASURED rather
#   than assumed. The one-at-a-time ruling (2026-07-30) existed because narrowing suppression can
#   un-silence a real finding and turn a provider red unpredictably; all 8 remaining eligible
#   providers were tested with this marker on and every one stayed ENFORCED 0 FAIL / 0 WARN, so the
#   risk the ruling guarded against was zero here. Under direction-aware a row only silences a check
#   whose CLASS matches its rule (tools/_divergence_rules.ps1); 'other' rows silence nothing.
# CA_CLETS_OCATS ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1. Format: query | keyRef | field | rule | reason | source | date
# Matching is on the first three columns (query | keyRef | field), case-insensitive.
#
# CONTEXT (v2.0 galvanization, 2026-07-23): the OOS combos promote RegistrationState to set[]
# (metadata has it in any[]). This is intentional -- OOS routing requires State present, and the
# combo also carries a RegistrationState EXISTS condition so the OOS vs in-state pair is mutually
# exclusive (same class as CA_CLETS's purposeCode promoted-to-set divergences). State remaining in
# any[] would let the OOS combo fire without State and shadow the in-state combo.

VehicleRegistrationQuery | RQ.P | RegistrationState | promoted-to-set | OOS plate combo requires State; EXISTS-gated for in/out mutual exclusion | design-decision | 2026-07-23
VehicleRegistrationQuery | RQ.V | RegistrationState | promoted-to-set | OOS VIN combo requires State; EXISTS-gated | design-decision | 2026-07-23
DriverLicenseQuery | DQ.N | RegistrationState | promoted-to-set | OOS Name combo requires State; EXISTS-gated | design-decision | 2026-07-23
DriverLicenseQuery | DQ.O | RegistrationState | promoted-to-set | OOS OLN combo requires State; EXISTS-gated | design-decision | 2026-07-23
BoatQuery | BQ.H | RegistrationState | promoted-to-set | OOS Hull combo requires State; EXISTS-gated | design-decision | 2026-07-23
BoatQuery | BQ.R | RegistrationState | promoted-to-set | OOS Reg combo requires State; EXISTS-gated | design-decision | 2026-07-23

# ROWS RETIRED 2026-08-18 at v2.6 -- 4K and VC are now BUILT, so their dropped-combo rows had to go.
#   An unbuilt-class rule (dropped-combo/not-built/shadow) makes audit_requirement_fidelity SKIP that
#   keyRef's ENTIRE comparison, so leaving these rows in place after building the combos would have
#   silenced the very branches we just added -- coverage would fall while the finding count read 0,
#   which is indistinguishable from a clean run. Retired text kept here for the audit trail:
#   4K: "Metadata 4K = Set[CaRequestPurposeCode, LicensePlateNumber, LicensePlateTypeCode]. NO devdoc
#       Basic combination makes Plate + LicensePlateTypeCode mandatory."
#   VC: "Metadata VC = Set[CaRequestPurposeCode, Name, BusinessIndicator]. BusinessIndicator appears
#       NOWHERE in the devdoc Basic VehicleRegistration section."
#   BOTH REASONS WERE TRUE AND BOTH ARE NOW SUPERSEDED by Rob 2026-08-18: "not sure why you stopped
#   short of making all the queries combinations worked from the dev doc and metat data". The devdoc is
#   QUERY authority (VehicleRegistrationQuery IS Basic-supported) and the metadata is FIELD authority
#   (it defines both variants and their mandatory fields) -- so a variant of an authorized query gets
#   BUILT, not skipped, even where the devdoc's flat combination list does not enumerate it.
# WITHDRAWN 2026-09-01 WITH v2.9 -- VehicleRegistrationQuery | 4 | LicensePlateNumber | dead-combo-userid-automated | UNBUILDABLE BY DIRECTIVE at v2.9, and this row names an UNBUILT keyRef by design. Rob 2026-09-01: 'we need to hide and automate the user id field ... for veh 1 and 2 combos become combined.' Metadata 4{LicensePlateNumber} = Set[CaRequestPurposeCode, LicensePlateNumber] with an EMPTY <Any>, i.e. devdoc VehicleRegistrationQuery #1 '(In) LicensePlateNumber'. userId is now a hidden handler-populated feeder, so it is ALWAYS PRESENT and AWVEHQ's effective set collapses to [purpose, plate] -- EXACTLY this variant's set. An exact collision is the one case ordering cannot separate (BUILD_RULES 24; AZ_AZDPS DQPN/DQP), so building both would leave one combo that counts toward coverage, can carry logs and can never fire. THE PATH IS NOT LOST, IT IS MERGED: AWVEHQ implements devdoc #2 '(In) LicensePlateNumber, UserId, [Authorization, ExactSearchIndicator, LicensePlateStateCode, PageNumber, VehicleMakeCode, VehicleYear]', and its metadata <Any> is a strict SUPERSET of this variant's (which defines nothing) -- so every field an officer could send on #1 is still sendable, and six more besides. Verified on the emitted v2.9 via _sim_helpers Get-FiringKeyRef: a plate-only fill routes to AWVEHQ. audit_query_trace reports this as PREFILL-DEAD (recoverable) and it is correct that un-prefilling would recover it -- recovering it is what the directive forbids, which is why this is recorded rather than fixed. | metadata-vs-devdoc | 2026-09-01
# WITHDRAWN 2026-09-01 WITH v2.9 -- VehicleRegistrationQuery | QV | LicensePlateNumber | dead-combo-routing-impossible | UNBUILDABLE, and NOT newly so -- this row makes an existing structural fact explicit at v2.9. Metadata QV{LicensePlateNumber} = Set[CaRequestPurposeCode, LicensePlateNumber] Any[LicensePlateStateCode]. Its required set is IDENTICAL to 4{LicensePlateNumber} and, once userId is automated, to AWVEHQ's effective set as well. Routing tests field PRESENCE against set[] plus conditions, and optionals cannot discriminate, so NO FILL CAN DISTINGUISH QV FROM ITS SIBLINGS -- whichever is ordered first takes everything. That is a routing IMPOSSIBILITY (usx-build 6a: impossible = no configuration satisfies it), not a preference or an omission. Nothing an officer could send is lost: AWVEHQ's <Any> includes LicensePlateStateCode, the only optional QV adds. DELIBERATELY NOT justified by citing FL_FCIC's registry, which calls QV a platform auto-send 'platform-confirmed' -- that claim is not in the knowledge base, so on this provider it would be a thing to verify, not a fact to cite. | metadata-vs-devdoc | 2026-09-01

# ---------------------------------------------------------------------------------------------
# THE TWO ROWS ABOVE MARKED "WITHDRAWN 2026-09-01 WITH v2.9" ARE COMMENTED OUT, NOT DELETED.
#   They were added the same day for v2.9, which hid and handler-automated the query-level `userId`
#   and merged devdoc Veh #1 into #2. v2.10 withdrew all of that and restored the v2.8 shape
#   (Rob: "revert to v2.8 shape"), so '4' IS BUILT AGAIN.
#   WHY COMMENTING OUT WAS MANDATORY, not tidiness: an existence-class rule (dead-combo/not-built/
#   shadow/unbuilt/dropped-combo) makes audit_requirement_fidelity SKIP THAT KEYREF'S ENTIRE
#   COMPARISON. Leaving a `dead-combo` row naming the now-BUILT '4' would have silenced a live branch
#   while the finding count still read 0 -- indistinguishable from a clean run, and exactly the
#   over-suppression the skill warns about (CA's IA.QV row dropped coverage 27 -> 26 that way).
#   Measured across the withdrawal: fidelity branches unchanged, so nothing was silenced either way.
#   THE REASONING IS KEPT because it is still the correct analysis of what would happen IF the
#   query-level userId were ever automated -- and the QV row in particular records a structural fact
#   (its required set is identical to its siblings' and optionals cannot discriminate) that is true
#   at v2.10 as well. Re-enable both only alongside re-applying v2.9 (git 875077f4).
