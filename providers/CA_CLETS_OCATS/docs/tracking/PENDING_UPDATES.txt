# PENDING_UPDATES.txt -- CA_CLETS_OCATS
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
# RETIRED 2026-08-14 -- This provider builds NO ImageIndicator control at all -- verified from the emitted JSON (absent from every QUERYINPUTFORM and every QIDM). The flag is NOT-APPLICABLE, not deferred. This is the SAME ruling already applied to CA_CLETS on 2026-08-13; it simply was never propagated to its five siblings. Adding controls to satisfy the rule would OVER-PERMIT -- the identical adjudication that scoped HI_HCJDC_OFML v4.17 to Vehicle-only and excluded Article on IL_LEADS_OFML v2.4. Zero JSON change, zero version bump, zero test package archived.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
[FLAG:fidelity-sibling-optional-leak] audit_requirement_fidelity keyed its metadata <Any> optional pool on transaction|keyRef instead of the full transaction|keyRef|primaryFieldReference triple, so EVERY alternative of a keyRef inherited its SIBLING alternatives' optionals and an over-permit against the narrower variant read as legal. Fixed 2026-09-02 (a keyRef is NOT a variant; _probe.ps1's Get-ProbeMetadataOptionals already keyed on the triple). THE FIX SURFACES REAL LIVE FINDINGS ON YOUR PROVIDER -- verified per-variant against the raw metadata <Requirements>, not inferred: a PLATE combo carries VehicleMakeCode and/or VehicleYear in its any[] while its OWN metadata variant does not define them (they belong to the VIN sibling). CA_CLETS_OCATS: RQ.P over-permits VehicleMakeCode+VehicleYear (metadata RQ{LicensePlateNumber} <Any> = [State, LicensePlateTypeCode]). CA_eSUN: RQ.P AND QV.P both over-permit the same two (metadata RQ{LicensePlateNumber} <Any> = [State]; QV{LicensePlateNumber} <Any> = EMPTY). FIX = remove those fields from the PLATE combo's any[] only; the VIN siblings legitimately keep them. Reference implementation: CA_SAN_LUIS_OBISPO v2.8, which removed any[VehicleMakeCode, vehicleYear] from RQ.P (its RQ{Plate} declares no <Any> at all) and whose devdoc out-of-state plate entry carries no brackets. NOT DONE FOR YOU (usx-tooling rule 8c): a shared-tool change must never trigger another provider's rebuild. CA_CLETS_OCATS is TENANT-VERIFIED, so this is a wire change that archives its 65-log package -- weigh that and take it at your own next rebuild. Portfolio measure of the tool fix: 420 branches BEFORE and AFTER (no coverage lost), UNDER 4 -> 4, OVER 3 -> 11, 6-provider regression fixture unmoved at 116 branches / 0 UNDER / 0 OVER. (flagged 2026-09-02)
