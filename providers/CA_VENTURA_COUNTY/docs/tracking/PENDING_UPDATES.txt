# PENDING_UPDATES.txt -- CA_VENTURA_COUNTY
# Lines without a leading '#' block enforce.ps1 and prevent testing.
#
# CLEARED 2026-08-01 at v2.2. Both flags resolved against THIS PROVIDER'S OWN devdoc + metadata.
# Kept as a record of what closed and why -- a bare empty file loses the reasoning.
#
# [FLAG:GUN-NAME-CHOICE-IN-SET]  RESOLVED at v2.1 -- REAL, and the worst case of its class here:
#   IG.QGH was set[caRequestPurposeCode,NameLast,NameFirst] with an EMPTY any[], so it carried NO
#   discriminator at all -- it could neither satisfy the mandatory Choice nor even transmit
#   Age/BirthDate as optionals. This provider's two authorities AGREE:
#     devdoc GunQuery  #2 "(in/Out) Name, Age"   #3 "(In/Out) Name, BirthDate"  -- UNBRACKETED, so
#                      both qualifiers are MANDATORY; the field row confirms it (M/C/O = C for each)
#     metadata         IG.QGH{Name} puts Choice[Age|BirthDate] INSIDE <Set>
#   FIX: split into IG.QGH.A (age) + IG.QGH.B (BirthDate) in devdoc listing order, with an Age+DOB row
#   added to the Firearm NAME card and NO initialValue on either (both are set[] routing fields --
#   BUILD_RULES 24). audit_defect_classes C1 went 1 -> 0 here, and this was the ONLY C1 candidate
#   left in the entire portfolio.
#
# [FLAG:METADATA-DEMOTION-ADJUDICATE]  RESOLVED at v2.2 -- demotions adjudicated, plus the three
#   devdoc combinations that were UNBUILT entirely:
#   BUILT  DriverLicenseQuery IR.QVC, read from the raw XML <Requirements> per <Combination> (the
#          sanctioned exception -- METADATA_REFERENCE.txt flattens Choice branches). Four variants:
#            {Name} Set[purpose, Set[Name,SexCode], Choice[BirthDate|Age], Any[County,Height,Race]]
#            {OLN} / {CriminalIdNumber} / {SocialSecurityNumber} = purpose + identifier, other two opt
#          -> IR.QVC.NB + IR.QVC.NA (Choice split; set[] has no OR), IR.QVC.C, IR.QVC.S, and ID.L1's
#          any[] widened so an OLN search TRANSMITS CII/SSN instead of silently dropping them.
#          Closes devdoc DriverLicenseQuery #3/#4/#5/#7; audit_devdoc_combinations now PASSes.
#          Field sizes taken from this XML's own <Field maxLength>: Age 2, CriminalIdNumber 11,
#          SocialSecurityNumber 9, AddressCounty 3 (a county CODE, not a name), Height 3, RaceCode 1.
#          Guessing two of them produced server-reject WARNs before they were read properly.
#   REG    IR.QVC.NB | BirthDate | promoted-to-set. audit_metadata flags it only because it flattens
#          a Choice-in-Set into any[]. THIS provider's {Name} has EXACTLY ONE variant, with the Choice
#          inside <Set>, so promotion is MANDATED and demotion would emit a request satisfying NO
#          variant. Do NOT generalise from any other provider: the same combo name elsewhere can have
#          looser variants and therefore the OPPOSITE correct answer.
#   REG    BoatQuery #4 | BirthDate | devdoc-optional-unreachable. Devdoc reads "(Out) BirthDate,
#          Name, [State]" -- State BRACKETED -- but metadata NLTS.BQ{Name} is
#          Set[purpose, Name, BirthDate, State] with State INSIDE <Set>, and there is no looser
#          variant. Metadata is FIELD-authority, devdoc is QUERY-authority (KB rule B4), so State
#          stays mandatory and the built NLTS.BQ.N matches metadata exactly. Building to the devdoc
#          bracket would require purpose+Name+DOB only, which no metadata variant accepts -- it would
#          MANUFACTURE the wire-invalid class this same rebuild just eliminated.
#   Also closed here: 21 of 27 combos had no purposeCode combo default AND the form control had no
#   initialValue, so a devdoc-faithful fill fired NOTHING (audit_devdoc_optionals 8 FAIL -> 0).
#   Prefilling it is LOAD-BEARING, not a BUILD_RULES 24 violation: it is mandatory in EVERY
#   combination, so it cannot shadow one path over another. Applied as a build-time normalization
#   loop (one rule, 21 sites) rather than 21 hand edits -- not a post-build JSON patch.
#
# ADJUDICATED ADVISORY -- SHADOW x12, deliberately NOT registered. Recorded here instead.
#   audit_query_trace reports 12 SHADOW combinations: this provider's metadata declares TWELVE
#   VehicleRegistrationQuery combinations with the IDENTICAL requirement set
#   [CaRequestPurposeCode, LicensePlateNumber, LicensePlateTypeCode] --
#   IV.4I / 4C / 4M / 4L / 4T / 4F / 4S / 4E / 4H / 4P / 4K and IL.A1.
#   They are INDISTINGUISHABLE FROM THE FORM: an officer supplies the same three fields for all
#   twelve, so at most one could ever fire. The server routes by the LicensePlateTypeCode VALUE, not
#   by keyRef. This provider's OWN DEVDOC collapses them into ONE combination --
#   VehicleRegistrationQuery #2 "(In) LicensePlateNumber, LicensePlateTypeCode" -- which the built
#   IA.QV implements (plate in set[], LicensePlateTypeCode in any[] with a PC default). Building them
#   separately would add eleven permanently-unreachable paths that still count toward coverage and
#   could carry a PASS log: the dead-combo class the reachability gate exists to prevent.
#   WHY NOT IN THE ACCEPTED-DIVERGENCE REGISTRY: I registered all twelve as dropped-combo and then
#   MEASURED the effect -- audit_requirement_fidelity went 45 branches -> 33 while UNDER stayed 1 and
#   OVER stayed 9, and audit_query_trace still reported SHADOW x12 (it does not registry-suppress
#   SHADOW, by design -- like MISSING it is a human-adjudication list). So the rows removed TWELVE
#   CLEAN COMPARISONS, changed no finding, and silenced nothing. A registration that costs coverage
#   and buys nothing is worse than no registration: it makes the branch count look like real
#   coverage loss and adds twelve rows that read as adjudicated suppression. REVERTED; the reasoning
#   lives here instead, where it is documentation rather than a silent -12 on the denominator.
#
# STILL OPEN (not blocking; recorded so it is not mistaken for finished):
#   Never USx-tenant-tested. v2.3 owes a full 5-entity sweep from T1.
#   audit_requirement_fidelity: 45 branches / 1 UNDER (RQ-family MessageKeyModifier, CLASS 3
#     mis-mapping) / 9 OVER (IA.QV year/make + IG.QGB sourceField alias -- see FIDELITY_TRIAGE).
#   build_phase1 random fuzz: 2 unaimed survivors to triage (swap-order @ BoatQuery[3];
#     over-permit @ DriverLicenseQuery[7] RegistrationState).
#   ArticleSingleQuery: metadata IP.QA is Set[purpose, <identifier>, Any[Brand,Category,TypeCode]],
#   so Brand/Category/TypeCode are OPTIONAL even though the devdoc prose reads Brand as mandatory.
#   Nothing to build -- noted because the devdoc wording invites a wrong "gap" every time it is read.
# RETIRED 2026-08-14 -- This provider builds NO ImageIndicator control at all -- verified from the emitted JSON (absent from every QUERYINPUTFORM and every QIDM). The flag is NOT-APPLICABLE, not deferred. This is the SAME ruling already applied to CA_CLETS on 2026-08-13; it simply was never propagated to its five siblings. Adding controls to satisfy the rule would OVER-PERMIT -- the identical adjudication that scoped HI_HCJDC_OFML v4.17 to Vehicle-only and excluded Article on IL_LEADS_OFML v2.4. Zero JSON change, zero version bump, zero test package archived.
# [FLAG:ncic-image-default-y-everywhere] NCIC Image (ImageIndicator) should default to 'Y' on EVERY entity, not just Person -- Rob 2026-08-12: 'ncic image should default to y everywhere'. Applied on FL_FCIC v7.21 (form initialValue AND the combo defaults[] CAD twin, since CAD ignores form initialValue). BEFORE APPLYING, MEASURE THIS PROVIDER: the flip is only safe where ImageIndicator is in NO combination's set[] and in no condition -- on AZ_AZDPS it is in 2 set[]s and on LA_LEMS in 1 set[] + 2 conditions, so a prefill there would hide combos (BUILD_RULES 24) and those two need Rob's ruling rather than a mechanical flip. Also clear the TX_TLETS T6 gate: confirm the devdoc has no 'must be filled if ImageIndicator = Y' conditional. Portfolio baseline measured 2026-08-12: 20/20 providers used Person='Y' and Vehicle/Firearm/Article/Boat='N', so FL is currently the only conformant-to-Rob provider and the rest are owed. (flagged 2026-08-12)
# RETIRED 2026-08-28 -- retired at the CA_VENTURA_COUNTY v2.5 rebuild 2026-08-28 -- plan regenerated under the dedupe filters (97 -> 95, the predicted -2); zero logs on disk so nothing was orphaned
# [FLAG:plan-dedupe-vacuous-tests] emit_test_plan.ps1 gained two plan-only filters 2026-08-21: (1) DUPLICATE -- drop a test whose (entity, fill-set) is byte-identical to an earlier one, which makes the generator agree with audit_log_inflation attack A by construction; (2) NO-CONTEST GUARDRAIL -- drop a guardrail whose fill adds no competing identifier over the winner's own combo test, because the pair differs only by a presence condition and the loser can never match. YOUR PLAN WILL SHRINK when you regenerate; measured non-destructively on 2026-08-21: AZ -5, CA_CLETS -11, CA_SLO -1, CA_VENTURA -2, FL -14, HI -1, IL -1, LA -1, MD -3, NJ -2, TN -1, TX_TLETS_CCH -4. NO COVERAGE IS LOST -- identical fill means identical wire, and NY held 16/16 combos at 100% across the change. NOT DONE FOR YOU ON PURPOSE: if you are tenant-verified, regenerating now makes your existing logs ORPHAN (more logs than plan tests) and drops you out of ALL-PASS, which is why this is a flag and not a sweep. Do it at your OWN next rebuild, and archive the superseded duplicate logs in the same pass (NY put them in logs/<Entity>/_archive_dedupe_v<ver>/). Reference: NY_NYSPIN_EJUSTICE v4.26, 73 -> 65 tests, inflation A 8 -> 0, four log gates 65/65. (flagged 2026-08-21)
# [FLAG:plan-dedupe-prefill-normalise]  TAKEN AND DONE 2026-09-01 -- not deferred, not waived.
#   Plan regenerated at the CURRENT version: 95 -> 94 tests. NOTHING ELSE CHANGED -- no JSON,
#   no version, no combination, no log. FREE BY CONSTRUCTION and that was VERIFIED not assumed: this
#   provider had 0 current-version logs, so there was nothing to orphan and no re-sweep to owe. The
#   script aborts if any current-version log exists, because the tenant-verified case needs the plan
#   regen PLUS archiving the now-duplicate logs (see FL_FCIC / HI / IL / NJ, 2026-08-31).
#   THE DELTA MATCHES THE PRE-MEASURED PREDICTION EXACTLY. When the tool change was made it was
#   measured in isolation across all 20 (before-tool vs after-tool, both to scratch) and predicted
#   only 4 movers: HI 49->48, CA_SAN_LUIS_OBISPO 59->58, CA_VENTURA_COUNTY 95->94, TX_TLETS_CCH
#   181->178. This pickup landed on exactly 95 -> 94. A prediction that holds is what makes
#   the measurement trustworthy rather than decorative.
#   WHAT THE FIX WAS: emit_test_plan now normalises out a fill that merely RESTATES the entity's form
#   default before the byte-identical-fill dedupe. The form ships that value whether or not the driver
#   types it, so two tests differing ONLY by such a fill submit identical state and emit identical
#   wire. A fill carrying a DIFFERENT value than the default is KEPT -- a toggle test is required to
#   differ from its default, so normalising on fieldId alone would have destroyed the tests that
#   matter. Found on HI_HCJDC_OFML, where vehicleTypeCode=1 IS the Vehicle form default.
#   NO COVERAGE LOST: identical submitted state means identical wire.
#   (original flag text retained below for the record)
# [FLAG:plan-dedupe-prefill-normalise] emit_test_plan now normalises out a fill that merely RESTATES the form default before the byte-identical-fill dedupe: the form ships that value whether or not the driver types it, so two tests differing ONLY by such a fill submit identical state and emit identical wire. Found on HI_HCJDC_OFML v4.20, where M55L_guardrail_vs_M55S (2 fills) and M55L_guardrail_vs_RQV (same 2 plus vehicleTypeCode=1, which IS HI's Vehicle form default) survived as separate tests and left audit_log_inflation attack A at 1 clone group after the plan-dedupe pickup cleared the other. A fill carrying a DIFFERENT value than the default is a real distinction and is KEPT (a toggle test must differ from its default). YOUR PLAN WILL SHRINK when you regenerate; measured in isolation across all 20 on 2026-08-31 (before-tool vs after-tool, both to scratch): only 4 move -- HI 49->48, CA_SAN_LUIS_OBISPO 59->58, CA_VENTURA_COUNTY 95->94, TX_TLETS_CCH 181->178; the other 16 are byte-identical. NO COVERAGE IS LOST: identical submitted state means identical wire. NOT DONE FOR YOU because it is out of the in-scope provider (rule 8c) -- but note you are NEVER-TESTED at your current version, so you have NO logs to orphan and the pickup is free: just regenerate the plan before your first sweep. If you sweep on the stale plan instead, you will produce 1-3 byte-identical log pairs and audit_log_inflation attack A will report them. Reference: HI_HCJDC_OFML 49->48, clone groups 1 -> 0, four log gates 48/48. (flagged 2026-08-31)
