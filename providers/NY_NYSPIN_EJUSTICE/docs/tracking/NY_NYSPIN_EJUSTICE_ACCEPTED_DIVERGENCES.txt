# SUPPRESSION-SCOPE: direction-aware
# Each row silences ONLY checks whose defect class its RULE names (tools\_divergence_rules.ps1).
# Opted in at the v4.18 rebuild, 2026-07-30 -- NY was at 6 over-broad suppressions.
# NY_NYSPIN_EJUSTICE ACCEPTED METADATA DIVERGENCES
# Format: query | keyRef | field | rule | reason | source | date
#

DriverHistoryQuery | DALHOUT | purposeCodeDH | prefilled-mandatory-autopopulated | AUDITED AND ACCEPTED 2026-07-30 (TX-derived attack pass applied to NY). purposeCodeDH='C' and requestorDH='X' are form-PREFILLED and ARE in the set[] of DALHOUT and DALLOUT, which is the BUILD_RULES 24 shape. It is NOT a violation, and here is the proof rather than an assertion: the discriminator between the out-of-state pair (DALHOUT/DALLOUT, RegistrationStateDH EXISTS) and the in-state pair (DALH/DALL, RegistrationStateDH NOT_EXISTS) is RegistrationStateDH, which is correctly NOT prefilled -- so no combination is hidden. audit_query_trace reports 0 PREFILL-DEAD and 0 SHADOW, and audit_gate_efficacy mutation 'ny-prefill-discriminator' CONFIRMS the gate would catch it: prefilling RegistrationStateDH immediately yields 'DEAD COMBO: DriverHistoryQuery/DALH'. Both fields are mandatory on the Nlets out-of-state DH path and are auto-populated, the same pattern as TX_TLETS' Attention/emailAddress gate-feeders. Recorded because TX has its equivalents written down and NY did not -- a defensible pattern with no written adjudication is the 'unrecorded but fine' class this session kept finding. | metadata-vs-devdoc | 2026-07-30
DriverHistoryQuery | DALLOUT | requestorDH | prefilled-mandatory-autopopulated | Same adjudication as the purposeCodeDH entry above -- see it for the full reasoning and the mutation evidence. | metadata-vs-devdoc | 2026-07-30
DriverLicenseQuery | DLICN | State | devdoc-models-oos-metadata-does-not | enforce PHASE 2p NOTE: devdoc DriverLicenseQuery #3 models a SEPARATE out-of-state path with State MANDATORY, but metadata carries State in Any[] for this combination -- audit_requirement_fidelity reports ZERO DriverLicenseQuery divergence, confirming the built any[] matches metadata. Metadata is FIELD-authority and devdoc is QUERY-authority (skill B4), so the built form is correct and no split is required. Contrast VehicleRegistrationQuery #3 where devdoc AND metadata BOTH make PlateTypeCode/PlateYear/State mandatory -- that one IS a defect and is fixed at v4.18. Adjudicated 2026-07-30 after dumping both DL combos against devdoc L79; recorded so this NOTE is not re-litigated every pass. | metadata-vs-devdoc | 2026-07-30
DriverLicenseQuery | DLIC | State | devdoc-models-oos-metadata-does-not | enforce PHASE 2p NOTE: devdoc DriverLicenseQuery #4 models a SEPARATE out-of-state path with State MANDATORY, but metadata carries State in Any[] for this combination -- audit_requirement_fidelity reports ZERO DriverLicenseQuery divergence, confirming the built any[] matches metadata. Metadata is FIELD-authority and devdoc is QUERY-authority (skill B4), so the built form is correct and no split is required. Contrast VehicleRegistrationQuery #3 where devdoc AND metadata BOTH make PlateTypeCode/PlateYear/State mandatory -- that one IS a defect and is fixed at v4.18. Adjudicated 2026-07-30 after dumping both DL combos against devdoc L79; recorded so this NOTE is not re-litigated every pass. | metadata-vs-devdoc | 2026-07-30
DriverHistoryQuery | DALHOUT | Requestor | demoted-to-any | Metadata's DALH/DALHOUT Choice branch (raw XML <Requirements><Set><Choice>, branch 2) places Requestor in the mandatory Set. Built in any[] instead: a hidden field with no initialValue submits no key at all, and set[] membership makes the CLIENT gate Send on the field having a value -- live-caught 2026-08-06, DALHOUT's Send button stayed permanently disabled. any[] avoids that client-side gate; the wire outcome is unchanged because CommsysGetLastNameFirstNameInitialRuleHandler populates Requestor unconditionally regardless of set[]/any[] placement (proven server-side on TX_TLETS v4.19/FL_FCIC v7.18/CA_CLETS v2.24). Routing unaffected: DALHOUT vs DALH is decided by RegistrationStateDH EXISTS/NOT_EXISTS, not Requestor. | metadata-vs-devdoc | 2026-08-06
DriverHistoryQuery | DALLOUT | Requestor | demoted-to-any | Same as DALHOUT (see that row) -- metadata's DALL/DALLOUT Choice branch 2 places Requestor in the mandatory Set; built in any[] because a hidden field with no initialValue cannot satisfy the client-side Send gate that set[] membership imposes. Wire outcome unchanged; routing decided by RegistrationStateDH, not Requestor. | metadata-vs-devdoc | 2026-08-06
