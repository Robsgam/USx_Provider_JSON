# PENDING_UPDATES.txt -- NY_NYSPIN_EJUSTICE
# Lines without a leading '#' block enforce.ps1 and prevent testing.
# Clear by running the build script (it removes this file on successful build).
#
[FLAG:NY-METADATA-PARSER-UNKNOWN] audit_query_trace reports 3 NY combos (DALL x2, RVEH) with an EMPTY set[]. Unresolved whether NY metadata genuinely omits Requirements for those combos or the parser misses NY XML shape. NY audit verdict is NOT trustworthy until this is settled -- resolve as step 1 of NY work, before acting on any NY finding. (flagged 2026-07-30)
