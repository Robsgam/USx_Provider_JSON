# SUPPRESSION-SCOPE: direction-aware
#   Opted in 2026-08-01. Without this marker a registry row suppresses EVERY check for its
#   (query, keyRef, field) triple regardless of what the recorded decision actually said -- so a row
#   granting "this field may ride in any[]" also silences the OPPOSITE defect (a field wrongly
#   promoted INTO set[]), and the gate goes mute somewhere it was never granted.
#   audit_suppression_scope.ps1 measures the portfolio at 317 over-broad suppressions across 19
#   providers; this provider is opted in so its 2 rows only license their own defect class.
#   Narrowing is STRICTER and can turn a green provider red by un-silencing a real finding -- which
#   is the point. Verified after opting in: still ENFORCED 0 FAIL / 0 WARN.
# CA_SAN_LUIS_OBISPO ACCEPTED METADATA DIVERGENCES
# Read by tools/audit_metadata.ps1 (CHECK 4 / 4d). Each non-comment line records a
# set/any divergence that is intentional and correct, so the gate treats it as [NOTE] not [FAIL].
# Format (pipe-delimited): query | keyRef | field | rule | reason | source | date
#

DriverHistoryQuery | B2.N | Name | devdoc-optional-unreachable | Devdoc DriverHistoryQuery #1 is "Name, [BirthDate]" -- BirthDate BRACKETED, i.e. a Name-only driver-history search. THIS providers metadata permits no such thing: BOTH name variants require BirthDate in <Set>. B2{Name} = Set[BirthDate, Name] Any[Attention, PurposeCode, SexCode] and KQ{Name} = Set[Name, BirthDate, SexCode] Any[Attention, PurposeCode, State]. There is no looser variant. The built B2.N (set[BirthDateDH, NameLastDH, NameFirstDH] any[SexCodeDH]) matches B2{Name} exactly. METADATA IS FIELD-AUTHORITY, devdoc is QUERY-authority (KB rule B4), so BirthDate stays mandatory. Building to the devdoc bracket would require a Name-only DH combination that NO metadata variant accepts -- it would MANUFACTURE a wire-invalid request. Verified against the raw XML Requirements 2026-08-01. | metadata-vs-devdoc | 2026-08-01
GunQuery | QGB | firearmMake | extra-field-in-json | TOOL ALIASING ARTEFACT, not an over-permit. Metadata GunQuery QGB is Set[GunSerialNumber] Any[GunCaliber, GunMake, GunTypeCode] -- GunMake IS permitted. The build carries it as sourceField firearmMake with targetField GunMake (the CAD-aligned camelCase source name; the wire contract is the targetField). audit_requirement_fidelity compares the SOURCEFIELD name against the metadata reference and has no alias map for firearmMake -> GunMake, so it reads a permitted field as extra. Nothing to fix in the build: renaming the sourceField would break CAD field alignment, and dropping it would remove a metadata-permitted refinement. Verified against the raw XML Requirements 2026-08-01. | metadata-vs-devdoc | 2026-08-01
