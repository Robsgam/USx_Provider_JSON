CONNECTCIC BUILD RULES -- PROVIDER JSON STRUCTURE
====================================================
How to structure a provider JSON from scratch.
Last updated: 2026-06-09
Source: NJ_NJCJIS (v2.3), NY_NYSPIN_EJUSTICE (v1.1), AZ_AZDPS (v2.0),
        FL_FCIC (v3.0), HI_HCJDC_OFML (v1.1), TX_TLETS (v2.1),
        LA_LEMS (v2.0), CA_CLETS (v1.5)

See also:
  RULE_HANDLERS.txt     -- Handler signatures, arguments, usage patterns
  FIELD_REFERENCE.txt   -- Field code-type pairings, attributeTypeId rules
  QIDM_REFERENCE.txt    -- QIDM combination rules, merge/split decisions
  IMPORT_ERRORS.txt     -- Import error messages and fixes

================================================================================
0. PROVIDER NAMING AND FOLDER SETUP
================================================================================

  NAMING RULE: Provider folder name MUST match the metadata XML filename
  (minus the .xml extension). This is the canonical name for the provider.

  Example:
    XML file:  CA_SAN_LUIS_OBISPO.xml  ->  folder: providers/CA_SAN_LUIS_OBISPO/
    XML file:  NM_NMLETS_OFML.xml      ->  folder: providers/NM_NMLETS_OFML/
    XML file:  MD_METERS.xml           ->  folder: providers/MD_METERS/

  WHY: Mismatched names require renaming the folder, all doc files, all
  references in CLAUDE.md, README.txt, new_test_log.ps1, and every internal
  doc file. This was learned when CA_SAN_LOUIS_OBISPO and NM_NMLETS had to
  be renamed to match their XML filenames (CA_SAN_LUIS_OBISPO and
  NM_NMLETS_OFML). Rename propagation touches 10+ files per provider.

  VERIFICATION: Before creating a provider folder, open the metadata XML and
  confirm the filename. Use that exact name (case-sensitive) for the folder.

  RENAME PROPAGATION CHECKLIST (if a rename is ever needed):
    [ ] git mv the provider folder
    [ ] Rename all docs: STATUS.txt, BUILD_NOTES.txt, SQVR.txt
    [ ] Update doc file contents (provider name references)
    [ ] Update JSON_INVENTORY.md
    [ ] Update CLAUDE.md provider table
    [ ] Update knowledge-base/README.txt provider list
    [ ] Update tools/new_test_log.ps1 knownPaths hash
    [ ] Update any build scripts referencing the old name
    [ ] Grep full repo for the old name to catch stragglers

  ONE JSON IN ROOT RULE:
    Exactly one JSON file in the provider root folder at all times.
    - Single-JSON model (repo-wide): one build script -> one versioned
      <PROVIDER>_v<X.Y>.json in root. No BASE/MC split -- it was merged away
      portfolio-wide (all 20 providers are galvanized single-JSON, PascalCase).
    - phases/ is retired: every prior version is recoverable from git history
      (git log / git show is authoritative), not a phases/ snapshot folder.
    - logs/ is the only test-log location (tests/ eliminated 2026-07-01).
    NEVER more than one JSON in root. Write-ProviderJson deletes stale siblings
    on build and enforce.ps1 FAILs on multiples.
    - Legacy holdout: the bare <PROVIDER>.json name is still accepted, and the
      JSON resolver keeps a legacy _MC/_BASE fallback for any un-galvanized
      provider (none remain today).

  SOURCE FOLDER SETUP (before any build):
    [ ] Copy metadata XML to source/<PROVIDER>.xml
    [ ] Copy devdoc PDF to source/<PROVIDER>.pdf
    [ ] Run extract_queries.ps1 on the XML to map query landscape:
        powershell.exe -ExecutionPolicy Bypass -File tools/extract_queries.ps1
          -XmlPath source/<PROVIDER>.xml -OutFile docs/<PROVIDER>_SQVR.txt

  DEVDOC TEXT EXTRACTION:
    [ ] Convert PDF to text for searchable reference:
        pdftotext "source/<PROVIDER>.pdf" "source/<PROVIDER>_DEVDOC.txt"
    [ ] Review "Basic Queries Supported" section -- this is the ONLY authority
        for WHICH queries to build. Metadata existence alone does NOT authorize
        building a query.

================================================================================
1. 3-BUNDLE STRUCTURE
================================================================================

  Every provider JSON contains exactly 3 bundles in this order:

  1. ENTITIES BUNDLE     name='ENTITIES', provider='MARK43'
     Contains all QUERYINPUTFORM configurations (entity input forms):
       - One QIF per entity sub-form (Vehicle-NY, Vehicle-OOS, Person-DL-NY, etc.)
       - Also contains the entity display order array.
     Rule: QUERYINPUTFORM belongs ONLY here. Never in the provider or RMS bundle.
       Adding a QIF to any other bundle causes duplicate entity form cards in the
       UI (LIMITATION #22).
     ENTITIES MUST BE FIRST. Confirmed AZ v2.0: forms do not render when ENTITIES
       is not the first bundle in the array.

  2. PROVIDER BUNDLE     name=[PROVIDER_NAME], provider=[PROVIDER_NAME]
     Contains all provider-specific configurations:
       - AUTHENTICATION (1)
       - QUERYRESULTDATAMAPPING (1)
       - QUERYMESSAGEFORMAT (1)
       - QUERYINPUTDATAMAPPING (1 per query/path -- most of the work)

  3. RMS BUNDLE          name='RMS', provider='RMS'
     Contains RMS-specific QIDMs for local record matching.
     Built from KB specs by tools/_build_rms_bundle.ps1.
     See Section 4 for RMS design decisions and per-provider flags.

  Top-level JSON structure:
    {
      "bundles": [ <entities_bundle>, <provider_bundle>, <rms_bundle> ]
    }

  Bundle-level properties (required on each bundle):
    configurations   array of config objects
    description      human-readable string (e.g., "Provider configuration for
                       AZ_AZDPS v1.0")
    name             bundle name
    type             'BUNDLE'   *** REQUIRED -- if omitted, platform silently
                       ignores the bundle. ***
                       Both the CommSys provider bundle AND the ENTITIES bundle
                       require this. Confirmed failure mode: AZ_AZDPS v2.1/v2.2
                       (forms did not render).
    provider         provider name (or 'MARK43' for ENTITIES, 'RMS' for RMS
                       bundle)

  BUNDLE ORDER: ENTITIES bundle must be first.
    Confirmed: AZ v2.0 -- forms do not render when ENTITIES is not first.
    Order: 1=ENTITIES (provider=MARK43), 2=PROVIDER, 3=RMS

  BUNDLE TYPE: Every bundle must have type='BUNDLE'.
    Confirmed: AZ v1.0. Missing type causes import rejection.

  LIMITATION #22: QUERYINPUTFORM in RMS bundle causes duplicate entity cards
    Confirmed: NJ, NY
    Rule: QUERYINPUTFORM belongs ONLY in the ENTITIES bundle (provider=MARK43).
    Adding it to any other bundle renders each form twice.

================================================================================
2. ENTITIES BUNDLE
================================================================================

  The ENTITIES bundle contains all QIFs and the entity display order.
  It is the FIRST bundle in the JSON.

  Bundle-level properties:
    name             'ENTITIES'
    type             'BUNDLE'
    provider         'MARK43'
    description      'Entity input form configurations'
    configurations   array of QUERYINPUTFORM objects
    order            entity display order object (see below)

  Each QUERYINPUTFORM targets one entity (Vehicle, Person, Boat, Firearm,
  Article). Multiple QIFs can target the same entity (e.g., Vehicle-NY and
  Vehicle-OOS). Platform groups all QIFs sharing a targetEntity into one
  tab/display group.

  Required QIF properties:
    type          'QUERYINPUTFORM'
    name          unique name (e.g., 'ENTITY_Vehicle_NY')
    label         display label (e.g., 'Vehicle - NY')
    targetEntity  entity name ('Vehicle','Person','Boat','Firearm','Article')
    provider      'MARK43'
    layout        object with default, CAD_DISPATCH, FIRST_RESPONDER variants

  Entity display order -- MUST be a nested object, NOT a flat array:
    WRONG:  "order": ["Vehicle","Person","Firearm","Article","Boat"]
    RIGHT:  "order": { "default": [...], "CAD_DISPATCH": [...],
                        "FIRST_RESPONDER": [...] }

    order: {
      default:          ['Person','Vehicle','Firearm','Article','Boat']
      CAD_DISPATCH:     ['Vehicle','Person','Firearm','Article','Boat']
      FIRST_RESPONDER:  ['Vehicle','Person','Firearm','Article','Boat']
    }

  Values MUST be targetEntity values. Not entity names, not config names,
  not entity labels. Confirmed: NJ, NY, AZ. Any other value type produces
  incorrect/alphabetical sort.
  Failure mode: flat array causes entity ordering failure; forms may not render.
    Confirmed: AZ_AZDPS v2.2 (2026-04-20). Fixed in v2.3 / v1.0 Phase 1.

  FIRST_RESPONDER layout key:
    Included in all three layout variants of each QIF.
    Whether FIRST_RESPONDER is actually recognized as a distinct rendering
    context by the platform is UNCONFIRMED as of 2026-04-14 (NY STATUS.txt).
    Include it in all builds (same as CAD_DISPATCH) -- functionally harmless.

================================================================================
3. PROVIDER BUNDLE CONFIGS
================================================================================

  The provider bundle contains four config types. Each has a required structure.
  Handler signatures and arguments are documented in RULE_HANDLERS.txt.

  ---- 3A. AUTHENTICATION CONFIG ----

  One per provider bundle. Handles login/credential authentication.

  Required properties:
    type                        'AUTHENTICATION'
    name                        [PROVIDER_NAME]
    description                 'Authentication configuration for [PROVIDER]'
    handlerFunction             'CommsysOriAuthenticationHandler'
    provider                    [PROVIDER_NAME]
    providerType                'Commsys'
    signInRequired              false
    deviceRegistrationOptional  false

  Standard attributes:
    ORI       size=12  sourceField=['ORI']          targetField='ORI'
    Mnemonic  size=25  sourceField=['mnemonic']     targetField='Mnemonic'
    UserName  rule=CommsysGetDexStateUserIdRuleHandler
              sourceField=['dexStateUserId']        targetField='UserName'

  Combination:
    requirements: set=['ORI','Mnemonic']  any=['dexStateUserId']

  ---- 3B. QUERYMESSAGEFORMAT CONFIG ----

  One per provider bundle. Defines the XML message wrapper structure.
  This config is IDENTICAL across providers -- copy as-is, change name and
  provider only.

  Required properties:
    type                  'QUERYMESSAGEFORMAT'
    name                  '[PROVIDER_NAME]_QueryMessageFormat'
    description           'Configuration for Query format'
    handlerFunction       'CommsysWsiOutgoingMessageHandler'
    provider              [PROVIDER_NAME]
    authenticationParent  'LawEnforcementTransaction'
    payloadParent         'LawEnforcementTransaction'

  No attributes or combinations. This config is structural only.

  ---- 3C. QUERYRESULTDATAMAPPING CONFIG ----

  One per provider bundle. Defines how provider XML responses are mapped to
  the ConnectCIC result display.

  Standard approach: built by Build-ProviderQrdm in _build_provider_helpers.ps1.
  The CommSys QRDM uses generic field mappings. Name and provider are set
  automatically from the -ProviderName parameter.

  When to validate: after first successful query returns data (not "No
  Returns"). Compare QRDM mapping field names against actual XML element
  names in the response. Update any mismatched field names. This is
  provider-specific work.

  ---- 3D. QUERYINPUTDATAMAPPING CONFIGS ----

  One per query/path. This is the bulk of the provider bundle work.
  Each QIDM uses handlerFunction='CommsysTransactionRequestHandler'.
  Full QIDM architecture, combination rules, and merge/split decisions
  are documented in QIDM_REFERENCE.txt.

================================================================================
4. RMS BUNDLE -- DESIGN DECISIONS
================================================================================

  The RMS bundle is built from KB specs by tools/_build_rms_bundle.ps1.
  No external template. The module encodes all design decisions below as
  code. Per-provider variations are controlled by flags (-KeepSsn, -SkipRace).

  DECISION 1 -- Vehicle: LicensePlateYear in licensePlateIn any[]
  -----------------------------------------------------------------------
    The standard RMS Vehicle QIDM includes LicensePlateYear in the
      licensePlateIn any[] combination. On instances without elastic mapping
      for this field, the combination may reject or misbehave.
    _build_rms_bundle.ps1 includes it by default.
    AZ_AZDPS: confirmed no elastic mapping — build script removes it.
    NJ, NY: LicensePlateYear kept (elastic mapping exists).

  DECISION 2 -- Vehicle: RegistrationState in licensePlateIn any[]
  ------------------------------------------------------------------
    The RMS Vehicle QIDM licensePlateIn combination includes
      RegistrationState in any[]. The hidden SelH 'RegistrationState'
      is populated and the RMS combination uses it for state filtering.
    _build_rms_bundle.ps1 includes it by default.
    Confirmed: NY v1.4, NJ v1.2, AZ v1.0.

  DECISION 3 -- Vehicle: licensePlateOutAndState combination
  ------------------------------------------------------------------
    The RMS Vehicle QIDM has a licensePlateOutAndState combination
      with set=['LicensePlateNumberOut','RegistrationStateOut']. When OOS
      plate uses licensePlateNumber instead of LicensePlateNumberOut, this
      combination can never fire.
    _build_rms_bundle.ps1 includes it by default. Providers that use
      single-field plate pattern remove it in their build scripts.
    Does NOT apply to: NJ (NJ uses LicensePlateNumberOut on OOS forms).
    Confirmed: NY v1.4.

  DECISION 4 -- Person: registrationStateAttrId (ALWAYS included)
  ----------------------------------------------------------------
    The RMS Person QIDM includes a registrationState attribute. Without
      this, person searches (OLN, Name) fire without a state filter in
      the RMS elastic query.
    _build_rms_bundle.ps1 always includes:
      name='registrationState', sourceField=['RegistrationState'],
      targetField='registrationStateAttrId', useAttributeId=true
      Add 'RegistrationState' to ALL person combination any[] arrays.
    IMPORTANT: registrationStateAttrId is a SINGULAR string (no
      ArrayWrapper). This differs from Vehicle which uses
      registrationStateAttrIds (array + ArrayWrapper).
    Applies to: all providers (always apply).
    Confirmed: NJ_NJCJIS v1.2 (2026-04-20) --
      registrationStateAttrId:"69509884952" (string).
    autoSelect=true: add to all RMS QIDMs so they fire automatically.
    Confirmed: AZ_AZDPS v1.0, NJ_NJCJIS v1.7, NY_NYSPIN_EJUSTICE v1.1.

  DECISION 5 -- Person: Sex attribute handling (INSTANCE-SPECIFIC)
  -----------------------------------------------------------------
    The RMS Person QIDM includes sex (sexAttrId) and sexOOS attributes
    with useAttributeId=true. Whether to keep or remove depends on the
    instance.

    KEEP (NIBRS reverse-lookup confirmed -- instance supports attr ID ->
      M/F/U):
      Form uses attributeTypeId=SEX + codeTypeProvider=NIBRS.
      Form stores numeric attr ID. CommSys QIDM codeTypeProvider=NIBRS
        converts to M/F/U.
      RMS useAttributeId=true passes attr ID as-is.
        sexAttrId:"69509891711" (string) CORRECT.
      DO NOT add AttributeArrayWrapperRuleHandler.
      DO NOT remove sex from RMS QIDM.
      CONFIRMED: NJ_NJCJIS, AZ_AZDPS, FL_FCIC (single-QIF only).
      NOTE: Prior "FL reverse-lookup broken" was wrong (2026-04-20). Actual
        failure was duplicate targetField from entity-split design. FL works
        with single Person QIF. See PROVIDER_CONSTRAINTS.txt FL-9.

    REMOVE (only when entity-split creates duplicate targetField):
      When duplicate Person QIFs cause two QIDM attrs with same targetField,
      reverse-lookup breaks. Form must use codeTypeCategory=NIBRS_SEX instead.
      useAttributeId=true + string "M" -> wraps in array ->
        sexAttrId:[69509891711] -> RMS 400 error.
      Fix: remove BOTH sex attributes from RMS QIDM attributes[] AND remove
        'SexCode' AND 'SexCodeOOS' from ALL combination any[]/set[] arrays.
      Pattern: filter by targetField='sexAttrId' (catches both 'sex' and
        'sexOOS' attributes).
      REQUIRED: NY_NYSPIN_EJUSTICE (uses entity-split design).
      UNCONFIRMED: any new instance -- test before assuming keep or remove.
      See PLATFORM_CONSTRAINTS.txt AP #18 for both-attr removal detail.

  DECISION 6 -- RMS CLEANUP: Remove unused fields (per-provider)
  ----------------------------------------------------------------
    The base RMS template in _build_rms_bundle.ps1 contains fields for all
    possible providers. Most providers only use a subset. Dead fields (no
    matching form fieldId) add JSON bloat and their combos never fire.
    Build scripts remove unused attrs/combos after calling Build-RmsBundle.

    Vehicle -- remove these attrs:
      LicensePlateNumberOut     (OOS dual-field plate pattern -- not used
                                  with NCIC single-field)
      RegistrationStateOut      (same)
      OwnerFirstName            (no owner search on form)
      OwnerLastName             (same)
    Vehicle -- remove these combos:
      licensePlateOutAndState   (references LicensePlateNumberOut +
                                  RegistrationStateOut)
      OwnerFirstAndLastName     (references OwnerFirstName + OwnerLastName)
    Vehicle -- clean any[] in remaining combos:
      Remove LicensePlateNumberOut, RegistrationStateOut, OwnerFirstName,
        OwnerLastName

    Person -- remove these attrs:
      socialSecurityNumber      (no SSN field on form)
      licenseNumberOOS          (no OOS-suffixed fieldIds on form)
      firstNameOOS              (same)
      lastNameOOS               (same)
      dateOfBirthOOS            (same)
      sexOOS                    (same)
    Person -- remove these combos:
      firstNameLastNameSocialSecurityNumber
        (references SocialSecurityNumber)
      driversLicenseNumberOOS
        (references OOS fieldIds)
      firstNameLastNameDriversLicenseNumberOOS
        (same)
      firstNameLastNameDateOfBirthOOS
        (same)
      firstNameLastNameOOS
        (same)

    Result: Vehicle goes from 10 attrs/4 combos to 6 attrs/2 combos.
            Person goes from 13 attrs/9 combos to 7 attrs/4 combos.
    Applies to: NJ, NY (confirmed 2026-04-24). Apply to all new providers.
    Exception: if a provider DOES use OOS-suffixed fieldIds (e.g., FL_FCIC
      DH-suffix), keep the OOS attrs/combos that match those fieldIds.

  Summary of AZ_AZDPS patches (as reference):
    PATCH 1: LicensePlateYear removed from Vehicle QIDM licensePlateIn any[]
    PATCH 2: RegistrationState added to Vehicle QIDM licensePlateIn any[]
    PATCH 3: RegistrationStateOut removed from licensePlateOutAndState set[]
    PATCH 4: registrationStateAttrId + RegistrationState added to all Person
               combos
    PATCH 5 (KEEP): sex retained in RMS (NIBRS confirmed AZ);
               autoSelect=true applied

  RMS POOL ISOLATION -- KNOWN OVER-SEND, FIX DESIGNED BUT GATED (2026-06-12):
    OBSERVED (FL v4.8 live, USx Tenant): full DL card -> RMS elastic query sent
    dlNumber + lastName + firstName + dateOfBirth + sexAttrId. Cause: RMS QIDMs
    follow the same union-pool serialization model as CommSys (LIMITATION #1) --
    all four Person combos' set[] are satisfied simultaneously, none have
    conditions, so the pool is the union of every combo's fields. The handler
    is not broken; the config has no pool isolation. Risk (UNVERIFIED): if the
    elastic search ANDs criteria, full-card searches over-constrain and can
    miss records an OLN-only search would find.

    FIX DESIGN (mirror CommSys FL v4.8 pattern, in _build_rms_bundle.ps1):
      1. driversLicenseNumber       set [operatorLicenseNumber]; any []
      2. firstNameLastNameDateOfBirth  set [first,last,dob];
         any [race,sex,state]; conditions [ field:["licenseNumber"], NOT_EXISTS ]
      3. firstNameLastName          set [first,last]; any [race,sex,state];
         same NOT_EXISTS condition
      Drop firstNameLastNameDriversLicenseNumber (unreachable under OLN-first).
      KeepSsn providers (AZ, TN): same treatment for the SSN combo at their rebuild.
      Effect: OLN filled -> dlNumber-only elastic query; name searches unchanged.
      Propagates to all 20 providers on rebuild (shared module).

    DO NOT APPLY UNTIL BOTH GATES CLEAR:
      G1 (impact): live RMS test with a record whose OLN matches but name
          differs -- compare OLN-only vs full-card results. If elastic ORs
          criteria there is no harm and the fix may be unnecessary.
      G2 (mechanism): conditions are live-proven ONLY on
          CommsysTransactionRequestHandler. Zero RmsRestPayloadHandler combos
          portfolio-wide use conditions -- verify the RMS handler honors them
          (or ask platform team) before relying on this fix.

================================================================================
5. QIF LAYOUT STRUCTURE
================================================================================

  All three layout variants required: default, CAD_DISPATCH, FIRST_RESPONDER.
  CAD_DISPATCH: same as default with CONTEXT_INFO_CARD (CAD unit + event
    dropdowns) prepended.
  FIRST_RESPONDER: same as CAD_DISPATCH.

  Node hierarchy:
    ROOT -> FORM_ROOT (Form, hidePageItems=true, layout='page')
         -> ROOT_PAGE (Page, title='Page 1')
         -> CARD_xxx (Card, optional title)
            -> ROW_xxx (Row, templateColumns=[...])
               -> FIELD_xxx (FormInput / FormSelect / FormDate / etc.)

  Multi-card layout: one card per query path (OLN card, Name/DOB card, etc.)
  BuildMultiCardLayout helper builds this automatically from cardDefs array.

  templateColumns: array of strings representing grid column widths.
    Single field: ['12']   (full width)
    Two fields:   ['6','6']  (equal width)
    Three fields: ['4','4','4'] or ['3','3','6'] (asymmetric)
    Four fields:  ['3','3','3','3']

================================================================================
6. BUILD MODEL -- SINGLE JSON, MULTI-CARD FROM START
================================================================================

  ONE BUILD SCRIPT → ONE JSON per provider. Always multi-card. No separate
  BASE/MC variants. The build script handles both QIDM definition and
  multi-card layout in a single pass.

  Output: <PROVIDER>_v<X.Y>.json (versioned filename; no _BASE or _MC suffix).
  Reports: docs/ (no docs/base/ or docs/mc/ subdirectories).

  HISTORY: Originally builds produced a BASE (single-card proof of concept)
  and then an MC (multi-card). In practice, BASE encouraged skipping queries
  and doubled maintenance (two scripts, two JSONs, two report directories
  per provider). All QIDMs were identical between BASE and MC; only the layout
  differed. Multi-card single-JSON is the standard build model.
  Merge/galvanization is COMPLETE for all 20 providers (single versioned JSON,
  native PascalCase USx CAD fields) -- no BASE/MC-split providers remain.

  BUILD DISCIPLINE (still applies -- just one JSON now):

  Step 1: QIDM confirmation (single card mentally, multi-card in layout).
    Build all QIDMs and combinations first. Every field on the form, every
    combo requirement mapped. Run test_commsys.ps1 to verify all combos
    fire. Do NOT declare any query "MC expansion candidate" -- build 100%
    coverage from the start.

  Step 2: Layout refinement.
    Entities with 2+ distinct search paths get one card per path.
    QIDM does NOT change -- layout only.
    Retest only affected entities.

    Which entities benefit from multi-card:
      PROMOTE:  Any entity where two paths use mutually exclusive required
                fields (Plate vs VIN, OLN vs Name+DOB). Multi-card prevents
                accidental dual-population and gives operators clear search
                path navigation.
      SKIP:     Entities with one required field set (Firearm: Serial only;
                Article: Serial+Type). No structural search path separation
                to offer.

    Card titles: use ALL CAPS (platform renders plain text only).
      Good: 'DRIVER LICENSE - LICENSE NUMBER', 'VEHICLE SEARCH - PLATE'
      Bad:  'OLN search', 'Search by plate'

    RegistrationState on multi-card forms:
      If state is needed on multiple cards (e.g. Person OLN card + Name
      card), put RegistrationState on both cards. Same fieldId -- shared
      field pool keeps them in sync. Operator sets state once; it carries to
      all cards.

  Step 3: Split entity (multiple QIFs per targetEntity -- most providers
    never need this).
    Only when multi-card reveals a genuine state model conflict: two search
    paths require fundamentally different hidden state fields that cannot
    coexist in one QIF.

    Use SPLIT ENTITY when:
      - In-state path needs SelH+InpH (dual-field); OOS path needs visible
          RegistrationStateOOS.
      - Paths are logically different enough to be navigated as separate
          sub-tabs.
    Use MULTI-CARD (stay in Step 2) when:
      - Paths share a common state model (NCIC pattern covers both).
      - Paths are logically grouped under one query type.

  WHY QIDM-FIRST (lessons from NJ v1.0--v1.6 and NY v1.0--v1.10):
    NJ built multi-card and split-entity before QIDM combinations were
    confirmed. When OOS state failed it was impossible to isolate: was it
    the QIDM, the layout, or the state model? Re-investigating took
    multiple phases and dead ends.
    NY added multi-card during standup. Same confusion.
    Confirm all QIDMs first. Every failure before layout changes is
    unambiguously a QIDM or combination problem -- not a form layout
    problem.

  Decision reference for Person forms:
    DL only (NJ pattern): one QIF, two cards (OLN + Name)
    DL + DH (AZ/FL pattern): single QIF with all paths on one card, then
      split cards
    DL + DH + WINQ/MINQ (AZ pattern): all on single Person QIF; cross-fire
      prevented by distinct required fields (RaceCode in ACQW set[], Age in
      ACQM set[], DH-suffix fieldIds)
    DL + DH (separate QIFs, NY pre-2026-04-20 pattern): CONFIRMED layout
      complexity causes QIDM isolation issues (LIMITATION #26 shared field
      pool). Prefer single QIF.

  PROVIDER VARIANTS (BASE-SYNC / lockstep):
    A base provider (TX_TLETS, NJ_NJCJIS, ...) is built directly from its own
    devdoc + metadata. A VARIANT provider (<BASE>_CCH today, more coming --
    a CCH variant for every provider eventually) REUSES the base provider's
    devdoc + metadata by default: the base devdoc is query-authority for the
    variant; the variant may carry its own merged metadata XML (base + variant
    transactions) but the devdoc stays the base's unless explicitly given one.

    Base + variants = ONE logical provider kept in LOCKSTEP. A variant is
    derived from the base (inherits its base-QIDMs), so ANY change to the base
    JSON -- rebuild, combo edit, label pass, CAD fix -- must propagate to a
    rebuild of every variant. Variants must never drift independently. When you
    touch a base provider, rebuild its variants in the same pass and re-run
    their gates.

    Enforced by convention + check: each variant's build script declares
      # BASE-SYNC: <BASE> vX.Y
    (the base version its base-QIDMs are synced to). tools/audit_variant_sync.ps1
    (composed into doctor.ps1) flags any declared variant whose marker is behind
    its base's current version. Detection is MARKER-DRIVEN, not name-based, so an
    independent provider that merely shares a name prefix (e.g. CA_CLETS_OCATS) is
    NOT mistaken for a variant. When the base bumps: re-sync the variant's
    base-QIDMs and bump its BASE-SYNC marker. TX_TLETS_CCH (base TX_TLETS) is the
    only current variant.

================================================================================
7. STATE FIELD PATTERNS
================================================================================

  Two confirmed patterns for state handling. Choose based on instance support.
  ALWAYS test NCIC pattern on first import (ST-1: in-state query) before
  relying on it. If CommSys <State> element is absent on ST-1, fall back to
  dual-field pattern (Section 7b).

  Field-level code-type config details are in FIELD_REFERENCE.txt. This
  section covers the structural layout patterns only.

  ---- 7A. NCIC STATE PATTERN (preferred when supported) ----

  CONFIRMED: NJ_NJCJIS (2026-04-20), AZ_AZDPS (2026-04-20), NY (confirmed).
  UNCONFIRMED: FL, HI, TX, LA, CA -- test before using.

  Single visible Sel 'RegistrationState':
    type: FormSelect
    props: { fieldId: 'RegistrationState', attributeTypeId: 'STATE',
             initialValue: '<state>' }
    visible: true (operator selects any state including OOS)

  CommSys QIDM State attribute:
    { name: 'State', sourceField: ['RegistrationState'],
      targetField: 'State', codeTypeProvider: 'NCIC', size: 2 }
    NCIC reverse-lookup: stored attr ID -> 2-letter state code for outbound
      XML.

  CommSys combination any[]:
    'RegistrationState'  (form fieldId, not attribute name 'State')

  RMS QIDM (standard from _build_rms_bundle.ps1 -- no changes needed):
    useAttributeId=true + AttributeArrayWrapperRuleHandler
    Sends registrationStateAttrIds:[<dynamic attr id>] for any selected
      state.

  RESULT: One field. Operator selects once. CommSys receives
    <State>XX</State> and RMS receives registrationStateAttrIds:[<attr id>]
    for any state including OOS.
  No hidden fields. No dual-field architecture needed.

  INSTANCE TEST: Run in-state plate query first. If <State>XX</State>
    present -> NCIC confirmed. If absent -> codeTypeProvider=NCIC not
    registered for state on this instance. Fall back to Section 7b.

  ---- 7B. DUAL-FIELD STATE PATTERN (fallback when NCIC not supported) ----

  Use when NCIC state pattern is not confirmed on the target instance.
  Two hidden fields -- both in the form layout, both hidden=true:

  FIELD 1: SelH 'RegistrationState'
    type: FormSelect
    hidden: true
    props: { fieldId: 'RegistrationState', attributeTypeId: 'STATE',
             initialValue: 'NY' }
    Purpose: RMS reads fieldId='RegistrationState' and gets the attribute
      ID for state filtering.
    Rule: DO NOT add to QIDM. Must not reach outbound XML.

  FIELD 2: InpH 'RegistrationStateNY' (or 'RegistrationStateNJ', etc.)
    type: FormInput
    hidden: true
    props: { fieldId: 'RegistrationStateNY', initialValue: 'NY' }
    Purpose: QIDM sourceField -> targetField='State'. Sends literal 'NY'
      to outbound XML.
    Rule: Add to QIDM as an attribute with targetField='State'.

  DO NOT use attributeTypeId='STATE' on a field that is mapped in the QIDM.
  DO NOT use InpH for the RMS field -- RMS needs attributeTypeId=STATE to
    recognize the value.
  LIMITATION: OOS state not achievable for RMS with this pattern. See
    LIMITATION #6.

  ---- 7C. OOS STATE FIELD PATTERN ----

  One visible dropdown:
    Sel 'RegistrationStateOOS'
    codeTypeCategory='NJ_NIBRS_STATE'  codeTypeSource='NJ_NIBRS'
    No attributeTypeId -- does not interact with RMS state attribute lookup.

  Add to QIDM with targetField='State' (or provider's XML element name).
  RMS fires without state filter on OOS queries -- this is expected
    (LIMITATION #6).

================================================================================
8. ENTITY FIELD PATTERNS
================================================================================

  Per-entity field lists. Field names, form types, and structural notes.
  Detailed code-type configurations go to FIELD_REFERENCE.txt.

  ---- VEHICLE ----

  Two valid approaches for plate fieldIds:

  APPROACH A: Shared plate fieldId (recommended when OOS doesn't need state)
    Both Vehicle-NY and Vehicle-OOS forms use fieldId='licensePlateNumber'.
    RVEH combination: set=['licensePlateNumber'] -- fires from either form.
    OOS form: State is optional in RVEH (OOS plate search without state
      filter).
    Advantage: simpler QIDM -- one RVEH combination covers both forms.
    Confirmed: NY v1.11-v1.15.

  APPROACH B: Separate plate fieldIds (when NJ and OOS combos must be
    distinct)
    Vehicle-NJ form uses fieldId='licensePlateNumber'.
    Vehicle-OOS form uses fieldId='LicensePlateNumberOut'.
    QIDM has separate combinations.
    Advantage: distinguishes NJ vs OOS plate by fieldId; state is required
      in set[] for each.
    Confirmed: NJ v3.23.

  Both approaches keep all combinations in ONE merged QIDM (avoids
    LIMITATION #2). Both require RegistrationState SelH (for RMS) and
    RegistrationStateNJ/NY InpH (for XML).

  Common vehicle fields:
    LicensePlateTypeCode    FormSelect
    LicensePlateYear        FormInput (4 chars)
    VehicleIdentificationNumber  FormInput, maxLength=20
    VehicleMakeCode         FormSelect (attributeTypeId=VEHICLE_MAKE, codeTypeProvider=NCIC). NEVER FormInput. Dropdown confirmed working on all tenants (NJ, FL, CA_CLETS, TX live-tested 2026-05-22).
    VehicleYear             FormInput, initialValue=current year
    ImageIndicator          FormSelect, initialValue='N' (vehicle default)

  ---- PERSON ----

  Phase 1 standup: one Person QIF, single card, ALL person query paths on
  that card.

  QIDM wiring for single-form (CONFIRMED NJ v1.7, AZ v1.0):
    VehicleRegistrationQuery   -- autoSelect=true
    DriverLicenseQuery (OLN)   -- autoSelect=true, NO queriesToDeselect
    DriverLicenseQuery (Name)  -- autoSelect=true, NO queriesToDeselect
    DriverHistoryQuery         -- autoSelect=true, DH-suffix isolation

  DH-suffix fieldIds for cross-fire prevention:
    OperatorLicenseNumberDH, NameFirstDH, NameLastDH, NameMiddleDH,
    NameSuffixDH, BirthDateDH, SexCodeDH

  Standard person fields:
    OperatorLicenseNumber   FormInput
    NameFirst               FormInput
    NameLast                FormInput
    NameMiddle              FormInput (confirm per provider metadata)
    NameSuffix              FormInput (confirm per provider metadata)
    BirthDate               FormDate
    SexCode                 FormSelect (instance-specific config)
    RaceCode                FormSelect
    RegistrationState       FormSelect

  PERSON (DriverLicenseQuery) COMBO PRIORITY ORDER -- DL-PRIORITY PATTERN:
    Combo ordering must be: OLN-based combo first, Name+Sex+DOB second, fallback
    (Name+DOB without Sex) last. This is the DL-priority order.

    Standard: DQN (OLN+State) > DQ (Name+SexCode+BirthDate) > QW (Name+BirthDate)
    DH variant: KQN (OLN+State) > KQ (Name+SexCode+BirthDate)

    RULE: OLN is the most-specific identifier (unique, no ambiguity). Name+Sex+DOB is
    next (identity triple). Name+DOB without Sex is the fallback for when SexCode is
    absent. Most-specific combo FIRST -- matches the general combo priority rule.

    MUTUAL EXCLUSION (DL/DH): DL autoSelect=true + queriesToDeselect=[DH]. DH
    autoSelect=true + queriesToDeselect=[DL] ONLY when DH opt-in checkbox is checked
    (one-directional). DL fires by default; DH fires only when officer opts in.
    This ensures DL always runs on OLN entry without requiring DH checkbox.

    LIVE-PROVEN HI v2.9 2026-06-22: DQN > DQ > QW priority confirmed (T6/T7/T8 +
    T22 DL/DH mutual exclusion). KQN > KQ confirmed (T9/T10).

  ---- FIREARM ----

  Standard fields:
    GunSerialNumber             FormInput, maxLength from XML (NY=20)
    GunMake                     FormInput (NY) or FormSelect (NJ)
    GunCaliber                  FormInput or FormSelect
    RelatedHitSearchIndicator   FormInput, maxLength=1 (Y-only flag, not
                                  FormSelect)
  Note: NY uses 'GunSerialNumber' (NJ uses 'SerialNumber').

  ---- ARTICLE ----

  Standard fields:
    ArticleSerialNumber         FormInput, maxLength from XML
    ArticleTypeCode             FormSelect
    ImageIndicator              FormSelect, initialValue='Y'
    RelatedHitSearchIndicator   FormInput, maxLength=1 (Y-only)
  Note: NY uses 'ArticleSerialNumber' (NJ uses 'SerialNumber').

  ---- BOAT ----

  Standard fields:
    RegistrationNumber          FormInput
    BoatHullIdNumber            FormInput
    RegistrationStateOOS        FormSelect (no initialValue -- blank fires
                                  in-state, selected state fires OOS)
    ImageIndicator              FormSelect

================================================================================
9. LAYOUT CONSTRAINTS
================================================================================

  LIMITATION #11: Card.props.title renders plain text only
    HTML tags appear verbatim. Use ALL CAPS for emphasis.
    Non-ASCII characters (em dash, smart quotes) render as mojibake
      (AP #26).
    Rule: ASCII only in card titles. Use hyphen-minus (U+002D), not em dash.

  LIMITATION #12: No conditional field visibility
    hidden is static (true/false only). Cannot show/hide based on user input.
    Workaround: All fields visible; QIDM set/any handles routing.

  LIMITATION #13: No field styling
    No font size, color, weight props on any node type.
    Workaround: Field ordering and Card titles only.

  LIMITATION #14: No standalone text/label component
    Components: Card, Row, FormInput, FormSelect, FormDate, FormCheckbox.
    Workaround: Card.props.title for instructional text.

================================================================================
10. TYPE SAFETY RULES
================================================================================

  templateColumns: MUST be array of strings (["6","6"]), not integers.
  maxLength: MUST be string ("20"), not number.
  autoSelect: MUST be boolean (true), not string.
  keyReference: Not keyRef. Wrong property name = silent null.
  rule: Use { function: "HandlerName" }, not ruleHandlers: [{name:...}].

  C# and PowerShell JSON serializers silently produce wrong types.
  Always run validator after build script execution.

================================================================================
11. MULTI-CARD LAYOUT RULES
================================================================================

  Multi-card is the standard build model.
  Multi-card isolates routing fields onto a separate OPTIONS card so officers
  cannot accidentally trigger the wrong query type.

  CORE PRINCIPLE:
    Routing fields (State, Stolen Search, etc.) go on an OPTIONS card.
    All search fields go on a single SEARCH card.
    The OPTIONS card is a SEPARATE card the officer must deliberately visit.

  DUPLICATE FIELDID ACROSS CARDS = INTERNAL SERVER ERROR (ISE):
    The same fieldId CANNOT appear on multiple cards within one entity.
    If two combos share a field, that field goes on ONE card only.
    Use the shared OPTIONS card pattern for fields in any[] of multiple combos.

  CARD STRUCTURE PER ENTITY (standard pattern):
    2-card layout: OPTIONS card + SEARCH card
    - OPTIONS: routing toggles (State, Stolen Search) + shared any[] (Image)
    - SEARCH: all primary search fields (Plate, VIN, Hull, Name, etc.)
    This is the tightest layout that still isolates routing.

    More cards only if the entity has fundamentally different search paths
    that would confuse officers when visible together. Most entities work
    with 2 cards.

  "LEAVE BLANK FOR [STATE]" LABEL RULE:
    On MC, "State (leave blank for [STATE])" is the standard label for
    ALL State fields that serve in-state vs OOS routing. This includes:

    - Vehicle Options card State: blank = FRQ (in-state), filled = RQ (OOS)
    - Boat Options card State: blank = FBQ (in-state), filled = BQ (OOS)
    - Person DL card State: blank = FDQ (in-state), filled = DQ (OOS)

    BOAT NAME/DOB EXCEPTION: Boat BQ name/DOB combos have State in set[],
    meaning State is REQUIRED even for in-state name searches (State=FL).
    Unlike Vehicle/Person, there is no in-state boat name query without State.
    Boat State label must add this exception: "leave blank for FL, required
    for name/DOB". FL_FCIC v4.1 confirmed during T18 testing.
    Check each provider's boat combos — if name/DOB requires State, add
    the exception to the label. Vehicle/Person do NOT need this because
    FRQ/FDQ name combos exist without State.

    This is safe on MC because DH uses its own registrationStateDH field
    (DH-suffix pattern). DL State does NOT affect DH queries.

    CONSISTENCY RULE: If one entity has the hint, ALL entities with
    in-state vs OOS routing must have it. Officers expect uniform labeling.
    FL_FCIC v4.1 confirmed: inconsistent labels confused the workflow.

  FIELD LABEL HINT STANDARD (ENFORCED by verify_build CHECK 13, 2026-06-26):
    Labels are the ONLY hint mechanism. The platform renders no helperText or
    placeholder attributes on form fields. Every label must carry enough context
    for an officer to understand routing without reading documentation.

    CANONICAL FIELD LABEL -- OLN (global, DEX-1284, 2026-07-27):
    The operator-license-number field (OperatorLicenseNumber and its DH-suffix variant) is
    labeled "OLN" on EVERY provider -- NOT "License Number" / "OL Number" / "Driver License
    Number". Applied to each provider on its revisit turn (not a retroactive one-shot sweep).
    When a routing-alternative hint is kept on it, the pattern is "OLN (or search by Name...)".
    (The card/query name stays "Driver License"/"Driver History" -- only the field label is OLN.)

    CANONICAL FIELD LABEL -- NCIC Image (global, DEX-1284, 2026-07-27):
    Every image-toggle field (ImageIndicator and its DH-suffix variant) is labeled exactly
    "NCIC Image" on EVERY provider -- NOT "NCIC Image - if available" / "Image (optional)" /
    "Image". Applied to each provider on its revisit turn (FUTURE RETROFIT -- not a one-shot
    sweep; NY_NYSPIN_EJUSTICE v4.12 is the first). verify_build CHECK 15 Rule 3 accepts the bare
    "NCIC Image" as a canonical label (its $canonicalBareLabels allowlist), so it needs NO
    "(optional)" qualifier and NO LABEL-OVERRIDE tag despite being an any[]-only field.

    FIELD SIZE & PLACEMENT convention (global, DEX-1284, 2026-07-27):
    Default each card to a UNIFORM column grid -- every row's templateColumns shares the same
    boundaries so fields line up left-to-right AND top-to-bottom (typically 4/4/4 for three-across
    cards, or 6/6 for two-across). Size the field to the grid; do NOT leave a row summing to <12
    with a lopsided split (the old NY Vehicle ROW_VEH_1 4/2/2 filled only 8 of 12 cols and cramped
    Plate Type/Year), and do NOT give a short code field (State, Image, Plate Type = 1-2 chars) a
    half-row box just because the row has two fields -- drop it onto the shared grid width instead
    (NY v4.13: Vehicle -> 4/4/4, VIN & State 6->4). A trailing row with fewer fields than the grid
    (e.g. State+Image on a 4/4 pair under a 4/4/4 card) is fine -- they still align to the same
    left-origin columns. Applied per-provider on its revisit turn.
    PERSON CARDS (DL + DH), top-row pattern (NY v4.14): put the primary identifier + the two
    routing/option fields together on the TOP line -- OLN + State + NCIC Image at 6/3/3 (OLN keeps
    the width, State/Image are short codes). Name fields on the next row, DOB/Sex below. Do NOT
    strand OLN on its own full-width row with State/Image pushed to a second row.

    LEAN-LABEL / CARD-TITLE-CARRIES-THE-PATH convention (DEX-1284, 2026-07-27, HI-style):
    Preferred officer-facing style is lean -- strip inline field helpers and let the CARD TITLE
    state the query paths (e.g. Vehicle "... Search by Plate, \"OR\" VIN"; Person "Driver License
    Search by OLN, \"OR\" Name"). The ONE field that always keeps its inline hint is State (its
    routing hint "leave blank for <HOME>" is mandatory -- CHECK 15 Rule 1). Firearm/Article
    stolen-hit toggles use the bare label "Stolen Check". Other deliberately-bare any[] fields
    (Make/Year/MI/Suffix/Caliber/etc.) carry a "# LABEL-OVERRIDE: <fieldId> -- <reason>" tag so
    CHECK 15 Rule 3 downgrades the WARN to accepted [INFO]. Applied per-provider on its revisit turn.

    CARD TITLES ARE UPPERCASE (global, Rob 2026-07-27 -- "everything needs to be upper case"):
    Every card `title` string is ALL-CAPS. The chosen form is UPPERCASE-the-current-wording (NOT a
    reword) -- query-path titles keep their paths, just uppercased (e.g. "DRIVER LICENSE SEARCH BY
    OLN, \"OR\" NAME", "FIREARM SEARCH BY SERIAL NUMBER", "SEARCH OPTIONS"). This is titles ONLY --
    field labels are NOT uppercased (OLN/NCIC Image stay as-is; "First Name" etc. keep normal case).
    Applied to the 7 rebuilt-today providers (TX/CCH/NY/FL/NJ/CA/HI, 2026-07-27); retrofit the rest
    on their revisit turn. Titles are free text (not field labels) so no CHECK 15 interaction; ASCII
    only (LIMITATION #11). Mechanical: uppercase every `title = '...'` string in the build script.

    SEVEN CANONICAL HINT TYPES (required on applicable fields):

    1. ROUTING ALTERNATIVES -- on any primary identifier where an alternative
       search path exists:
         "Plate Number (or search by VIN or Owner Name)"
         "OLN (or search by Name + Sex)"
         "Serial Number (or search by Owner Name)"
         "Hull ID (or Reg Number; Hull wins if both)"
       Pattern: <Field Name> (or search by <alternative>)

    2. IDENTIFIER PRIORITY -- on the lower-priority field when a guardrail exists:
         "VIN (Plate wins if both entered)"
         "Registration Number (or Hull ID)"
       Pattern: <Field Name> (<higher-priority field> wins if both entered)
       Note: always pair with a "routing alternatives" hint on the higher-priority field.

    3. ROUTING CONTEXT -- on fields that only apply to a specific search path:
         "Plate Type (out-of-state plates)"
         "Plate Year (out-of-state plates)"
         "First Name (vehicle owner search)"
         "First Name (out-of-state only; State + DOB required)"
       Pattern: <Field Name> (<context qualifier>)

    4. IN-STATE DEFAULT (State routing hint -- TWO valid patterns):
       a. State NOT defaulted (blank routes to the OOS keyRef):
          "State (leave blank for [STATE])"
          Boat exception: "State (leave blank for [STATE]; required with Name)"
       b. State DEFAULTED to home in combo defaults[] (approved pattern, e.g. NJ State=NJ):
          the officer CHANGES it for OOS, so "leave blank for" is WRONG guidance -- use
          "State (default [STATE] - change for out-of-state)".
       ENFORCED: verify_build CHECK 13 FAILs if a State-suffix fieldId label lacks BOTH a
       "leave blank for" hint AND an out-of-state/"change for" hint. (Refined 2026-06-26,
       RND-62365: the old rule demanded "leave blank for" on every State field and FAILed
       NJ's design-correct defaulted-State label. Now accepts either valid pattern.)

    5. OPTIONAL INDICATOR -- on every any[]-only field (never in set[]):
         "Vehicle Make (optional)"
         "Age (optional)"
         "Brand (optional)"
       Pattern: <Field Name> (optional)
       ENFORCED: verify_build CHECK 13 WARNs if a pure any[] field has no '('
       or ' - ' qualifier.

    6. CONDITIONAL REQUIREMENT -- on set[] fields that only fire with a co-field:
         "Sex (required with Name)"
         "Date of Birth (DH) - required with Name"
         "APPS Check - prohibited persons (Name search)"
       Pattern: <Field Name> (required with <co-field>) or
                <Field Name> - <qualifier> (<context>)

    7. ABBREVIATION EXPANSION -- when the field name uses an abbreviation:
         "Criminal ID (CII) - criminal records"
         "SSN - criminal records (no OLN)"
         "Owner Applied Number (OAN)"
         "License Number (DH) - or Name + DOB + Sex"
       DH suffix RECOMMENDED, not enforced when the DH field lives on its own
       separate, distinctly-titled card -- the card title itself disambiguates
       DH from DL (e.g. FL_FCIC v7.2's "Driver History (Out-of-State) By Name
       "OR" Drivers License Number" card, DEX-1278, 2026-07-16: Gordon Hallof's
       explicit UX feedback dropped the per-field "(DH)" tags in favor of the
       card title alone). Same-card / single-card DH designs (e.g. AZ_AZDPS,
       "single QIF with all paths on one card") should still tag fields --
       there the DH and DL inputs are visually interleaved with no card-title
       separation to rely on. verify_build CHECK 13 downgraded this from FAIL
       to advisory (Info) 2026-07-16 -- it no longer blocks a build either way.
       (Prior: FAILed if a DH-suffix fieldId label lacked a "(DH..." qualifier,
       word-boundary matched. Refined 2026-06-26, RND-62365.)

    IMPLEMENTATION NOTES:
    - Hints embedded in the label string only -- no separate helperText/placeholder
    - One-sentence max per label; avoid redundancy
    - "Purpose Code" fields need no hint -- exempted from rule 5's any[]-only
      qualifier requirement regardless of whether the field is auto-filled or
      officer-entered (verify_build CHECK 13 Rule 3 exemption added 2026-07-16,
      DEX-1278 -- previously documented here but never actually implemented in
      the validator, so a manually-entered Purpose Code field like FL_FCIC's
      DH one still WARNed; the code now matches this long-standing KB note)
    - "First Name" / "Last Name" on a primary entity (Person) need no hint;
      only cross-entity or context-specific uses need qualifiers
    - All verified as of CA_CLETS v2.8; prior providers need label audit on rebuild

    8. LABEL-OVERRIDE ESCAPE HATCH (added 2026-07-17, TX_TLETS v4.4/v4.5, Rob's standing
       directive): cosmetic label edits explicitly called for by Rob must not block the
       pipeline with FAIL/WARN noise -- only actual query/routing breakage should. When a
       user-directed bare/short label trips rule 4 (State routing hint, a FAIL) or rule 5
       (any[]-only qualifier, a WARN), do NOT silently restore wording just to satisfy the
       tool. Add a comment anywhere in the provider's build script:
         # LABEL-OVERRIDE: <fieldId> -- <short reason>
       verify_build CHECK 13 scans the build script's own source lines for this tag (not
       proximity-based -- one tag anywhere in the file covers every occurrence of that
       fieldId) and downgrades the finding to an accepted, visible [INFO] line instead of
       FAIL/WARN. Still auditable, just doesn't block. Scoped to CHECK 13 rules 4/5 only --
       never use it to wave off a finding that indicates a real query/routing problem. See
       tools/verify_build.ps1 CHECK 13 for the implementation; TX_TLETS's
       RegistrationState/regionId/reasonCode fields are the reference example.

  OPTIONS CARD TITLE:
    Use "Options" as the card title. Keep it simple and generic.
    The "leave blank for [STATE]" hint belongs on the FIELD LABEL, not the card title.
    Card titles describe the card's purpose; field-level instructions stay on field labels.
    ASCII only (LIMITATION #11). FL_FCIC v4.1 confirmed: verbose card titles cluttered the UI.

  WHICH FIELDS GO ON OPTIONS VS SEARCH:
    OPTIONS card fields:
    - registrationState (State) -- routes in-state vs OOS combos
    - relatedHitSearchIndicator (Stolen Search) -- routes FBQ vs QB (Boat)
    - imageIndicator (Image) -- shared any[] across all combos
    - Any field that appears in any[] of MULTIPLE combo types

    SEARCH card fields:
    - All primary identifiers (Plate, VIN, Hull, RegNum, Decal, etc.)
    - All secondary fields (PlateType, PlateYear, Make, Year, etc.)
    - Name fields (Last, First, DOB) for name-based combos
    - NCIC lookup fields (CG#, NCIC#, PCN)

  PERSON IS DIFFERENT:
    Person uses DH-suffix fieldIds instead of an OPTIONS card.
    DL card: OLN, State, Image, Name, DOB, Sex, Suffix
    DH card: OLN_DH, StateDH, PurposeCode, Name_DH, DOB_DH, Sex_DH, Attention
    queriesToDeselect isolates DL from DH (one-directional: DH deselects DL).

  OPTIONS-FOLD (fold a shared Person OPTIONS card into DL+DH -- TX_TLETS v4.12, Rob
  2026-07-27 "fix person to be 2 card and duplicate the search options into each card"):
    When a provider carries a standalone Person SEARCH OPTIONS card holding fields shared by
    both DL and DH (State / Image / Reason / etc.), you can collapse Person to a clean 2-card
    (DL + DH) by DUPLICATING those option fields onto the BOTTOM row of each card:
      - DL card gets the ORIGINAL shared fieldIds (DriverLicenseQuery unchanged).
      - DH card gets DH-SUFFIXED copies (RegistrationStateDH/ImageIndicatorDH/reasonCodeDH) --
        unique fieldIds, self-contained like NY/HI. Re-source the DriverHistoryQuery attributes
        and its combo any[] to the DH-suffixed names; attribute NAMES + wire targetFields stay
        the same, so the wire request is byte-identical (layout+sourcing change only).
    HANDLER-OWNED fields are the exception -- do NOT duplicate them. The single hidden
    EmailAddress gate-feeder stays once on the DL card and serves BOTH QIDMs (both still source
    'emailAddress'), so the RND-57165 handler (separate eng team) is untouched. General rule:
    duplicate shared plain fields per card; keep one shared hidden feeder for any field owned by
    a rule-handler you must not modify.

  QIDMS ARE INDEPENDENT OF LAYOUT:
    All QIDM combinations, attributes, and keyReferences are defined
    independently of the card structure. Layout changes do not affect QIDMs.

  CONFIRMED PROVIDERS USING THIS PATTERN:
    FL_FCIC v4.1: Vehicle(2 cards), Person(2 cards), Boat(2 cards)
    CA_eSUN v1.5: Vehicle(4 cards), Person(2 cards)
    OH_LEADS v1.3: Vehicle(6 cards), Person(2 cards)
    TX_TLETS v2.5: Vehicle(3 cards), Person(2 cards)
    HI_HCJDC_OFML v1.6: Vehicle(3 cards), Person(2 cards)

  DEVDOC-ORDER COMBOS + ROUTING CONDITIONS (standard as of FL v4.7, 2026-06-12):
    USER DIRECTIVE: combo array order follows the devdoc "Possible Combinations"
    listing order in EVERY QIDM. Devdocs list in-state combos first, so under
    first-match evaluation the in-state combos must carry routing conditions or
    later OOS/stolen combos become unreachable:
      - In-state (FRQ/FBQ/FDQ-style) combos: conditions State NOT_EXISTS
      - Stolen-toggle counterparts (FBQ Hull/Reg vs QB): additionally
        RelatedHitSearchIndicator NOT_EQUALS Y
      - Name combos in OLN+Name families: OperatorLicenseNumber NOT_EXISTS
        (the over-send fix: a fired combo serializes the union pool of all
        condition-matched combos -- LIMITATION #1 -- so failing the Name combos'
        conditions when OLN is filled removes Name/DOB/Sex from the XML)
    Conditions both route firing AND filter the serialization pool. NOT_EXISTS
    pool-filtering is now LIVE-PROVEN on two tenants (FL v4.8 + HI v3.4 T4/T5).
    Validator G-16 understands this: it only flags an earlier UNCONDITIONED combo
    whose set[] is a subset of a later combo's set[] (true shadowing).
    WIRE FORMAT IS MANDATORY: conditions nested in requirements; field = the FORM
    sourceField / fieldId (NOT the attribute name -- CORRECTED 2026-06-22, HI v3.4
    T5; an attribute-name field whose sourceField differs is silently INERT);
    value arrays (QIDM_REFERENCE Section 2a). Combo-level conditions are silently
    ignored (FL v4.7 live failure 2026-06-12).

  QW / "DATA-MINED TRANSACTION" PLATFORM AUTO-SEND -- DO NOT BUILD (FL_FCIC v4.2,
  HI_HCJDC_OFML v4.8): `QW` (NCIC Wanted Person Query) is a platform-auto-sent shadow
  combo, not a client-buildable one -- CommSys sends the Wanted Person check itself
  as a side effect of another query. FL_FCIC_BUILD_NOTES.txt v4.2 (2026-05-19):
  "Remove WantedPersonQuery (QW) QIDM... CommSys auto-sends QW query; no JSON-side
  QIDM needed. Confirmed by platform team." HI_HCJDC_OFML built QW explicitly since
  v1.0 (a documented PDF-vs-XML gap resolved wrong at the time -- "include per
  metadata" despite the devdoc's own "Possible Combinations" list never showing a
  Sex-optional Name variant) and removed it at v4.8 once the FL precedent surfaced.
  QW is a standard NCIC-level key, not FL-specific -- treat any metadata-defined QW
  combo the same way (not built, `not-built` entry in ACCEPTED_DIVERGENCES.txt) on
  any future provider, unless a NEW platform-team confirmation says otherwise for
  that specific tenant. Do NOT extrapolate this to other devdoc "data-mined" list
  members (QA/QB/QG) without their own confirmation -- FL_FCIC's own BQ was
  initially miscategorized this way and later restored once proven it's a real,
  devdoc-authorized combo (BQ_BUILD_NOTES "BQ restore" entry); only QW itself has
  an actual platform-team confirmation behind it. `QV` (NCIC Stolen Vehicle) is
  still unconfirmed/PENDING on FL as of v4.7 -- do not assume it's the same class
  as QW without its own confirmation (HI documents QV as not-built per a separate,
  weaker justification -- see HI_HCJDC_OFML_ACCEPTED_DIVERGENCES.txt).

    DEFAULTED-FIELD SUBSET-SHADOWING (HI VehicleRegistrationQuery, 2026-06-22):
    a combo whose set[] contains a DEFAULTED form field (e.g. vehicleTypeCode
    initialValue=1) plus a shared primary (LicensePlateNumber) is ALWAYS satisfied
    once the primary is filled -- the defaulted field is never blank. So it sits in
    the union pool permanently and bleeds its fields (VehicleTypeCode) into any
    co-satisfied OOS combo's XML. Fixes: (a) order the more-specific OOS combo first
    AND (b) give the defaulted-field combo an existence-only condition on the OOS
    discriminator's sourceField (M55L: LicensePlateTypeCode NOT_EXISTS; M55S:
    RegistrationState NOT_EXISTS) so it LEAVES the pool when the OOS combo applies.
    For routing purposes, a combo's EFFECTIVE required fields = set[] minus any
    field that has an initialValue. Validator flags when an earlier combo's effective
    set is a subset of a later one's (it will shadow it). Do NOT put the OOS
    discriminator in the defaulted combo's any[] -- it can never serialize there and
    it breaks the test conductor (HI v3.3 T16: RegistrationState in M55S any[] +
    NOT_EXISTS condition contradicted; removed v3.4).

  IDENTIFIER-PRIORITY GUARDRAIL (generalized HI v3.6 plate-wins + FL DL OLN-priority):

    PRINCIPLE: Within ONE QIDM, when two combos represent different search identifiers
    an officer can fill at the same time, the platform serializes the UNION of every
    satisfied combo's set[]+any[] (LIMITATION #1) -- not just the first match. To keep
    the higher-priority identifier's query clean, gate the lower-priority combos out of
    the union pool with a NOT_EXISTS condition on the higher-priority identifier's
    sourceField. Standard priority order:
      - Vehicle: Plate > VIN  -> every VIN-path combo gets <plate> NOT_EXISTS
      - Person : OLN  > Name  -> every Name-path combo gets <oln>   NOT_EXISTS
    NOT_EXISTS-only (existence test). NEVER a value-comparison condition (poisoned-array;
    those are silently inert -- QIDM_REFERENCE Sec 2a).

    CASING: conditions[].field = the provider's actual form sourceField token (FIELD =
    SOURCEFIELD rule). PascalCase providers (NJ/FL/HI/NY/CA_CLETS/TX/AZ) use ['LicensePlateNumber'] /
    ['OperatorLicenseNumber']; camelCase providers (TX/CA_CLETS/NY/AZ) use
    ['licensePlateNumber'] / ['operatorLicenseNumber']. Verify the token before editing.

  --- VEHICLE half (Plate > VIN) -- HI v3.6 plate-wins ---
    When a VehicleRegistrationQuery QIDM has both plate-path combos (set includes
    LicensePlateNumber) and VIN-path combos (set includes VehicleIdentificationNumber),
    add a LicensePlateNumber NOT_EXISTS condition to ALL VIN-path combos. Without this,
    filling both Plate and VIN simultaneously causes every VIN-path combo to also satisfy
    (union pool), and the platform serializes the union of all fired-combo set[]+any[] --
    sending VehicleIdentificationNumber and VehicleMakeCode into what should be a clean
    plate XML request (LIMITATION #1 over-send).

    RULE: On every VIN-path combo in VehicleRegistrationQuery QIDM --
      RQV, QVV, any in-state VIN combo (M55S, etc.) -- add:
        conditions: [ { field: ['LicensePlateNumber'], operator: 'NOT_EXISTS' } ]
    For combos with an existing condition (e.g. M55S has RegistrationState NOT_EXISTS),
    ADD a second condition object to the array; both must pass.

    WHAT NOT TO DO: Do NOT add LicensePlateNumber NOT_EXISTS to QVP (set requires
    LicensePlateNumber -- self-contradictory) or to RQ/M55L (plate-path combos).
    QVP is already dormant and shadowed; if Plate+State present, M55L/RQ win via
    first-match ordering, and QVP's union contribution is a subset of their output.

    ALSO: VehicleYear should be in any[] of VIN-path combos only (RQV, M55S, QVV).
    Do NOT add it to plate-path combos (RQ, M55L, QVP) -- Year is not a plate attribute.
    sourceField='vehicleYear' (camelCase); attribute targetField='VehicleYear'.

    LIVE-PROVEN HI v3.6 2026-06-22 (sim + live T6 + T7):
    T6 all-fields (Plate+PlateType+PlateYear+VIN+State+Make): CommSys XML = plate only;
    VehicleIdentificationNumber+VehicleMakeCode ABSENT. RQ wins. DECISIVE.
    T7 bare Plate+VIN (no State, no PlateType/Year): M55L fires; VIN absent from CommSys XML.
    M55S exits on LicensePlateNumber NOT_EXISTS condition. DECISIVE.
    Test logs: the prior v3.6 working-tree logs (RQ all-fields-stress; M55L T7 bare Plate+VIN) were
               never committed to git; the plate-wins guardrail is being re-validated in the HI v4.7
               Vehicle re-test -- see logs/Vehicle once complete. (Rule also live-proven on FL/NJ/TX.)

  --- PERSON half (OLN > Name) -- FL DL live precedent ---
    When a DriverLicenseQuery (or DriverHistoryQuery) has both an OLN-set combo (set
    includes OperatorLicenseNumber) and one or more Name-set combos (set includes
    NameLast/NameFirst), add an <oln> NOT_EXISTS condition to EVERY Name-set combo.
    When OLN is present the Name combo(s) exit the pool and the OLN query fires alone --
    Name/DOB/Sex fields don't bleed into the OLN XML. This makes OLN win regardless of
    combo array order (the condition, not ordering, enforces priority).

    RULE: on every Name-set combo --
      conditions: [ { field: ['OperatorLicenseNumber'], operator: 'NOT_EXISTS' } ]   (PascalCase)
      conditions: [ { field: ['operatorLicenseNumber'], operator: 'NOT_EXISTS' } ]   (camelCase)
    Append to any existing condition (e.g. an OOS State NOT_EXISTS gate); all must pass.

    *** APPLIES TO DriverHistoryQuery TOO -- NOT JUST DriverLicenseQuery. *** The DH KQ-type
    Name combo (set has NameLastDH/NameFirstDH) needs the SAME guardrail, keyed on the DH-suffixed
    OLN sourceField:
      conditions: [ { field: ['OperatorLicenseNumberDH'], operator: 'NOT_EXISTS' } ]
    conditions[].field = the actual DH sourceField token (DH-suffix per Section "DH-suffix
    fieldIds"). Without it, KQ (Name) + KQN (OLN) BOTH satisfy when the officer fills the DH OLN
    AND DH Name, and the union pool over-sends Name+OLN together on the DriverHistoryQuery wire.
    This was missed in the first guardrail rollout (matrix listed only DL combos) -- always check
    BOTH the DL and DH QIDMs for an OLN/Name pair.

    LIVE PRECEDENT (DECISIVE, both halves on the wire): HI_HCJDC_OFML --
      - DL (v3.7): DQ + QW carry OperatorLicenseNumber NOT_EXISTS. T6: OLN+Name+Sex+DOB ->
        DriverLicenseQuery XML = OperatorLicenseNumber only (Name/Sex/DOB absent). T8 regression:
        Name no-OLN -> DQ fires with Name.
      - DH (v3.8): KQ carries OperatorLicenseNumberDH NOT_EXISTS. KQN-decisive: DH OLN+Name ->
        DriverHistoryQuery XML = OperatorLicenseNumber + Attention + PurposeCode only (Name absent).
        KQ regression: DH Name no-OLN -> KQ fires with Name.
    EARLIER PRECEDENT: FL_FCIC DriverLicenseQuery (FDQName/DQName carry OLN NOT_EXISTS) -- DL only.
    Apply to BOTH DL and DH on multi-Name-combo providers: TX (DL DQName/QWName/CPLName + DH KQName),
    AZ (DL ACWL/DQN + DH), CA_CLETS (DL NLTS.DQ.N/IR.QVC.N/IN.L1 + DH), NJ (DL DQ + DH -- DH not yet
    done, add at next NJ touch), NY (DL DLICN + DH DALL/DALH Name combos). FL DL conformant; FL DH to add.

    NOTE: A SEPARATE opt-in Name query (e.g. NY DGRP NyNyspinDriverLicenseNameQuery,
    autoSelect=false) needs NO condition -- it does not co-fire by default, so there is
    no union-pool bleed to gate. The guardrail is only for combos WITHIN one autoSelect
    QIDM that can co-satisfy.

    MULTI-IDENTIFIER QIDMs (CA_CLETS: OLN, Name, SSN, CII): the same principle extends --
    rank identifiers by specificity/priority and gate each lower-priority combo out when a
    higher-priority identifier is present. Scope per devdoc; review co-fire pairs at build.

  --- BOAT half (Hull > Reg) -- HI v3.9 live-found ---
    When a BoatQuery has both a Hull combo (set includes BoatHullIdNumber) and a Registration
    combo (set includes RegistrationNumber), add a BoatHullIdNumber NOT_EXISTS condition to the
    Reg combo. Hull ID (HIN) is the unique permanent identifier (VIN-like); Registration Number
    is reassignable (plate-like) -- so HULL WINS. When the officer enters a Hull ID the Reg combo
    exits the pool and the Hull query fires alone.
    RULE: on every Reg-set combo --
      conditions: [ { field: ['BoatHullIdNumber'], operator: 'NOT_EXISTS' } ]
    LIVE PRECEDENT: HI_HCJDC_OFML v3.9 -- BQ (RegistrationNumber) carries BoatHullIdNumber
    NOT_EXISTS; QB (Hull) is the priority combo (no condition).
    Apply to every provider whose BoatQuery has both a Reg and a Hull combo (portfolio-wide;
    most providers do) at its next touch. CHECK 12 enforces it (Boat: Hull>Reg).

  *** SCOPE REMINDER: an autoSelect QIDM with ANY two mutually-substitutable identifier combos
  has the LIMITATION #1 union-bleed. Known pairs: Plate/VIN (Vehicle), OLN/Name (Person DL+DH),
  Hull/Reg (Boat). At every build, scan ALL QIDMs for co-satisfiable identifier combos -- do not
  assume only the three named pairs exist for a given provider. ***

  *** INTENTIONAL DUAL-IDENTIFIER COMBOS ARE EXEMPT: if a lower-priority combo lists the
  higher-priority identifier in its OWN any[] (so the officer may supply BOTH and the combo
  deliberately accepts both), it is NOT a pure single-identifier combo and the guardrail does
  NOT apply -- co-sending both is the intended behavior, not a bleed. Example: FL_FCIC Boat QB
  (NCIC stolen) and BQ (Nlets OOS) carry both Hull and Reg as companions, so only the in-state
  FBQ family (separate single-identifier primaries) gets the Hull>Reg guardrail. verify_build
  CHECK 12 encodes this: it flags a pure-lower combo only when the higher id is in NEITHER set[]
  NOR any[] (Any-HasToken exemption, 2026-06-23). ***

  *** GATE XOR COMPANION -- never both (TX v3.12 2026-06-23): the two treatments above are
  MUTUALLY EXCLUSIVE for a given (combo, higher-id) pair. EITHER gate the combo (add the
  <higher-id> NOT_EXISTS condition AND keep that field out of the combo's set[]/any[]),
  OR treat it as a companion (leave the field in any[] and add NO condition). Doing BOTH --
  a NOT_EXISTS condition on a field that also sits in the same combo's any[] -- is dead config:
  whenever the field has a value the condition blocks the combo, so the any[] entry can NEVER
  serialize. It is harmless but redundant on the live platform, and it actively BREAKS the test
  conductor (run_test_matrix Build-MinimalData pulls any[] fields into the minimal test data,
  which then trips the NOT_EXISTS and the combo falsely fails to fire). TX v3.12
  QBRegistrationNumber hit this: boatHullIdNumber was in both its any[] and its NOT_EXISTS
  condition -> conductor FAIL until removed from any[]. When you ADD a guardrail condition,
  scrub the gated field from that combo's any[]. ***

  OOS-ONLY QUERY FAMILY (destination state) (FL KQ + NY DALHOUT/DALLOUT pattern):
    When the provider/state switch documents a query as out-of-state only with a
    required destination (FL KQ per FCIC: "destination ... must be something
    other than FL"):
      - Destination state goes in set[] (mandatory, officer must pick)
      - NO initialValue and NO CAD combo defaults (a destination cannot be
        defaulted; home state is an INVALID value)
      - conditions State NOT_EQUALS <home state> on every combo in the family
      - Card title hint ("... (Out-of-State Only)") + field label
        "Destination State"
      - CAD dispatch cannot supply a destination -> the family never auto-fires
        from CAD. This is correct, not a gap.

  DEVDOC "Data-Mined Transactions" SECTION (PROVISIONAL, pending platform
  confirmation 2026-06): the page-1 list (FL: "NCIC (QA, QB, QG, QV, QW) and DMV
  (Person and Vehicle)") is believed to enumerate CommSys auto-sent SECONDARY
  queries. Precedent: FL QW removal v4.2 was platform-confirmed ("CommSys
  auto-sends QW") and QW is in that list. Implication: a key appearing in this
  list may not need a JSON-side QIDM even when devdoc Basic combos exist for it
  (FL QV pending). Do NOT treat the list as build scope in either direction
  until the platform confirms the semantics.

  DEVDOCS CONTAIN NO KEY MNEMONICS: never justify building/skipping by a devdoc
  "key list" -- FL's devdoc has ZERO occurrences of FBQ/BQ/KQ/FDQ/RQ/DQ. Key
  mnemonics exist only in metadata MessageKey definitions. The devdoc "Possible
  Combinations" are the build authority (FL v4.4 misread cost 5 combos).

================================================================================
12. CAD DEFAULTS -- COMBINATION-LEVEL DEFAULT VALUES
================================================================================

  CAD dispatch does NOT apply QIF form initialValues. When CAD sends a query,
  only the fields CAD explicitly populates are present. Fields with form
  initialValues (ImageIndicator=N, RandomRequest=N, PlateType=PC, etc.) are
  absent from CAD-dispatched XML unless the QIDM combo has a defaults[] entry.

  RULE: Every field that meets ALL of these conditions needs a combo default:
    1. The field is in any[] (optional, not in set[])
    2. The corresponding form field has an initialValue
    3. The provider expects a value (field appears in XML spec)

  SYNTAX (inside requirements alongside set/any):
    defaults = @(
        [PSCustomObject]@{ field = 'ImageIndicator'; value = 'N' }
        [PSCustomObject]@{ field = 'RandomRequest';  value = 'N' }
    )

  In JSON output:
    "requirements": {
      "set": ["licensePlateNumber"],
      "any": ["randomRequest", "imageIndicator"],
      "defaults": [
        { "field": "RandomRequest", "value": "N" },
        { "field": "ImageIndicator", "value": "N" }
      ]
    }

  FIELD NAME: defaults[].field uses the QIDM attribute name (PascalCase
  targetField), NOT the sourceField (camelCase). This matches the server's
  attribute-level injection point.

  VALUE: The literal string value the server injects. Must match what the
  provider expects (e.g., 'N' for ImageIndicator, 'PC' for PlateType).

  STATE DEFAULTS:
    When a provider has State in any[] with a form initialValue (e.g., 'NJ'),
    add State defaults to every combo where registrationState is in any[].
    defaults[].field = 'State', defaults[].value = '<HOME_STATE>' (2-char NCIC
    code matching the form initialValue).
    NOTE: State defaults may trigger an RMS 400 because RMS expects a UUID
    attribute ID, not a literal NCIC code. This is a known platform issue
    (Cringer investigating 2026-05-19). Deploy State defaults anyway —
    CommSys correctness is the priority; RMS fix is platform team's scope.
    Providers with no State initialValue (CA_CLETS, LA_LEMS, NY, MD) do not
    need State defaults.

  NOT ALL PROVIDERS HAVE THE SAME FIELDS:
    - ImageIndicator: NJ, HI, AZ, FL, TX, LA, CA_VENTURA, CA_eSUN, OH, TN,
      NM, OR, IL, MD (but NOT CA_CLETS, CA_CLETS_OCATS, CA_SAN_LUIS_OBISPO)
    - RandomRequest: NJ only
    - LicensePlateTypeCode: most providers with plate search
    - LicensePlateYear: most providers with plate search
    - PurposeCode: CA providers
    - State: NJ, HI, AZ, FL, TX (providers with home state initialValue)
    Check each provider's metadata — do NOT assume any field is universal.

  AUDIT: audit_cad.ps1 CHECK 5 validates defaults coverage automatically.
  Run after every build. Any[] fields with form initialValues that lack
  combo defaults[] are flagged as FAIL.

  COEXISTENCE WITH FORM INITIALVALUE:
    Form initialValue and combo defaults[] serve different channels:
    - initialValue: UI-side, shown in form when officer uses the browser
    - defaults[]: server-side, injected when field is absent from input
    Both should be set. initialValue handles the officer workflow, defaults[]
    handles CAD dispatch. They do not conflict — explicit user values override
    both mechanisms.

  CONDITIONS-VARIANT COMBOS MUST INHERIT DEFAULTS:
    When a combo uses conditions (e.g., RandomRequest EQUALS Y) to create a
    routing variant, it MUST carry the same defaults[] as the base combo for
    all shared any[] fields. The condition restricts WHICH combo fires, not
    WHAT fields are available. If the base combo defaults PlateType=PC and
    PlateYear=currentYear, the conditions variant must also default them.
    CONFIRMED: NJ v3.2 -- RQ_RAND had conditions but was missing defaults
    present on RQ. CAD dispatch hit the RAND combo (when RandomRequest=Y was
    sent) but PlateType/PlateYear were absent from the XML.
    AUDIT: Compare defaults[] arrays between base and conditions-variant
    combos in the same QIDM. Every default on the base combo should appear
    on the variant unless the field is the routing toggle itself.

================================================================================
13. POST-REBUILD CLEANUP CHECKLIST
================================================================================

  REBUILD RESTARTS TESTING (MANDATORY):
    A version bump invalidates prior live test logs -- routing, set[]/any[],
    conditions, or defaults may have changed, so old logs no longer line up
    with the shipped JSON. pipeline.ps1 runs reset_test_package.ps1 after a
    successful build: archives logs/<Entity>/*.txt -> logs/<Entity>/_archive_pre_v<ver>/,
    resets SQVR markers [CONFIRMED]/[FAILED] -> [PENDING], clears STATUS live
    rows, stamps logs/.test_version. Re-run the FULL test matrix from Test 1;
    never resume mid-matrix across a rebuild. If you ran the build script
    directly (not pipeline), run reset_test_package.ps1 -Provider <NAME>
    manually. See TESTING_REQUIREMENTS.txt Section 11 GATE 1.

  OBSOLETE: the BASE/MC-era post-rebuild cleanup checklist (delete
  _BASE/_MC test matrices, rename _MC-suffix JSONs, consolidate dual JSONs)
  was made obsolete by galvanization -- all 20 providers are already single
  versioned JSON, PascalCase. No BASE/MC artifacts remain to clean up. The
  still-live rule is the REBUILD RESTARTS TESTING block above.

================================================================================
14. VISIBLE-FIRST MANDATE  (MAJOR DIRECTIVE)
================================================================================

  RULE: Every officer-facing query field MUST be built as a VISIBLE form field
  (hidden=false) by default. Do NOT hide a field, and do NOT auto-populate a
  field via a rule handler in place of a visible input, WITHOUT explicit user
  approval or live-test evidence that the platform requires it.

  ATTENTION FIELD -- EXCEPTION TO THE RULE (automated, by directive 2026-06-22):
    The Attention field is the ONE standing exception. Wherever Attention is
    part of a query, it MUST be auto-populated via the
    CommsysGetLastNameFirstNameInitialRuleHandler rule handler (no visible form
    field). This is the approved STANDARD, not a per-provider exception.
      QIDM attribute:
        name='Attention'; size=30; sourceField=['Attention'];
        targetField='Attention';
        rule={ function='CommsysGetLastNameFirstNameInitialRuleHandler' }
    The handler derives "LASTNAME F" (last name + first initial) from the
    query's name and serves as a response-correlation tag. Scope it to ONLY the
    query whose metadata lists Attention (e.g. DriverHistoryQuery); never attach
    it to queries whose metadata does not include Attention.
    SECURITY: the auto-derived value is name-sourced, so the outbound request
    XML MUST be properly escaped -- verify with an injection test (a name
    containing < & " ' ]]> must not break or inject into the XML).

  HISTORY / WHY THE REVERSAL: An earlier policy (FL_FCIC v3.9) exposed Attention
  as a visible field, attributing a form load/submit slowdown to the handler.
  That attribution is now doubted -- the slowdown was likely not handler-related,
  and WNG has since posted a platform performance fix that makes any residual
  degradation negligible. Per user directive (2026-06-22) the automated handler
  is restored as the standard; form-load performance is re-verified during HI
  live testing. The general visible-first rule still governs ALL OTHER fields.

  HOW TO APPLY (all fields EXCEPT Attention):
    - Build query fields as Inp (FormInput) / Sel (FormSelect) / Dt (FormDate),
      hidden=false. Use the field's natural fieldId.
    - Do NOT reach for InpH/SelH (hidden helpers) or an auto-populate rule
      handler as a first move. If you believe another field should be hidden or
      auto-filled, STOP and get user approval first, or wait for live-test
      evidence that the visible field breaks something.

  APPROVED EXCEPTIONS (fields that may be hidden / auto-populated without
  case-by-case approval -- documented platform requirements or standing
  directives, not conveniences):
    1. Attention -- auto-populated via CommsysGetLastNameFirstNameInitialRuleHandler
       wherever Attention is part of a query (standing directive 2026-06-22; see
       above). This is the standard, not a violation.
    2. RMS dual-field State -- SelH 'RegistrationState' (RMS) + InpH outbound
       state field -- ONLY when the NCIC single-visible-State pattern is
       unavailable for the provider. See Section 7. Prefer the single visible
       RegistrationState (NCIC) pattern, which has NO hidden fields.
    3. dexStateUserId -- auto-populated from the officer's RMS profile (AUTH).
    4. CAD / First-Responder dispatch context fields (CadUnit, CadEvent,
       LinkToEvent) -- injected by the CAD_DISPATCH / FIRST_RESPONDER variants.

  REQUIRED ATTENTION (CCH) -- EXEMPT: the automated handler is for OPTIONAL
  Attention only (DriverHistory-style, metadata any[]). Where Attention is
  REQUIRED (metadata set[], e.g. the CCH Criminal History query family), the
  officer must supply the routing value, so a VISIBLE field is correct and the
  name-derived handler is NOT applied. (TX_TLETS_CCH is the standing example;
  it is out of scope for the optional-Attention conversion.)

  ENFORCEMENT: verify_build.ps1 CHECK 8 ("Visible-First Mandate") scans every
  build. It WARNs on any hidden form field not on the whitelist above, AND on an
  OPTIONAL Attention attribute that LACKS the handler (the manual/visible case to
  convert). An Attention attribute WITH the handler is conforming (Info); a
  REQUIRED Attention (in a combo set[]) is exempt (Info). The check is
  non-blocking (WARN, not FAIL) so it does not break legacy providers mid-flight,
  but every WARN must be resolved before a build is declared DONE (0 WARN target).

  ATTENTION CONFORMANCE STATUS (automated handler = conforming):
    CONFORMING (handler present): HI_HCJDC_OFML, LA_LEMS, CA_eSUN, CA_VENTURA,
      OH_LEADS, TN_TIES.
    TO CONVERT (manual visible field -> automated handler): FL_FCIC, TX_TLETS.
      TX_TLETS_CCH is explicitly OUT OF SCOPE -- do not touch.
    N/A (no Attention in any query): NJ_NJCJIS, NY_NYSPIN_EJUSTICE, CA_CLETS, etc.
    Run verify_build.ps1 CHECK 8 on any provider for its current status.

================================================================================
15. SYNTHETIC KEYREF DOCUMENTATION (MANDATORY)
================================================================================

  RULE: Every QIDM in a build script that has more than one combo MUST include
  a LIMITATION comment block immediately before the $...Query = [PSCustomObject]@{
  line. This is mandatory on every new build and on every rebuild.

  WHY: ConnectCIC requires unique keyRefs per QIDM (LIMITATION #21). Provider
  metadata XML often uses the same keyReference for multiple transactions. Build
  scripts invent synthetic keyRef labels (e.g., DLICN, DALH, KQName) to satisfy
  the platform constraint. Without documentation, these invented names appear to be
  real provider transaction codes and cause confusion during debugging, auditing,
  and operator training. NY_NYSPIN testing found DLICN with no explanation --
  this standard was created 2026-06-09 to prevent recurrence.

  STANDARD COMMENT BLOCK FORMAT (insert before every $...Query definition
  whose QIDM has >1 combo):

    # PLATFORM CONSTRAINT: ConnectCIC requires unique keyRefs per QIDM (LIMITATION #21).
    # Metadata uses keyRef '<BASE>' for all <N> combos; synthetic labels (<LIST>) are
    # invented for platform routing only. NOT real <PROVIDER> transaction codes.
    # See PLATFORM_CONSTRAINTS.txt -- synthetic keyRef naming convention.

  For OOS-split combos (LIMITATION #36 Choice OOS rule), use instead:
    # PLATFORM CONSTRAINT: <KEYREF> is a synthetic keyRef (NOT a real <PROVIDER> code).
    # Metadata uses a Choice combo with State in extended-Set; LIMITATION #36 requires a
    # dedicated OOS combo with State in set[]. <IN_STATE_KEYREF> stays in-state only.
    # See PLATFORM_CONSTRAINTS.txt -- Choice OOS rule (LIMITATION #36).

  SYNTHETIC KEYREF NAMING CONVENTIONS (from PLATFORM_CONSTRAINTS.txt):
    - DH Name path:   DALL → DALH   (H = history/name path)
    - DH OOS:         DALL → DALLOUT, DALH → DALHOUT  (OUT = out-of-state)
    - DL Name path:   DLIC → DLICN  (N = name path)
    - FL/TX pattern:  Base_FieldName (e.g., KQName, FRQLicensePlateNumber)
    - CA pattern:     Base.suffix  (e.g., NLTS.DQ.N, IR.QVC.O)
    - NJ VehReg:      RQ_RAND, RQN_RAND  (_RAND = RandomRequest=Y routing split)

  ENFORCEMENT: verify_build.ps1 CHECK 7 ("Synthetic keyRef Documentation")
  WARNs on any multi-combo QIDM that lacks a LIMITATION #21/#36 comment within
  40 lines of the first keyReference in the build script. 0 WARN is required
  before declaring any build DONE.

  STATUS (2026-06-09): NJ, FL, CA_CLETS, TX, NY all CLEAN (CHECK 7 PASS).
  Remaining 14 providers: add comment blocks on next scheduled rebuild.

================================================================================
16. ADDING AN OVERLAPPING QUERY FAMILY (SUFFIX-ISOLATION)
================================================================================

  When you add a NEW family of queries to an entity that ALREADY has cards
  (e.g., CCH criminal-history queries onto the Person form that already hosts
  DL/DH/OPTIONS), the new queries almost always reuse field NAMES that the
  existing cards use (Name, BirthDate, SexCode, RaceCode, ImageIndicator,
  State, etc.). A fieldId may appear on only ONE card per entity QIF --
  duplicate fieldId across cards = Internal Server Error (AP: no_duplicate_fieldid).

  RULE -- SUFFIX-ISOLATION (generalizes the DH-suffix pattern):
    Give EVERY form field of the new family a family suffix on the fieldId /
    sourceField (e.g. nameLastCCH, birthDateCCH, sexCodeCCH, imageIndicatorCCH).
    Keep the XML-facing targetField as the metadata name (BirthDate, Name, ...).
    This guarantees zero collision with the existing cards. Put the new family
    on its own card(s); a shared "<FAMILY> OPTIONS" card holds the fields used
    by nearly every combo (the OPTIONS pattern).

  AUTOSELECT FOR OPT-IN / RESTRICTED FAMILIES:
    Set autoSelect=false on the new family's QIDMs and expose each as a named
    checkbox via queryLabel = the transaction. The officer explicitly picks the
    query. This prevents co-fire with the base autoSelect queries and removes
    any need to share/reuse base fields (reuse couples inputs and risks co-fire;
    full isolation is cleaner -- confirmed TX_TLETS_CCH 2026-06-09).

  CHOICE SPLITS: a metadata <Choice> of multiple <Set>s (e.g. CCH QH name path
  with SSN-or-Misc, QWI with DOB|Misc|SSN) becomes one combo PER inner <Set>,
  each with a synthetic keyRef (QH.NAME.SSN, QH.NAME.MISC, QWI.DOB, ...) and the
  Section 15 LIMITATION #21/#36 comment.

  ADDING NEW queryLabels (MANDATORY pair-update):
    A new query family needs new queryLabel values. You MUST add them in BOTH:
      1. tools/verify_build.ps1  -> $validLabels array
      2. CLAUDE.md  -> "### queryLabel Standard" table
    audit_repo.ps1 CATEGORY 3 fails unless the two match exactly. queryLabels
    MAY contain parentheses (e.g. "CCH Criminal History (QH)") -- the
    Get-ValidLabels parser is paren-safe as of 2026-06-09.

  NEW-PROVIDER ONBOARDING FRICTION (new_provider.ps1):
    The scaffolder emits a legacy BASE+MC stub pair and stub docs. For the
    single-JSON model: delete the build_*_mc.ps1 stub, and populate the three
    stub docs so repo audit passes -- JSON_INVENTORY.md must NOT contain
    "(no builds yet)" and must cite the version; STATUS.txt must carry the
    validator score (NNP/0F) not "Not built"; SQVR.txt must mention the version.
    sync_version_docs does not fully de-stub a brand-new provider.


================================================================================
17. HYPOTHESIS QUARANTINE + ITERATE-PHASE GATE (gap-proofing)
================================================================================

  WHY: builds were always hard-gated (validate/verify_build), but the
  test->change->iterate phase was prose-trusted. Two failure modes recurred:
  (1) a provider carried stale [CONFIRMED] markers / untested combos / missing
  XML / a stale TEST_MATRIX yet still "passed" and got called done; (2) an
  unverified platform HYPOTHESIS was written into the KB + both simulators as
  "live-proven" before the confirming test ran (the FL v4.7->v5.0 churn). Both
  are now tool-enforced.

  RULE A -- CLAIM STATUS CONVENTION (enforced by tools/verify_claims.ps1):
    Every platform-behavior assertion in knowledge-base/*.txt and in the
    simulators (test_commsys.ps1, run_test_matrix.ps1) carries a status line:
      STATUS: LIVE-PROVEN <test-log path>            -- confirmed by a COMMITTED log
      STATUS: HYPOTHESIS <named discriminating test>  -- not yet proven
    * LIVE-PROVEN MUST cite a path that exists (archived logs under
      logs/<Entity>/_archive_pre_v*/ count -- they are committed evidence).
    * A HYPOTHESIS MUST NOT be encoded in a simulator as fact. Simulate it only
      as an explicit assumption, and do not write the verdict to KB until the
      discriminating test log is committed.
    verify_claims.ps1 also flags any concrete test-log path citation that no
    longer resolves (e.g. a log moved to _archive on a reset).

  RULE B -- ITERATE-PHASE GATE (tools/audit_test_coverage.ps1 -Gate):
    Per provider, the gate computes one verdict:
      CLOSED                -- every combo [CONFIRMED]-with-XML or [APPROVED SKIP];
                               TEST_MATRIX combo count == JSON; version aligned.
      INCOMPLETE-consistent -- combos still [PENDING], no contradiction (a fresh,
                               not-yet-tested build -- legitimate, but NOT done).
      INCONSISTENT          -- blocks: stale [CONFIRMED] vs logs/.test_version,
                               more [CONFIRMED] markers than XML-bearing logs, or
                               TEST_MATRIX combo count != JSON combo count.

  Both run as enforce.ps1 PHASE 6 (blocking). "DONE" = CLOSED + claims verified.
  TEST_MATRIX is regenerated automatically by reset_test_package.ps1 on every
  rebuild so its combo count never drifts from the JSON.
  See TESTING_REQUIREMENTS.txt Section 11 GATES 4-5, QIDM_REFERENCE.txt Sec 2a.

================================================================================
18. CONDITIONAL-REQUIRED FIELD PATTERN (devdoc "must be filled if X=Y")
================================================================================

  PROBLEM: Devdoc says field A is required when field X=Y (e.g. EmailAddress required
  when ImageIndicator=Y). Value-comparison conditions are inert (poisoned-array rule --
  QIDM_REFERENCE.txt Sec 2a), so you cannot enforce "required if X=Y" via conditions[].

  SOLUTION: Image-variant combo split.

  Design rules:
  1. Create TWO combos per routing path: an "Image" variant and a "plain" variant.
  2. Image variant: put the dependent field (X, e.g. imageIndicator) AND any co-required
     fields (e.g. reasonCode) in any[] ONLY. Put the conditionally-required field (A,
     e.g. emailAddress) in set[]. This combo fires only when A is present.
  3. Plain variant: OMIT X, A, and co-required fields entirely from both set[] and any[].
     This combo fires when A is absent. By union-exclusion semantics (QIDM_REFERENCE.txt
     Sec 1d), X is excluded from the XML because it appears only in the non-matching
     Image variant. Field X therefore CANNOT serialize without A.
  4. Combo ordering: Image variant FIRST (more set[] fields = more specific).
  5. Toggle field must be a typed FormInput with no initialValue. Do NOT use an
     initialValue field as the toggle (LIMITATION #31 -- initialValue not counted for
     set[] matching). For Email/Image, emailAddress (typed FormInput) is correct;
     imageIndicator (initialValue=Y) is NOT.
  6. defaults[]: $imgDefsDH on Image variants (inject Image=Y, Reason=C, State=TX);
     $noImgDefsDH on plain variants (inject only Purpose=C, State=TX).

  RESULT: X=Y (e.g. Image=Y) can serialize ONLY when A (Email) is present. No-photo
  (A absent) fires freely. CAD path fires the plain combo (no email source available).

  PRECEDENT: TX_TLETS v3.8 DriverHistoryQuery (KQNameImg/KQName/KQOLNImg/KQOLN).
             LA_LEMS DP/DQ (ImageIndicator in set[] of DP vs any[] of DQ -- same split
             with imageIndicator as the toggle; LA_LEMS is not constrained by Lim #31
             because its imageIndicator field has no initialValue).

  PORTFOLIO NOTE: Other providers with DH may have the same devdoc constraint. Check
  at each provider's rebuild -- do not mass-sweep (one-provider-at-a-time rule).

================================================================================
19. BUILD REPRODUCIBILITY CONTRACT
================================================================================

  CONTRACT: a provider's committed <PROVIDER>.json MUST equal what its build script
  produces from current inputs (KB + shared modules). The build is the source of
  truth; the committed JSON is a cached artifact of it.

  TWO DISTINCT FAILURE MODES (audit_reproducible.ps1 separates them):
    - NON-DETERMINISTIC: two fresh builds (same moment, same inputs) differ.
      That is a BUILD BUG -- fix it. The build must not depend on anything that
      varies run-to-run.
    - STALE: builds are deterministic, but the committed JSON differs from a fresh
      build. The commit predates a KB / shared-module change. Not a bug -- rebuild
      that provider to bring it current (one-provider-at-a-time).

  INTENTIONALLY VARIANT FIELDS (normalized out of the reproducibility compare; do
  NOT treat their drift as non-determinism):
    - top-level "version" (stamped by Write-ProviderJson from build-script $Version).
    - PlateYear / LicensePlateYear initialValue = current year (Get-Date). This
      makes a raw build reproducible only WITHIN a calendar year. audit_reproducible
      normalizes it; if you ever need cross-year byte-stability, pin the year
      instead of deriving it. (No other Get-Date value reaches JSON content -- dates
      appear only in the phase-archive FILENAME.)

  DETERMINISM REQUIREMENTS already in place: [ordered] dicts everywhere (stable
  key order), ConvertTo-Json -Depth 100, UTF-8 no-BOM. Do not introduce Get-Random,
  unordered hashtables, or time/now-derived JSON content.

  KEEPING A REBUILD = a real version bump: bump build-script $Version, add a
  BUILD_NOTES entry (CHANGED/REASON), reset_test_package, sync_version_docs +
  CLAUDE.md row, build_report, enforce green. (feedback: full-rebuild-audit,
  rebuild-restarts-testing, bump-build-script-version.)

  GATE: audit_reproducible.ps1 -Path <json>  (or enforce.ps1 -Reproducible; opt-in
  because it re-runs the build twice per provider). Build to scratch via the
  $env:REPRO_OUTPATH hook in Write-ProviderJson -- never compare by overwriting the
  committed file.

  PRECEDENT (2026-06-24): NJ_NJCJIS v4.5 reproduced byte-identical; FL_FCIC v6.5 and
  HI_HCJDC_OFML v4.1 were DETERMINISTIC-but-STALE (predated the version field + the
  VehicleMakeName QRDM fix) -> rebuilt to v6.6 / v4.2.

================================================================================
12. TENANT CODE-TABLE RULE (added 2026-07-02)
================================================================================

  Code-table CONTENTS are TENANT DATA, not platform constants. The same
  codeTypeCategory/codeTypeSource pair serves DIFFERENT option sets per tenant:
  live-proven 2026-07-02 -- NJ's NCIC_FIREARM_MAKE/NJ_NIBRS table holds numeric
  NIBRS make codes ('03 - Armalite AR-180 type') while HI's identically-named
  pair holds NCIC letter codes ('IMI'); CA's NCIC_FIREARM_TYPE table had no 'HP'.
  A pair renders options only if THAT tenant's table was created and populated
  (USx Codes doc: "as long as a code type exists and has the proper values").

  RULES:
  - Every FormSelect's pair must be TENANT-VERIFIED before USx Tenant Testing:
    scope the tenant (emit_picklist_scope.ps1 -> __usxScopePicklists in the
    browser -> import_picklists.ps1 -> docs/reference/TENANT_PICKLISTS.json).
    emit_test_plan.ps1 hard-FAILs any select test value not present in the
    scoped tenant options.
  - Per-provider test values for selects live in docs/reference/
    TEST_VALUE_OVERRIDES.txt (fieldId=value), never in per-provider forks of
    the shared Get-TestValue table.
  - Nationally-standard code sets (firearm make/caliber/type, plate type,
    article type) standardize on codeTypeSource=NCIC at each provider's NEXT
    rebuild (one provider at a time; never churn a live-proven config for this
    alone). State-specific sets (NJ_NIBRS_STATE, HI VEHICLE_TYPE, LA purpose
    codes) stay per-state.
  - An empty tenant table found during scoping is a platform/tenant-admin item
    (Leo's attribute-mapping runbook; 1h cache), NOT a JSON fix.

================================================================================
13. SHARED-TOOL KEYREF SCOPING (cross-QIDM collision) -- added 2026-07-20
================================================================================

  keyReference is only required to be unique WITHIN a single QIDM (platform
  rejects duplicates there -- AP #5/#10 above). The platform does NOT require
  keyRefs to be unique ACROSS a provider's different QIDMs, and providers
  legitimately reuse short keyRef names for unrelated combos (NY_NYSPIN_EJUSTICE's
  Boat and Vehicle entities both define RVEH/RCAR -- Boat's RVEH is a
  RegistrationNumber lookup, Vehicle's RVEH is a LicensePlateNumber lookup;
  same string, different fields, different QIDM).

  This is fine for the platform. It is NOT fine for any shared tool that builds
  a lookup table keyed purely on the bare keyRef string without also scoping by
  the owning query/entity -- whichever QIDM's combo is processed last silently
  overwrites the other's entry, and the tool then reports/acts on the wrong
  QIDM's data for one of the two colliding combos.

  CONFIRMED instances of this exact bug, found and fixed independently (no
  shared helper was ever extracted -- each tool solved it its own way):
  - tools/audit_test_coverage.ps1 (fixed 2026-07-16): tagged each scanned log
    file with its logs/<Entity>/ folder name; coverage-matrix matching skips
    any log whose Entity tag doesn't match the combo's own entity.
  - tools/post_test.ps1: parses SQVR.txt into entity-scoped line ranges via the
    section-header regex ('^\s*\d+[a-z]?\.\s+.*--\s+(\w+)\s+Entity'); SQVR
    marker updates only search within the target -Entity's range.
  - tools/reset_test_package.ps1: same SQVR-header-regex entity scoping, applied
    when resetting [CONFIRMED]/[FAILED] markers back to [PENDING] on rebuild.
  - tools/audit_log_content.ps1: filters guardrail-winner lookups by the test
    plan's own structured .entity field before falling back to unscoped.
  - tools/extract_metadata_reference.ps1 (fixed 2026-07-20): $builtCombos was
    keyed by bare keyRef string across the WHOLE provider JSON; two QIDMs
    reusing a keyRef silently overwrote each other, so the BUILD COVERAGE
    section for one query printed the OTHER query's fields (Vehicle's
    RVEH/RCAR rows showed Boat's RegistrationNumber/BoatHullIdNumber instead
    of Vehicle's own LicensePlateNumber/VehicleIdentificationNumber). Fixed by
    keying on (query, keyRef) instead of bare keyRef.

  RULE: any new tool that builds a lookup/index over a provider's combos by
  keyRef MUST scope that key by query name or entity from the start (e.g. a
  composite "$query::$keyRef" key, or a first-class .entity/.query field
  carried on every record) -- do not assume keyRef alone is unique across a
  provider. Do not wait to discover this the hard way on the next tool built
  against combo data.


================================================================================
20. COMBO REACHABILITY -- FIRST-MATCH SHADOWING (added 2026-07-29)
================================================================================

  The platform fires the FIRST matching combination in a QIDM's array, and the
  serialized field pool is the UNION of set[]+any[] of ALL matching combos
  (LIMITATION #1). Two consequences that are easy to build wrong:

  20a. A DEFAULTED SIBLING SET FIELD KILLS THE COMBO BELOW IT.
    Combo A is unreachable if some B ordered before it matches whenever A does.
    The silent case: every set[] field B needs that A does not is form-PREFILLED
    (initialValue), so B's set is always satisfied and B always wins.
    A dead combo still validates, still counts toward metadata coverage, and can
    even carry a PASS test log -- because the wire XML contains NO keyRef, a log
    named for A is indistinguishable from one where B fired. It is also not inert:
    its any[] still unions into the pool, so it can push fields onto the wire that
    the firing transaction does not define.
    Gate: tools/audit_combo_reachability.ps1 (enforce PHASE 2h). Fill-independent.
    Found 2026-07-29 -- TX_TLETS RQLicensePlateNumber (behind REG) and
    RQVehicleIdentificationNumber (behind VIN), both because
    FinancialResponsibilityType is prefilled 'E'; the RQLicensePlateNumber log's
    wire carries BOTH FinancialResponsibilityType (REG's set field) AND
    LicensePlateTypeCode (RQ's), proving REG fired and the pool unioned.
    CA_SAN_LUIS_OBISPO B2.O was an ungated identical twin of KQ.O.

  20b. NEVER EXISTENCE-GATE ON A MANDATORILY-DEFAULTED FIELD.
    ImageIndicator MUST carry a FormSelect initialValue or it does not serialize at
    all (FIELD_REFERENCE.txt Section 9). So it ALWAYS exists, and routing on
    "ImageIndicator EXISTS / NOT_EXISTS" guarantees the NOT_EXISTS branch is dead.
    A value comparison is not an escape hatch either -- any value-comparison
    operator poisons the whole conditions array. Same applies to RandomRequest and
    any other field the platform requires to be defaulted.
    LA_LEMS DriverLicenseQuery DP/DQ is the portfolio's only instance (2026-07-29).
    This is LIMITATION #30 generalized: do not default a field whose EXISTENCE
    routes combos -- and if the platform forces the default, that field cannot be
    the discriminator at all.

  20c. A GATE DOES NOT SERIALIZE -- KEEP THE GATING FIELD IN THE OOS any[].
    conditions[] only decide WHICH combo fires; they contribute nothing to the
    serialized pool. An OOS combo gated "RegistrationState EXISTS" that does not
    also list RegistrationState in set[]/any[] goes out with NO destination state.
    Gate-XOR-companion: the OOS combo keeps the field in any[]; its in-state twin
    (NOT_EXISTS) deliberately omits it.
    CA_SAN_LUIS_OBISPO KQ.N shipped this way -- union pool was BirthDate+Name+
    SexCode with no State (fixed v2.1, 2026-07-29).

  20d. TESTING IMPLICATION -- A LOG CANNOT PROVE WHICH COMBO FIRED.
    The wire XML has no keyRef, so combo identity is NOT evidenced by the log.
    A guardrail test must differentiate by a field the shadower would ADD or the
    dead combo would OMIT, and identifier-priority guardrails (Plate>VIN, OLN>Name)
    do NOT cover defaulted-sibling pairs. Verify combo identity with
    test_commsys.ps1 (which seeds form initialValues as of 2026-07-29) rather than
    inferring it from the captured wire.

  RECURRENCE LOG -- this rule was violated THREE times on 2026-07-29 alone, each time
  producing something that looked exactly like a real provider defect:
    1. audit_log_combo_attribution.ps1 -- bare-keyRef lookup grabbed NY_NYSPIN_EJUSTICE's
       BoatQuery RVEH/RCAR while replaying a Vehicle fill -> 8 bogus "NO COMBO FIRES".
       Nearly reported as an NY functional regression.
    2. an ad-hoc wire-vs-set consistency scan -- same unscoped lookup -> 6 bogus
       "set field absent from wire" on the same NY combos.
    3. (earlier) the same class is why audit_metadata keys by (query, keyRef).
  MECHANISM FIX: _sim_helpers.ps1 now exports Get-ComboByKeyRef($qidms, $keyRef, $queryHint),
  which is query-scoped and returns $null on AMBIGUITY rather than guessing. Reach for it
  instead of writing another combo search. NY_NYSPIN_EJUSTICE is the canary provider -- if a
  new tool reports Vehicle/Boat weirdness ONLY on NY, suspect an unscoped keyRef lookup first.


================================================================================
21. GALVANIZED: COMBO-FIRING MODEL + THE THREE INTEGRITY GATES (2026-07-29)
================================================================================

  Read this before re-analysing shadow queries, dead combos, or test-log coverage.
  It exists so the 2026-07-29 investigation never has to be repeated.

  21a. THE FIRING MODEL -- LIVE-PROVEN, DO NOT RE-DERIVE.
    Client side: ONE QIDM produces ONE transaction. The FIRST matching combination in
    the array fires; later matches do NOT produce additional transactions. The
    serialized field pool is the UNION of set[]+any[] of ALL matching combos
    (LIMITATION #1), so a losing match still puts its fields on the wire.

    EVIDENCE (committed log, not inference):
      providers/TX_TLETS/logs/Vehicle/TX_TLETS_v4.12_REGLicensePlateNumber.txt
      A plate fill matches BOTH REGLicensePlateNumber (set incl. FinancialResponsibility
      Type) and RQLicensePlateNumber (set incl. LicensePlateTypeCode). The captured
      envelope contains exactly ONE <api:Transaction> / ONE <MessageType>, carrying
      FinancialResponsibilityType AND LicensePlateTypeCode TOGETHER. Two separate
      per-combo transactions would have shown two transactions with disjoint fields.
      Corroborated portfolio-wide: 425/425 logs = 1 transaction, 1 MessageType.

    THIS DOES NOT CONTRADICT the v4.9 "platform auto-fires the subset query" ruling --
    they are DIFFERENT mechanisms at different layers:
      client  = one QIDM -> one transaction (first match) + union pool   [above]
      state   = CommSys/NCIC auto-running SECONDARY queries from that one request
                (QW, and the devdoc "Data-Mined Transactions" family)
    Both models give the SAME disposition for a no-discriminator subset shadow: remove
    it. Either it can never fire, or the state derives it anyway. So the removal ruling
    is model-independent -- do not reopen it on the basis of which model is "right".

  21b. THE THREE GATES (enforce 2h / 2i / 2j) -- what each ASSERTION used to go unchecked.
    2h audit_combo_reachability   a built combo can actually fire.
        Dead when a sibling ordered EARLIER matches whenever it does -- the silent case
        being that the sibling's extra set[] fields are all form-prefilled
        (initialValue), so its set is always satisfied.
    2i audit_log_combo_attribution  a log's named combo is what actually fired.
        The wire carries NO keyRef, so a filename was NEVER evidence. Replays the log's
        recorded QUERY STRING; routing is existence-based so presence decides.
    2j audit_sqvr_integrity        the SQVR still describes the JSON.
        SQVR-named keyRefs must exist; stated totals + version must match.

    ALL THREE ARE NEGATIVE-TESTED (2026-07-29): an injected dead combo, a deliberately
    relabelled log, and an SQVR naming a nonexistent combo with a wrong total each
    produced exit 1 with the correct diagnosis, and enforce's parsers were confirmed to
    match those exact FAIL lines. A gate that cannot fail is worthless -- if you change
    one of these tools, re-run that negative test.

  21c. WHY THIS WAS INVISIBLE FOR SO LONG (the root cause, now fixed).
    Tools MODELLED A FORM STATE OFFICERS NEVER SEE. test_commsys did not seed form
    initialValue, so with FinancialResponsibilityType blank the REG/VIN combos reported
    "[SKIP] missing set" and the dead RQ combos looked like the live ones -- which is
    exactly why the v4.9 shadow review, which was otherwise correct, missed them.
    Any tool that simulates a form MUST seed initialValue (visible AND hidden).
    Corollary: a plan's formDefaults omits hidden handler-fed fields, so simulate from
    the JSON's initialValues, not from the plan alone.

  21d. ONE CANONICAL FIRING WALK.
    _sim_helpers.ps1 owns Get-SimFilledRefs / Test-ComboMatches / Get-FiringKeyRef.
    Five tools had each grown a private copy while only the CONDITIONS predicate was
    shared, so audit_simulator_parity could report "parity" while the set[]-satisfaction
    halves disagreed. Never re-walk combinations; call Get-FiringKeyRef.
    audit_simulator_parity now cross-checks the ANSWER: every plan test's expectedKeyRef
    is recomputed with the canonical walk and must agree (1257 checked).

  21e. INDETERMINATE IS A VALID ANSWER.
    A combo requiring a field no static model can supply -- handler-populated with no
    initialValue -- cannot be predicted statically. AZ_AZDPS's badge combos
    (ACVR/ACVRV/ACWL/ACQB/ACQBH) all require dexStateUserId. Statically the no-badge
    fallbacks (DQN/BQ/BQH) win; at runtime the badge combo may. Report INDETERMINATE,
    never a mismatch. AZ RISK: if the platform does not populate dexStateUserId, the whole
    badge family silently falls back -- CONFIRM ON THE FIRST QUERY of AZ's first sweep.


================================================================================
22. ONE PROVIDER PER TENANT -- WHAT THIS MEANS FOR PRIORITISATION (Rob 2026-07-29)
================================================================================

  RULING: a tenant uses EXACTLY ONE provider. No customer is ever configured with two
  states' providers. A genuine multi-state provider can only be built by CommSys, and it
  would arrive as its OWN provider with its OWN discrete metadata XML + devdoc -- i.e. it
  comes through the normal new-provider path, it is NOT a merge of existing providers.

  CONSEQUENCE -- CROSS-PROVIDER COSMETIC CONSISTENCY HAS NO CUSTOMER VALUE.
  A Florida officer never sees Hawaii's wording. So a label/title difference BETWEEN
  providers is invisible to every customer, always. Its only value is maintenance comfort
  for whoever builds providers.

  THEREFORE, when deciding whether to spend a version bump on cosmetics, weigh it against
  the real cost: ANY bump resets that provider's ENTIRE test package and buys a full tenant
  re-sweep (35-118 tests of Rob's hands-on time per provider). Cross-provider label
  alignment does NOT justify that. WITHIN-provider inconsistency still can -- one officer
  does see one form, so a contradiction on a single card (e.g. "Sex (optional)" next to
  "Date of Birth (required with Name)") is a real defect.

  WORKED EXAMPLE (2026-07-29): the portfolio was split 3-3 on "VIN" vs "Vehicle
  Identification Number", plus assorted per-provider drift (NJ Firearm/Article titles, HI
  "License Plate Number", CA "Hull ID"). Fixing all of it meant 6 bumps and ~440 tenant
  tests. Under this ruling that work buys ZERO customer benefit, so it was deliberately NOT
  done -- strategy "freeze and ship". Record such a decision, do not silently re-open it
  every time someone notices the drift again.

  DO NOT use this ruling to excuse: functional routing differences, within-provider
  contradictions, or a stale/wrong OFFICER GUIDE (that IS customer-facing, per Section 21).
