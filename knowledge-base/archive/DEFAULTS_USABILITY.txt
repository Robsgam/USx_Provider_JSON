DEFAULTS & USABILITY AUDIT
============================
Standard check for every new JSON project. Run after Phase 1 standup and before
declaring any variant DONE. Ensures cross-provider consistency and safe defaults.
Last updated: 2026-04-29

================================================================================
WHY THIS MATTERS
================================================================================

  Default values determine what the officer sees when they open a query form.
  Good defaults reduce keystrokes and prevent common errors. Bad defaults silently
  change combo routing, causing queries to fire against the wrong system.

  This audit catches:
    - Missing convenience defaults (PlateType, PlateYear, ImageIndicator)
    - Dangerous State defaults that change in-state/OOS combo routing
    - Inconsistency across providers (one has PlateType=PC, another doesn't)
    - Provider-specific defaults that need to be carried into new builds

================================================================================
STANDARD DEFAULTS (apply to every provider unless noted)
================================================================================

  VEHICLE ENTITY:
    PlateType (LicensePlateTypeCode)  = "PC"      (Passenger Car -- most common)
    PlateYear (LicensePlateYear)      = "<year>"   (current calendar year)
    ImageIndicator                    = "N"        (not needed for vehicle queries)

  PERSON ENTITY:
    ImageIndicator                    = "Y"        (officer wants subject photo)

  FIREARM / ARTICLE / BOAT ENTITY:
    ImageIndicator                    = "N"        (not needed)

  These are ALL in any[] -- they never appear in set[] and do NOT affect combo
  routing. Safe to add everywhere without impacting QIDM behavior.

================================================================================
STATE DEFAULT RULE (LIMITATION #30)
================================================================================

  State initialValue determines whether in-state or OOS combos fire by default.

  SAFE to default State=XX when:
    - Provider uses NCIC state pattern (single QIDM, all combos "In/Out")
    - No keyRef has state="In" or state="Out" separately
    - Examples: NJ, AZ

  DO NOT default State=XX when:
    - Provider has separate in-state vs OOS keyRefs (state="In" / state="Out")
    - Setting a default would silently route queries to in-state system
    - Officer must consciously choose their state for correct routing
    - Examples: NY Vehicle (RVEH In vs RVIN Out), FL Vehicle (FRQ In vs QV/RQ Out)

  When in doubt: leave State blank and use card title hints instead.

================================================================================
PROVIDER-SPECIFIC DEFAULTS (not cross-applicable)
================================================================================

  These defaults are unique to their provider. Carry forward when rebuilding
  that provider, but do NOT add to other providers.

  NJ:  RandomRequest = "N" (full request is standard; Y = random check per NJ law)
  HI:  TypeCode = "1", RelatedSearchHitIndicator = "Y"
  FL:  Driver History Purpose = "C"

================================================================================
CROSS-PROJECT AUDIT (2026-04-29)
================================================================================

  | Default      | NJ     | NY     | HI     | AZ     | FL     | TX     |
  |--------------|--------|--------|--------|--------|--------|--------|
  | State        | NJ (S) | -- (S) | HI (!) | AZ (S) | FL (!) | TX (!) |
  | PlateType=PC | PC     | PC     | PC     | --     | --     | --     |
  | PlateYear    | 2026   | 2026   | 2026   | --     | --     | --     |
  | Image(Veh)   | N      | --     | N      | --     | --     | --     |
  | Image(Per)   | Y      | --     | --     | --     | Y      | --     |
  | Image(Other) | N      | --     | --     | --     | --     | --     |

  Legend: (S) = Safe/correct   (!) = Has in/out split, review needed   -- = Missing

  STATE DEFAULT SAFETY:
    NJ  -- SAFE. NCIC state pattern, all combos In/Out. State=NJ correct.
    NY  -- SAFE. Correctly blank. Has in/out split on Vehicle and Person DL/DH.
    HI  -- REVIEW. Has in/out split on Vehicle (M55L/M55S In, RQ/RQV Out).
           State=HI currently set. Verify during import testing.
    AZ  -- SAFE. NCIC state pattern confirmed. State=AZ correct.
    FL  -- REVIEW. Has in/out split on Vehicle (FRQ In) and Boat (FBQ In, BQ Out).
           State=FL currently set. Review when entity-split unblocked.
    TX  -- REVIEW. Person has in-state-only combos (DQ In, CPL In, RSDWW In, KQ In).
           State=TX currently set. Review during rebuild.

  CONSISTENCY GAPS (safe to address):
    AZ, FL, TX: Missing PlateType=PC, PlateYear=<year>
    NY, AZ, TX: Missing ImageIndicator defaults
    FL:         Missing ImageIndicator on non-Person entities

================================================================================
AUDIT PROCEDURE (run for every new JSON)
================================================================================

  STEP 1 -- Extract current defaults
    Grep for "initialValue" in the provider JSON.
    List every field with a default, organized by entity.

  STEP 2 -- Check standard defaults present
    [ ] Vehicle: PlateType=PC
    [ ] Vehicle: PlateYear=<current year>
    [ ] Vehicle: ImageIndicator=N (if field exists in metadata)
    [ ] Person:  ImageIndicator=Y (if field exists in metadata)
    [ ] All other entities: ImageIndicator=N (if field exists)
    [ ] Provider-specific defaults from metadata/devdoc (e.g., RandomRequest)

  STEP 3 -- Check State default safety
    [ ] Grep for keyReference with state="In" or state="Out" in QIDM combos
    [ ] If ANY combo has state="In" or state="Out" on that entity:
        -> DO NOT set State initialValue on that entity's form
        -> Document in provider LIMITATIONS file
    [ ] If ALL combos are state="In/Out" (or no state property):
        -> State=<home state> is safe to set

  STEP 4 -- Cross-reference with audit table above
    [ ] Compare against the cross-project audit table
    [ ] Flag any new inconsistencies
    [ ] Update the audit table when this provider ships

  STEP 5 -- Document
    [ ] Record defaults in provider STATUS.txt
    [ ] Note any defaults intentionally omitted and why
    [ ] Update cross-project audit table in this file

================================================================================
NOTES
================================================================================

  - PlateType/PlateYear/ImageIndicator can be added by direct JSON edit
    (no full rebuild required). Re-run validator after edit.
  - State default changes require careful analysis of QIDM combo routing
    before any edit. Never change State defaults without checking keyRefs.
  - This audit replaces ad-hoc default decisions. Every new provider
    must go through this checklist before Phase 1 is declared complete.
  - When updating PlateYear for a new calendar year, update ALL providers
    simultaneously to maintain consistency.
