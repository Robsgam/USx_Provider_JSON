PROVIDER AUDIT -- 2026-05-06
=============================
Cross-provider audit triggered by TX metadata update.
CA and NJ updates applied separately. This file captures proposed updates
for the remaining 6 providers to be applied when each project is picked up.

================================================================================
TX_TLETS (v2.0) -- 73P/0F/14W/3LIM
================================================================================
METADATA UPDATE: New TX_TLETS.xml received 2026-05-06.
  - TxRSDWMultiQuery v4->v5: removed CommercialOperatorLicenseNumber field
  - TxTletsQfa v2: new transaction (address lookup by zip code, 3 fields, 1 combo)
  - All 6 basic queries IDENTICAL between old and new metadata (same versions, fields, combos)
  RESULT: No rebuild needed. v2.0 JSON is current.

PROPOSED UPDATES:
  1. Archive TX_TLETS.old.xml (rename or move to archive/)
  2. 14 WARNs: 5 EmailAddress (QIDM-only, pending handler), 3 Attention (handler pattern),
     1 PlateYear, 1 ImageIndicator, 4 combo ordering. All acknowledged.
  3. 2 Verify FAIL: EmailAddress not in QIF -- expected, pending engineering handler
  4. BirthDate format: TX uses MMddyyyy. Verify against TX devdoc before import.
  5. TxTletsQfa: Not a basic supported query. Document in SQVR "NOT IMPLEMENTED" section.
  PRIORITY: LOW (no code changes needed)

================================================================================
AZ_AZDPS (v2.0) -- 69P/0F/0W/4LIM
================================================================================
STATUS: Clean. 0 WARN, 0 FAIL. All 18 combos FIRE.
PROPOSED UPDATES:
  1. BirthDate format: AZ uses MMddyyyy. Verify against AZ devdoc before import.
  2. Check devdoc "Basic Queries Supported" to confirm all 8 QIDMs are authorized
     (lesson learned from CA_CLETS v1.3 audit)
  PRIORITY: LOW (clean build, just devdoc cross-check)

================================================================================
FL_FCIC (v3.0) -- 100P/0F/1W/4LIM
================================================================================
STATUS: Near-clean. 1 WARN (DH non-suffixed sourceFields). 34/35 combos FIRE.
PROPOSED UPDATES:
  1. BirthDate format: FL likely uses MMddyyyy. Verify against FL devdoc.
  2. 1 WARN: DH QIDM has 3 non-suffixed sourceFields -- known FL pattern, documented.
  3. Check devdoc basic query list vs built QIDMs.
  PRIORITY: LOW (minor, well-documented)

================================================================================
HI_HCJDC_OFML (v1.0) -- 64P/0F/2W/4LIM
================================================================================
STATUS: 2 WARNs.
PROPOSED UPDATES:
  1. WARN: Vehicle ImageIndicator not in any combo set[]/any[] -- verify if it should be
  2. WARN: Vehicle State initialValue='HI' with In/Out combos -- check if State routing
     causes OOS combos to fire unexpectedly (same issue as LIMITATION #30)
  3. BirthDate format: HI likely uses MMddyyyy. Verify.
  4. Version still v1.0 -- consider v1.1 bump if WARNs are addressed.
  5. Check devdoc basic query list.
  PRIORITY: MEDIUM (State initialValue WARN could affect combo routing)

================================================================================
LA_LEMS (v2.0) -- 48P/0F/32W/4LIM
================================================================================
STATUS: 32 WARNs is the highest of any provider.
PROPOSED UPDATES:
  1. 24 WARNs: Attention handler pattern (CommsysGetLastNameFirstNameInitialRuleHandler
     references Attention field not on QIF). All expected -- Attention is auto-populated.
  2. BirthDate format: LA likely uses MMddyyyy. Verify against LA devdoc.
  3. 32 WARN count inflates perceived risk. Consider validator enhancement to suppress
     known-pattern WARNs (Attention handler is standard, not a problem).
  4. Check devdoc basic query list.
  PRIORITY: MEDIUM (WARN count cleanup, but no functional issues)

================================================================================
NY_NYSPIN_EJUSTICE (v1.1) -- 71P/0F/0W/5LIM
================================================================================
STATUS: Clean. 0 WARN, 0 FAIL. All 13 combos FIRE.
PROPOSED UPDATES:
  1. BirthDate format: NY uses MMddyyyy. Verify against NY devdoc.
  2. Check devdoc basic query list -- devdoc reviewed previously (project memory exists).
  3. No code changes needed.
  PRIORITY: LOW (clean build)

================================================================================
CROSS-PROVIDER FINDINGS
================================================================================
1. BirthDate format varies by state:
   - CA: yyyyMMdd (confirmed from CA_ESUN production JSON)
   - NJ/TX/AZ/FL/HI/LA/NY: MMddyyyy (standard, unverified for some)
   RULE: Always check production JSON or devdoc for provider-specific date format.
   Added to CA_CLETS as a fix (v1.3 -> needs v1.4 for this).

2. Devdoc "Basic Queries Supported" cross-check:
   - CA_CLETS: DONE (v1.3 audit, removed 3 unsupported QIDMs)
   - NJ_NJCJIS: DONE (6 queries confirmed from metadata, WMPI excluded per devdoc)
   - Others: PENDING (should be checked before first import of each)
   RULE: Added as process memory. Check devdoc before building.

3. WARN count normalization:
   - LA has 32 WARNs but no functional issues (all Attention handler pattern)
   - TX has 14 WARNs (EmailAddress pending handler + Attention + cosmetic)
   - Consider validator enhancement to categorize known-pattern WARNs separately

4. Metadata freshness:
   - TX: new metadata received 2026-05-06 (no impact on basic queries)
   - All others: no new metadata reported
