# DEX Provider Build Tickets -- SUPERSEDED (2026-07-29)

This board snapshot is no longer maintained (last updated 2026-06-25) and duplicated the
provider-to-ticket mapping that is kept current elsewhere.

**Maintained source:** knowledge-base/JIRA_REFERENCE.txt (updated 2026-07-20) --
provider -> DEX ticket map, cloudId, per-provider status and last-comment tracking.

Also see providers/JIRA_REFERENCE.txt where a provider-local pointer exists.

Previous content is in git history:
git show 2d9abfaf^:DEX_TICKETS.md

Collapsed to a pointer because two ticket lists drift, and a stale ticket number sends a
changelog comment to the wrong place.
